# Composer dependencies
vendor/

# JS dependencies
node_modules/
dist/

# Piwigo specific directories
_data/

# Legacy CSS/JSON/MD under themes/ — excluded until P49 (docs/PLAN.md's
# additive-only rule). Listed by extension rather than a blanket `themes/`
# directory ignore: gitignore-style negation can't re-include a path whose
# parent directory is already fully excluded, and P30's Latte formatter
# (tools/latte-prettier/) needs `themes/**/*.latte` reachable in the same tree.
themes/**/*.css
themes/**/*.json
themes/**/*.md
template-extension/**/*.css
tools/ws/

# Large hand-formatted docs (tables, deliberate line breaks) — excluded
# from auto-reformatting, not from review. README.md/SECURITY.md/
# docs/CONTRIBUTING.md (the previous entries here) were deleted outright
# during the docs/ consolidation, not reformatted -- removed from this
# list along with them.
docs/PLAN.md
docs/REFERENCE.md

# Generated/vendored, not hand-formatted
composer.lock
bun.lock
eslint-suppressions.json
# Written by composer-patches (note its own `_hash` field), 4-space indent.
# Reformatting it would be undone by the next patch-set change.
patches.lock.json
