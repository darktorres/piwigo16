
;pm = dynamic
;pm.max_children = 50         ; Max simultaneous PHP requests
;pm.start_servers = 15        ; Start with 15 workers
;pm.min_spare_servers = 10    ; Always keep at least 10 idle workers
;pm.max_spare_servers = 30    ; Don't keep more than 30 idle
;pm.max_requests = 500        ; Restart workers after 500 requests to prevent leaks

pm.status_path = /fpm-status
