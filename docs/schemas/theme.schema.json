{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/darktorres/piwigo16/16.x-rewrite/docs/schemas/theme.schema.json",
  "title": "Piwigo theme manifest",
  "description": "Declarative manifest for Piwigo 17+ themes. One file per theme at themes/<id>/theme.json. Read by ThemeRegistry at load time; validated via opis/json-schema.",
  "type": "object",
  "required": ["id", "version", "name", "main"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "format": "uri",
      "description": "URI pointing at this schema for IDE/editor autocomplete. Optional; not enforced."
    },
    "id": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9_-]+$",
      "minLength": 1,
      "maxLength": 64,
      "description": "Unique theme identifier. Must match the theme directory basename."
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+(?:\\.[0-9]+)?(?:[-+][A-Za-z0-9._-]+)?$",
      "description": "Theme version — SemVer or PEM-revision-string."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Human-readable theme name shown in the admin UI."
    },
    "parent": {
      "type": ["string", "null"],
      "pattern": "^[a-zA-Z0-9_-]+$",
      "description": "Parent theme id for inheritance. Null (or omitted) for root themes."
    },
    "loadParentCss": {
      "type": "boolean",
      "description": "Whether the parent's CSS should be loaded before this theme's CSS. Default: false."
    },
    "assets": {
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "minLength": 1
      },
      "description": "Map of asset kind ('img', 'icon', 'mimeIcon', plus theme-specific) to directory path relative to the theme root.",
      "properties": {
        "img": {"type": "string", "minLength": 1},
        "icon": {"type": "string", "minLength": 1},
        "mimeIcon": {"type": "string", "minLength": 1}
      }
    },
    "localHead": {
      "type": "string",
      "minLength": 1,
      "description": "Latte template file (relative to theme root) rendered inside <head> for this theme."
    },
    "main": {
      "type": "string",
      "pattern": "^[A-Za-z_][A-Za-z0-9_]*(\\\\[A-Za-z_][A-Za-z0-9_]*)*$",
      "description": "Fully-qualified class name implementing Piwigo\\Theme\\ThemeInterface."
    },
    "autoload": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "psr-4": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "PSR-4 namespace prefix to directory map, relative to the theme root."
        }
      },
      "description": "Autoload configuration for the theme's source files."
    }
  }
}
