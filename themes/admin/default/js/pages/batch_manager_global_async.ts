// Real per-page bundle entry for batch_manager_global.php's own
// Async-mode script group (docs/PLAN.md P48) -- this page also has a
// genuinely separate Footer group, which gets its own separate
// Footer-mode bundle entry once that group's own shared-library files
// land in later batches (Design §2's per-(page × LoadMode) rule, not
// merged into one file).
import "../addAlbum?dup";
