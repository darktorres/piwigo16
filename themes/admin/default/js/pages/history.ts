// Real per-page bundle entry for history.php (docs/PLAN.md P48).
// datepicker.ts has 4 real registrant pages, so Rollup emits it as a
// shared chunk that all four entries import.
import "../datepicker";
