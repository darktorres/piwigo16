// Real per-page bundle entry for notification_by_mail.php (docs/
// PLAN.md P48). autosize.ts has 3 real registrant pages, so Rollup
// emits it as a shared chunk that all three entries import.
import "../autosize";
