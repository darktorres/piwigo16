// Real per-page bundle entry for photos_add_direct.php's own
// Footer-mode script group (docs/PLAN.md P48) -- extended by each
// later batch that folds one more of this page's shared-library files
// into a real import here instead of a separate <script> tag.
import "../addAlbum?dup";
