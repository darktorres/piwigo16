// Real per-page bundle entry for plugins_installed.php (docs/PLAN.md
// P48) -- Footer-mode script group. plugins_installated.ts's own real `import` of
// plugins_installed_config.ts guarantees the config file's module
// evaluates first regardless of the order listed here.
import "../plugins_installed_config";
import "../plugins_installated";
