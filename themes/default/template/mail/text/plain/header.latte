{contentType text}
{$MAIL_TITLE}
{if !empty($MAIL_SUBTITLE)}{$MAIL_SUBTITLE}
{/if}
----
{*
  One of cat_group_info.latte / notification_by_mail.latte /
  notification_admin.latte is appended here next, followed by
  footer.latte -- all three rendered independently by real Latte and
  joined by raw PHP string concatenation in MailService.php, not
  {include}.
*}
