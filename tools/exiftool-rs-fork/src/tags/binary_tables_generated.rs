//! Auto-generated decoders for ExifTool's binary sub-tables.
//!
//! Do not edit: regenerate with
//! `perl scripts/gen_binary_tables.pl ../exiftool/lib > src/tags/binary_tables_generated.rs`.
//!
//! 283 tables, 4782 fields. A binary sub-table is a block of
//! bytes addressed by index: ExifTool's ProcessBinaryData reads the entry at
//! `(index - FIRST_ENTRY) * sizeof(FORMAT)`, and a field's own Format says
//! what to read there. What the generator could not express is on its stderr.
//! Generated code: the shape of a table decides what is written, so a helper
//! no table happens to need and a cast that happens to be a no-op are both
//! ordinary here rather than something to tidy away by hand.
#![allow(dead_code, unused_parens, unused_mut, unused_variables)]
#![allow(
    clippy::too_many_arguments,
    clippy::useless_conversion,
    clippy::double_parens
)]
#![allow(
    clippy::too_many_lines,
    clippy::match_same_arms,
    clippy::unreadable_literal,
    clippy::unnecessary_cast,
    clippy::identity_op,
    clippy::cast_lossless
)]

use std::sync::LazyLock;

use regex_lite::Regex;

use crate::tag::{Tag, TagGroup, TagId};
use crate::tags::conv_expr::{self, Val as Conv};
use crate::value::Value;

/// Which end the file puts first.
pub type ByteOrder = crate::metadata::exif::ByteOrderMark;

/// What the fields of this block have read so far, by the name ExifTool
/// stores them under.
/// A member is whatever the field read: a Nikon FirmwareVersion is a string,
/// and the conditions on the Z9's custom settings compare it to one.
pub type State = Vec<(String, Conv)>;

fn dm_val<'a>(dm: &'a State, name: &str) -> Option<&'a Conv> {
    dm.iter().rev().find(|(n, _)| n == name).map(|(_, v)| v)
}

fn dm_get(dm: &State, name: &str) -> Option<f64> {
    dm_val(dm, name).map(Conv::as_num)
}

/// What a conversion of this block can ask the file about.
///
/// ExifTool keeps these on the object: `$$self{Model}` tells one encoding of
/// TargetExposureTime from another, and `$$self{FILE_TYPE} eq "CRW"` decides
/// whether an ExposureTime of zero means one second or nothing at all.
struct Ctx<'a> {
    make: &'a str,
    model: &'a str,
    file_type: &'a str,
    dm: &'a State,
}

impl conv_expr::ParseState for Ctx<'_> {
    /// The reader options a conversion asks about. ExifTool's ByteUnit
    /// defaults to SI, and nothing here can set it to Binary.
    fn option(&self, name: &str) -> Option<Conv> {
        match name {
            "ByteUnit" => Some(Conv::Str("SI".to_string())),
            _ => None,
        }
    }

    fn member(&self, name: &str) -> Option<Conv> {
        match name {
            "Make" => Some(Conv::Str(self.make.to_string())),
            "Model" => Some(Conv::Str(self.model.to_string())),
            "FILE_TYPE" | "FileType" => Some(Conv::Str(self.file_type.to_string())),
            _ => dm_val(self.dm, name).cloned(),
        }
    }
}

fn u8_at(d: &[u8], o: usize) -> Option<u8> {
    d.get(o).copied()
}
fn i8_at(d: &[u8], o: usize) -> Option<i8> {
    d.get(o).map(|b| *b as i8)
}

fn u16_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<u16> {
    let b = [*d.get(o)?, *d.get(o + 1)?];
    Some(if bo == ByteOrder::BigEndian {
        u16::from_be_bytes(b)
    } else {
        u16::from_le_bytes(b)
    })
}
fn i16_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<i16> {
    u16_at(d, o, bo).map(|v| v as i16)
}

fn u32_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<u32> {
    let b = [*d.get(o)?, *d.get(o + 1)?, *d.get(o + 2)?, *d.get(o + 3)?];
    Some(if bo == ByteOrder::BigEndian {
        u32::from_be_bytes(b)
    } else {
        u32::from_le_bytes(b)
    })
}
fn i32_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<i32> {
    u32_at(d, o, bo).map(|v| v as i32)
}

fn f32_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<f32> {
    let b = [*d.get(o)?, *d.get(o + 1)?, *d.get(o + 2)?, *d.get(o + 3)?];
    Some(if bo == ByteOrder::BigEndian {
        f32::from_be_bytes(b)
    } else {
        f32::from_le_bytes(b)
    })
}

fn f64_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<f64> {
    let mut b = [0u8; 8];
    b.copy_from_slice(d.get(o..o + 8)?);
    Some(if bo == ByteOrder::BigEndian {
        f64::from_be_bytes(b)
    } else {
        f64::from_le_bytes(b)
    })
}

/// A 16-bit value stored the other way round from the rest of the block.
fn u16rev_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<u16> {
    let other = if bo == ByteOrder::BigEndian {
        ByteOrder::LittleEndian
    } else {
        ByteOrder::BigEndian
    };
    u16_at(d, o, other)
}

/// ExifTool's rational32 is two 16-bit halves -- four bytes, not the eight of
/// the rational64 EXIF writes. A zero denominator reads as infinity, and 0/0
/// as nothing at all.
fn rat32u_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<f64> {
    ratio(
        f64::from(u16_at(d, o, bo)?),
        f64::from(u16_at(d, o + 2, bo)?),
    )
}
fn rat32s_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<f64> {
    ratio(
        f64::from(i16_at(d, o, bo)?),
        f64::from(i16_at(d, o + 2, bo)?),
    )
}
/// The eight-byte rational EXIF writes: two 32-bit halves.
fn rat64u_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<f64> {
    ratio(
        f64::from(u32_at(d, o, bo)?),
        f64::from(u32_at(d, o + 4, bo)?),
    )
}
fn rat64s_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<f64> {
    ratio(
        f64::from(i32_at(d, o, bo)?),
        f64::from(i32_at(d, o + 4, bo)?),
    )
}

/// A 32-bit fixed-point value: the number over 65536.
fn fix32u_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<f64> {
    u32_at(d, o, bo).map(|v| f64::from(v) / 65536.0)
}
fn fix32s_at(d: &[u8], o: usize, bo: ByteOrder) -> Option<f64> {
    i32_at(d, o, bo).map(|v| f64::from(v) / 65536.0)
}

fn ratio(n: f64, d: f64) -> Option<f64> {
    if d == 0.0 {
        return if n == 0.0 { None } else { Some(f64::INFINITY) };
    }
    Some(n / d)
}

/// N bytes as UTF-16 text, in the block's byte order.
fn utf16_at(d: &[u8], o: usize, n: usize, bo: ByteOrder) -> Option<String> {
    let raw = d.get(o..o + n)?;
    let units: Vec<u16> = raw
        .chunks_exact(2)
        .map(|c| {
            if bo == ByteOrder::BigEndian {
                u16::from_be_bytes([c[0], c[1]])
            } else {
                u16::from_le_bytes([c[0], c[1]])
            }
        })
        .take_while(|u| *u != 0)
        .collect();
    Some(String::from_utf16_lossy(&units))
}

/// N bytes as text. A `string` stops at its first NUL, as ExifTool's reader
/// does; an `undef` is the bytes as they are.
fn text_at(d: &[u8], o: usize, n: usize, stop_at_nul: bool) -> Option<String> {
    let raw = d.get(o..o + n)?;
    let end = if stop_at_nul {
        raw.iter().position(|b| *b == 0).unwrap_or(raw.len())
    } else {
        raw.len()
    };
    Some(raw[..end].iter().map(|b| *b as char).collect())
}

/// Whether the block opens with these bytes. A position is `None` when the
/// pattern accepts anything there, and otherwise a set of ranges the byte has
/// to fall in -- `[ ]` is four of them.
type BytePat<'a> = &'a [Option<&'a [(u8, u8)]>];

fn prefix_matches(d: &[u8], pat: BytePat) -> bool {
    if d.len() < pat.len() {
        return false;
    }
    pat.iter()
        .zip(d)
        .all(|(p, b)| p.is_none_or(|set| set.iter().any(|(lo, hi)| *b >= *lo && *b <= *hi)))
}

/// Whether the block opens the way any one of these patterns says.
fn prefix_matches_any(d: &[u8], pats: &[BytePat]) -> bool {
    pats.iter().any(|p| prefix_matches(d, p))
}

fn mk(
    name: &str,
    id: u16,
    print_value: String,
    raw: Value,
    grp0: &'static str,
    grp1: &'static str,
    grp2: &'static str,
    priority: i32,
) -> Tag {
    Tag {
        id: TagId::Numeric(id),
        name: name.to_string(),
        description: name.to_string(),
        group: TagGroup {
            family0: grp0.into(),
            family1: grp1.into(),
            family2: grp2.into(),
            family3: crate::tag::MAIN_DOCUMENT.into(),
        },
        raw_value: raw,
        print_value,
        priority,
    }
}

static MODEL_RE_0: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS").expect("generated pattern"));
static MODEL_RE_1: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS-1DS?$").expect("generated pattern"));
static MODEL_RE_2: LazyLock<Regex> = LazyLock::new(|| {
    Regex::new("\\b(20D|350D|REBEL XT|Kiss Digital N)\\b").expect("generated pattern")
});
static MODEL_RE_3: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(1D|5D)").expect("generated pattern"));
static MODEL_RE_4: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS R\\d").expect("generated pattern"));
static MODEL_RE_5: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("5D ").expect("generated pattern"));
static MODEL_RE_6: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS-1D X|EOS R").expect("generated pattern"));
static MODEL_RE_7: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b1D$").expect("generated pattern"));
static MODEL_RE_8: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b1DS$").expect("generated pattern"));
static MODEL_RE_9: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b1D Mark III$").expect("generated pattern"));
static MODEL_RE_10: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS 60D$").expect("generated pattern"));
static MODEL_RE_11: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(1200D|REBEL T5|Kiss X70)\\b").expect("generated pattern"));
static MODEL_RE_12: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("(650D|REBEL T4i|Kiss X6i)\\b").expect("generated pattern"));
static MODEL_RE_13: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("(700D|REBEL T5i|Kiss X7i)\\b").expect("generated pattern"));
static MODEL_RE_14: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("^Canon EOS 5DS").expect("generated pattern"));
static MODEL_RE_15: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("(?i)Kodak").expect("generated pattern"));
static MODEL_RE_16: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("(?i)^NIKON Z6_3\\b").expect("generated pattern"));
static MODEL_RE_17: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("(?i)^NIKON Z f\\b").expect("generated pattern"));
static MODEL_RE_18: LazyLock<Regex> = LazyLock::new(|| {
    Regex::new("(?i)^NIKON Z (30|5|50|6|6_2|7|7_2|8|fc)\\b").expect("generated pattern")
});
static MODEL_RE_19: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("P6000\\b").expect("generated pattern"));
static MODEL_RE_20: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\bD500\\b").expect("generated pattern"));
static MODEL_RE_21: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\bD5\\b").expect("generated pattern"));
static MODEL_RE_22: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("^NIKON 7").expect("generated pattern"));
static MODEL_RE_23: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("D3S\\b").expect("generated pattern"));
static MODEL_RE_24: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("D3[SX]?\\b").expect("generated pattern"));
static MODEL_RE_25: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("D300S?\\b").expect("generated pattern"));
static MODEL_RE_26: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("(D3S|D300S)\\b").expect("generated pattern"));
static MODEL_RE_27: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\bD4S").expect("generated pattern"));
static MODEL_RE_28: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("E-(3|5|30)\\b").expect("generated pattern"));
static MODEL_RE_29: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("E-(520|600|620)\\b").expect("generated pattern"));
static MODEL_RE_30: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("^(E-M|OM-)").expect("generated pattern"));
static MODEL_RE_31: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("^E-M|^OM-").expect("generated pattern"));
static MODEL_RE_32: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b1DS?$").expect("generated pattern"));
static MODEL_RE_33: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b1Ds? Mark II$").expect("generated pattern"));
static MODEL_RE_34: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b1Ds? Mark II N$").expect("generated pattern"));
static MODEL_RE_35: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b1Ds? Mark III$").expect("generated pattern"));
static MODEL_RE_36: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b1D Mark IV$").expect("generated pattern"));
static MODEL_RE_37: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS-1D X$").expect("generated pattern"));
static MODEL_RE_38: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS 5D$").expect("generated pattern"));
static MODEL_RE_39: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS 5D Mark II$").expect("generated pattern"));
static MODEL_RE_40: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS 5D Mark III$").expect("generated pattern"));
static MODEL_RE_41: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS 6D$").expect("generated pattern"));
static MODEL_RE_42: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS 7D$").expect("generated pattern"));
static MODEL_RE_43: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS 40D$").expect("generated pattern"));
static MODEL_RE_44: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS 50D$").expect("generated pattern"));
static MODEL_RE_45: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS 70D$").expect("generated pattern"));
static MODEL_RE_46: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS 80D$").expect("generated pattern"));
static MODEL_RE_47: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(450D|REBEL XSi|Kiss X2)\\b").expect("generated pattern"));
static MODEL_RE_48: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(500D|REBEL T1i|Kiss X3)\\b").expect("generated pattern"));
static MODEL_RE_49: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(550D|REBEL T2i|Kiss X4)\\b").expect("generated pattern"));
static MODEL_RE_50: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(600D|REBEL T3i|Kiss X5)\\b").expect("generated pattern"));
static MODEL_RE_51: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(650D|REBEL T4i|Kiss X6i)\\b").expect("generated pattern"));
static MODEL_RE_52: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(700D|REBEL T5i|Kiss X7i)\\b").expect("generated pattern"));
static MODEL_RE_53: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(750D|Rebel T6i|Kiss X8i)\\b").expect("generated pattern"));
static MODEL_RE_54: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(760D|Rebel T6s|8000D)\\b").expect("generated pattern"));
static MODEL_RE_55: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(1000D|REBEL XS|Kiss F)\\b").expect("generated pattern"));
static MODEL_RE_56: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\b(1100D|REBEL T3|Kiss X50)\\b").expect("generated pattern"));
static MODEL_RE_57: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\bEOS R[56]$").expect("generated pattern"));
static MODEL_RE_58: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\bEOS (R6m2|R8|R50)$").expect("generated pattern"));
static MODEL_RE_59: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\bEOS R6 Mark III$").expect("generated pattern"));
static MODEL_RE_60: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("\\bG5 X Mark II$").expect("generated pattern"));
static MODEL_RE_61: LazyLock<Regex> =
    LazyLock::new(|| Regex::new("EOS 5D").expect("generated pattern"));

/// Decode one binary sub-table by the name ExifTool gives it, module first:
/// `Canon::ShotInfo`. Two modules can name a table the same thing.
#[must_use]
pub fn decode(
    table: &str,
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    match table {
        "Canon::ColorData1" => canon_colordata1(data, make, model, bo, file_type, format, dm),
        "Canon::ColorData2" => canon_colordata2(data, make, model, bo, file_type, format, dm),
        "Canon::ColorData3" => canon_colordata3(data, make, model, bo, file_type, format, dm),
        "Canon::ColorData4" => canon_colordata4(data, make, model, bo, file_type, format, dm),
        "Canon::ColorData5" => canon_colordata5(data, make, model, bo, file_type, format, dm),
        "Canon::ColorData6" => canon_colordata6(data, make, model, bo, file_type, format, dm),
        "Canon::ColorData7" => canon_colordata7(data, make, model, bo, file_type, format, dm),
        "Canon::ColorData8" => canon_colordata8(data, make, model, bo, file_type, format, dm),
        "Canon::ColorData9" => canon_colordata9(data, make, model, bo, file_type, format, dm),
        "Canon::ColorData10" => canon_colordata10(data, make, model, bo, file_type, format, dm),
        "Canon::ColorData11" => canon_colordata11(data, make, model, bo, file_type, format, dm),
        "Canon::ColorData12" => canon_colordata12(data, make, model, bo, file_type, format, dm),
        "Canon::ColorDataUnknown" => {
            canon_colordataunknown(data, make, model, bo, file_type, format, dm)
        }
        "Canon::ShotInfo" => canon_shotinfo(data, make, model, bo, file_type, format, dm),
        "Canon::RawBurstInfo" => canon_rawburstinfo(data, make, model, bo, file_type, format, dm),
        "Canon::IAD1" => canon_iad1(data, make, model, bo, file_type, format, dm),
        "Canon::Panorama" => canon_panorama(data, make, model, bo, file_type, format, dm),
        "Canon::UnknownD30" => canon_unknownd30(data, make, model, bo, file_type, format, dm),
        "Canon::MovieInfo" => canon_movieinfo(data, make, model, bo, file_type, format, dm),
        "Canon::MyColors" => canon_mycolors(data, make, model, bo, file_type, format, dm),
        "Canon::FaceDetect1" => canon_facedetect1(data, make, model, bo, file_type, format, dm),
        "Canon::FaceDetect2" => canon_facedetect2(data, make, model, bo, file_type, format, dm),
        "Canon::FaceDetect3" => canon_facedetect3(data, make, model, bo, file_type, format, dm),
        "Canon::WBInfo" => canon_wbinfo(data, make, model, bo, file_type, format, dm),
        "Canon::TimeInfo" => canon_timeinfo(data, make, model, bo, file_type, format, dm),
        "Canon::SerialInfo" => canon_serialinfo(data, make, model, bo, file_type, format, dm),
        "Canon::MeasuredColor" => canon_measuredcolor(data, make, model, bo, file_type, format, dm),
        "Canon::Flags" => canon_flags(data, make, model, bo, file_type, format, dm),
        "Canon::ModifiedInfo" => canon_modifiedinfo(data, make, model, bo, file_type, format, dm),
        "Canon::PreviewImageInfo" => {
            canon_previewimageinfo(data, make, model, bo, file_type, format, dm)
        }
        "Canon::AFMicroAdj" => canon_afmicroadj(data, make, model, bo, file_type, format, dm),
        "Canon::VignettingCorr" => {
            canon_vignettingcorr(data, make, model, bo, file_type, format, dm)
        }
        "Canon::VignettingCorr2" => {
            canon_vignettingcorr2(data, make, model, bo, file_type, format, dm)
        }
        "Canon::VignettingCorrUnknown" => {
            canon_vignettingcorrunknown(data, make, model, bo, file_type, format, dm)
        }
        "Canon::LightingOpt" => canon_lightingopt(data, make, model, bo, file_type, format, dm),
        "Canon::Ambience" => canon_ambience(data, make, model, bo, file_type, format, dm),
        "Canon::MultiExp" => canon_multiexp(data, make, model, bo, file_type, format, dm),
        "Canon::HDRInfo" => canon_hdrinfo(data, make, model, bo, file_type, format, dm),
        "Canon::LogInfo" => canon_loginfo(data, make, model, bo, file_type, format, dm),
        "Canon::AFConfig" => canon_afconfig(data, make, model, bo, file_type, format, dm),
        "Canon::FocusBracketingInfo" => {
            canon_focusbracketinginfo(data, make, model, bo, file_type, format, dm)
        }
        "Canon::LevelInfo" => canon_levelinfo(data, make, model, bo, file_type, format, dm),
        "Canon::FocalInfo" => canon_focalinfo(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfo1D" => canon_camerainfo1d(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfo1DmkII" => {
            canon_camerainfo1dmkii(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo1DmkIIN" => {
            canon_camerainfo1dmkiin(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo1DmkIII" => {
            canon_camerainfo1dmkiii(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo1DmkIV" => {
            canon_camerainfo1dmkiv(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo1DX" => canon_camerainfo1dx(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfo5D" => canon_camerainfo5d(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfo5DmkII" => {
            canon_camerainfo5dmkii(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo5DmkIII" => {
            canon_camerainfo5dmkiii(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo6D" => canon_camerainfo6d(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfo7D" => canon_camerainfo7d(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfo40D" => canon_camerainfo40d(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfo50D" => canon_camerainfo50d(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfo60D" => canon_camerainfo60d(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfo70D" => canon_camerainfo70d(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfo80D" => canon_camerainfo80d(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfo450D" => {
            canon_camerainfo450d(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo500D" => {
            canon_camerainfo500d(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo550D" => {
            canon_camerainfo550d(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo600D" => {
            canon_camerainfo600d(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo650D" => {
            canon_camerainfo650d(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo750D" => {
            canon_camerainfo750d(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfo1000D" => {
            canon_camerainfo1000d(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfoR6" => canon_camerainfor6(data, make, model, bo, file_type, format, dm),
        "Canon::CameraInfoR6m2" => {
            canon_camerainfor6m2(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfoR6m3" => {
            canon_camerainfor6m3(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfoG5XII" => {
            canon_camerainfog5xii(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfoPowerShot" => {
            canon_camerainfopowershot(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfoPowerShot2" => {
            canon_camerainfopowershot2(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfoUnknown32" => {
            canon_camerainfounknown32(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfoUnknown16" => {
            canon_camerainfounknown16(data, make, model, bo, file_type, format, dm)
        }
        "Canon::CameraInfoUnknown" => {
            canon_camerainfounknown(data, make, model, bo, file_type, format, dm)
        }
        "Canon::ColorCalib" => canon_colorcalib(data, make, model, bo, file_type, format, dm),
        "Canon::ColorCoefs" => canon_colorcoefs(data, make, model, bo, file_type, format, dm),
        "Canon::ColorCoefs2" => canon_colorcoefs2(data, make, model, bo, file_type, format, dm),
        "Canon::ColorCalib2" => canon_colorcalib2(data, make, model, bo, file_type, format, dm),
        "Canon::PSInfo" => canon_psinfo(data, make, model, bo, file_type, format, dm),
        "Canon::PSInfo2" => canon_psinfo2(data, make, model, bo, file_type, format, dm),
        "CanonCustom::PersonalFuncs" => {
            canoncustom_personalfuncs(data, make, model, bo, file_type, format, dm)
        }
        "CanonCustom::PersonalFuncValues" => {
            canoncustom_personalfuncvalues(data, make, model, bo, file_type, format, dm)
        }
        "CanonRaw::WhiteSample" => {
            canonraw_whitesample(data, make, model, bo, file_type, format, dm)
        }
        "CanonVRD::DustInfo" => canonvrd_dustinfo(data, make, model, bo, file_type, format, dm),
        "CanonVRD::DLOInfo" => canonvrd_dloinfo(data, make, model, bo, file_type, format, dm),
        "Casio::FaceInfo1" => casio_faceinfo1(data, make, model, bo, file_type, format, dm),
        "Casio::FaceInfo2" => casio_faceinfo2(data, make, model, bo, file_type, format, dm),
        "Casio::QVCI" => casio_qvci(data, make, model, bo, file_type, format, dm),
        "Casio::AVI" => casio_avi(data, make, model, bo, file_type, format, dm),
        "FLIR::GainDeadData" => flir_gaindeaddata(data, make, model, bo, file_type, format, dm),
        "FLIR::CoarseData" => flir_coarsedata(data, make, model, bo, file_type, format, dm),
        "FLIR::PaintData" => flir_paintdata(data, make, model, bo, file_type, format, dm),
        "FLIR::SerialNums" => flir_serialnums(data, make, model, bo, file_type, format, dm),
        "FLIR::UnknownUUID" => flir_unknownuuid(data, make, model, bo, file_type, format, dm),
        "FLIR::GPS_UUID" => flir_gps_uuid(data, make, model, bo, file_type, format, dm),
        "FLIR::AFF1" => flir_aff1(data, make, model, bo, file_type, format, dm),
        "FLIR::AFF5" => flir_aff5(data, make, model, bo, file_type, format, dm),
        "FLIR::EmbeddedImage" => flir_embeddedimage(data, make, model, bo, file_type, format, dm),
        "FLIR::GPSInfo" => flir_gpsinfo(data, make, model, bo, file_type, format, dm),
        "FLIR::Header" => flir_header(data, make, model, bo, file_type, format, dm),
        "FLIR::MeterLink" => flir_meterlink(data, make, model, bo, file_type, format, dm),
        "FLIR::MoreInfo" => flir_moreinfo(data, make, model, bo, file_type, format, dm),
        "FLIR::Params" => flir_params(data, make, model, bo, file_type, format, dm),
        "FLIR::PiP" => flir_pip(data, make, model, bo, file_type, format, dm),
        "FlashPix::PreviewInfo" => {
            flashpix_previewinfo(data, make, model, bo, file_type, format, dm)
        }
        "FujiFilm::FFMV" => fujifilm_ffmv(data, make, model, bo, file_type, format, dm),
        "FujiFilm::RAFData" => fujifilm_rafdata(data, make, model, bo, file_type, format, dm),
        "FujiFilm::MOV" => fujifilm_mov(data, make, model, bo, file_type, format, dm),
        "H264::Camera1" => h264_camera1(data, make, model, bo, file_type, format, dm),
        "H264::Camera2" => h264_camera2(data, make, model, bo, file_type, format, dm),
        "H264::FrameInfo" => h264_frameinfo(data, make, model, bo, file_type, format, dm),
        "H264::MakeModel" => h264_makemodel(data, make, model, bo, file_type, format, dm),
        "H264::RecInfo" => h264_recinfo(data, make, model, bo, file_type, format, dm),
        "JPEG::Ocad" => jpeg_ocad(data, make, model, bo, file_type, format, dm),
        "Kodak::Type9" => kodak_type9(data, make, model, bo, file_type, format, dm),
        "Kodak::Type2" => kodak_type2(data, make, model, bo, file_type, format, dm),
        "Kodak::Type3" => kodak_type3(data, make, model, bo, file_type, format, dm),
        "Kodak::Type4" => kodak_type4(data, make, model, bo, file_type, format, dm),
        "Kodak::Type5" => kodak_type5(data, make, model, bo, file_type, format, dm),
        "Kodak::Type6" => kodak_type6(data, make, model, bo, file_type, format, dm),
        "Kodak::Type7" => kodak_type7(data, make, model, bo, file_type, format, dm),
        "Kodak::Processing" => kodak_processing(data, make, model, bo, file_type, format, dm),
        "Kodak::Unknown" => kodak_unknown(data, make, model, bo, file_type, format, dm),
        "Kodak::MOV" => kodak_mov(data, make, model, bo, file_type, format, dm),
        "Microsoft::Stitch" => microsoft_stitch(data, make, model, bo, file_type, format, dm),
        "Minolta::CameraSettings7D" => {
            minolta_camerasettings7d(data, make, model, bo, file_type, format, dm)
        }
        "Minolta::CameraInfoA100" => {
            minolta_camerainfoa100(data, make, model, bo, file_type, format, dm)
        }
        "Minolta::WBInfoA100" => minolta_wbinfoa100(data, make, model, bo, file_type, format, dm),
        "Minolta::MOV1" => minolta_mov1(data, make, model, bo, file_type, format, dm),
        "Minolta::MOV2" => minolta_mov2(data, make, model, bo, file_type, format, dm),
        "Minolta::CameraSettings5D" => {
            minolta_camerasettings5d(data, make, model, bo, file_type, format, dm)
        }
        "Minolta::CameraSettingsA100" => {
            minolta_camerasettingsa100(data, make, model, bo, file_type, format, dm)
        }
        "Minolta::ISInfoA100" => minolta_isinfoa100(data, make, model, bo, file_type, format, dm),
        "Nikon::LensDataUnknown" => {
            nikon_lensdataunknown(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::ShotInfoD40" => nikon_shotinfod40(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD80" => nikon_shotinfod80(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD90" => nikon_shotinfod90(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD3a" => nikon_shotinfod3a(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD3b" => nikon_shotinfod3b(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD3X" => nikon_shotinfod3x(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD3S" => nikon_shotinfod3s(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD300a" => nikon_shotinfod300a(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD300b" => nikon_shotinfod300b(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD300S" => nikon_shotinfod300s(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD700" => nikon_shotinfod700(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD780" => nikon_shotinfod780(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD7500" => nikon_shotinfod7500(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD800" => nikon_shotinfod800(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD810" => nikon_shotinfod810(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD850" => nikon_shotinfod850(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD5000" => nikon_shotinfod5000(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD5100" => nikon_shotinfod5100(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD5200" => nikon_shotinfod5200(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD7000" => nikon_shotinfod7000(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD4" => nikon_shotinfod4(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD4S" => nikon_shotinfod4s(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD500" => nikon_shotinfod500(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD6" => nikon_shotinfod6(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoD610" => nikon_shotinfod610(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoZ6III" => nikon_shotinfoz6iii(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoZ7II" => nikon_shotinfoz7ii(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoZ8" => nikon_shotinfoz8(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfoZ9" => nikon_shotinfoz9(data, make, model, bo, file_type, format, dm),
        "Nikon::ShotInfo" => nikon_shotinfo(data, make, model, bo, file_type, format, dm),
        "Nikon::OrientationInfo" => {
            nikon_orientationinfo(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::SettingsInfoD810" => {
            nikon_settingsinfod810(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::BracketingInfoD810" => {
            nikon_bracketinginfod810(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::ISOAutoInfoD810" => {
            nikon_isoautoinfod810(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MenuSettingsD850" => {
            nikon_menusettingsd850(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MoreSettingsD850" => {
            nikon_moresettingsd850(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::RotationInfoD500" => {
            nikon_rotationinfod500(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::JPGInfoD500" => nikon_jpginfod500(data, make, model, bo, file_type, format, dm),
        "Nikon::BracketingInfoD500" => {
            nikon_bracketinginfod500(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::ShootingMenuD500" => {
            nikon_shootingmenud500(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::CustomSettingsD500" => {
            nikon_customsettingsd500(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::OtherInfoD500" => nikon_otherinfod500(data, make, model, bo, file_type, format, dm),
        "Nikon::SeqInfoD6" => nikon_seqinfod6(data, make, model, bo, file_type, format, dm),
        "Nikon::IntervalInfoD6" => {
            nikon_intervalinfod6(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MenuSettingsZ6III" => {
            nikon_menusettingsz6iii(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::IntervalInfoZ7II" => {
            nikon_intervalinfoz7ii(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::PortraitInfoZ7II" => {
            nikon_portraitinfoz7ii(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MenuInfoZ7II" => nikon_menuinfoz7ii(data, make, model, bo, file_type, format, dm),
        "Nikon::SeqInfoZ9" => nikon_seqinfoz9(data, make, model, bo, file_type, format, dm),
        "Nikon::AutoCaptureInfo" => {
            nikon_autocaptureinfo(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MenuInfoZ8" => nikon_menuinfoz8(data, make, model, bo, file_type, format, dm),
        "Nikon::Offset13InfoZ9" => {
            nikon_offset13infoz9(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MenuInfoZ9" => nikon_menuinfoz9(data, make, model, bo, file_type, format, dm),
        "Nikon::MenuSettingsZ7II" => {
            nikon_menusettingsz7ii(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MenuSettingsZ8v1" => {
            nikon_menusettingsz8v1(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MenuSettingsZ8v2" => {
            nikon_menusettingsz8v2(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MenuSettingsZ9" => {
            nikon_menusettingsz9(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MenuSettingsZ9v3" => {
            nikon_menusettingsz9v3(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MenuSettingsZ9v4" => {
            nikon_menusettingsz9v4(data, make, model, bo, file_type, format, dm)
        }
        "Nikon::MenuSettingsZ8" => {
            nikon_menusettingsz8(data, make, model, bo, file_type, format, dm)
        }
        "NikonCapture::DLightingHQ" => {
            nikoncapture_dlightinghq(data, make, model, bo, file_type, format, dm)
        }
        "NikonCapture::DLightingHS" => {
            nikoncapture_dlightinghs(data, make, model, bo, file_type, format, dm)
        }
        "NikonCapture::HighlightData" => {
            nikoncapture_highlightdata(data, make, model, bo, file_type, format, dm)
        }
        "NikonCapture::PictureCtrl" => {
            nikoncapture_picturectrl(data, make, model, bo, file_type, format, dm)
        }
        "NikonCapture::UnsharpData" => {
            nikoncapture_unsharpdata(data, make, model, bo, file_type, format, dm)
        }
        "NikonCapture::WBAdjData" => {
            nikoncapture_wbadjdata(data, make, model, bo, file_type, format, dm)
        }
        "NikonCapture::Brightness" => {
            nikoncapture_brightness(data, make, model, bo, file_type, format, dm)
        }
        "NikonCapture::ColorBoost" => {
            nikoncapture_colorboost(data, make, model, bo, file_type, format, dm)
        }
        "NikonCapture::Exposure" => {
            nikoncapture_exposure(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD40" => {
            nikoncustom_settingsd40(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD810" => {
            nikoncustom_settingsd810(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD850" => {
            nikoncustom_settingsd850(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsZ6III" => {
            nikoncustom_settingsz6iii(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsZ8" => {
            nikoncustom_settingsz8(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsZ9" => {
            nikoncustom_settingsz9(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsZ9v4" => {
            nikoncustom_settingsz9v4(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD80" => {
            nikoncustom_settingsd80(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD90" => {
            nikoncustom_settingsd90(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD3" => {
            nikoncustom_settingsd3(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD700" => {
            nikoncustom_settingsd700(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD800" => {
            nikoncustom_settingsd800(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD5000" => {
            nikoncustom_settingsd5000(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD5100" => {
            nikoncustom_settingsd5100(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD5200" => {
            nikoncustom_settingsd5200(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD7000" => {
            nikoncustom_settingsd7000(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD4" => {
            nikoncustom_settingsd4(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD610" => {
            nikoncustom_settingsd610(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD5" => {
            nikoncustom_settingsd5(data, make, model, bo, file_type, format, dm)
        }
        "NikonCustom::SettingsD500" => {
            nikoncustom_settingsd500(data, make, model, bo, file_type, format, dm)
        }
        "Nintendo::CameraInfo" => nintendo_camerainfo(data, make, model, bo, file_type, format, dm),
        "Olympus::MOV1" => olympus_mov1(data, make, model, bo, file_type, format, dm),
        "Olympus::MOV2" => olympus_mov2(data, make, model, bo, file_type, format, dm),
        "Olympus::prms" => olympus_prms(data, make, model, bo, file_type, format, dm),
        "Olympus::AFInfo" => olympus_afinfo(data, make, model, bo, file_type, format, dm),
        "Olympus::AFTargetInfo" => {
            olympus_aftargetinfo(data, make, model, bo, file_type, format, dm)
        }
        "Olympus::MovableInfo" => olympus_movableinfo(data, make, model, bo, file_type, format, dm),
        "Olympus::SubjectDetectInfo" => {
            olympus_subjectdetectinfo(data, make, model, bo, file_type, format, dm)
        }
        "Olympus::Thumbnail" => olympus_thumbnail(data, make, model, bo, file_type, format, dm),
        "Olympus::AVI" => olympus_avi(data, make, model, bo, file_type, format, dm),
        "Olympus::MP4" => olympus_mp4(data, make, model, bo, file_type, format, dm),
        "Olympus::WAV" => olympus_wav(data, make, model, bo, file_type, format, dm),
        "Olympus::FocusInfo" => olympus_focusinfo(data, make, model, bo, file_type, format, dm),
        "Olympus::CameraSettings" => {
            olympus_camerasettings(data, make, model, bo, file_type, format, dm)
        }
        "Olympus::thmb2" => olympus_thmb2(data, make, model, bo, file_type, format, dm),
        "Panasonic::Data1" => panasonic_data1(data, make, model, bo, file_type, format, dm),
        "Panasonic::Data2" => panasonic_data2(data, make, model, bo, file_type, format, dm),
        "Panasonic::FaceDetInfo" => {
            panasonic_facedetinfo(data, make, model, bo, file_type, format, dm)
        }
        "Panasonic::FaceRecInfo" => {
            panasonic_facerecinfo(data, make, model, bo, file_type, format, dm)
        }
        "Panasonic::FocusInfo" => panasonic_focusinfo(data, make, model, bo, file_type, format, dm),
        "Panasonic::SerialInfo" => {
            panasonic_serialinfo(data, make, model, bo, file_type, format, dm)
        }
        "Panasonic::ShotInfo" => panasonic_shotinfo(data, make, model, bo, file_type, format, dm),
        "Panasonic::TimeInfo" => panasonic_timeinfo(data, make, model, bo, file_type, format, dm),
        "Panasonic::Type2" => panasonic_type2(data, make, model, bo, file_type, format, dm),
        "PanasonicRaw::DistortionInfo" => {
            panasonicraw_distortioninfo(data, make, model, bo, file_type, format, dm)
        }
        "PanasonicRaw::WBInfo" => panasonicraw_wbinfo(data, make, model, bo, file_type, format, dm),
        "PanasonicRaw::WBInfo2" => {
            panasonicraw_wbinfo2(data, make, model, bo, file_type, format, dm)
        }
        "Parrot::ARCoreAccel" => parrot_arcoreaccel(data, make, model, bo, file_type, format, dm),
        "Parrot::ARCoreAccel0" => parrot_arcoreaccel0(data, make, model, bo, file_type, format, dm),
        "Parrot::ARCoreGyro" => parrot_arcoregyro(data, make, model, bo, file_type, format, dm),
        "Parrot::ARCoreGyro0" => parrot_arcoregyro0(data, make, model, bo, file_type, format, dm),
        "Parrot::ARCoreVideo" => parrot_arcorevideo(data, make, model, bo, file_type, format, dm),
        "Parrot::ARCoreCustom" => parrot_arcorecustom(data, make, model, bo, file_type, format, dm),
        "Pentax::Junk2" => pentax_junk2(data, make, model, bo, file_type, format, dm),
        "Pentax::CAFPointInfo" => pentax_cafpointinfo(data, make, model, bo, file_type, format, dm),
        "Photoshop::PixelInfo" => photoshop_pixelinfo(data, make, model, bo, file_type, format, dm),
        "QuickTime::HTCBinary" => quicktime_htcbinary(data, make, model, bo, file_type, format, dm),
        "QuickTime::AV1Config" => quicktime_av1config(data, make, model, bo, file_type, format, dm),
        "QuickTime::ContentLightLevel" => {
            quicktime_contentlightlevel(data, make, model, bo, file_type, format, dm)
        }
        "QuickTime::Flip" => quicktime_flip(data, make, model, bo, file_type, format, dm),
        "QuickTime::HEVCConfig" => {
            quicktime_hevcconfig(data, make, model, bo, file_type, format, dm)
        }
        "RIFF::AVIHeader" => riff_aviheader(data, make, model, bo, file_type, format, dm),
        "Reconyx::HyperFire" => reconyx_hyperfire(data, make, model, bo, file_type, format, dm),
        "Reconyx::HyperFire2" => reconyx_hyperfire2(data, make, model, bo, file_type, format, dm),
        "Reconyx::HyperFire4K" => reconyx_hyperfire4k(data, make, model, bo, file_type, format, dm),
        "Reconyx::MicroFire" => reconyx_microfire(data, make, model, bo, file_type, format, dm),
        "Reconyx::UltraFire" => reconyx_ultrafire(data, make, model, bo, file_type, format, dm),
        "Ricoh::FaceInfo" => ricoh_faceinfo(data, make, model, bo, file_type, format, dm),
        "Ricoh::AVI" => ricoh_avi(data, make, model, bo, file_type, format, dm),
        "Ricoh::Subdir" => ricoh_subdir(data, make, model, bo, file_type, format, dm),
        "Ricoh::FirmwareInfo" => ricoh_firmwareinfo(data, make, model, bo, file_type, format, dm),
        "Ricoh::SerialInfo" => ricoh_serialinfo(data, make, model, bo, file_type, format, dm),
        "Samsung::DualShotExtra" => {
            samsung_dualshotextra(data, make, model, bo, file_type, format, dm)
        }
        "Samsung::Thumbnail2" => samsung_thumbnail2(data, make, model, bo, file_type, format, dm),
        "Samsung::Thumbnail" => samsung_thumbnail(data, make, model, bo, file_type, format, dm),
        "Samsung::OrientationInfo" => {
            samsung_orientationinfo(data, make, model, bo, file_type, format, dm)
        }
        "Samsung::PictureWizard" => {
            samsung_picturewizard(data, make, model, bo, file_type, format, dm)
        }
        "Sanyo::FaceInfo" => sanyo_faceinfo(data, make, model, bo, file_type, format, dm),
        "Sanyo::Thumbnail" => sanyo_thumbnail(data, make, model, bo, file_type, format, dm),
        _ => Vec::new(),
    }
}

/// `Image::ExifTool::Canon::ColorData1` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata1(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x32 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x19,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x3a, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x1d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x3c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAuto",
                0x1e,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x44, bo) {
        dm.push(("ColorTempAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAuto",
            0x22,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x46 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0x23,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x4e, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0x27,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x50 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0x28,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x58, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0x2c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x5a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0x2d,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x62, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0x31,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x64 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0x32,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x6c, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0x36,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x6e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0x37,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x76, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0x3b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x78 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0x3c,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x80, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0x40,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x82 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCustom1",
                0x41,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x8a, bo) {
        dm.push(("ColorTempCustom1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCustom1",
            0x45,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x8c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCustom2",
                0x46,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x94, bo) {
        dm.push(("ColorTempCustom2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCustom2",
            0x4a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x96..0x96 + 120) {
        tags.extend(canon_colorcalib(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorData2` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x30 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAuto",
                0x18,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x38, bo) {
        dm.push(("ColorTempAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAuto",
            0x1c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x3a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x42, bo) {
        dm.push(("ColorTempUnknown".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x44 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x22,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x4c, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x26,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x4e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0x27,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x56, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0x2b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x58 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0x2c,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x60, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0x30,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x62 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0x31,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x6a, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0x35,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x6c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0x36,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x74, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0x3a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x76 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0x3b,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x7e, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0x3f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x80 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsKelvin",
                0x40,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x88, bo) {
        dm.push(("ColorTempKelvin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempKelvin",
            0x44,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x8a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0x45,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x92, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0x49,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x94 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x9c, bo) {
        dm.push(("ColorTempUnknown2".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x9e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xa6, bo) {
        dm.push(("ColorTempUnknown3".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xa8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xb0, bo) {
        dm.push(("ColorTempUnknown4".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xb2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xba, bo) {
        dm.push(("ColorTempUnknown5".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xbc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xc4, bo) {
        dm.push(("ColorTempUnknown6".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xc6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xce, bo) {
        dm.push(("ColorTempUnknown7".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xd0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xd8, bo) {
        dm.push(("ColorTempUnknown8".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xda + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xe2, bo) {
        dm.push(("ColorTempUnknown9".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xe4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xec, bo) {
        dm.push(("ColorTempUnknown10".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xee + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xf6, bo) {
        dm.push(("ColorTempUnknown11".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xf8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x100, bo) {
        dm.push(("ColorTempUnknown12".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x102 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x10a, bo) {
        dm.push(("ColorTempUnknown13".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x10c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x114, bo) {
        dm.push(("ColorTempUnknown14".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x116 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x11e, bo) {
        dm.push(("ColorTempUnknown15".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x120 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsPC1",
                0x90,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x128, bo) {
        dm.push(("ColorTempPC1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempPC1",
            0x94,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x12a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsPC2",
                0x95,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x132, bo) {
        dm.push(("ColorTempPC2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempPC2",
            0x99,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x134 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsPC3",
                0x9a,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x13c, bo) {
        dm.push(("ColorTempPC3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempPC3",
            0x9e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x13e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x146, bo) {
        dm.push(("ColorTempUnknown16".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u32_at(data, 0x4d4 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Canon::SwapWords($val)", &cv, &ctx)
            {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "RawMeasuredRGGB",
                0x26a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x148..0x148 + 120) {
        tags.extend(canon_colorcalib(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorData3` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata3(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ColorDataVersion".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "1 (1DmkIIN/5D/30D/400D)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorDataVersion", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x7e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x3f,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x86, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x43,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x88 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAuto",
                0x44,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x90, bo) {
        dm.push(("ColorTempAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAuto",
            0x48,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x92 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsMeasured",
                0x49,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x9a, bo) {
        dm.push(("ColorTempMeasured".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempMeasured",
            0x4d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x9c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0x4e,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xa4, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0x52,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xa6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0x53,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xae, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0x57,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xb0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0x58,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xb8, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0x5c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xba + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0x5d,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xc2, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0x61,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xc4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0x62,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xcc, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0x66,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xce + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsKelvin",
                0x67,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xd6, bo) {
        dm.push(("ColorTempKelvin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempKelvin",
            0x6b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xd8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0x6c,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xe0, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0x70,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xe2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsPC1",
                0x71,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xea, bo) {
        dm.push(("ColorTempPC1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempPC1",
            0x75,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xec + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsPC2",
                0x76,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xf4, bo) {
        dm.push(("ColorTempPC2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempPC2",
            0x7a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xf6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsPC3",
                0x7b,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xfe, bo) {
        dm.push(("ColorTempPC3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempPC3",
            0x7f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x100 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCustom",
                0x80,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x108, bo) {
        dm.push(("ColorTempCustom".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCustom",
            0x84,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x188 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "PerChannelBlackLevel",
                0xc4,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x490, bo) {
        dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val >= 255 ? 255 : exp(($val-200)/16*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val == 255 ? \"Strobe or Misfire\" : sprintf(\"%.0f%%\", $val * 100)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "FlashOutput",
                0x248,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x492, bo) {
        dm.push(("FlashBatteryLevel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "$val ? sprintf(\"%.2fV\", $val * 5 / 186) : \"n/a\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "FlashBatteryLevel",
            0x249,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x494, bo) {
        dm.push(("ColorTempFlashData".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("($val < 2000 or $val > 12000) ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ColorTempFlashData",
                0x24a,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u32_at(data, 0x50e + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Canon::SwapWords($val)", &cv, &ctx)
            {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "MeasuredRGGBData",
                0x287,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x10a..0x10a + 120) {
        tags.extend(canon_colorcalib(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorData4` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata4(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ColorDataVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                2 => "2 (1DmkIII)".to_string(),
                3 => "3 (40D)".to_string(),
                4 => "4 (1DSmkIII)".to_string(),
                5 => "5 (450D/1000D)".to_string(),
                6 => "6 (50D/5DmkII)".to_string(),
                7 => "7 (500D/550D/7D/1DmkIV)".to_string(),
                9 => "9 (60D/1100D)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ColorDataVersion", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x1ce + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "AverageBlackLevel",
                0xe7,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x4d6, bo) {
        dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val >= 255 ? 255 : exp(($val-200)/16*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val == 255 ? \"Strobe or Misfire\" : sprintf(\"%.0f%%\", $val * 100)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "FlashOutput",
                0x26b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x4d8, bo) {
        dm.push(("FlashBatteryLevel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "$val ? sprintf(\"%.2fV\", $val * 5 / 186) : \"n/a\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "FlashBatteryLevel",
            0x26c,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u32_at(data, 0x500 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Canon::SwapWords($val)", &cv, &ctx)
            {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "RawMeasuredRGGB",
                0x280,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 4.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 5.0))
    {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x568 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "PerChannelBlackLevel",
                    0x2b4,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 4.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 5.0))
    {
        if let Some(v) = u16_at(data, 0x570, bo) {
            dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "NormalWhiteLevel",
                    0x2b8,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 4.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 5.0))
    {
        if let Some(v) = u16_at(data, 0x572, bo) {
            dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SpecularWhiteLevel",
                0x2b9,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 4.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 5.0))
    {
        if let Some(v) = u16_at(data, 0x574, bo) {
            dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "LinearityUpperMargin",
                0x2ba,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 6.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 7.0))
    {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x596 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "PerChannelBlackLevel",
                    0x2cb,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 6.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 7.0))
    {
        if let Some(v) = u16_at(data, 0x59e, bo) {
            dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "NormalWhiteLevel",
                    0x2cf,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if !(dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 6.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 7.0))
        && dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 9.0)
    {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x59e + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "PerChannelBlackLevel",
                    0x2cf,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 6.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 7.0))
    {
        if let Some(v) = u16_at(data, 0x5a0, bo) {
            dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SpecularWhiteLevel",
                0x2d0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 6.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 7.0))
    {
        if let Some(v) = u16_at(data, 0x5a2, bo) {
            dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "LinearityUpperMargin",
                0x2d1,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 9.0) {
        if let Some(v) = u16_at(data, 0x5a6, bo) {
            dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "NormalWhiteLevel",
                    0x2d3,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 9.0) {
        if let Some(v) = u16_at(data, 0x5a8, bo) {
            dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SpecularWhiteLevel",
                0x2d4,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 9.0) {
        if let Some(v) = u16_at(data, 0x5aa, bo) {
            dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "LinearityUpperMargin",
                0x2d5,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x7e..0x7e + 210) {
        tags.extend(canon_colorcoefs(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = data.get(0x150..0x150 + 120) {
        tags.extend(canon_colorcalib(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorData5` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata5(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ColorDataVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                -4 => "-4 (M100/M5/M6)".to_string(),
                -3 => "-3 (M10/M3)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ColorDataVersion", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == -3.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match i16_at(data, 0x210 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "PerChannelBlackLevel",
                    0x108,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == -4.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match i16_at(data, 0x29a + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "PerChannelBlackLevel",
                    0x14d,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == -3.0) {
        if let Some(v) = u16_at(data, 0x52c, bo) {
            dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SpecularWhiteLevel",
                0x296,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == -4.0) {
        if let Some(v) = u16_at(data, 0xad2, bo) {
            dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "NormalWhiteLevel",
                0x569,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == -4.0) {
        if let Some(v) = u16_at(data, 0xad4, bo) {
            dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SpecularWhiteLevel",
                0x56a,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == -3.0) {
        if let Some(sub) = data.get(0x8e..0x8e + 230) {
            tags.extend(canon_colorcoefs(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if !(dm_get(dm, "ColorDataVersion").is_some_and(|v| v == -3.0))
        && dm_get(dm, "ColorDataVersion").is_some_and(|v| v == -4.0)
    {
        if let Some(sub) = data.get(0x8e..0x8e + 368) {
            tags.extend(canon_colorcoefs2(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == -3.0) {
        if let Some(sub) = data.get(0x174..0x174 + 150) {
            tags.extend(canon_colorcalib2(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == -4.0) {
        if let Some(sub) = data.get(0x1fe..0x1fe + 150) {
            tags.extend(canon_colorcalib2(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::ColorData6` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata6(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ColorDataVersion".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            10 => "10 (600D/1200D)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorDataVersion", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x7e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x3f,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x86, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x43,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x88 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAuto",
                0x44,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x90, bo) {
        dm.push(("ColorTempAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAuto",
            0x48,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x92 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsMeasured",
                0x49,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x9a, bo) {
        dm.push(("ColorTempMeasured".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempMeasured",
            0x4d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x9c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xa4, bo) {
        dm.push(("ColorTempUnknown".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xa6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xae, bo) {
        dm.push(("ColorTempUnknown2".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xb0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xb8, bo) {
        dm.push(("ColorTempUnknown3".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xba + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xc2, bo) {
        dm.push(("ColorTempUnknown4".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xc4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xcc, bo) {
        dm.push(("ColorTempUnknown5".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xce + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0x67,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xd6, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0x6b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xd8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0x6c,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xe0, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0x70,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xe2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0x71,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xea, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0x75,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xec + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0x76,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xf4, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0x7a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xf6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0x7b,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xfe, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0x7f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x100 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsKelvin",
                0x80,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x108, bo) {
        dm.push(("ColorTempKelvin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempKelvin",
            0x84,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x10a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0x85,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x112, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0x89,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x114 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x11c, bo) {
        dm.push(("ColorTempUnknown6".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x11e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x126, bo) {
        dm.push(("ColorTempUnknown7".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x128 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x130, bo) {
        dm.push(("ColorTempUnknown8".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x132 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x13a, bo) {
        dm.push(("ColorTempUnknown9".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x13c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x144, bo) {
        dm.push(("ColorTempUnknown10".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x146 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x14e, bo) {
        dm.push(("ColorTempUnknown11".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x150 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x158, bo) {
        dm.push(("ColorTempUnknown12".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x15a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x162, bo) {
        dm.push(("ColorTempUnknown13".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x164 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x16c, bo) {
        dm.push(("ColorTempUnknown14".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x16e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x176, bo) {
        dm.push(("ColorTempUnknown15".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x1f6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "AverageBlackLevel",
                0xfb,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u32_at(data, 0x328 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Canon::SwapWords($val)", &cv, &ctx)
            {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "RawMeasuredRGGB",
                0x194,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x3be + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "PerChannelBlackLevel",
                0x1df,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x3c6, bo) {
        dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "NormalWhiteLevel",
                0x1e3,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x3c8, bo) {
        dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SpecularWhiteLevel",
            0x1e4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x3ca, bo) {
        dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LinearityUpperMargin",
            0x1e5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x178..0x178 + 120) {
        tags.extend(canon_colorcalib(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorData7` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata7(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ColorDataVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                10 => "10 (1DX/5DmkIII/6D/70D/100D/650D/700D/M/M2)".to_string(),
                11 => "11 (7DmkII/750D/760D/8000D)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ColorDataVersion", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x7e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x3f,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x86, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x43,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x88 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAuto",
                0x44,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x90, bo) {
        dm.push(("ColorTempAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAuto",
            0x48,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x92 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsMeasured",
                0x49,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x9a, bo) {
        dm.push(("ColorTempMeasured".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempMeasured",
            0x4d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x9c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xa4, bo) {
        dm.push(("ColorTempUnknown".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xa6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xae, bo) {
        dm.push(("ColorTempUnknown2".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xb0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xb8, bo) {
        dm.push(("ColorTempUnknown3".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xba + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xc2, bo) {
        dm.push(("ColorTempUnknown4".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xc4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xcc, bo) {
        dm.push(("ColorTempUnknown5".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xce + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xd6, bo) {
        dm.push(("ColorTempUnknown6".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xd8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xe0, bo) {
        dm.push(("ColorTempUnknown7".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xe2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xea, bo) {
        dm.push(("ColorTempUnknown8".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xec + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xf4, bo) {
        dm.push(("ColorTempUnknown9".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xf6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xfe, bo) {
        dm.push(("ColorTempUnknown10".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x100 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0x80,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x108, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0x84,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x10a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0x85,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x112, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0x89,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x114 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0x8a,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x11c, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0x8e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x11e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0x8f,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x126, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0x93,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x128 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0x94,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x130, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0x98,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x132 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsKelvin",
                0x99,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x13a, bo) {
        dm.push(("ColorTempKelvin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempKelvin",
            0x9d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x13c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0x9e,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x144, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0xa2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x146 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x14e, bo) {
        dm.push(("ColorTempUnknown11".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x150 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x158, bo) {
        dm.push(("ColorTempUnknown12".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x15a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x162, bo) {
        dm.push(("ColorTempUnknown13".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x164 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x16c, bo) {
        dm.push(("ColorTempUnknown14".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x16e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x176, bo) {
        dm.push(("ColorTempUnknown15".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x178 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x180, bo) {
        dm.push(("ColorTempUnknown16".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x182 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x18a, bo) {
        dm.push(("ColorTempUnknown17".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x18c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x194, bo) {
        dm.push(("ColorTempUnknown18".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x196 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x19e, bo) {
        dm.push(("ColorTempUnknown19".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1a0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1a8, bo) {
        dm.push(("ColorTempUnknown20".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x228 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "AverageBlackLevel",
                0x114,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x330, bo) {
        dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val >= 255 ? 255 : exp(($val-200)/16*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val == 255 ? \"Strobe or Misfire\" : sprintf(\"%.0f%%\", $val * 100)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "FlashOutput",
                0x198,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x332, bo) {
        dm.push(("FlashBatteryLevel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "$val ? sprintf(\"%.2fV\", $val * 5 / 186) : \"n/a\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "FlashBatteryLevel",
            0x199,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 10.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u32_at(data, 0x35a + k * 4, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                let ctx = Ctx {
                    make,
                    model,
                    file_type,
                    dm,
                };
                let mut cv = Conv::Str(s.clone());
                if let Some(x) =
                    conv_expr::eval_with("Image::ExifTool::Canon::SwapWords($val)", &cv, &ctx)
                {
                    cv = x;
                }
                let raw = Value::String(cv.as_string());
                tags.push(mk(
                    "RawMeasuredRGGB",
                    0x1ad,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 10.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x3f0 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "PerChannelBlackLevel",
                    0x1f8,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 10.0) {
        if let Some(v) = u16_at(data, 0x3f8, bo) {
            dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "NormalWhiteLevel",
                    0x1fc,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 10.0) {
        if let Some(v) = u16_at(data, 0x3fa, bo) {
            dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SpecularWhiteLevel",
                0x1fd,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 10.0) {
        if let Some(v) = u16_at(data, 0x3fc, bo) {
            dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "LinearityUpperMargin",
                0x1fe,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 11.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u32_at(data, 0x4d6 + k * 4, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                let ctx = Ctx {
                    make,
                    model,
                    file_type,
                    dm,
                };
                let mut cv = Conv::Str(s.clone());
                if let Some(x) =
                    conv_expr::eval_with("Image::ExifTool::Canon::SwapWords($val)", &cv, &ctx)
                {
                    cv = x;
                }
                let raw = Value::String(cv.as_string());
                tags.push(mk(
                    "RawMeasuredRGGB",
                    0x26b,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 11.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x5b0 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "PerChannelBlackLevel",
                    0x2d8,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 11.0) {
        if let Some(v) = u16_at(data, 0x5b8, bo) {
            dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "NormalWhiteLevel",
                    0x2dc,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 11.0) {
        if let Some(v) = u16_at(data, 0x5ba, bo) {
            dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SpecularWhiteLevel",
                0x2dd,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 11.0) {
        if let Some(v) = u16_at(data, 0x5bc, bo) {
            dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "LinearityUpperMargin",
                0x2de,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x1aa..0x1aa + 120) {
        tags.extend(canon_colorcalib(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorData8` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata8(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ColorDataVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                12 => "12 (1DXmkII/5DS/5DSR)".to_string(),
                13 => "13 (80D/5DmkIV)".to_string(),
                14 => "14 (1300D/2000D/4000D)".to_string(),
                15 => "15 (6DmkII/77D/200D/800D,9000D)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ColorDataVersion", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x7e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x3f,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x86, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x43,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x88 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAuto",
                0x44,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x90, bo) {
        dm.push(("ColorTempAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAuto",
            0x48,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x92 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsMeasured",
                0x49,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x9a, bo) {
        dm.push(("ColorTempMeasured".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempMeasured",
            0x4d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x9c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xa4, bo) {
        dm.push(("ColorTempUnknown".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xa6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xae, bo) {
        dm.push(("ColorTempUnknown2".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xb0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xb8, bo) {
        dm.push(("ColorTempUnknown3".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xba + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xc2, bo) {
        dm.push(("ColorTempUnknown4".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xc4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xcc, bo) {
        dm.push(("ColorTempUnknown5".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xce + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xd6, bo) {
        dm.push(("ColorTempUnknown6".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xd8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xe0, bo) {
        dm.push(("ColorTempUnknown7".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xe2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xea, bo) {
        dm.push(("ColorTempUnknown8".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xec + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xf4, bo) {
        dm.push(("ColorTempUnknown9".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xf6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xfe, bo) {
        dm.push(("ColorTempUnknown10".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x100 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x108, bo) {
        dm.push(("ColorTempUnknown11".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x10a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0x85,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x112, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0x89,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x114 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0x8a,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x11c, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0x8e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x11e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0x8f,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x126, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0x93,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x128 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0x94,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x130, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0x98,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x132 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0x99,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x13a, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0x9d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x13c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsKelvin",
                0x9e,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x144, bo) {
        dm.push(("ColorTempKelvin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempKelvin",
            0xa2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x146 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0xa3,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x14e, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0xa7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x150 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x158, bo) {
        dm.push(("ColorTempUnknown12".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x15a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x162, bo) {
        dm.push(("ColorTempUnknown13".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x164 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x16c, bo) {
        dm.push(("ColorTempUnknown14".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x16e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x176, bo) {
        dm.push(("ColorTempUnknown15".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x178 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x180, bo) {
        dm.push(("ColorTempUnknown16".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x182 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x18a, bo) {
        dm.push(("ColorTempUnknown17".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x18c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x194, bo) {
        dm.push(("ColorTempUnknown18".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x196 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x19e, bo) {
        dm.push(("ColorTempUnknown19".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1a0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1a8, bo) {
        dm.push(("ColorTempUnknown20".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1aa + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1b2, bo) {
        dm.push(("ColorTempUnknown21".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1b4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1bc, bo) {
        dm.push(("ColorTempUnknown22".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1be + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1c6, bo) {
        dm.push(("ColorTempUnknown23".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1c8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1d0, bo) {
        dm.push(("ColorTempUnknown24".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1d2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1da, bo) {
        dm.push(("ColorTempUnknown25".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1dc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1e4, bo) {
        dm.push(("ColorTempUnknown26".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1e6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1ee, bo) {
        dm.push(("ColorTempUnknown27".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1f0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1f8, bo) {
        dm.push(("ColorTempUnknown28".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1fa + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x202, bo) {
        dm.push(("ColorTempUnknown29".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x204 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x20c, bo) {
        dm.push(("ColorTempUnknown30".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x28c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "AverageBlackLevel",
                0x146,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 14.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x458 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "PerChannelBlackLevel",
                    0x22c,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 14.0) {
        if let Some(v) = u16_at(data, 0x460, bo) {
            dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "NormalWhiteLevel",
                    0x230,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 14.0) {
        if let Some(v) = u16_at(data, 0x462, bo) {
            dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SpecularWhiteLevel",
                0x231,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 14.0) {
        if let Some(v) = u16_at(data, 0x464, bo) {
            dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "LinearityUpperMargin",
                0x232,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v < 14.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 15.0))
    {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x614 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "PerChannelBlackLevel",
                    0x30a,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v < 14.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 15.0))
    {
        if let Some(v) = u16_at(data, 0x61c, bo) {
            dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "NormalWhiteLevel",
                    0x30e,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v < 14.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 15.0))
    {
        if let Some(v) = u16_at(data, 0x61e, bo) {
            dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SpecularWhiteLevel",
                0x30f,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_get(dm, "ColorDataVersion").is_some_and(|v| v < 14.0)
        || dm_get(dm, "ColorDataVersion").is_some_and(|v| v == 15.0))
    {
        if let Some(v) = u16_at(data, 0x620, bo) {
            dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "LinearityUpperMargin",
                0x310,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x20e..0x20e + 120) {
        tags.extend(canon_colorcalib(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorData9` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata9(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ColorDataVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                16 => "16 (M50)".to_string(),
                17 => "17 (R)".to_string(),
                18 => "18 (RP/250D)".to_string(),
                19 => "19 (90D/850D/M6mkII/M200)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ColorDataVersion", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x8e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x47,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x96, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x4b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x98 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAuto",
                0x4c,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xa0, bo) {
        dm.push(("ColorTempAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAuto",
            0x50,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xa2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsMeasured",
                0x51,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xaa, bo) {
        dm.push(("ColorTempMeasured".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempMeasured",
            0x55,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xac + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xb4, bo) {
        dm.push(("ColorTempUnknown".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xb6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xbe, bo) {
        dm.push(("ColorTempUnknown2".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xc0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xc8, bo) {
        dm.push(("ColorTempUnknown3".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xca + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xd2, bo) {
        dm.push(("ColorTempUnknown4".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xd4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xdc, bo) {
        dm.push(("ColorTempUnknown5".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xde + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xe6, bo) {
        dm.push(("ColorTempUnknown6".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xe8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xf0, bo) {
        dm.push(("ColorTempUnknown7".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xf2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xfa, bo) {
        dm.push(("ColorTempUnknown8".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xfc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x104, bo) {
        dm.push(("ColorTempUnknown9".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x106 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x10e, bo) {
        dm.push(("ColorTempUnknown10".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x110 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0x88,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x118, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0x8c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x11a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0x8d,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x122, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0x91,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x124 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0x92,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x12c, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0x96,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x12e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0x97,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x136, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0x9b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x138 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0x9c,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x140, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0xa0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x142 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsKelvin",
                0xa1,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x14a, bo) {
        dm.push(("ColorTempKelvin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempKelvin",
            0xa5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x14c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0xa6,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x154, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0xaa,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x156 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x15e, bo) {
        dm.push(("ColorTempUnknown11".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x160 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x168, bo) {
        dm.push(("ColorTempUnknown12".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x16a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x172, bo) {
        dm.push(("ColorTempUnknown13".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x174 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x17c, bo) {
        dm.push(("ColorTempUnknown14".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x17e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x186, bo) {
        dm.push(("ColorTempUnknown15".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x188 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x190, bo) {
        dm.push(("ColorTempUnknown16".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x192 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x19a, bo) {
        dm.push(("ColorTempUnknown17".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x19c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1a4, bo) {
        dm.push(("ColorTempUnknown18".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1a6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1ae, bo) {
        dm.push(("ColorTempUnknown19".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1b0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1b8, bo) {
        dm.push(("ColorTempUnknown20".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1ba + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1c2, bo) {
        dm.push(("ColorTempUnknown21".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1c4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1cc, bo) {
        dm.push(("ColorTempUnknown22".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1ce + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1d6, bo) {
        dm.push(("ColorTempUnknown23".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1d8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1e0, bo) {
        dm.push(("ColorTempUnknown24".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1e2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1ea, bo) {
        dm.push(("ColorTempUnknown25".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1ec + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1f4, bo) {
        dm.push(("ColorTempUnknown26".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1f6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1fe, bo) {
        dm.push(("ColorTempUnknown27".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x200 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x208, bo) {
        dm.push(("ColorTempUnknown28".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x20a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x212, bo) {
        dm.push(("ColorTempUnknown29".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x292 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "PerChannelBlackLevel",
                0x149,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x638, bo) {
        dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "NormalWhiteLevel",
                0x31c,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x63a, bo) {
        dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SpecularWhiteLevel",
            0x31d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x63c, bo) {
        dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LinearityUpperMargin",
            0x31e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x214..0x214 + 120) {
        tags.extend(canon_colorcalib(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorData10` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata10(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ColorDataVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                32 => "32 (1DXmkIII)".to_string(),
                33 => "33 (R5/R6)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ColorDataVersion", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xaa + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x55,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xb2, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x59,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xb4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAuto",
                0x5a,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xbc, bo) {
        dm.push(("ColorTempAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAuto",
            0x5e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xbe + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsMeasured",
                0x5f,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xc6, bo) {
        dm.push(("ColorTempMeasured".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempMeasured",
            0x63,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xc8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xd0, bo) {
        dm.push(("ColorTempUnknown".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xd2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xda, bo) {
        dm.push(("ColorTempUnknown2".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xdc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xe4, bo) {
        dm.push(("ColorTempUnknown3".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xe6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xee, bo) {
        dm.push(("ColorTempUnknown4".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xf0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xf8, bo) {
        dm.push(("ColorTempUnknown5".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xfa + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x102, bo) {
        dm.push(("ColorTempUnknown6".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x104 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x10c, bo) {
        dm.push(("ColorTempUnknown7".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x10e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x116, bo) {
        dm.push(("ColorTempUnknown8".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x118 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x120, bo) {
        dm.push(("ColorTempUnknown9".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x122 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x12a, bo) {
        dm.push(("ColorTempUnknown10".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x12c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0x96,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x134, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0x9a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x136 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0x9b,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x13e, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0x9f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x140 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0xa0,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x148, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0xa4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x14a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0xa5,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x152, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0xa9,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x154 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0xaa,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x15c, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0xae,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x15e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsKelvin",
                0xaf,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x166, bo) {
        dm.push(("ColorTempKelvin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempKelvin",
            0xb3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x168 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0xb4,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x170, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0xb8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x172 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x17a, bo) {
        dm.push(("ColorTempUnknown11".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x17c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x184, bo) {
        dm.push(("ColorTempUnknown12".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x186 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x18e, bo) {
        dm.push(("ColorTempUnknown13".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x190 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x198, bo) {
        dm.push(("ColorTempUnknown14".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x19a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1a2, bo) {
        dm.push(("ColorTempUnknown15".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1a4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1ac, bo) {
        dm.push(("ColorTempUnknown16".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1ae + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1b6, bo) {
        dm.push(("ColorTempUnknown17".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1b8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1c0, bo) {
        dm.push(("ColorTempUnknown18".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1c2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1ca, bo) {
        dm.push(("ColorTempUnknown19".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1cc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1d4, bo) {
        dm.push(("ColorTempUnknown20".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1d6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1de, bo) {
        dm.push(("ColorTempUnknown21".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1e0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1e8, bo) {
        dm.push(("ColorTempUnknown22".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1ea + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1f2, bo) {
        dm.push(("ColorTempUnknown23".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1f4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1fc, bo) {
        dm.push(("ColorTempUnknown24".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1fe + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x206, bo) {
        dm.push(("ColorTempUnknown25".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x208 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x210, bo) {
        dm.push(("ColorTempUnknown26".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x212 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x21a, bo) {
        dm.push(("ColorTempUnknown27".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x21c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x224, bo) {
        dm.push(("ColorTempUnknown28".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x226 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x22e, bo) {
        dm.push(("ColorTempUnknown29".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x2ae + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "PerChannelBlackLevel",
                0x157,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x532, bo) {
        dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val >= 255 ? 255 : exp(($val-200)/16*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val == 255 ? \"Strobe or Misfire\" : sprintf(\"%.0f%%\", $val * 100)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "FlashOutput",
                0x299,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x534, bo) {
        dm.push(("FlashBatteryLevel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "$val ? sprintf(\"%.2fV\", $val * 5 / 186) : \"n/a\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "FlashBatteryLevel",
            0x29a,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x654, bo) {
        dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "NormalWhiteLevel",
                0x32a,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x656, bo) {
        dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SpecularWhiteLevel",
            0x32b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x658, bo) {
        dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LinearityUpperMargin",
            0x32c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x230..0x230 + 120) {
        tags.extend(canon_colorcalib(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorData11` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata11(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ColorDataVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                34 => "34 (R3)".to_string(),
                48 => "48 (R7/R10/R50/R6mkII)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ColorDataVersion", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xd2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x69,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xda, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x6d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xdc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAuto",
                0x6e,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xe4, bo) {
        dm.push(("ColorTempAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAuto",
            0x72,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xe6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsMeasured",
                0x73,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xee, bo) {
        dm.push(("ColorTempMeasured".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempMeasured",
            0x77,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xf0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xf8, bo) {
        dm.push(("ColorTempUnknown".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xfa + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x102, bo) {
        dm.push(("ColorTempUnknown2".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x104 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x10c, bo) {
        dm.push(("ColorTempUnknown3".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x10e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x116, bo) {
        dm.push(("ColorTempUnknown4".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x118 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x120, bo) {
        dm.push(("ColorTempUnknown5".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x122 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x12a, bo) {
        dm.push(("ColorTempUnknown6".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x12c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x134, bo) {
        dm.push(("ColorTempUnknown7".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x136 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x13e, bo) {
        dm.push(("ColorTempUnknown8".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x140 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x148, bo) {
        dm.push(("ColorTempUnknown9".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x14a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x152, bo) {
        dm.push(("ColorTempUnknown10".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x154 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x15c, bo) {
        dm.push(("ColorTempUnknown11".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x15e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x166, bo) {
        dm.push(("ColorTempUnknown12".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x168 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x170, bo) {
        dm.push(("ColorTempUnknown13".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x172 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x17a, bo) {
        dm.push(("ColorTempUnknown14".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x17c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x184, bo) {
        dm.push(("ColorTempUnknown15".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x186 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x18e, bo) {
        dm.push(("ColorTempUnknown16".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x190 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x198, bo) {
        dm.push(("ColorTempUnknown17".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x19a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0xcd,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x1a2, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0xd1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1a4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0xd2,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x1ac, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0xd6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1ae + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0xd7,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x1b6, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0xdb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1b8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0xdc,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x1c0, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0xe0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1c2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0xe1,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x1ca, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0xe5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1cc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsKelvin",
                0xe6,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x1d4, bo) {
        dm.push(("ColorTempKelvin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempKelvin",
            0xea,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1d6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0xeb,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x1de, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0xef,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1e0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1e8, bo) {
        dm.push(("ColorTempUnknown18".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1ea + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1f2, bo) {
        dm.push(("ColorTempUnknown19".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1f4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1fc, bo) {
        dm.push(("ColorTempUnknown20".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1fe + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x206, bo) {
        dm.push(("ColorTempUnknown21".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x208 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x210, bo) {
        dm.push(("ColorTempUnknown22".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x212 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x21a, bo) {
        dm.push(("ColorTempUnknown23".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x21c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x224, bo) {
        dm.push(("ColorTempUnknown24".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x226 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x22e, bo) {
        dm.push(("ColorTempUnknown25".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x230 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x238, bo) {
        dm.push(("ColorTempUnknown26".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x23a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x242, bo) {
        dm.push(("ColorTempUnknown27".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x244 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x24c, bo) {
        dm.push(("ColorTempUnknown28".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x2d6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "PerChannelBlackLevel",
                0x16b,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x500, bo) {
        dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "NormalWhiteLevel",
                0x280,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x502, bo) {
        dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SpecularWhiteLevel",
            0x281,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x504, bo) {
        dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LinearityUpperMargin",
            0x282,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x258..0x258 + 120) {
        tags.extend(canon_colorcalib(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorData12` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordata12(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ColorDataVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                64 => "64 (R1/R5mkII)".to_string(),
                65 => "65 (R50V)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ColorDataVersion", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xd2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x69,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xda, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x6d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xdc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0x6e,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xe4, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0x72,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xe6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0x73,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xee, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0x77,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xf0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0x78,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xf8, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0x7c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xfa + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0x7d,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x102, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0x81,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x104 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0x82,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x10c, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0x86,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x10e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0x87,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x116, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0x8b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x118 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x120, bo) {
        dm.push(("ColorTempUnknown2".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x122 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x12a, bo) {
        dm.push(("ColorTempUnknown3".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x12c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x134, bo) {
        dm.push(("ColorTempUnknown4".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x136 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x13e, bo) {
        dm.push(("ColorTempUnknown5".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x140 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x148, bo) {
        dm.push(("ColorTempUnknown6".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x14a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x152, bo) {
        dm.push(("ColorTempUnknown7".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x154 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x15c, bo) {
        dm.push(("ColorTempUnknown8".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x15e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x166, bo) {
        dm.push(("ColorTempUnknown9".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x168 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x170, bo) {
        dm.push(("ColorTempUnknown10".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x172 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x17a, bo) {
        dm.push(("ColorTempUnknown11".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x17c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x184, bo) {
        dm.push(("ColorTempUnknown12".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x186 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x18e, bo) {
        dm.push(("ColorTempUnknown13".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x190 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x198, bo) {
        dm.push(("ColorTempUnknown14".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x19a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1a2, bo) {
        dm.push(("ColorTempUnknown15".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1a4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1ac, bo) {
        dm.push(("ColorTempUnknown16".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1ae + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1b6, bo) {
        dm.push(("ColorTempUnknown17".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1b8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1c0, bo) {
        dm.push(("ColorTempUnknown18".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1c2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1ca, bo) {
        dm.push(("ColorTempUnknown19".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1cc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1d4, bo) {
        dm.push(("ColorTempUnknown20".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1d6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1de, bo) {
        dm.push(("ColorTempUnknown21".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1e0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1e8, bo) {
        dm.push(("ColorTempUnknown22".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1ea + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1f2, bo) {
        dm.push(("ColorTempUnknown23".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1f4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1fc, bo) {
        dm.push(("ColorTempUnknown24".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1fe + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x206, bo) {
        dm.push(("ColorTempUnknown25".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x208 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x210, bo) {
        dm.push(("ColorTempUnknown26".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x212 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x21a, bo) {
        dm.push(("ColorTempUnknown27".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x21c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x224, bo) {
        dm.push(("ColorTempUnknown28".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x226 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x22e, bo) {
        dm.push(("ColorTempUnknown29".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x230 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x238, bo) {
        dm.push(("ColorTempUnknown30".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x23a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x242, bo) {
        dm.push(("ColorTempUnknown31".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x244 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x24c, bo) {
        dm.push(("ColorTempUnknown32".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x24e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x256, bo) {
        dm.push(("ColorTempUnknown33".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x2fe + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "PerChannelBlackLevel",
                0x17f,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x406, bo) {
        dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val >= 255 ? 255 : exp(($val-200)/16*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val == 255 ? \"Strobe or Misfire\" : sprintf(\"%.0f%%\", $val * 100)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "FlashOutput",
                0x203,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x408, bo) {
        dm.push(("FlashBatteryLevel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "$val ? sprintf(\"%.2fV\", $val * 5 / 186) : \"n/a\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "FlashBatteryLevel",
            0x204,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x528, bo) {
        dm.push(("NormalWhiteLevel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "NormalWhiteLevel",
                0x294,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x52a, bo) {
        dm.push(("SpecularWhiteLevel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SpecularWhiteLevel",
            0x295,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x52c, bo) {
        dm.push(("LinearityUpperMargin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LinearityUpperMargin",
            0x296,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x280..0x280 + 120) {
        tags.extend(canon_colorcalib(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorDataUnknown` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colordataunknown(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ColorDataVersion".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorDataVersion",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ShotInfo` -- FORMAT int16s, FIRST_ENTRY 1.
fn canon_shotinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x2, bo) {
        dm.push(("AutoISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("exp($val/32*log(2))*100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "AutoISO",
                0x1,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x4, bo) {
        dm.push(("BaseISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp($val/32*log(2))*100/32", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "BaseISO",
                    0x2,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = i16_at(data, 0x6, bo) {
        dm.push(("MeasuredEV".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 32 + 5", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.2f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "MeasuredEV",
                0x3,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x8, bo) {
        dm.push(("TargetAperture".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val > 0 ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(Image::ExifTool::Canon::CanonEv($val)*log(2)/2)",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "TargetAperture",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = i16_at(data, 0xa, bo) {
        dm.push(("TargetExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with(
            "($val > -1000 and ($val or $$self{Model}=~/(EOS|PowerShot|IXUS|IXY)/))? $val : undef",
            &base,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(-Image::ExifTool::Canon::CanonEv($val)*log(2))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "TargetExposureTime",
                    0x5,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = i16_at(data, 0xc, bo) {
        dm.push(("ExposureCompensation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("Image::ExifTool::Canon::CanonEv($val)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureCompensation",
                0x6,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xe, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0x10, bo) {
        dm.push(("SlowShutter".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            0 => "Off".to_string(),
            1 => "Night Scene".to_string(),
            2 => "On".to_string(),
            3 => "None".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SlowShutter", 0x8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0x12, bo) {
        dm.push(("SequenceNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SequenceNumber",
            0x9,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x14, bo) {
        dm.push(("OpticalZoomCode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("$val == 8 ? \"n/a\" : $val", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "OpticalZoomCode",
            0xa,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if (MODEL_RE_0.is_match(model) && !MODEL_RE_1.is_match(model)) {
        if let Some(v) = i16_at(data, 0x18, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                let mut cv = base;
                let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
                if vc.as_ref() != Some(&Conv::Undef) {
                    if let Some(x) = vc {
                        cv = x;
                    }
                    let raw = Value::F64(cv.as_num());
                    if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                        cv = x;
                    }
                    tags.push(mk(
                        "CameraTemperature",
                        0xc,
                        cv.as_string(),
                        raw,
                        GRP0,
                        GRP1,
                        GRP2,
                        PRIO,
                    ));
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1a, bo) {
        dm.push(("FlashGuideNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val==-1 ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 32", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "FlashGuideNumber",
                    0xd,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = i16_at(data, 0x1c, bo) {
        dm.push(("AFPointsInFocus".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val==0 ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                12288 => "None (MF)".to_string(),
                12289 => "Right".to_string(),
                12290 => "Center".to_string(),
                12291 => "Center+Right".to_string(),
                12292 => "Left".to_string(),
                12293 => "Left+Right".to_string(),
                12294 => "Left+Center".to_string(),
                12295 => "All".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFPointsInFocus", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = i16_at(data, 0x1e, bo) {
        dm.push(("FlashExposureComp".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("Image::ExifTool::Canon::CanonEv($val)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FlashExposureComp",
                0xf,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x20, bo) {
        dm.push((
            "AutoExposureBracketing".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "On".to_string(),
            0 => "Off".to_string(),
            1 => "On (shot 1)".to_string(),
            2 => "On (shot 2)".to_string(),
            3 => "On (shot 3)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoExposureBracketing",
            0x10,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x22, bo) {
        dm.push(("AEBBracketValue".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("Image::ExifTool::Canon::CanonEv($val)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "AEBBracketValue",
                0x11,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x24, bo) {
        dm.push(("ControlMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "n/a".to_string(),
            1 => "Camera Local Control".to_string(),
            3 => "Computer Remote Control".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ControlMode", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x26, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("($val) || undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) =
                    conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
                {
                    cv = x;
                }
                tags.push(mk(
                    "FocusDistanceUpper",
                    0x13,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_val(dm, "FocusDistanceUpper").is_some_and(conv_expr::Val::truthy) {
        if let Some(v) = u16_at(data, 0x28, bo) {
            dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) =
                    conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
                {
                    cv = x;
                }
                tags.push(mk(
                    "FocusDistanceLower",
                    0x14,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = i16_at(data, 0x2a, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(Image::ExifTool::Canon::CanonEv($val)*log(2)/2)",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x15,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if MODEL_RE_2.is_match(model) {
        if let Some(v) = i16_at(data, 0x2c, bo) {
            dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with(
                "($val or $$self{FILE_TYPE} eq \"CRW\") ? $val : undef",
                &base,
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                let mut cv = base;
                let vc = conv_expr::eval_with(
                    "exp(-Image::ExifTool::Canon::CanonEv($val)*log(2))*1000/32",
                    &cv,
                    &ctx,
                );
                if vc.as_ref() != Some(&Conv::Undef) {
                    if let Some(x) = vc {
                        cv = x;
                    }
                    let raw = Value::F64(cv.as_num());
                    if let Some(x) = conv_expr::eval_with(
                        "Image::ExifTool::Exif::PrintExposureTime($val)",
                        &cv,
                        &ctx,
                    ) {
                        cv = x;
                    }
                    tags.push(mk(
                        "ExposureTime",
                        0x16,
                        cv.as_string(),
                        raw,
                        GRP0,
                        GRP1,
                        GRP2,
                        PRIO,
                    ));
                }
            }
        }
    }
    if !(MODEL_RE_2.is_match(model)) {
        if let Some(v) = i16_at(data, 0x2c, bo) {
            dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with(
                "($val or $$self{FILE_TYPE} eq \"CRW\") ? $val : undef",
                &base,
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                let mut cv = base;
                let vc = conv_expr::eval_with(
                    "exp(-Image::ExifTool::Canon::CanonEv($val)*log(2))",
                    &cv,
                    &ctx,
                );
                if vc.as_ref() != Some(&Conv::Undef) {
                    if let Some(x) = vc {
                        cv = x;
                    }
                    let raw = Value::F64(cv.as_num());
                    if let Some(x) = conv_expr::eval_with(
                        "Image::ExifTool::Exif::PrintExposureTime($val)",
                        &cv,
                        &ctx,
                    ) {
                        cv = x;
                    }
                    tags.push(mk(
                        "ExposureTime",
                        0x16,
                        cv.as_string(),
                        raw,
                        GRP0,
                        GRP1,
                        GRP2,
                        PRIO,
                    ));
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x2e, bo) {
        dm.push(("MeasuredEV2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 8 - 6", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "MeasuredEV2",
                    0x17,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = i16_at(data, 0x30, bo) {
        dm.push(("BulbDuration".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "BulbDuration",
                0x18,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x34, bo) {
        dm.push(("CameraType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "n/a".to_string(),
            248 => "EOS High-end".to_string(),
            250 => "Compact".to_string(),
            252 => "EOS Mid-range".to_string(),
            255 => "DV Camera".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("CameraType", 0x1a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0x36, bo) {
        dm.push(("AutoRotate".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val >= 0 ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                -1 => "n/a".to_string(),
                0 => "None".to_string(),
                1 => "Rotate 90 CW".to_string(),
                2 => "Rotate 180".to_string(),
                3 => "Rotate 270 CW".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AutoRotate", 0x1b, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = i16_at(data, 0x38, bo) {
        dm.push(("NDFilter".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NDFilter", 0x1c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0x3a, bo) {
        dm.push(("SelfTimer2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val >= 0 ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 10", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "SelfTimer2",
                    0x1d,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = i16_at(data, 0x42, bo) {
        dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with(
            "($$self{Model}=~/(PowerShot|IXUS|IXY)/ or $val) ? $val : undef",
            &base,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FlashOutput",
                0x21,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::RawBurstInfo` -- FORMAT int32u, FIRST_ENTRY 1.
fn canon_rawburstinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push(("RawBurstImageNum".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "RawBurstImageNum",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push(("RawBurstImageCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "RawBurstImageCount",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::IAD1` -- FORMAT int16u, FIRST_ENTRY 0.
fn canon_iad1(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    tags
}

/// `Image::ExifTool::Canon::Panorama` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_panorama(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x4, bo) {
        dm.push(("PanoramaFrameNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PanoramaFrameNumber",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0xa, bo) {
        dm.push(("PanoramaDirection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Left to Right".to_string(),
            1 => "Right to Left".to_string(),
            2 => "Bottom to Top".to_string(),
            3 => "Top to Bottom".to_string(),
            4 => "2x2 Matrix (Clockwise)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PanoramaDirection", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Canon::UnknownD30` -- FORMAT int16s, FIRST_ENTRY 1.
fn canon_unknownd30(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    tags
}

/// `Image::ExifTool::Canon::MovieInfo` -- FORMAT int16u, FIRST_ENTRY 1.
fn canon_movieinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Video";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("FrameRate".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 65535 ? undef: $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FrameRate",
                0x1,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push(("FrameCount".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 65535 ? undef: $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FrameCount",
                0x2,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push(("FrameCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FrameCount",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat32u_at(data, 0xc, bo) {
        dm.push(("FrameRate".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("int($val * 1000 + 0.5) / 1000", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FrameRate",
            0x6,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xd4, bo) {
        dm.push(("Duration".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "Duration",
                0x6a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0xd8, bo) {
        dm.push(("AudioBitrate".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("ConvertBitrate($val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "AudioBitrate",
            0x6c,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xdc, bo) {
        dm.push(("AudioSampleRate".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AudioSampleRate",
            0x6e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xe0, bo) {
        dm.push(("AudioChannels".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AudioChannels",
            0x70,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0xe8, 4, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "GetByteOrder() eq \"MM\" ? $val : pack(\"N\",unpack(\"V\",$val))",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "VideoCodec",
                0x74,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::MyColors` -- FORMAT int16u, FIRST_ENTRY 0.
fn canon_mycolors(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push(("MyColorMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Positive Film".to_string(),
            2 => "Light Skin Tone".to_string(),
            3 => "Dark Skin Tone".to_string(),
            4 => "Vivid Blue".to_string(),
            5 => "Vivid Green".to_string(),
            6 => "Vivid Red".to_string(),
            7 => "Color Accent".to_string(),
            8 => "Color Swap".to_string(),
            9 => "Custom".to_string(),
            12 => "Vivid".to_string(),
            13 => "Neutral".to_string(),
            14 => "Sepia".to_string(),
            15 => "B&W".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MyColorMode", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Canon::FaceDetect1` -- FORMAT int16u, FIRST_ENTRY 0.
fn canon_facedetect1(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push(("FacesDetected".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FacesDetected",
                0x2,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "FaceDetectFrameSize",
                0x3,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match i16_at(data, 0x10 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesDetected} < 1 ? undef: $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face1Position",
                    0x8,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match i16_at(data, 0x14 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesDetected} < 2 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face2Position",
                    0xa,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match i16_at(data, 0x18 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesDetected} < 3 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face3Position",
                    0xc,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match i16_at(data, 0x1c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesDetected} < 4 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face4Position",
                    0xe,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match i16_at(data, 0x20 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesDetected} < 5 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face5Position",
                    0x10,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match i16_at(data, 0x24 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesDetected} < 6 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face6Position",
                    0x12,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match i16_at(data, 0x28 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesDetected} < 7 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face7Position",
                    0x14,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match i16_at(data, 0x2c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesDetected} < 8 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face8Position",
                    0x16,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match i16_at(data, 0x30 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesDetected} < 9 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face9Position",
                    0x18,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Canon::FaceDetect2` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_facedetect2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x1) {
        dm.push(("FaceWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FaceWidth",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2) {
        dm.push(("FacesDetected".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FacesDetected",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::FaceDetect3` -- FORMAT int16u, FIRST_ENTRY 1.
fn canon_facedetect3(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x6, bo) {
        dm.push(("FacesDetected".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FacesDetected",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::WBInfo` -- FORMAT int32u, FIRST_ENTRY 1.
fn canon_wbinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i32_at(data, 0x8 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_GRBGLevelsAuto",
                0x2,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i32_at(data, 0x28 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_GRBGLevelsDaylight",
                0xa,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i32_at(data, 0x48 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_GRBGLevelsCloudy",
                0x12,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i32_at(data, 0x68 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_GRBGLevelsTungsten",
                0x1a,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i32_at(data, 0x88 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_GRBGLevelsFluorescent",
                0x22,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i32_at(data, 0xa8 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_GRBGLevelsFluorHigh",
                0x2a,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i32_at(data, 0xc8 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_GRBGLevelsFlash",
                0x32,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i32_at(data, 0xe8 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_GRBGLevelsUnderwater",
                0x3a,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i32_at(data, 0x108 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_GRBGLevelsCustom1",
                0x42,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i32_at(data, 0x128 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_GRBGLevelsCustom2",
                0x4a,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::TimeInfo` -- FORMAT int32s, FIRST_ENTRY 1.
fn canon_timeinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Time";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x4, bo) {
        dm.push(("TimeZone".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("Image::ExifTool::TimeZoneString($val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "TimeZone",
            0x1,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x8, bo) {
        dm.push(("TimeZoneCity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "n/a".to_string(),
            1 => "Chatham Islands".to_string(),
            2 => "Wellington".to_string(),
            3 => "Solomon Islands".to_string(),
            4 => "Sydney".to_string(),
            5 => "Adelaide".to_string(),
            6 => "Tokyo".to_string(),
            7 => "Hong Kong".to_string(),
            8 => "Bangkok".to_string(),
            9 => "Yangon".to_string(),
            10 => "Dhaka".to_string(),
            11 => "Kathmandu".to_string(),
            12 => "Delhi".to_string(),
            13 => "Karachi".to_string(),
            14 => "Kabul".to_string(),
            15 => "Dubai".to_string(),
            16 => "Tehran".to_string(),
            17 => "Moscow".to_string(),
            18 => "Cairo".to_string(),
            19 => "Paris".to_string(),
            20 => "London".to_string(),
            21 => "Azores".to_string(),
            22 => "Fernando de Noronha".to_string(),
            23 => "Sao Paulo".to_string(),
            24 => "Newfoundland".to_string(),
            25 => "Santiago".to_string(),
            26 => "Caracas".to_string(),
            27 => "New York".to_string(),
            28 => "Chicago".to_string(),
            29 => "Denver".to_string(),
            30 => "Los Angeles".to_string(),
            31 => "Anchorage".to_string(),
            32 => "Honolulu".to_string(),
            33 => "Samoa".to_string(),
            32766 => "(not set)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("TimeZoneCity", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0xc, bo) {
        dm.push(("DaylightSavings".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            60 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DaylightSavings", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Canon::SerialInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_serialinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 9, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val =~ /^\\w{6}/ ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "InternalSerialNumber2",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x9, data.len().saturating_sub(0x9), true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val =~ /^\\w{6}/ ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "InternalSerialNumber",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::MeasuredColor` -- FORMAT int16u, FIRST_ENTRY 1.
fn canon_measuredcolor(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "MeasuredRGGB",
                0x1,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::Flags` -- FORMAT int16s, FIRST_ENTRY 1.
fn canon_flags(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x2, bo) {
        dm.push(("ModifiedParamFlag".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ModifiedParamFlag",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::ModifiedInfo` -- FORMAT int16s, FIRST_ENTRY 1.
fn canon_modifiedinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x2, bo) {
        dm.push(("ModifiedToneCurve".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Manual".to_string(),
            2 => "Custom".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModifiedToneCurve", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if MODEL_RE_3.is_match(model) {
        if let Some(v) = i16_at(data, 0x4, bo) {
            dm.push(("ModifiedSharpness".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ModifiedSharpness",
                0x2,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x6, bo) {
        dm.push(("ModifiedSharpnessFreq".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "n/a".to_string(),
            1 => "Lowest".to_string(),
            2 => "Low".to_string(),
            3 => "Standard".to_string(),
            4 => "High".to_string(),
            5 => "Highest".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ModifiedSharpnessFreq",
            0x3,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x8, bo) {
        dm.push((
            "ModifiedSensorRedLevel".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ModifiedSensorRedLevel",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0xa, bo) {
        dm.push((
            "ModifiedSensorBlueLevel".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ModifiedSensorBlueLevel",
            0x5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0xc, bo) {
        dm.push((
            "ModifiedWhiteBalanceRed".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ModifiedWhiteBalanceRed",
            0x6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0xe, bo) {
        dm.push((
            "ModifiedWhiteBalanceBlue".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ModifiedWhiteBalanceBlue",
            0x7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x10, bo) {
        dm.push(("ModifiedWhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ModifiedWhiteBalance",
            0x8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x12, bo) {
        dm.push(("ModifiedColorTemp".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ModifiedColorTemp",
            0x9,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x14, bo) {
        dm.push(("ModifiedPictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ModifiedPictureStyle",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x16, bo) {
        dm.push(("ModifiedDigitalGain".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ModifiedDigitalGain",
                0xb,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::PreviewImageInfo` -- FORMAT int32u, FIRST_ENTRY 1.
fn canon_previewimageinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push(("PreviewQuality".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Economy".to_string(),
            2 => "Normal".to_string(),
            3 => "Fine".to_string(),
            4 => "RAW".to_string(),
            5 => "Superfine".to_string(),
            7 => "CRAW".to_string(),
            130 => "Light (RAW)".to_string(),
            131 => "Standard (RAW)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PreviewQuality", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push(("PreviewImageLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreviewImageLength",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xc, bo) {
        dm.push(("PreviewImageWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreviewImageWidth",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x10, bo) {
        dm.push(("PreviewImageHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreviewImageHeight",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x14, bo) {
        dm.push(("PreviewImageStart".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreviewImageStart",
            0x5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::AFMicroAdj` -- FORMAT int32s, FIRST_ENTRY 1.
fn canon_afmicroadj(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x4, bo) {
        dm.push(("AFMicroAdjMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Disable".to_string(),
            1 => "Adjust all by the same amount".to_string(),
            2 => "Adjust by lens".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFMicroAdjMode", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = rat64s_at(data, 0x8, bo) {
        dm.push(("AFMicroAdjValue".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFMicroAdjValue",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::VignettingCorr` -- FORMAT int16s, FIRST_ENTRY 1.
fn canon_vignettingcorr(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("VignettingCorrVersion".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "VignettingCorrVersion",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x4, bo) {
        dm.push(("PeripheralLighting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PeripheralLighting",
            0x2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x6, bo) {
        dm.push(("DistortionCorrection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DistortionCorrection",
            0x3,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x8, bo) {
        dm.push((
            "ChromaticAberrationCorr".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ChromaticAberrationCorr",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0xa, bo) {
        dm.push((
            "ChromaticAberrationCorr".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ChromaticAberrationCorr",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0xc, bo) {
        dm.push((
            "PeripheralLightingValue".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PeripheralLightingValue",
            0x6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x12, bo) {
        dm.push((
            "DistortionCorrectionValue".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "DistortionCorrectionValue",
            0x9,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x16, bo) {
        dm.push(("OriginalImageWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "OriginalImageWidth",
            0xb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x18, bo) {
        dm.push(("OriginalImageHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "OriginalImageHeight",
            0xc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::VignettingCorr2` -- FORMAT int32s, FIRST_ENTRY 1.
fn canon_vignettingcorr2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x14, bo) {
        dm.push((
            "PeripheralLightingSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PeripheralLightingSetting",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x18, bo) {
        dm.push((
            "ChromaticAberrationSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ChromaticAberrationSetting",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x1c, bo) {
        dm.push((
            "DistortionCorrectionSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DistortionCorrectionSetting",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x24, bo) {
        dm.push((
            "DigitalLensOptimizerSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DigitalLensOptimizerSetting",
            0x9,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::VignettingCorrUnknown` -- FORMAT int16s, FIRST_ENTRY 1.
fn canon_vignettingcorrunknown(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("VignettingCorrVersion".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "VignettingCorrVersion",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::LightingOpt` -- FORMAT int32s, FIRST_ENTRY 1.
fn canon_lightingopt(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x4, bo) {
        dm.push((
            "PeripheralIlluminationCorr".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PeripheralIlluminationCorr",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x8, bo) {
        dm.push(("AutoLightingOptimizer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Low".to_string(),
            2 => "Strong".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoLightingOptimizer",
            0x2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xc, bo) {
        dm.push(("HighlightTonePriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "Enhanced".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighlightTonePriority",
            0x3,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x10, bo) {
        dm.push((
            "LongExposureNoiseReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Auto".to_string(),
            2 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LongExposureNoiseReduction",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x14, bo) {
        dm.push(("HighISONoiseReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Low".to_string(),
            2 => "Strong".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighISONoiseReduction",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x28, bo) {
        dm.push(("DigitalLensOptimizer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Standard".to_string(),
            2 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DigitalLensOptimizer",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x2c, bo) {
        dm.push(("DualPixelRaw".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DualPixelRaw", 0xb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Canon::Ambience` -- FORMAT int32s, FIRST_ENTRY 1.
fn canon_ambience(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x4, bo) {
        dm.push(("AmbienceSelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Vivid".to_string(),
            2 => "Warm".to_string(),
            3 => "Soft".to_string(),
            4 => "Cool".to_string(),
            5 => "Intense".to_string(),
            6 => "Brighter".to_string(),
            7 => "Darker".to_string(),
            8 => "Monochrome".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AmbienceSelection", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Canon::MultiExp` -- FORMAT int32s, FIRST_ENTRY 1.
fn canon_multiexp(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x4, bo) {
        dm.push(("MultiExposure".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (RAW)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MultiExposure", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x8, bo) {
        dm.push(("MultiExposureControl".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Additive".to_string(),
            1 => "Average".to_string(),
            2 => "Bright (comparative)".to_string(),
            3 => "Dark (comparative)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiExposureControl",
            0x2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xc, bo) {
        dm.push(("MultiExposureShots".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MultiExposureShots",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::HDRInfo` -- FORMAT int32s, FIRST_ENTRY 1.
fn canon_hdrinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x4, bo) {
        dm.push(("HDR".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Auto".to_string(),
            2 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("HDR", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x8, bo) {
        dm.push(("HDREffect".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Natural".to_string(),
            1 => "Art (standard)".to_string(),
            2 => "Art (vivid)".to_string(),
            3 => "Art (bold)".to_string(),
            4 => "Art (embossed)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("HDREffect", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Canon::LogInfo` -- FORMAT int32s, FIRST_ENTRY 1.
fn canon_loginfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x10, bo) {
        dm.push(("CompressionFormat".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Editing (ALL-I)".to_string(),
            1 => "Standard (IPB)".to_string(),
            2 => "Light (IPB)".to_string(),
            3 => "Motion JPEG".to_string(),
            4 => "RAW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("CompressionFormat", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x18, bo) {
        dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0x7fffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "Sharpness",
                0x6,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x1c, bo) {
        dm.push(("Saturation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0x7fffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "Saturation",
                0x7,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x20, bo) {
        dm.push(("ColorTone".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0x7fffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ColorTone",
                0x8,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x24, bo) {
        dm.push(("ColorSpace2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0x7fffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "BT.709".to_string(),
                1 => "BT.2020".to_string(),
                2 => "CinemaGamut".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ColorSpace2", 0x9, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = i32_at(data, 0x28, bo) {
        dm.push(("ColorMatrix".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0x7fffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "EOS Original".to_string(),
                1 => "Neutral".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ColorMatrix", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = i32_at(data, 0x2c, bo) {
        dm.push(("CanonLogVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0x7fffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "OFF".to_string(),
                1 => "CLogV1".to_string(),
                2 => "CLogV2".to_string(),
                3 => "CLogV3".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("CanonLogVersion", 0xb, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::AFConfig` -- FORMAT int32s, FIRST_ENTRY 1.
fn canon_afconfig(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x4, bo) {
        dm.push(("AFConfigTool".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("$val + 1", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                11 => "Case A".to_string(),
                2147483648 => "n/a".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFConfigTool", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = i32_at(data, 0x8, bo) {
        dm.push(("AFTrackingSensitivity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            127 => "Auto".to_string(),
            2147483647 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFTrackingSensitivity",
            0x2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xc, bo) {
        dm.push(("AFAccelDecelTracking".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            127 => "Auto".to_string(),
            2147483647 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFAccelDecelTracking",
            0x3,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x10, bo) {
        dm.push(("AFPointSwitching".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            2147483647 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPointSwitching", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x14, bo) {
        dm.push(("AIServoFirstImage".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Equal Priority".to_string(),
            1 => "Release Priority".to_string(),
            2 => "Focus Priority".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AIServoFirstImage", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x18, bo) {
        dm.push(("AIServoSecondImage".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Equal Priority".to_string(),
            1 => "Release Priority".to_string(),
            2 => "Focus Priority".to_string(),
            3 => "Release High Priority".to_string(),
            4 => "Focus High Priority".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AIServoSecondImage",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_4.is_match(model) {
        if let Some(v) = i32_at(data, 0x1c, bo) {
            dm.push(("USMLensElectronicMF".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Disable After One-Shot".to_string(),
                1 => "One-Shot -> Enabled".to_string(),
                2 => "One-Shot -> Enabled (magnify)".to_string(),
                3 => "Disable in AF Mode".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "USMLensElectronicMF",
                0x7,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(MODEL_RE_4.is_match(model)) {
        if let Some(v) = i32_at(data, 0x1c, bo) {
            dm.push(("USMLensElectronicMF".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Enable After AF".to_string(),
                1 => "Disable After AF".to_string(),
                2 => "Disable in AF Mode".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "USMLensElectronicMF",
                0x7,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x20, bo) {
        dm.push(("AFAssistBeam".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Enable".to_string(),
            1 => "Disable".to_string(),
            2 => "IR AF Assist Beam Only".to_string(),
            3 => "LED AF Assist Beam Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssistBeam", 0x8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x24, bo) {
        dm.push(("OneShotAFRelease".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus Priority".to_string(),
            1 => "Release Priority".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("OneShotAFRelease", 0x9, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if !MODEL_RE_5.is_match(model) {
        if let Some(v) = i32_at(data, 0x28, bo) {
            dm.push((
                "AutoAFPointSelEOSiTRAF".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Enable".to_string(),
                1 => "Disable".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "AutoAFPointSelEOSiTRAF",
                0xa,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x2c, bo) {
        dm.push((
            "LensDriveWhenAFImpossible".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Continue Focus Search".to_string(),
            1 => "Stop Focus Search".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LensDriveWhenAFImpossible",
            0xb,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x30, bo) {
        dm.push((
            "SelectAFAreaSelectionMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("Image::ExifTool::DecodeBits($val, { 0 => 'Single-point AF', 1 => 'Auto', 2 => 'Zone AF', 3 => 'AF Point Expansion (4 point)', 4 => 'Spot AF', 5 => 'AF Point Expansion (8 point)' })", &cv, &ctx) { cv = x; }
        tags.push(mk(
            "SelectAFAreaSelectionMode",
            0xc,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x34, bo) {
        dm.push(("AFAreaSelectionMethod".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "M-Fn Button".to_string(),
            1 => "Main Dial".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFAreaSelectionMethod",
            0xd,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x38, bo) {
        dm.push(("OrientationLinkedAF".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Same for Vert/Horiz Points".to_string(),
            1 => "Separate Vert/Horiz Points".to_string(),
            2 => "Separate Area+Points".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "OrientationLinkedAF",
            0xe,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x3c, bo) {
        dm.push((
            "ManualAFPointSelPattern".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Stops at AF Area Edges".to_string(),
            1 => "Continuous".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ManualAFPointSelPattern",
            0xf,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x40, bo) {
        dm.push((
            "AFPointDisplayDuringFocus".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Selected (constant)".to_string(),
            1 => "All (constant)".to_string(),
            2 => "Selected (pre-AF, focused)".to_string(),
            3 => "Selected (focused)".to_string(),
            4 => "Disabled".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointDisplayDuringFocus",
            0x10,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x44, bo) {
        dm.push(("VFDisplayIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Enable".to_string(),
            2 => "Disable".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VFDisplayIllumination",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_6.is_match(model) {
        if let Some(v) = i32_at(data, 0x48, bo) {
            dm.push(("AFStatusViewfinder".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Show in Field of View".to_string(),
                1 => "Show Outside View".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "AFStatusViewfinder",
                0x12,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_6.is_match(model) {
        if let Some(v) = i32_at(data, 0x4c, bo) {
            dm.push(("InitialAFPointInServo".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Initial AF Point Selected".to_string(),
                1 => "Manual AF Point".to_string(),
                2 => "Auto".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "InitialAFPointInServo",
                0x13,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x50, bo) {
        dm.push(("SubjectToDetect".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "People".to_string(),
            2 => "Animals".to_string(),
            3 => "Vehicles".to_string(),
            4 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SubjectToDetect", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x54, bo) {
        dm.push(("SubjectSwitching".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Initial Priority".to_string(),
            1 => "On Subject".to_string(),
            2 => "Switch Subject".to_string(),
            2147483647 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SubjectSwitching", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x60, bo) {
        dm.push(("EyeDetection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Auto".to_string(),
            2 => "Left Eye".to_string(),
            3 => "Right Eye".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("EyeDetection", 0x18, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x68, bo) {
        dm.push(("WholeAreaTracking".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "WholeAreaTracking",
            0x1a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x6c, bo) {
        dm.push((
            "ServoAFCharacteristics".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Case Auto".to_string(),
            1 => "Case Manual".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ServoAFCharacteristics",
            0x1b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x70, bo) {
        dm.push(("CaseAutoSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "Locked On".to_string(),
            0 => "Standard".to_string(),
            1 => "Responsive".to_string(),
            2147483647 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("CaseAutoSetting", 0x1c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x74, bo) {
        dm.push(("ActionPriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ActionPriority", 0x1d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x78, bo) {
        dm.push(("SportEvents".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Soccer".to_string(),
            1 => "Basketball".to_string(),
            2 => "Volleyball".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SportEvents", 0x1e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Canon::FocusBracketingInfo` -- FORMAT int32s, FIRST_ENTRY 1.
fn canon_focusbracketinginfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x4, bo) {
        dm.push(("FocusBracketing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusBracketing", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x8, bo) {
        dm.push((
            "FocusBracketingImageCount".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusBracketingImageCount",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xc, bo) {
        dm.push((
            "FocusBracketingFocusIncrement".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusBracketingFocusIncrement",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x10, bo) {
        dm.push((
            "FocusBracketingExposureSmoothing".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusBracketingExposureSmoothing",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x14, bo) {
        dm.push((
            "FocusBracketingDepthComposite".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusBracketingDepthComposite",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x18, bo) {
        dm.push((
            "FocusBracketingCropDepthComposite".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusBracketingCropDepthComposite",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x1c, bo) {
        dm.push((
            "FocusBracketingFlashInterval".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusBracketingFlashInterval",
            0x7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::LevelInfo` -- FORMAT int32s, FIRST_ENTRY 1.
fn canon_levelinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x10, bo) {
        dm.push(("RollAngle".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val > 1800 and $val -= 3600; -$val / 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "RollAngle",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x14, bo) {
        dm.push(("PitchAngle".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val > 1800 and $val -= 3600; $val / 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "PitchAngle",
                0x5,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x1c, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x20, bo) {
        dm.push(("MinFocalLength2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "MinFocalLength2",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x24, bo) {
        dm.push(("MaxFocalLength2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "MaxFocalLength2",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::FocalInfo` -- FORMAT int32u, FIRST_ENTRY 0.
fn canon_focalinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = rat32u_at(data, 0x0, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f mm\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FocalLength",
            0x0,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo1D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo1d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16_at(data, 0xa, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0xa,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0xd, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                -1 => "n/a".to_string(),
                1 => "Canon EF 50mm f/1.8".to_string(),
                2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
                3 => "Canon EF 135mm f/2.8 Soft".to_string(),
                4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
                5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
                6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
                7 => "Canon EF 100-300mm f/5.6L".to_string(),
                8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
                9 => "Canon EF 70-210mm f/4".to_string(),
                10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
                11 => "Canon EF 35mm f/2".to_string(),
                13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
                14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
                15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
                16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
                17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
                18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
                20 => "Canon EF 100-200mm f/4.5A".to_string(),
                21 => "Canon EF 80-200mm f/2.8L".to_string(),
                22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
                23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
                24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
                25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
                26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
                27 => "Canon EF 35-80mm f/4-5.6".to_string(),
                28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
                29 => "Canon EF 50mm f/1.8 II".to_string(),
                30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
                31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
                32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
                33 => "Voigtlander or Carl Zeiss Lens".to_string(),
                35 => "Canon EF 35-80mm f/4-5.6".to_string(),
                36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
                37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
                38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
                39 => "Canon EF 75-300mm f/4-5.6".to_string(),
                40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
                41 => "Canon EF 28-90mm f/4-5.6".to_string(),
                42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
                43 => "Canon EF 28-105mm f/4-5.6".to_string(),
                44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
                45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
                46 => "Canon EF 28-90mm f/4-5.6".to_string(),
                47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
                48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
                49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
                50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
                51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
                52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
                53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
                54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
                60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
                63 => "Irix 30mm F1.4 Dragonfly".to_string(),
                80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
                81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
                82 => "Canon TS-E 135mm f/4L Macro".to_string(),
                94 => "Canon TS-E 17mm f/4L".to_string(),
                95 => "Canon TS-E 24mm f/3.5L II".to_string(),
                103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
                106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
                112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
                117 => {
                    "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string()
                }
                124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
                125 => "Canon TS-E 24mm f/3.5L".to_string(),
                126 => "Canon TS-E 45mm f/2.8".to_string(),
                127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
                129 => "Canon EF 300mm f/2.8L USM".to_string(),
                130 => "Canon EF 50mm f/1.0L USM".to_string(),
                131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
                132 => "Canon EF 1200mm f/5.6L USM".to_string(),
                134 => "Canon EF 600mm f/4L IS USM".to_string(),
                135 => "Canon EF 200mm f/1.8L USM".to_string(),
                136 => "Canon EF 300mm f/2.8L USM".to_string(),
                137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
                138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
                139 => "Canon EF 400mm f/2.8L USM".to_string(),
                140 => "Canon EF 500mm f/4.5L USM".to_string(),
                141 => "Canon EF 500mm f/4.5L USM".to_string(),
                142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
                143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
                144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
                145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
                146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
                147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
                148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                149 => "Canon EF 100mm f/2 USM".to_string(),
                150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
                151 => "Canon EF 200mm f/2.8L USM".to_string(),
                152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
                153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
                154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
                155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
                156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
                160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
                161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
                162 => "Canon EF 200mm f/2.8L USM".to_string(),
                163 => "Canon EF 300mm f/4L".to_string(),
                164 => "Canon EF 400mm f/5.6L".to_string(),
                165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
                166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
                167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
                168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
                169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
                170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
                171 => "Canon EF 300mm f/4L USM".to_string(),
                172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
                173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
                174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
                175 => "Canon EF 400mm f/2.8L USM".to_string(),
                176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
                177 => "Canon EF 300mm f/4L IS USM".to_string(),
                178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
                179 => "Canon EF 24mm f/1.4L USM".to_string(),
                180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
                181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
                182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
                183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
                184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
                185 => "Canon EF 600mm f/4L IS USM".to_string(),
                186 => "Canon EF 70-200mm f/4L USM".to_string(),
                187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
                188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
                189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
                190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
                191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
                193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
                194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
                195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
                196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
                197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
                198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
                199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
                201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
                208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
                209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
                210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
                211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
                212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
                213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
                214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
                215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
                217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
                220 => "Yongnuo YN 50mm f/1.8".to_string(),
                224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
                225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
                226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
                227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
                228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
                229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
                230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
                231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
                232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
                233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
                234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
                235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
                236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
                237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
                238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
                239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
                240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
                241 => "Canon EF 50mm f/1.2L USM".to_string(),
                242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
                243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
                244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
                245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
                246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
                247 => "Canon EF 14mm f/2.8L II USM".to_string(),
                248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
                249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
                250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
                251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
                252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
                253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
                254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
                255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
                368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
                488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
                489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
                490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
                491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
                492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
                493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
                494 => "Canon EF 600mm f/4L IS II USM".to_string(),
                495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
                496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
                499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
                502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
                503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
                504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
                505 => "Canon EF 35mm f/2 IS USM".to_string(),
                506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
                507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
                508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
                624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
                747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
                748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
                749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
                750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
                751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
                752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
                753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
                754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
                757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
                758 => "Canon EF 600mm f/4L IS III USM".to_string(),
                923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
                1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
                4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
                4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
                4144 => "Canon EF 40mm f/2.8 STM".to_string(),
                4145 => "Canon EF-M 22mm f/2 STM".to_string(),
                4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
                4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
                4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
                4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
                4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
                4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
                4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
                4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
                4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
                4156 => "Canon EF 50mm f/1.8 STM".to_string(),
                4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
                4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
                4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
                4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
                4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
                4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
                6512 => "Sigma 12mm F1.4 DC | C".to_string(),
                36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
                36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
                61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
                61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
                61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
                61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
                61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
                61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
                65535 => "n/a".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("LensType", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u16_at(data, 0xe, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0xe,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x10, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x10,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_7.is_match(model) {
        if let Some(v) = u8_at(data, 0x41) {
            dm.push(("SharpnessFrequency".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "n/a".to_string(),
                1 => "Lowest".to_string(),
                2 => "Low".to_string(),
                3 => "Standard".to_string(),
                4 => "High".to_string(),
                5 => "Highest".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "SharpnessFrequency",
                0x41,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_7.is_match(model) {
        if let Some(v) = i8_at(data, 0x42) {
            dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "Sharpness",
                0x42,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_7.is_match(model) {
        if let Some(v) = u8_at(data, 0x44) {
            dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Auto".to_string(),
                1 => "Daylight".to_string(),
                2 => "Cloudy".to_string(),
                3 => "Tungsten".to_string(),
                4 => "Fluorescent".to_string(),
                5 => "Flash".to_string(),
                6 => "Custom".to_string(),
                7 => "Black & White".to_string(),
                8 => "Shade".to_string(),
                9 => "Manual Temperature (Kelvin)".to_string(),
                10 => "PC Set1".to_string(),
                11 => "PC Set2".to_string(),
                12 => "PC Set3".to_string(),
                14 => "Daylight Fluorescent".to_string(),
                15 => "Custom 1".to_string(),
                16 => "Custom 2".to_string(),
                17 => "Underwater".to_string(),
                18 => "Custom 3".to_string(),
                19 => "Custom 4".to_string(),
                20 => "PC Set4".to_string(),
                21 => "PC Set5".to_string(),
                23 => "Auto (ambience priority)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("WhiteBalance", 0x44, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_8.is_match(model) {
        if let Some(v) = u8_at(data, 0x47) {
            dm.push(("SharpnessFrequency".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "n/a".to_string(),
                1 => "Lowest".to_string(),
                2 => "Low".to_string(),
                3 => "Standard".to_string(),
                4 => "High".to_string(),
                5 => "Highest".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "SharpnessFrequency",
                0x47,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_7.is_match(model) {
        if let Some(v) = u16_at(data, 0x48, bo) {
            dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ColorTemperature",
                0x48,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(MODEL_RE_7.is_match(model)) && MODEL_RE_8.is_match(model) {
        if let Some(v) = i8_at(data, 0x48) {
            dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "Sharpness",
                0x48,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_8.is_match(model) {
        if let Some(v) = u8_at(data, 0x4a) {
            dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Auto".to_string(),
                1 => "Daylight".to_string(),
                2 => "Cloudy".to_string(),
                3 => "Tungsten".to_string(),
                4 => "Fluorescent".to_string(),
                5 => "Flash".to_string(),
                6 => "Custom".to_string(),
                7 => "Black & White".to_string(),
                8 => "Shade".to_string(),
                9 => "Manual Temperature (Kelvin)".to_string(),
                10 => "PC Set1".to_string(),
                11 => "PC Set2".to_string(),
                12 => "PC Set3".to_string(),
                14 => "Daylight Fluorescent".to_string(),
                15 => "Custom 1".to_string(),
                16 => "Custom 2".to_string(),
                17 => "Underwater".to_string(),
                18 => "Custom 3".to_string(),
                19 => "Custom 4".to_string(),
                20 => "PC Set4".to_string(),
                21 => "PC Set5".to_string(),
                23 => "Auto (ambience priority)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("WhiteBalance", 0x4a, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_7.is_match(model) {
        if let Some(v) = u8_at(data, 0x4b) {
            dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Standard".to_string(),
                2 => "Portrait".to_string(),
                3 => "High Saturation".to_string(),
                4 => "Adobe RGB".to_string(),
                5 => "Low Saturation".to_string(),
                6 => "CM Set 1".to_string(),
                7 => "CM Set 2".to_string(),
                33 => "User Def. 1".to_string(),
                34 => "User Def. 2".to_string(),
                35 => "User Def. 3".to_string(),
                65 => "PC 1".to_string(),
                66 => "PC 2".to_string(),
                67 => "PC 3".to_string(),
                129 => "Standard".to_string(),
                130 => "Portrait".to_string(),
                131 => "Landscape".to_string(),
                132 => "Neutral".to_string(),
                133 => "Faithful".to_string(),
                134 => "Monochrome".to_string(),
                135 => "Auto".to_string(),
                136 => "Fine Detail".to_string(),
                255 => "n/a".to_string(),
                65535 => "n/a".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("PictureStyle", 0x4b, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_8.is_match(model) {
        if let Some(v) = u16_at(data, 0x4e, bo) {
            dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ColorTemperature",
                0x4e,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_8.is_match(model) {
        if let Some(v) = u8_at(data, 0x51) {
            dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Standard".to_string(),
                2 => "Portrait".to_string(),
                3 => "High Saturation".to_string(),
                4 => "Adobe RGB".to_string(),
                5 => "Low Saturation".to_string(),
                6 => "CM Set 1".to_string(),
                7 => "CM Set 2".to_string(),
                33 => "User Def. 1".to_string(),
                34 => "User Def. 2".to_string(),
                35 => "User Def. 3".to_string(),
                65 => "PC 1".to_string(),
                66 => "PC 2".to_string(),
                67 => "PC 3".to_string(),
                129 => "Standard".to_string(),
                130 => "Portrait".to_string(),
                131 => "Landscape".to_string(),
                132 => "Neutral".to_string(),
                133 => "Faithful".to_string(),
                134 => "Monochrome".to_string(),
                135 => "Auto".to_string(),
                136 => "Fine Detail".to_string(),
                255 => "n/a".to_string(),
                65535 => "n/a".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("PictureStyle", 0x51, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo1DmkII` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo1dmkii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16rev_at(data, 0x9, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0xc, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                -1 => "n/a".to_string(),
                1 => "Canon EF 50mm f/1.8".to_string(),
                2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
                3 => "Canon EF 135mm f/2.8 Soft".to_string(),
                4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
                5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
                6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
                7 => "Canon EF 100-300mm f/5.6L".to_string(),
                8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
                9 => "Canon EF 70-210mm f/4".to_string(),
                10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
                11 => "Canon EF 35mm f/2".to_string(),
                13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
                14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
                15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
                16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
                17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
                18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
                20 => "Canon EF 100-200mm f/4.5A".to_string(),
                21 => "Canon EF 80-200mm f/2.8L".to_string(),
                22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
                23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
                24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
                25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
                26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
                27 => "Canon EF 35-80mm f/4-5.6".to_string(),
                28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
                29 => "Canon EF 50mm f/1.8 II".to_string(),
                30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
                31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
                32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
                33 => "Voigtlander or Carl Zeiss Lens".to_string(),
                35 => "Canon EF 35-80mm f/4-5.6".to_string(),
                36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
                37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
                38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
                39 => "Canon EF 75-300mm f/4-5.6".to_string(),
                40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
                41 => "Canon EF 28-90mm f/4-5.6".to_string(),
                42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
                43 => "Canon EF 28-105mm f/4-5.6".to_string(),
                44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
                45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
                46 => "Canon EF 28-90mm f/4-5.6".to_string(),
                47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
                48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
                49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
                50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
                51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
                52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
                53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
                54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
                60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
                63 => "Irix 30mm F1.4 Dragonfly".to_string(),
                80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
                81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
                82 => "Canon TS-E 135mm f/4L Macro".to_string(),
                94 => "Canon TS-E 17mm f/4L".to_string(),
                95 => "Canon TS-E 24mm f/3.5L II".to_string(),
                103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
                106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
                112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
                117 => {
                    "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string()
                }
                124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
                125 => "Canon TS-E 24mm f/3.5L".to_string(),
                126 => "Canon TS-E 45mm f/2.8".to_string(),
                127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
                129 => "Canon EF 300mm f/2.8L USM".to_string(),
                130 => "Canon EF 50mm f/1.0L USM".to_string(),
                131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
                132 => "Canon EF 1200mm f/5.6L USM".to_string(),
                134 => "Canon EF 600mm f/4L IS USM".to_string(),
                135 => "Canon EF 200mm f/1.8L USM".to_string(),
                136 => "Canon EF 300mm f/2.8L USM".to_string(),
                137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
                138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
                139 => "Canon EF 400mm f/2.8L USM".to_string(),
                140 => "Canon EF 500mm f/4.5L USM".to_string(),
                141 => "Canon EF 500mm f/4.5L USM".to_string(),
                142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
                143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
                144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
                145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
                146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
                147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
                148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                149 => "Canon EF 100mm f/2 USM".to_string(),
                150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
                151 => "Canon EF 200mm f/2.8L USM".to_string(),
                152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
                153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
                154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
                155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
                156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
                160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
                161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
                162 => "Canon EF 200mm f/2.8L USM".to_string(),
                163 => "Canon EF 300mm f/4L".to_string(),
                164 => "Canon EF 400mm f/5.6L".to_string(),
                165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
                166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
                167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
                168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
                169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
                170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
                171 => "Canon EF 300mm f/4L USM".to_string(),
                172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
                173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
                174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
                175 => "Canon EF 400mm f/2.8L USM".to_string(),
                176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
                177 => "Canon EF 300mm f/4L IS USM".to_string(),
                178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
                179 => "Canon EF 24mm f/1.4L USM".to_string(),
                180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
                181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
                182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
                183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
                184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
                185 => "Canon EF 600mm f/4L IS USM".to_string(),
                186 => "Canon EF 70-200mm f/4L USM".to_string(),
                187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
                188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
                189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
                190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
                191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
                193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
                194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
                195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
                196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
                197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
                198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
                199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
                201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
                208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
                209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
                210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
                211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
                212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
                213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
                214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
                215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
                217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
                220 => "Yongnuo YN 50mm f/1.8".to_string(),
                224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
                225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
                226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
                227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
                228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
                229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
                230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
                231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
                232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
                233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
                234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
                235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
                236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
                237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
                238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
                239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
                240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
                241 => "Canon EF 50mm f/1.2L USM".to_string(),
                242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
                243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
                244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
                245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
                246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
                247 => "Canon EF 14mm f/2.8L II USM".to_string(),
                248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
                249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
                250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
                251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
                252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
                253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
                254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
                255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
                368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
                488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
                489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
                490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
                491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
                492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
                493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
                494 => "Canon EF 600mm f/4L IS II USM".to_string(),
                495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
                496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
                499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
                502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
                503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
                504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
                505 => "Canon EF 35mm f/2 IS USM".to_string(),
                506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
                507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
                508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
                624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
                747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
                748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
                749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
                750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
                751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
                752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
                753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
                754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
                757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
                758 => "Canon EF 600mm f/4L IS III USM".to_string(),
                923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
                1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
                4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
                4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
                4144 => "Canon EF 40mm f/2.8 STM".to_string(),
                4145 => "Canon EF-M 22mm f/2 STM".to_string(),
                4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
                4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
                4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
                4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
                4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
                4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
                4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
                4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
                4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
                4156 => "Canon EF 50mm f/1.8 STM".to_string(),
                4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
                4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
                4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
                4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
                4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
                4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
                6512 => "Sigma 12mm F1.4 DC | C".to_string(),
                36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
                36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
                61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
                61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
                61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
                61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
                61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
                61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
                65535 => "n/a".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("LensType", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u16rev_at(data, 0x11, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x11,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x13, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x13,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2d) {
        dm.push(("FocalType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Fixed".to_string(),
            2 => "Zoom".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocalType", 0x2d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x36) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x36, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x37, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x37,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x39, bo) {
        dm.push(("CanonImageSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            0 => "Large".to_string(),
            1 => "Medium".to_string(),
            2 => "Small".to_string(),
            5 => "Medium 1".to_string(),
            6 => "Medium 2".to_string(),
            7 => "Medium 3".to_string(),
            8 => "Postcard".to_string(),
            9 => "Widescreen".to_string(),
            10 => "Medium Widescreen".to_string(),
            14 => "Small 1".to_string(),
            15 => "Small 2".to_string(),
            16 => "Small 3".to_string(),
            128 => "640x480 Movie".to_string(),
            129 => "Medium Movie".to_string(),
            130 => "Small Movie".to_string(),
            137 => "1280x720 Movie".to_string(),
            142 => "1920x1080 Movie".to_string(),
            143 => "4096x2160 Movie".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("CanonImageSize", 0x39, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x66) {
        dm.push(("JPEGQuality".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "JPEGQuality",
            0x66,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6c) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0x6c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i8_at(data, 0x6e) {
        dm.push(("Saturation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Saturation",
            0x6e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x6f) {
        dm.push(("ColorTone".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTone",
            0x6f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x72) {
        dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Sharpness",
            0x72,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x73) {
        dm.push(("Contrast".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Contrast",
            0x73,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x75, 5, true) {
        tags.push(mk(
            "ISO",
            0x75,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo1DmkIIN` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo1dmkiin(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16rev_at(data, 0x9, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0xc, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                -1 => "n/a".to_string(),
                1 => "Canon EF 50mm f/1.8".to_string(),
                2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
                3 => "Canon EF 135mm f/2.8 Soft".to_string(),
                4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
                5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
                6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
                7 => "Canon EF 100-300mm f/5.6L".to_string(),
                8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
                9 => "Canon EF 70-210mm f/4".to_string(),
                10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
                11 => "Canon EF 35mm f/2".to_string(),
                13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
                14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
                15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
                16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
                17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
                18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
                20 => "Canon EF 100-200mm f/4.5A".to_string(),
                21 => "Canon EF 80-200mm f/2.8L".to_string(),
                22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
                23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
                24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
                25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
                26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
                27 => "Canon EF 35-80mm f/4-5.6".to_string(),
                28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
                29 => "Canon EF 50mm f/1.8 II".to_string(),
                30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
                31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
                32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
                33 => "Voigtlander or Carl Zeiss Lens".to_string(),
                35 => "Canon EF 35-80mm f/4-5.6".to_string(),
                36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
                37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
                38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
                39 => "Canon EF 75-300mm f/4-5.6".to_string(),
                40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
                41 => "Canon EF 28-90mm f/4-5.6".to_string(),
                42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
                43 => "Canon EF 28-105mm f/4-5.6".to_string(),
                44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
                45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
                46 => "Canon EF 28-90mm f/4-5.6".to_string(),
                47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
                48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
                49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
                50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
                51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
                52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
                53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
                54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
                60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
                63 => "Irix 30mm F1.4 Dragonfly".to_string(),
                80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
                81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
                82 => "Canon TS-E 135mm f/4L Macro".to_string(),
                94 => "Canon TS-E 17mm f/4L".to_string(),
                95 => "Canon TS-E 24mm f/3.5L II".to_string(),
                103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
                106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
                112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
                117 => {
                    "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string()
                }
                124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
                125 => "Canon TS-E 24mm f/3.5L".to_string(),
                126 => "Canon TS-E 45mm f/2.8".to_string(),
                127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
                129 => "Canon EF 300mm f/2.8L USM".to_string(),
                130 => "Canon EF 50mm f/1.0L USM".to_string(),
                131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
                132 => "Canon EF 1200mm f/5.6L USM".to_string(),
                134 => "Canon EF 600mm f/4L IS USM".to_string(),
                135 => "Canon EF 200mm f/1.8L USM".to_string(),
                136 => "Canon EF 300mm f/2.8L USM".to_string(),
                137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
                138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
                139 => "Canon EF 400mm f/2.8L USM".to_string(),
                140 => "Canon EF 500mm f/4.5L USM".to_string(),
                141 => "Canon EF 500mm f/4.5L USM".to_string(),
                142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
                143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
                144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
                145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
                146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
                147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
                148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                149 => "Canon EF 100mm f/2 USM".to_string(),
                150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
                151 => "Canon EF 200mm f/2.8L USM".to_string(),
                152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
                153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
                154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
                155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
                156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
                160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
                161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
                162 => "Canon EF 200mm f/2.8L USM".to_string(),
                163 => "Canon EF 300mm f/4L".to_string(),
                164 => "Canon EF 400mm f/5.6L".to_string(),
                165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
                166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
                167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
                168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
                169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
                170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
                171 => "Canon EF 300mm f/4L USM".to_string(),
                172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
                173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
                174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
                175 => "Canon EF 400mm f/2.8L USM".to_string(),
                176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
                177 => "Canon EF 300mm f/4L IS USM".to_string(),
                178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
                179 => "Canon EF 24mm f/1.4L USM".to_string(),
                180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
                181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
                182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
                183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
                184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
                185 => "Canon EF 600mm f/4L IS USM".to_string(),
                186 => "Canon EF 70-200mm f/4L USM".to_string(),
                187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
                188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
                189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
                190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
                191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
                193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
                194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
                195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
                196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
                197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
                198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
                199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
                201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
                208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
                209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
                210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
                211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
                212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
                213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
                214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
                215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
                217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
                220 => "Yongnuo YN 50mm f/1.8".to_string(),
                224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
                225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
                226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
                227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
                228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
                229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
                230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
                231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
                232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
                233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
                234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
                235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
                236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
                237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
                238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
                239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
                240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
                241 => "Canon EF 50mm f/1.2L USM".to_string(),
                242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
                243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
                244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
                245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
                246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
                247 => "Canon EF 14mm f/2.8L II USM".to_string(),
                248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
                249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
                250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
                251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
                252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
                253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
                254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
                255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
                368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
                488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
                489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
                490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
                491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
                492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
                493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
                494 => "Canon EF 600mm f/4L IS II USM".to_string(),
                495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
                496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
                499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
                502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
                503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
                504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
                505 => "Canon EF 35mm f/2 IS USM".to_string(),
                506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
                507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
                508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
                624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
                747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
                748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
                749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
                750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
                751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
                752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
                753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
                754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
                757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
                758 => "Canon EF 600mm f/4L IS III USM".to_string(),
                923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
                1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
                4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
                4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
                4144 => "Canon EF 40mm f/2.8 STM".to_string(),
                4145 => "Canon EF-M 22mm f/2 STM".to_string(),
                4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
                4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
                4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
                4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
                4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
                4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
                4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
                4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
                4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
                4156 => "Canon EF 50mm f/1.8 STM".to_string(),
                4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
                4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
                4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
                4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
                4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
                4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
                6512 => "Sigma 12mm F1.4 DC | C".to_string(),
                36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
                36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
                61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
                61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
                61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
                61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
                61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
                61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
                65535 => "n/a".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("LensType", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u16rev_at(data, 0x11, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x11,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x13, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x13,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x36) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x36, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x37, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x37,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x73) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0x73, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i8_at(data, 0x74) {
        dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Sharpness",
            0x74,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x75) {
        dm.push(("Contrast".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Contrast",
            0x75,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x76) {
        dm.push(("Saturation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Saturation",
            0x76,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x77) {
        dm.push(("ColorTone".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTone",
            0x77,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x79, 5, true) {
        tags.push(mk(
            "ISO",
            0x79,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo1DmkIII` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo1dmkiii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x18) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x18,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "LensType").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "LensType").is_some_and(|v| v == 124.0))
    {
        if let Some(v) = u8_at(data, 0x1b) {
            dm.push(("MacroMagnification".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("exp((75-$val) * log(2) * 3 / 40)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.1fx\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "MacroMagnification",
                    0x1b,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16rev_at(data, 0x1d, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x30) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x43, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x43,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x45, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x45,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x5e, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x5e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x62, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x62,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x86) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0x86, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x111, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x111, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x113, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x113,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x115, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x115,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x136, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x136,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x172, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x172,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x176, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ShutterCount",
                0x176,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x17e, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x17e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_9.is_match(model) {
        if let Some(v) = u32_at(data, 0x45a, bo) {
            dm.push(("TimeStamp1".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                let mut cv = base;
                let vc = conv_expr::eval_with("ConvertUnixTime($val)", &cv, &ctx);
                if vc.as_ref() != Some(&Conv::Undef) {
                    if let Some(x) = vc {
                        cv = x;
                    }
                    let raw = Value::F64(cv.as_num());
                    if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx)
                    {
                        cv = x;
                    }
                    tags.push(mk(
                        "TimeStamp1",
                        0x45a,
                        cv.as_string(),
                        raw,
                        GRP0,
                        GRP1,
                        GRP2,
                        PRIO,
                    ));
                }
            }
        }
    }
    if let Some(v) = u32_at(data, 0x45e, bo) {
        dm.push(("TimeStamp".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("ConvertUnixTime($val)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "TimeStamp",
                    0x45e,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(sub) = data.get(0x2aa..) {
        tags.extend(canon_psinfo(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo1DmkIV` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo1dmkiv(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 509, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "
            my $t = substr($val, 0x1e8, 6); # 1 = firmware 4.2.1
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 1, return undef;
            $t = substr($val, 0x1ed, 6);    # 2 = firmware 1.0.4
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 2, return undef;
            $self->Warn('Unrecognized CameraInfo1DmkIV firmware version');
            0;
            return undef;   # not a real tag
        ",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersionLookAhead",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x7) {
        dm.push(("HighlightTonePriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighlightTonePriority",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        dm.push(("MeasuredEV2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 8 - 6", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "MeasuredEV2",
                    0x8,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        dm.push(("MeasuredEV3".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 8 - 6", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "MeasuredEV3",
                    0x9,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push(("FlashMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "E-TTL".to_string(),
            3 => "TTL".to_string(),
            4 => "External Auto".to_string(),
            5 => "External Manual".to_string(),
            6 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashMeteringMode",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x19) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x1e, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x35) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x35,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x54, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x54,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x56, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x56,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x78, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x78, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x7c, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x7c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x14f, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x14f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x151, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x151,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x153, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x153,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x1ed, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x1ed,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x22c, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x22c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x238, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x238,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x368..) {
        tags.extend(canon_psinfo(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo1DX` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo1dx(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 651, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "
            my $t = substr($val, 0x271, 6); # 1 = firmware 5.7.1
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 1, return undef;
            $t = substr($val, 0x279, 6);    # 2 = firmware 6.5.1
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 2, return undef;
            $t = substr($val, 0x280, 6);    # 3 = firmware 0.0.8/1.0.2/1.1.1
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 3, return undef;
            $t = substr($val, 0x285, 6);    # 4 = firmware 2.1.0
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 4, return undef;
            $self->Warn('Unrecognized CameraInfo1DX firmware version');
            0;
            return undef;   # not a real tag
        ",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersionLookAhead",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x1b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x23, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x23,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x7d) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x7d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x8c, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x8c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x8e, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x8e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xbc, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0xbc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xc0, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0xc0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf4) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0xf4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x1a7, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x1a7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x1a9, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x1a9,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x1ab, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x1ab,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x280, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x280,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x2d0, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x2d0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x2dc, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x2dc,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x3f4..) {
        tags.extend(canon_psinfo2(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo5D` -- FORMAT int8s, FIRST_ENTRY 0.
fn canon_camerainfo5d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u16rev_at(data, 0xc, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                -1 => "n/a".to_string(),
                1 => "Canon EF 50mm f/1.8".to_string(),
                2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
                3 => "Canon EF 135mm f/2.8 Soft".to_string(),
                4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
                5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
                6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
                7 => "Canon EF 100-300mm f/5.6L".to_string(),
                8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
                9 => "Canon EF 70-210mm f/4".to_string(),
                10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
                11 => "Canon EF 35mm f/2".to_string(),
                13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
                14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
                15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
                16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
                17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
                18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
                20 => "Canon EF 100-200mm f/4.5A".to_string(),
                21 => "Canon EF 80-200mm f/2.8L".to_string(),
                22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
                23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
                24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
                25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
                26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
                27 => "Canon EF 35-80mm f/4-5.6".to_string(),
                28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
                29 => "Canon EF 50mm f/1.8 II".to_string(),
                30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
                31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
                32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
                33 => "Voigtlander or Carl Zeiss Lens".to_string(),
                35 => "Canon EF 35-80mm f/4-5.6".to_string(),
                36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
                37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
                38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
                39 => "Canon EF 75-300mm f/4-5.6".to_string(),
                40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
                41 => "Canon EF 28-90mm f/4-5.6".to_string(),
                42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
                43 => "Canon EF 28-105mm f/4-5.6".to_string(),
                44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
                45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
                46 => "Canon EF 28-90mm f/4-5.6".to_string(),
                47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
                48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
                49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
                50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
                51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
                52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
                53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
                54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
                60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
                63 => "Irix 30mm F1.4 Dragonfly".to_string(),
                80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
                81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
                82 => "Canon TS-E 135mm f/4L Macro".to_string(),
                94 => "Canon TS-E 17mm f/4L".to_string(),
                95 => "Canon TS-E 24mm f/3.5L II".to_string(),
                103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
                106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
                112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
                117 => {
                    "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string()
                }
                124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
                125 => "Canon TS-E 24mm f/3.5L".to_string(),
                126 => "Canon TS-E 45mm f/2.8".to_string(),
                127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
                129 => "Canon EF 300mm f/2.8L USM".to_string(),
                130 => "Canon EF 50mm f/1.0L USM".to_string(),
                131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
                132 => "Canon EF 1200mm f/5.6L USM".to_string(),
                134 => "Canon EF 600mm f/4L IS USM".to_string(),
                135 => "Canon EF 200mm f/1.8L USM".to_string(),
                136 => "Canon EF 300mm f/2.8L USM".to_string(),
                137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
                138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
                139 => "Canon EF 400mm f/2.8L USM".to_string(),
                140 => "Canon EF 500mm f/4.5L USM".to_string(),
                141 => "Canon EF 500mm f/4.5L USM".to_string(),
                142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
                143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
                144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
                145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
                146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
                147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
                148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                149 => "Canon EF 100mm f/2 USM".to_string(),
                150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
                151 => "Canon EF 200mm f/2.8L USM".to_string(),
                152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
                153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
                154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
                155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
                156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
                160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
                161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
                162 => "Canon EF 200mm f/2.8L USM".to_string(),
                163 => "Canon EF 300mm f/4L".to_string(),
                164 => "Canon EF 400mm f/5.6L".to_string(),
                165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
                166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
                167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
                168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
                169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
                170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
                171 => "Canon EF 300mm f/4L USM".to_string(),
                172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
                173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
                174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
                175 => "Canon EF 400mm f/2.8L USM".to_string(),
                176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
                177 => "Canon EF 300mm f/4L IS USM".to_string(),
                178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
                179 => "Canon EF 24mm f/1.4L USM".to_string(),
                180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
                181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
                182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
                183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
                184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
                185 => "Canon EF 600mm f/4L IS USM".to_string(),
                186 => "Canon EF 70-200mm f/4L USM".to_string(),
                187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
                188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
                189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
                190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
                191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
                193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
                194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
                195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
                196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
                197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
                198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
                199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
                201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
                202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
                208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
                209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
                210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
                211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
                212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
                213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
                214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
                215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
                217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
                220 => "Yongnuo YN 50mm f/1.8".to_string(),
                224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
                225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
                226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
                227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
                228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
                229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
                230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
                231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
                232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
                233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
                234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
                235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
                236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
                237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
                238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
                239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
                240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
                241 => "Canon EF 50mm f/1.2L USM".to_string(),
                242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
                243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
                244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
                245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
                246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
                247 => "Canon EF 14mm f/2.8L II USM".to_string(),
                248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
                249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
                250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
                251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
                252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
                253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
                254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
                255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
                368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
                488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
                489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
                490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
                491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
                492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
                493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
                494 => "Canon EF 600mm f/4L IS II USM".to_string(),
                495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
                496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
                499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
                502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
                503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
                504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
                505 => "Canon EF 35mm f/2 IS USM".to_string(),
                506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
                507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
                508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
                624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
                747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
                748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
                749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
                750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
                751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
                752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
                753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
                754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
                757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
                758 => "Canon EF 600mm f/4L IS III USM".to_string(),
                923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
                1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
                4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
                4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
                4144 => "Canon EF 40mm f/2.8 STM".to_string(),
                4145 => "Canon EF-M 22mm f/2 STM".to_string(),
                4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
                4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
                4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
                4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
                4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
                4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
                4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
                4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
                4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
                4156 => "Canon EF 50mm f/1.8 STM".to_string(),
                4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
                4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
                4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
                4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
                4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
                4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
                6512 => "Sigma 12mm F1.4 DC | C".to_string(),
                36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
                36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
                61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
                61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
                61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
                61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
                61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
                61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
                65535 => "n/a".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("LensType", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x17) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x17,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "LensType").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "LensType").is_some_and(|v| v == 124.0))
    {
        if let Some(v) = i8_at(data, 0x1b) {
            dm.push(("MacroMagnification".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("exp((75-$val) * log(2) * 3 / 40)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.1fx\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "MacroMagnification",
                    0x1b,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = i8_at(data, 0x27) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x27,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x28, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x28,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x38, bo) {
        dm.push(("AFPointsInFocus5D".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Center".to_string(),
            1 => "Top".to_string(),
            2 => "Bottom".to_string(),
            3 => "Upper-left".to_string(),
            4 => "Upper-right".to_string(),
            5 => "Lower-left".to_string(),
            6 => "Lower-right".to_string(),
            7 => "Left".to_string(),
            8 => "Right".to_string(),
            9 => "AI Servo1".to_string(),
            10 => "AI Servo2".to_string(),
            11 => "AI Servo3".to_string(),
            12 => "AI Servo4".to_string(),
            13 => "AI Servo5".to_string(),
            14 => "AI Servo6".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointsInFocus5D",
            0x38,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x54, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x54, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x58, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x58,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6c) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0x6c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x93, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x93,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x95, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x95,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x97, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x97, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(text) = text_at(data, 0xa4, 8, true) {
        tags.push(mk(
            "FirmwareRevision",
            0xa4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0xac, 16, true) {
        tags.push(mk(
            "ShortOwnerName",
            0xac,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xcc, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "DirectoryIndex",
            0xcc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xd0, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0xd0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0xe8) {
        dm.push(("ContrastStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ContrastStandard",
            0xe8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xe9) {
        dm.push(("ContrastPortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ContrastPortrait",
            0xe9,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xea) {
        dm.push(("ContrastLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ContrastLandscape",
            0xea,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xeb) {
        dm.push(("ContrastNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ContrastNeutral",
            0xeb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xec) {
        dm.push(("ContrastFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ContrastFaithful",
            0xec,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xed) {
        dm.push(("ContrastMonochrome".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ContrastMonochrome",
            0xed,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xee) {
        dm.push(("ContrastUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ContrastUserDef1",
            0xee,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xef) {
        dm.push(("ContrastUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ContrastUserDef2",
            0xef,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xf0) {
        dm.push(("ContrastUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ContrastUserDef3",
            0xf0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xf1) {
        dm.push(("SharpnessStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SharpnessStandard",
            0xf1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xf2) {
        dm.push(("SharpnessPortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SharpnessPortrait",
            0xf2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xf3) {
        dm.push(("SharpnessLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SharpnessLandscape",
            0xf3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xf4) {
        dm.push(("SharpnessNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SharpnessNeutral",
            0xf4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xf5) {
        dm.push(("SharpnessFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SharpnessFaithful",
            0xf5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xf6) {
        dm.push(("SharpnessMonochrome".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SharpnessMonochrome",
            0xf6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xf7) {
        dm.push(("SharpnessUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SharpnessUserDef1",
            0xf7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xf8) {
        dm.push(("SharpnessUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SharpnessUserDef2",
            0xf8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xf9) {
        dm.push(("SharpnessUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SharpnessUserDef3",
            0xf9,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xfa) {
        dm.push(("SaturationStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SaturationStandard",
            0xfa,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xfb) {
        dm.push(("SaturationPortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SaturationPortrait",
            0xfb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xfc) {
        dm.push(("SaturationLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SaturationLandscape",
            0xfc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xfd) {
        dm.push(("SaturationNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SaturationNeutral",
            0xfd,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xfe) {
        dm.push(("SaturationFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SaturationFaithful",
            0xfe,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0xff) {
        dm.push((
            "FilterEffectMonochrome".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Yellow".to_string(),
            2 => "Orange".to_string(),
            3 => "Red".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FilterEffectMonochrome",
            0xff,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x100) {
        dm.push(("SaturationUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SaturationUserDef1",
            0x100,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x101) {
        dm.push(("SaturationUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SaturationUserDef2",
            0x101,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x102) {
        dm.push(("SaturationUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SaturationUserDef3",
            0x102,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x103) {
        dm.push(("ColorToneStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorToneStandard",
            0x103,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x104) {
        dm.push(("ColorTonePortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTonePortrait",
            0x104,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x105) {
        dm.push(("ColorToneLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorToneLandscape",
            0x105,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x106) {
        dm.push(("ColorToneNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorToneNeutral",
            0x106,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x107) {
        dm.push(("ColorToneFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorToneFaithful",
            0x107,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x108) {
        dm.push((
            "ToningEffectMonochrome".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Sepia".to_string(),
            2 => "Blue".to_string(),
            3 => "Purple".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ToningEffectMonochrome",
            0x108,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x109) {
        dm.push(("ColorToneUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorToneUserDef1",
            0x109,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x10a) {
        dm.push(("ColorToneUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorToneUserDef2",
            0x10a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x10b) {
        dm.push(("ColorToneUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorToneUserDef3",
            0x10b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x10c, bo) {
        dm.push(("UserDef1PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "UserDef1PictureStyle",
            0x10c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x10e, bo) {
        dm.push(("UserDef2PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "UserDef2PictureStyle",
            0x10e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x110, bo) {
        dm.push(("UserDef3PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "UserDef3PictureStyle",
            0x110,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x11c, bo) {
        dm.push(("TimeStamp".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("ConvertUnixTime($val)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "TimeStamp",
                    0x11c,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo5DmkII` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo5dmkii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 388, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "
            my $t = substr($val, 0x15a, 6); # 1 = firmware 3.4.6/3.6.1
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 1, return undef;
            $t = substr($val, 0x17e, 6);    # 2 = firmware 4.1.1/1.0.6
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 2, return undef;
            $self->Warn('Unrecognized CameraInfo5DmkII firmware version');
            0;
            return undef;   # not a real tag
        ",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersionLookAhead",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x7) {
        dm.push(("HighlightTonePriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighlightTonePriority",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = v & 0x7f;
        dm.push(("FlashModel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "n/a".to_string(),
            4 => "Speedlite 540EZ".to_string(),
            5 => "Speedlite 380EX".to_string(),
            6 => "Speedlite 550EX".to_string(),
            8 => "Speedlite ST-E2".to_string(),
            9 => "Speedlite MR-14EX".to_string(),
            12 => "Speedlite 580EX".to_string(),
            13 => "Speedlite 430EX".to_string(),
            17 => "Speedlite 580EX II".to_string(),
            18 => "Speedlite 430EX II".to_string(),
            22 => "Speedlite 600EX-RT".to_string(),
            23 => "Speedlite 600EX II-RT".to_string(),
            24 => "Speedlite 90EX".to_string(),
            25 => "Speedlite 430EX III-RT".to_string(),
            31 => "Speedlite EL-1 ver2".to_string(),
            33 => "Speedlite EL-5".to_string(),
            34 => "Speedlite EL-10".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashModel", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push(("FlashMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "E-TTL".to_string(),
            3 => "TTL".to_string(),
            4 => "External Auto".to_string(),
            5 => "External Manual".to_string(),
            6 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashMeteringMode",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x19) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "LensType").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "LensType").is_some_and(|v| v == 124.0))
    {
        if let Some(v) = u8_at(data, 0x1b) {
            dm.push(("MacroMagnification".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("exp((75-$val) * log(2) * 3 / 40)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.1fx\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "MacroMagnification",
                    0x1b,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16rev_at(data, 0x1e, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x31) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x31,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x50, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x50,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x52, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x52,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x6f, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x6f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x73, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x73,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa7) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0xa7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xbd) {
        dm.push(("HighISONoiseReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Low".to_string(),
            2 => "Strong".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighISONoiseReduction",
            0xbd,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xbf) {
        dm.push(("AutoLightingOptimizer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Low".to_string(),
            2 => "Strong".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoLightingOptimizer",
            0xbf,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xe6, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0xe6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0xe8, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0xe8,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xea, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0xea,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x17e, 6, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val=~/^\\d+\\.\\d+\\.\\d+\\s*$/ ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x17e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x18e, 32, true) {
        tags.push(mk(
            "OwnerName",
            0x18e,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x1bb, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x1bb,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1c7, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x1c7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x2f7..) {
        tags.extend(canon_psinfo(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo5DmkIII` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo5dmkiii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 589, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "
            my $t = substr($val, 0x22c, 6); # 1 = firmware 4.5.4/4.5.6
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 1, return undef;
            $t = substr($val, 0x22d, 6);    # 2 = firmware 5.2.2/5.3.1/5.4.2
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 2, return undef;
            $t = substr($val, 0x23c, 6);    # 3 = firmware 1.0.3/1.0.7
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 3, return undef;
            $t = substr($val, 0x242, 6);    # 4 = firmware 1.2.1
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 4, return undef;
            $t = substr($val, 0x247, 6);    # 5 = firmware 1.3.5
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 5, return undef;
            $self->Warn('Unrecognized CameraInfo5DmkIII firmware version');
            0;
            return undef;   # not a real tag
        ",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersionLookAhead",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x1b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x23, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x23,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x7d) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x7d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x8c, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x8c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x8e, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x8e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xbc, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0xbc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xc0, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0xc0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf4) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0xf4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x153, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x153, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x155, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x155,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x157, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x157,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x164, 5, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with("unpack(\"H*\",$val)", &cv, &ctx) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        tags.push(mk(
            "LensSerialNumber",
            0x164,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x23c, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x23c,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x28c, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x28c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x290, bo) {
        dm.push(("FileIndex2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex2",
                0x290,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x298, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x298,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x29c, bo) {
        dm.push(("DirectoryIndex2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex2",
                0x29c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x3b0..) {
        tags.extend(canon_psinfo2(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo6D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo6d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x1b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x23, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x23,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x83) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x83,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x92, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x92,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x94, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x94,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xc2, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0xc2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xc6, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0xc6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xfa) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0xfa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x161, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x161, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x163, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x163,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x165, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x165,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x256, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x256,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x2aa, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x2aa,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x2b6, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x2b6,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x3c6..) {
        tags.extend(canon_psinfo2(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo7D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo7d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 434, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "
            my $t = substr($val, 0x1a8, 6); # 1 = firmware 3.7.5
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 1, return undef;
            $t = substr($val, 0x1ac, 6);    # 2 = firmware 1.0.7/1.0.8/1.1.0/1.2.1/1.2.2
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 2, return undef;
            $self->Warn('Unrecognized CameraInfo7D firmware version');
            0;
            return undef;   # not a real tag
        ",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersionLookAhead",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x7) {
        dm.push(("HighlightTonePriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighlightTonePriority",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        dm.push(("MeasuredEV2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 8 - 6", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "MeasuredEV2",
                    0x8,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        dm.push(("MeasuredEV".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 8 - 6", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "MeasuredEV",
                    0x9,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push(("FlashMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "E-TTL".to_string(),
            3 => "TTL".to_string(),
            4 => "External Auto".to_string(),
            5 => "External Manual".to_string(),
            6 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashMeteringMode",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x19) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x1e, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x35) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x35,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x54, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x54,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x56, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x56,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x77, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x77, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x7b, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x7b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xaf) {
        dm.push(("CameraPictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            33 => "User Defined 1".to_string(),
            34 => "User Defined 2".to_string(),
            35 => "User Defined 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraPictureStyle",
            0xaf,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc9) {
        dm.push(("HighISONoiseReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Low".to_string(),
            2 => "Strong".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighISONoiseReduction",
            0xc9,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x112, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x112, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x114, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x114,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x116, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x116,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x1ac, 6, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val=~/^\\d+\\.\\d+\\.\\d+\\s*$/ ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x1ac,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1eb, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x1eb,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1f7, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x1f7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x327..) {
        tags.extend(canon_psinfo(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo40D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo40d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push(("FlashMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "E-TTL".to_string(),
            3 => "TTL".to_string(),
            4 => "External Auto".to_string(),
            5 => "External Manual".to_string(),
            6 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashMeteringMode",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x18) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x18,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "LensType").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "LensType").is_some_and(|v| v == 124.0))
    {
        if let Some(v) = u8_at(data, 0x1b) {
            dm.push(("MacroMagnification".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("exp((75-$val) * log(2) * 3 / 40)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.1fx\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "MacroMagnification",
                    0x1b,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16rev_at(data, 0x1d, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x30) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x43, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x43,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x45, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x45,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x6f, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x6f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x73, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x73,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xd6, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0xd6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0xd8, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0xd8,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xda, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0xda,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0xff, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0xff,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x133, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x133,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x13f, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x13f,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x92b, 64, true) {
        tags.push(mk(
            "LensModel",
            0x92b,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x25b..) {
        tags.extend(canon_psinfo(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo50D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo50d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 356, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "
            my $t = substr($val, 0x15a, 6); # 1 = firmware 2.6.1
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 1, return undef;
            $t = substr($val, 0x15e, 6);    # 2 = firmware 2.9.1/3.1.1/1.0.2/1.0.3
            $t =~ /^\\d+\\.\\d+\\.\\d+/ and 2, return undef;
            $self->Warn('Unrecognized CameraInfo50D firmware version');
            0;
            return undef;   # not a real tag
        ",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersionLookAhead",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x7) {
        dm.push(("HighlightTonePriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighlightTonePriority",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push(("FlashMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "E-TTL".to_string(),
            3 => "TTL".to_string(),
            4 => "External Auto".to_string(),
            5 => "External Manual".to_string(),
            6 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashMeteringMode",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x19) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x1e, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x31) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x31,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x50, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x50,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x52, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x52,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x6f, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x6f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x73, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x73,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa7) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0xa7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xbd) {
        dm.push(("HighISONoiseReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Low".to_string(),
            2 => "Strong".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighISONoiseReduction",
            0xbd,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xbf) {
        dm.push(("AutoLightingOptimizer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Low".to_string(),
            2 => "Strong".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoLightingOptimizer",
            0xbf,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xea, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0xea, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0xec, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0xec,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xee, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0xee,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x15e, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x15e,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x19b, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x19b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1a7, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x1a7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x2d7..) {
        tags.extend(canon_psinfo(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo60D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo60d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x19) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x1e, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_10.is_match(model) {
        if let Some(v) = u8_at(data, 0x36) {
            dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Horizontal (normal)".to_string(),
                1 => "Rotate 90 CW".to_string(),
                2 => "Rotate 270 CW".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CameraOrientation",
                0x36,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_11.is_match(model) {
        if let Some(v) = u8_at(data, 0x3a) {
            dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Horizontal (normal)".to_string(),
                1 => "Rotate 90 CW".to_string(),
                2 => "Rotate 270 CW".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CameraOrientation",
                0x3a,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_10.is_match(model) {
        if let Some(v) = u16rev_at(data, 0x55, bo) {
            dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) =
                    conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
                {
                    cv = x;
                }
                tags.push(mk(
                    "FocusDistanceUpper",
                    0x55,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if MODEL_RE_10.is_match(model) {
        if let Some(v) = u16rev_at(data, 0x57, bo) {
            dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) =
                    conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
                {
                    cv = x;
                }
                tags.push(mk(
                    "FocusDistanceLower",
                    0x57,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if MODEL_RE_10.is_match(model) {
        if let Some(v) = u16_at(data, 0x7d, bo) {
            dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ColorTemperature",
                0x7d,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0xe8, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0xe8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0xea, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0xea,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xec, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0xec,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x199, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x199,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_10.is_match(model) {
        if let Some(v) = u32_at(data, 0x1d9, bo) {
            dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "FileIndex",
                    0x1d9,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if MODEL_RE_10.is_match(model) {
        if let Some(v) = u32_at(data, 0x1e5, bo) {
            dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "DirectoryIndex",
                    0x1e5,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if MODEL_RE_11.is_match(model) {
        if let Some(sub) = data.get(0x2f9..) {
            tags.extend(canon_psinfo2(sub, make, model, bo, file_type, format, dm));
        }
    }
    if MODEL_RE_10.is_match(model) {
        if let Some(sub) = data.get(0x321..) {
            tags.extend(canon_psinfo2(sub, make, model, bo, file_type, format, dm));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo70D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo70d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x1b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x23, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x23,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x84) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x84,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x93, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x93,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x95, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x95,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xc7, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0xc7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x166, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x166, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x168, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x168,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x16a, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x16a,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x25e, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x25e,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x2b3, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x2b3,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x2bf, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x2bf,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x3cf..) {
        tags.extend(canon_psinfo2(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo80D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo80d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x1b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x23, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x23,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x96) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x96,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xa5, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0xa5,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0xa7, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0xa7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x13a, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x13a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x189, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x189, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x18b, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x18b,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x18d, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x18d,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x45a, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x45a,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x4ae, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x4ae,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x4ba, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x4ba,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo450D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo450d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push(("FlashMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "E-TTL".to_string(),
            3 => "TTL".to_string(),
            4 => "External Auto".to_string(),
            5 => "External Manual".to_string(),
            6 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashMeteringMode",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x18) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x18,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "LensType").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "LensType").is_some_and(|v| v == 124.0))
    {
        if let Some(v) = u8_at(data, 0x1b) {
            dm.push(("MacroMagnification".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("exp((75-$val) * log(2) * 3 / 40)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.1fx\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "MacroMagnification",
                    0x1b,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16rev_at(data, 0x1d, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x30) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x43, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x43,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x45, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x45,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x6f, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x6f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x73, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x73,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xde, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0xde, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(text) = text_at(data, 0x107, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x107,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x10f, 32, true) {
        tags.push(mk(
            "OwnerName",
            0x10f,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x133, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "DirectoryIndex",
            0x133,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x13f, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x13f,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x933, 64, true) {
        tags.push(mk(
            "LensModel",
            0x933,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x263..) {
        tags.extend(canon_psinfo(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo500D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo500d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x7) {
        dm.push(("HighlightTonePriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighlightTonePriority",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push(("FlashMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "E-TTL".to_string(),
            3 => "TTL".to_string(),
            4 => "External Auto".to_string(),
            5 => "External Manual".to_string(),
            6 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashMeteringMode",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x19) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x1e, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x31) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x31,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x50, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x50,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x52, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x52,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x73, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x73, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x77, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x77,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xab) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0xab, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xbc) {
        dm.push(("HighISONoiseReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Low".to_string(),
            2 => "Strong".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighISONoiseReduction",
            0xbc,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xbe) {
        dm.push(("AutoLightingOptimizer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Low".to_string(),
            2 => "Strong".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoLightingOptimizer",
            0xbe,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xf6, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0xf6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0xf8, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0xf8,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xfa, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0xfa,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x190, 6, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val=~/^\\d+\\.\\d+\\.\\d+\\s*$/ ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x190,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1d3, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x1d3,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1df, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x1df,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x30b..) {
        tags.extend(canon_psinfo(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo550D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo550d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x7) {
        dm.push(("HighlightTonePriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighlightTonePriority",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push(("FlashMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "E-TTL".to_string(),
            3 => "TTL".to_string(),
            4 => "External Auto".to_string(),
            5 => "External Manual".to_string(),
            6 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashMeteringMode",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x19) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x1e, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x35) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x35,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x54, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x54,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x56, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x56,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x78, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x78, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x7c, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x7c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb0) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0xb0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0xff, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0xff, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x101, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x101,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x103, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x103,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x1a4, 6, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val=~/^\\d+\\.\\d+\\.\\d+\\s*$/ ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x1a4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1e4, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x1e4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1f0, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x1f0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x31c..) {
        tags.extend(canon_psinfo(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo600D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo600d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x7) {
        dm.push(("HighlightTonePriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighlightTonePriority",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push(("FlashMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "E-TTL".to_string(),
            3 => "TTL".to_string(),
            4 => "External Auto".to_string(),
            5 => "External Manual".to_string(),
            6 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashMeteringMode",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x19) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x1e, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x38) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x38,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x57, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x57,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x59, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x59,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x7b, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x7b, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x7f, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x7f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb3) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0xb3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0xea, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0xea, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0xec, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0xec,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xee, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0xee,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x19b, 6, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val=~/^\\d+\\.\\d+\\.\\d+\\s*$/ ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x19b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1db, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x1db,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1e7, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DirectoryIndex",
                0x1e7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = data.get(0x2fb..) {
        tags.extend(canon_psinfo2(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo650D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo650d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x1b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x23, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x23,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x7d) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x7d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x8c, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x8c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x8e, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x8e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xbc, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0xbc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xc0, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0xc0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf4) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0xf4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x127, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x127, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x129, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x129,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x12b, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x12b,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_12.is_match(model) {
        if let Some(text) = text_at(data, 0x21b, 6, true) {
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(text);
            let rc =
                conv_expr::eval_with("$val=~/^\\d+\\.\\d+\\.\\d+\\s*$/ ? $val : undef", &cv, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = rc {
                    cv = x;
                }
                let raw = Value::String(cv.as_string());
                tags.push(mk(
                    "FirmwareVersion",
                    0x21b,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if MODEL_RE_13.is_match(model) {
        if let Some(text) = text_at(data, 0x220, 6, true) {
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(text);
            let rc =
                conv_expr::eval_with("$val=~/^\\d+\\.\\d+\\.\\d+\\s*$/ ? $val : undef", &cv, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = rc {
                    cv = x;
                }
                let raw = Value::String(cv.as_string());
                tags.push(mk(
                    "FirmwareVersion",
                    0x220,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if MODEL_RE_12.is_match(model) {
        if let Some(v) = u32_at(data, 0x270, bo) {
            dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "FileIndex",
                    0x270,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if MODEL_RE_13.is_match(model) {
        if let Some(v) = u32_at(data, 0x274, bo) {
            dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "FileIndex",
                    0x274,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if MODEL_RE_12.is_match(model) {
        if let Some(v) = u32_at(data, 0x27c, bo) {
            dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "DirectoryIndex",
                    0x27c,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if MODEL_RE_13.is_match(model) {
        if let Some(v) = u32_at(data, 0x280, bo) {
            dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val - 1", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "DirectoryIndex",
                    0x280,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(sub) = data.get(0x390..) {
        tags.extend(canon_psinfo2(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo750D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo750d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x1b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x23, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x23,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x96) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x96,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xa5, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0xa5,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0xa7, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0xa7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x131, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x131, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x135, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x135,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x169) {
        dm.push(("PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Standard".to_string(),
            2 => "Portrait".to_string(),
            3 => "High Saturation".to_string(),
            4 => "Adobe RGB".to_string(),
            5 => "Low Saturation".to_string(),
            6 => "CM Set 1".to_string(),
            7 => "CM Set 2".to_string(),
            33 => "User Def. 1".to_string(),
            34 => "User Def. 2".to_string(),
            35 => "User Def. 3".to_string(),
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            136 => "Fine Detail".to_string(),
            255 => "n/a".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureStyle", 0x169, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x184, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x184, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0x186, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0x186,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x188, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0x188,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x43d, 6, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val=~/^\\d+\\.\\d+\\.\\d+\\s*$/ ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x43d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x449, 6, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val=~/^\\d+\\.\\d+\\.\\d+\\s*$/ ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x449,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfo1000D` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfo1000d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("exp(($val-8)/16*log(2))", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FNumber",
                    0x3,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(4*log(2)*(1-Image::ExifTool::Canon::CanonEv($val-24)))",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ExposureTime",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/8-9)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x6, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = v & 0x7f;
        dm.push(("FlashModel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "n/a".to_string(),
            4 => "Speedlite 540EZ".to_string(),
            5 => "Speedlite 380EX".to_string(),
            6 => "Speedlite 550EX".to_string(),
            8 => "Speedlite ST-E2".to_string(),
            9 => "Speedlite MR-14EX".to_string(),
            12 => "Speedlite 580EX".to_string(),
            13 => "Speedlite 430EX".to_string(),
            17 => "Speedlite 580EX II".to_string(),
            18 => "Speedlite 430EX II".to_string(),
            22 => "Speedlite 600EX-RT".to_string(),
            23 => "Speedlite 600EX II-RT".to_string(),
            24 => "Speedlite 90EX".to_string(),
            25 => "Speedlite 430EX III-RT".to_string(),
            31 => "Speedlite EL-1 ver2".to_string(),
            33 => "Speedlite EL-5".to_string(),
            34 => "Speedlite EL-10".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashModel", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push(("FlashMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "E-TTL".to_string(),
            3 => "TTL".to_string(),
            4 => "External Auto".to_string(),
            5 => "External Manual".to_string(),
            6 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashMeteringMode",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x18) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x18,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "LensType").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "LensType").is_some_and(|v| v == 124.0))
    {
        if let Some(v) = u8_at(data, 0x1b) {
            dm.push(("MacroMagnification".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("exp((75-$val) * log(2) * 3 / 40)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.1fx\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "MacroMagnification",
                    0x1b,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16rev_at(data, 0x1d, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocalLength",
                0x1d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x30) {
        dm.push(("CameraOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CameraOrientation",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0x43, bo) {
        dm.push(("FocusDistanceUpper".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceUpper",
                0x43,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x45, bo) {
        dm.push(("FocusDistanceLower".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 655.345 ? \"inf\" : \"$val m\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistanceLower",
                0x45,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x6f, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Tungsten".to_string(),
            4 => "Fluorescent".to_string(),
            5 => "Flash".to_string(),
            6 => "Custom".to_string(),
            7 => "Black & White".to_string(),
            8 => "Shade".to_string(),
            9 => "Manual Temperature (Kelvin)".to_string(),
            10 => "PC Set1".to_string(),
            11 => "PC Set2".to_string(),
            12 => "PC Set3".to_string(),
            14 => "Daylight Fluorescent".to_string(),
            15 => "Custom 1".to_string(),
            16 => "Custom 2".to_string(),
            17 => "Underwater".to_string(),
            18 => "Custom 3".to_string(),
            19 => "Custom 4".to_string(),
            20 => "PC Set4".to_string(),
            21 => "PC Set5".to_string(),
            23 => "Auto (ambience priority)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x6f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x73, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTemperature",
            0x73,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xe2, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -1 => "n/a".to_string(),
            1 => "Canon EF 50mm f/1.8".to_string(),
            2 => "Canon EF 28mm f/2.8 or Sigma Lens".to_string(),
            3 => "Canon EF 135mm f/2.8 Soft".to_string(),
            4 => "Canon EF 35-105mm f/3.5-4.5 or Sigma Lens".to_string(),
            5 => "Canon EF 35-70mm f/3.5-4.5".to_string(),
            6 => "Canon EF 28-70mm f/3.5-4.5 or Sigma or Tokina Lens".to_string(),
            7 => "Canon EF 100-300mm f/5.6L".to_string(),
            8 => "Canon EF 100-300mm f/5.6 or Sigma or Tokina Lens".to_string(),
            9 => "Canon EF 70-210mm f/4".to_string(),
            10 => "Canon EF 50mm f/2.5 Macro or Sigma Lens".to_string(),
            11 => "Canon EF 35mm f/2".to_string(),
            13 => "Canon EF 15mm f/2.8 Fisheye".to_string(),
            14 => "Canon EF 50-200mm f/3.5-4.5L".to_string(),
            15 => "Canon EF 50-200mm f/3.5-4.5".to_string(),
            16 => "Canon EF 35-135mm f/3.5-4.5".to_string(),
            17 => "Canon EF 35-70mm f/3.5-4.5A".to_string(),
            18 => "Canon EF 28-70mm f/3.5-4.5".to_string(),
            20 => "Canon EF 100-200mm f/4.5A".to_string(),
            21 => "Canon EF 80-200mm f/2.8L".to_string(),
            22 => "Canon EF 20-35mm f/2.8L or Tokina Lens".to_string(),
            23 => "Canon EF 35-105mm f/3.5-4.5".to_string(),
            24 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            25 => "Canon EF 35-80mm f/4-5.6 Power Zoom".to_string(),
            26 => "Canon EF 100mm f/2.8 Macro or Other Lens".to_string(),
            27 => "Canon EF 35-80mm f/4-5.6".to_string(),
            28 => "Canon EF 80-200mm f/4.5-5.6 or Tamron Lens".to_string(),
            29 => "Canon EF 50mm f/1.8 II".to_string(),
            30 => "Canon EF 35-105mm f/4.5-5.6".to_string(),
            31 => "Canon EF 75-300mm f/4-5.6 or Tamron Lens".to_string(),
            32 => "Canon EF 24mm f/2.8 or Sigma Lens".to_string(),
            33 => "Voigtlander or Carl Zeiss Lens".to_string(),
            35 => "Canon EF 35-80mm f/4-5.6".to_string(),
            36 => "Canon EF 38-76mm f/4.5-5.6".to_string(),
            37 => "Canon EF 35-80mm f/4-5.6 or Tamron Lens".to_string(),
            38 => "Canon EF 80-200mm f/4.5-5.6 II".to_string(),
            39 => "Canon EF 75-300mm f/4-5.6".to_string(),
            40 => "Canon EF 28-80mm f/3.5-5.6".to_string(),
            41 => "Canon EF 28-90mm f/4-5.6".to_string(),
            42 => "Canon EF 28-200mm f/3.5-5.6 or Tamron Lens".to_string(),
            43 => "Canon EF 28-105mm f/4-5.6".to_string(),
            44 => "Canon EF 90-300mm f/4.5-5.6".to_string(),
            45 => "Canon EF-S 18-55mm f/3.5-5.6 [II]".to_string(),
            46 => "Canon EF 28-90mm f/4-5.6".to_string(),
            47 => "Zeiss Milvus 35mm f/2 or 50mm f/2".to_string(),
            48 => "Canon EF-S 18-55mm f/3.5-5.6 IS".to_string(),
            49 => "Canon EF-S 55-250mm f/4-5.6 IS".to_string(),
            50 => "Canon EF-S 18-200mm f/3.5-5.6 IS".to_string(),
            51 => "Canon EF-S 18-135mm f/3.5-5.6 IS".to_string(),
            52 => "Canon EF-S 18-55mm f/3.5-5.6 IS II".to_string(),
            53 => "Canon EF-S 18-55mm f/3.5-5.6 III".to_string(),
            54 => "Canon EF-S 55-250mm f/4-5.6 IS II".to_string(),
            60 => "Irix 11mm f/4 or 15mm f/2.4".to_string(),
            63 => "Irix 30mm F1.4 Dragonfly".to_string(),
            80 => "Canon TS-E 50mm f/2.8L Macro".to_string(),
            81 => "Canon TS-E 90mm f/2.8L Macro".to_string(),
            82 => "Canon TS-E 135mm f/4L Macro".to_string(),
            94 => "Canon TS-E 17mm f/4L".to_string(),
            95 => "Canon TS-E 24mm f/3.5L II".to_string(),
            103 => "Samyang AF 14mm f/2.8 EF or Rokinon Lens".to_string(),
            106 => "Rokinon SP / Samyang XP 35mm f/1.2".to_string(),
            112 => "Sigma 28mm f/1.5 FF High-speed Prime or other Sigma Lens".to_string(),
            117 => "Tamron 35-150mm f/2.8-4.0 Di VC OSD (A043) or other Tamron Lens".to_string(),
            124 => "Canon MP-E 65mm f/2.8 1-5x Macro Photo".to_string(),
            125 => "Canon TS-E 24mm f/3.5L".to_string(),
            126 => "Canon TS-E 45mm f/2.8".to_string(),
            127 => "Canon TS-E 90mm f/2.8 or Tamron Lens".to_string(),
            129 => "Canon EF 300mm f/2.8L USM".to_string(),
            130 => "Canon EF 50mm f/1.0L USM".to_string(),
            131 => "Canon EF 28-80mm f/2.8-4L USM or Sigma Lens".to_string(),
            132 => "Canon EF 1200mm f/5.6L USM".to_string(),
            134 => "Canon EF 600mm f/4L IS USM".to_string(),
            135 => "Canon EF 200mm f/1.8L USM".to_string(),
            136 => "Canon EF 300mm f/2.8L USM".to_string(),
            137 => "Canon EF 85mm f/1.2L USM or Sigma or Tamron Lens".to_string(),
            138 => "Canon EF 28-80mm f/2.8-4L".to_string(),
            139 => "Canon EF 400mm f/2.8L USM".to_string(),
            140 => "Canon EF 500mm f/4.5L USM".to_string(),
            141 => "Canon EF 500mm f/4.5L USM".to_string(),
            142 => "Canon EF 300mm f/2.8L IS USM".to_string(),
            143 => "Canon EF 500mm f/4L IS USM or Sigma Lens".to_string(),
            144 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            145 => "Canon EF 100-300mm f/4.5-5.6 USM".to_string(),
            146 => "Canon EF 70-210mm f/3.5-4.5 USM".to_string(),
            147 => "Canon EF 35-135mm f/4-5.6 USM".to_string(),
            148 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            149 => "Canon EF 100mm f/2 USM".to_string(),
            150 => "Canon EF 14mm f/2.8L USM or Sigma Lens".to_string(),
            151 => "Canon EF 200mm f/2.8L USM".to_string(),
            152 => "Canon EF 300mm f/4L IS USM or Sigma Lens".to_string(),
            153 => "Canon EF 35-350mm f/3.5-5.6L USM or Sigma or Tamron Lens".to_string(),
            154 => "Canon EF 20mm f/2.8 USM or Zeiss Lens".to_string(),
            155 => "Canon EF 85mm f/1.8 USM or Sigma Lens".to_string(),
            156 => "Canon EF 28-105mm f/3.5-4.5 USM or Tamron Lens".to_string(),
            160 => "Canon EF 20-35mm f/3.5-4.5 USM or Tamron or Tokina Lens".to_string(),
            161 => "Canon EF 28-70mm f/2.8L USM or Other Lens".to_string(),
            162 => "Canon EF 200mm f/2.8L USM".to_string(),
            163 => "Canon EF 300mm f/4L".to_string(),
            164 => "Canon EF 400mm f/5.6L".to_string(),
            165 => "Canon EF 70-200mm f/2.8L USM".to_string(),
            166 => "Canon EF 70-200mm f/2.8L USM + 1.4x".to_string(),
            167 => "Canon EF 70-200mm f/2.8L USM + 2x".to_string(),
            168 => "Canon EF 28mm f/1.8 USM or Sigma Lens".to_string(),
            169 => "Canon EF 17-35mm f/2.8L USM or Sigma Lens".to_string(),
            170 => "Canon EF 200mm f/2.8L II USM or Sigma Lens".to_string(),
            171 => "Canon EF 300mm f/4L USM".to_string(),
            172 => "Canon EF 400mm f/5.6L USM or Sigma Lens".to_string(),
            173 => "Canon EF 180mm Macro f/3.5L USM or Sigma Lens".to_string(),
            174 => "Canon EF 135mm f/2L USM or Other Lens".to_string(),
            175 => "Canon EF 400mm f/2.8L USM".to_string(),
            176 => "Canon EF 24-85mm f/3.5-4.5 USM".to_string(),
            177 => "Canon EF 300mm f/4L IS USM".to_string(),
            178 => "Canon EF 28-135mm f/3.5-5.6 IS".to_string(),
            179 => "Canon EF 24mm f/1.4L USM".to_string(),
            180 => "Canon EF 35mm f/1.4L USM or Other Lens".to_string(),
            181 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 1.4x or Sigma Lens".to_string(),
            182 => "Canon EF 100-400mm f/4.5-5.6L IS USM + 2x or Sigma Lens".to_string(),
            183 => "Canon EF 100-400mm f/4.5-5.6L IS USM or Sigma Lens".to_string(),
            184 => "Canon EF 400mm f/2.8L USM + 2x".to_string(),
            185 => "Canon EF 600mm f/4L IS USM".to_string(),
            186 => "Canon EF 70-200mm f/4L USM".to_string(),
            187 => "Canon EF 70-200mm f/4L USM + 1.4x".to_string(),
            188 => "Canon EF 70-200mm f/4L USM + 2x".to_string(),
            189 => "Canon EF 70-200mm f/4L USM + 2.8x".to_string(),
            190 => "Canon EF 100mm f/2.8 Macro USM".to_string(),
            191 => "Canon EF 400mm f/4 DO IS or Sigma Lens".to_string(),
            193 => "Canon EF 35-80mm f/4-5.6 USM".to_string(),
            194 => "Canon EF 80-200mm f/4.5-5.6 USM".to_string(),
            195 => "Canon EF 35-105mm f/4.5-5.6 USM".to_string(),
            196 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            197 => "Canon EF 75-300mm f/4-5.6 IS USM or Sigma Lens".to_string(),
            198 => "Canon EF 50mm f/1.4 USM or Other Lens".to_string(),
            199 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            200 => "Canon EF 75-300mm f/4-5.6 USM".to_string(),
            201 => "Canon EF 28-80mm f/3.5-5.6 USM".to_string(),
            202 => "Canon EF 28-80mm f/3.5-5.6 USM IV".to_string(),
            208 => "Canon EF 22-55mm f/4-5.6 USM".to_string(),
            209 => "Canon EF 55-200mm f/4.5-5.6".to_string(),
            210 => "Canon EF 28-90mm f/4-5.6 USM".to_string(),
            211 => "Canon EF 28-200mm f/3.5-5.6 USM".to_string(),
            212 => "Canon EF 28-105mm f/4-5.6 USM".to_string(),
            213 => "Canon EF 90-300mm f/4.5-5.6 USM or Tamron Lens".to_string(),
            214 => "Canon EF-S 18-55mm f/3.5-5.6 USM".to_string(),
            215 => "Canon EF 55-200mm f/4.5-5.6 II USM".to_string(),
            217 => "Tamron AF 18-270mm f/3.5-6.3 Di II VC PZD".to_string(),
            220 => "Yongnuo YN 50mm f/1.8".to_string(),
            224 => "Canon EF 70-200mm f/2.8L IS USM".to_string(),
            225 => "Canon EF 70-200mm f/2.8L IS USM + 1.4x".to_string(),
            226 => "Canon EF 70-200mm f/2.8L IS USM + 2x".to_string(),
            227 => "Canon EF 70-200mm f/2.8L IS USM + 2.8x".to_string(),
            228 => "Canon EF 28-105mm f/3.5-4.5 USM".to_string(),
            229 => "Canon EF 16-35mm f/2.8L USM".to_string(),
            230 => "Canon EF 24-70mm f/2.8L USM".to_string(),
            231 => "Canon EF 17-40mm f/4L USM or Sigma Lens".to_string(),
            232 => "Canon EF 70-300mm f/4.5-5.6 DO IS USM".to_string(),
            233 => "Canon EF 28-300mm f/3.5-5.6L IS USM".to_string(),
            234 => "Canon EF-S 17-85mm f/4-5.6 IS USM or Tokina Lens".to_string(),
            235 => "Canon EF-S 10-22mm f/3.5-4.5 USM".to_string(),
            236 => "Canon EF-S 60mm f/2.8 Macro USM".to_string(),
            237 => "Canon EF 24-105mm f/4L IS USM".to_string(),
            238 => "Canon EF 70-300mm f/4-5.6 IS USM".to_string(),
            239 => "Canon EF 85mm f/1.2L II USM or Rokinon Lens".to_string(),
            240 => "Canon EF-S 17-55mm f/2.8 IS USM or Sigma Lens".to_string(),
            241 => "Canon EF 50mm f/1.2L USM".to_string(),
            242 => "Canon EF 70-200mm f/4L IS USM".to_string(),
            243 => "Canon EF 70-200mm f/4L IS USM + 1.4x".to_string(),
            244 => "Canon EF 70-200mm f/4L IS USM + 2x".to_string(),
            245 => "Canon EF 70-200mm f/4L IS USM + 2.8x".to_string(),
            246 => "Canon EF 16-35mm f/2.8L II USM".to_string(),
            247 => "Canon EF 14mm f/2.8L II USM".to_string(),
            248 => "Canon EF 200mm f/2L IS USM or Sigma Lens".to_string(),
            249 => "Canon EF 800mm f/5.6L IS USM".to_string(),
            250 => "Canon EF 24mm f/1.4L II USM or Sigma Lens".to_string(),
            251 => "Canon EF 70-200mm f/2.8L IS II USM".to_string(),
            252 => "Canon EF 70-200mm f/2.8L IS II USM + 1.4x".to_string(),
            253 => "Canon EF 70-200mm f/2.8L IS II USM + 2x".to_string(),
            254 => "Canon EF 100mm f/2.8L Macro IS USM or Tamron Lens".to_string(),
            255 => "Sigma 24-105mm f/4 DG OS HSM | A or Other Lens".to_string(),
            368 => "Sigma 14-24mm f/2.8 DG HSM | A or other Sigma Lens".to_string(),
            488 => "Canon EF-S 15-85mm f/3.5-5.6 IS USM".to_string(),
            489 => "Canon EF 70-300mm f/4-5.6L IS USM".to_string(),
            490 => "Canon EF 8-15mm f/4L Fisheye USM".to_string(),
            491 => "Canon EF 300mm f/2.8L IS II USM or Tamron Lens".to_string(),
            492 => "Canon EF 400mm f/2.8L IS II USM".to_string(),
            493 => "Canon EF 500mm f/4L IS II USM or EF 24-105mm f4L IS USM".to_string(),
            494 => "Canon EF 600mm f/4L IS II USM".to_string(),
            495 => "Canon EF 24-70mm f/2.8L II USM or Sigma Lens".to_string(),
            496 => "Canon EF 200-400mm f/4L IS USM".to_string(),
            499 => "Canon EF 200-400mm f/4L IS USM + 1.4x".to_string(),
            502 => "Canon EF 28mm f/2.8 IS USM or Tamron Lens".to_string(),
            503 => "Canon EF 24mm f/2.8 IS USM".to_string(),
            504 => "Canon EF 24-70mm f/4L IS USM".to_string(),
            505 => "Canon EF 35mm f/2 IS USM".to_string(),
            506 => "Canon EF 400mm f/4 DO IS II USM".to_string(),
            507 => "Canon EF 16-35mm f/4L IS USM".to_string(),
            508 => "Canon EF 11-24mm f/4L USM or Tamron Lens".to_string(),
            624 => "Sigma 70-200mm f/2.8 DG OS HSM | S or other Sigma Lens".to_string(),
            747 => "Canon EF 100-400mm f/4.5-5.6L IS II USM or Tamron Lens".to_string(),
            748 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 1.4x or Tamron Lens".to_string(),
            749 => "Canon EF 100-400mm f/4.5-5.6L IS II USM + 2x or Tamron Lens".to_string(),
            750 => "Canon EF 35mm f/1.4L II USM or Tamron Lens".to_string(),
            751 => "Canon EF 16-35mm f/2.8L III USM".to_string(),
            752 => "Canon EF 24-105mm f/4L IS II USM".to_string(),
            753 => "Canon EF 85mm f/1.4L IS USM".to_string(),
            754 => "Canon EF 70-200mm f/4L IS II USM".to_string(),
            757 => "Canon EF 400mm f/2.8L IS III USM".to_string(),
            758 => "Canon EF 600mm f/4L IS III USM".to_string(),
            923 => "Meike/SKY 85mm f/1.8 DCM".to_string(),
            1136 => "Sigma 24-70mm f/2.8 DG OS HSM | A".to_string(),
            4142 => "Canon EF-S 18-135mm f/3.5-5.6 IS STM".to_string(),
            4143 => "Canon EF-M 18-55mm f/3.5-5.6 IS STM or Tamron Lens".to_string(),
            4144 => "Canon EF 40mm f/2.8 STM".to_string(),
            4145 => "Canon EF-M 22mm f/2 STM".to_string(),
            4146 => "Canon EF-S 18-55mm f/3.5-5.6 IS STM".to_string(),
            4147 => "Canon EF-M 11-22mm f/4-5.6 IS STM".to_string(),
            4148 => "Canon EF-S 55-250mm f/4-5.6 IS STM".to_string(),
            4149 => "Canon EF-M 55-200mm f/4.5-6.3 IS STM".to_string(),
            4150 => "Canon EF-S 10-18mm f/4.5-5.6 IS STM".to_string(),
            4152 => "Canon EF 24-105mm f/3.5-5.6 IS STM".to_string(),
            4153 => "Canon EF-M 15-45mm f/3.5-6.3 IS STM".to_string(),
            4154 => "Canon EF-S 24mm f/2.8 STM".to_string(),
            4155 => "Canon EF-M 28mm f/3.5 Macro IS STM".to_string(),
            4156 => "Canon EF 50mm f/1.8 STM".to_string(),
            4157 => "Canon EF-M 18-150mm f/3.5-6.3 IS STM".to_string(),
            4158 => "Canon EF-S 18-55mm f/4-5.6 IS STM".to_string(),
            4159 => "Canon EF-M 32mm f/1.4 STM".to_string(),
            4160 => "Canon EF-S 35mm f/2.8 Macro IS STM".to_string(),
            4208 => "Sigma 56mm f/1.4 DC DN | C or other Sigma Lens".to_string(),
            4976 => "Sigma 16-300mm F3.5-6.7 DC OS | C (025)".to_string(),
            6512 => "Sigma 12mm F1.4 DC | C".to_string(),
            36910 => "Canon EF 70-300mm f/4-5.6 IS II USM".to_string(),
            36912 => "Canon EF-S 18-135mm f/3.5-5.6 IS USM".to_string(),
            61182 => "Canon RF 50mm F1.2L USM or other Canon RF Lens".to_string(),
            61491 => "Canon CN-E 14mm T3.1 L F".to_string(),
            61492 => "Canon CN-E 24mm T1.5 L F".to_string(),
            61494 => "Canon CN-E 85mm T1.3 L F".to_string(),
            61495 => "Canon CN-E 135mm T2.2 L F".to_string(),
            61496 => "Canon CN-E 35mm T1.5 L F".to_string(),
            65535 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0xe2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16rev_at(data, 0xe4, bo) {
        dm.push(("MinFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MinFocalLength",
            0xe4,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16rev_at(data, 0xe6, bo) {
        dm.push(("MaxFocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val mm\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MaxFocalLength",
            0xe6,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x10b, 6, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x10b,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x137, bo) {
        dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "DirectoryIndex",
            0x137,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x143, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FileIndex",
                0x143,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x937, 64, true) {
        tags.push(mk(
            "LensModel",
            0x937,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x267..) {
        tags.extend(canon_psinfo(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfoR6` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfor6(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x9da) {
        dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x9da,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0xaf1, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0xaf1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfoR6m2` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfor6m2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0xd29, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0xd29,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfoR6m3` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfor6m3(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x86d, bo) {
        dm.push(("ImageCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ImageCount",
            0x86d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfoG5XII` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_camerainfog5xii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if file_type == "JPEG" {
        if let Some(v) = u32_at(data, 0x293, bo) {
            dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ShutterCount",
                0x293,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if file_type == "CR3" {
        if let Some(v) = u32_at(data, 0xa95, bo) {
            dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ShutterCount",
                0xa95,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if file_type == "JPEG" {
        if let Some(v) = u32_at(data, 0xb21, bo) {
            dm.push(("DirectoryIndex".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "DirectoryIndex",
                0xb21,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if file_type == "JPEG" {
        if let Some(v) = u32_at(data, 0xb2d, bo) {
            dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "FileIndex",
                    0xb2d,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfoPowerShot` -- FORMAT int32s, FIRST_ENTRY 0.
fn canon_camerainfopowershot(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x0, bo) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp((($val-411)/96)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x0, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = i32_at(data, 0x14, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("exp($val/192*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FNumber",
                0x5,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x18, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("exp(-$val/96*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x6,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x5c, bo) {
        dm.push(("Rotation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Rotation",
            0x17,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 138.0) {
        if let Some(v) = i32_at(data, 0x21c, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x87,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 148.0) {
        if let Some(v) = i32_at(data, 0x244, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x91,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfoPowerShot2` -- FORMAT int32s, FIRST_ENTRY 0.
fn canon_camerainfopowershot2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x4, bo) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp((($val-411)/96)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.0f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk("ISO", 0x1, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = i32_at(data, 0x18, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("exp($val/192*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FNumber",
                0x6,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x1c, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("exp(-$val/96*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i32_at(data, 0x60, bo) {
        dm.push(("Rotation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Rotation",
            0x18,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 156.0) {
        if let Some(v) = i32_at(data, 0x264, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x99,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 162.0) {
        if let Some(v) = i32_at(data, 0x27c, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x9f,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 167.0) {
        if let Some(v) = i32_at(data, 0x290, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0xa4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 171.0) {
        if let Some(v) = i32_at(data, 0x2a0, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0xa8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 264.0) {
        if let Some(v) = i32_at(data, 0x414, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x105,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfoUnknown32` -- FORMAT int32s, FIRST_ENTRY 0.
fn canon_camerainfounknown32(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 72.0) {
        if let Some(v) = i32_at(data, 0x11c, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x47,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 85.0) {
        if let Some(v) = i32_at(data, 0x14c, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x53,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 93.0)
        || dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 94.0))
    {
        if let Some(v) = i32_at(data, 0x16c, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x5b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 96.0) {
        if let Some(v) = i32_at(data, 0x170, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x5c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CameraInfoCount").is_some_and(|v| v == 104.0) {
        if let Some(v) = i32_at(data, 0x190, bo) {
            dm.push(("CameraTemperature".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CameraTemperature",
                0x64,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::CameraInfoUnknown16` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_camerainfounknown16(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    tags
}

/// `Image::ExifTool::Canon::CameraInfoUnknown` -- FORMAT int8s, FIRST_ENTRY 0.
fn canon_camerainfounknown(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if MODEL_RE_14.is_match(model) {
        if let Some(text) = text_at(data, 0x16b, 5, false) {
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(text);
            if let Some(x) = conv_expr::eval_with("unpack(\"H*\",$val)", &cv, &ctx) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "LensSerialNumber",
                0x16b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if prefix_matches(
        data.get(0x5c1..).unwrap_or(&[]),
        &[
            Some(&[(0x30, 0x39)]),
            Some(&[(46, 46)]),
            Some(&[(0x30, 0x39)]),
            Some(&[(46, 46)]),
            Some(&[(0x30, 0x39)]),
            Some(&[(0, 0)]),
        ],
    ) {
        if let Some(text) = text_at(data, 0x5c1, 6, true) {
            tags.push(mk(
                "FirmwareVersion",
                0x5c1,
                text.clone(),
                Value::String(text),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Canon::ColorCalib` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colorcalib(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x10 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x18 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x20 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x28 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x30 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x38 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x40 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x48 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x50 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x58 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x60 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x68 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x70 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    tags
}

/// `Image::ExifTool::Canon::ColorCoefs` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colorcoefs(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x0,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x8, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xa + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAuto",
                0x5,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x12, bo) {
        dm.push(("ColorTempAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAuto",
            0x9,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x14 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsMeasured",
                0xa,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x1c, bo) {
        dm.push(("ColorTempMeasured".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempMeasured",
            0xe,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x1e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x26, bo) {
        dm.push(("ColorTempUnknown".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x28 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0x14,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x30, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0x18,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x32 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0x19,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x3a, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0x1d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x3c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0x1e,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x44, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0x22,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x46 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0x23,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x4e, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0x27,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x50 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0x28,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x58, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0x2c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x5a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsKelvin",
                0x2d,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x62, bo) {
        dm.push(("ColorTempKelvin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempKelvin",
            0x31,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x64 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0x32,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x6c, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0x36,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x6e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x76, bo) {
        dm.push(("ColorTempUnknown2".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x78 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x80, bo) {
        dm.push(("ColorTempUnknown3".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x82 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x8a, bo) {
        dm.push(("ColorTempUnknown4".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x8c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x94, bo) {
        dm.push(("ColorTempUnknown5".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x96 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x9e, bo) {
        dm.push(("ColorTempUnknown6".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xa0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xa8, bo) {
        dm.push(("ColorTempUnknown7".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xaa + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xb2, bo) {
        dm.push(("ColorTempUnknown8".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xb4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xbc, bo) {
        dm.push(("ColorTempUnknown9".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xbe + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xc6, bo) {
        dm.push(("ColorTempUnknown10".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xc8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xd0, bo) {
        dm.push(("ColorTempUnknown11".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xd2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xda, bo) {
        dm.push(("ColorTempUnknown12".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xdc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xe4, bo) {
        dm.push(("ColorTempUnknown13".to_string(), Conv::Num(f64::from(v))));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorCoefs2` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colorcoefs2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAsShot",
                0x0,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xe, bo) {
        dm.push(("ColorTempAsShot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAsShot",
            0x7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x10 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsAuto",
                0x8,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x1e, bo) {
        dm.push(("ColorTempAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempAuto",
            0xf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x20 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsMeasured",
                0x10,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x2e, bo) {
        dm.push(("ColorTempMeasured".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempMeasured",
            0x17,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x30 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x3e, bo) {
        dm.push(("ColorTempUnknown".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x40 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsDaylight",
                0x20,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x4e, bo) {
        dm.push(("ColorTempDaylight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempDaylight",
            0x27,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x50 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsShade",
                0x28,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x5e, bo) {
        dm.push(("ColorTempShade".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempShade",
            0x2f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x60 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsCloudy",
                0x30,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x6e, bo) {
        dm.push(("ColorTempCloudy".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempCloudy",
            0x37,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x70 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsTungsten",
                0x38,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x7e, bo) {
        dm.push(("ColorTempTungsten".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempTungsten",
            0x3f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x80 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFluorescent",
                0x40,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x8e, bo) {
        dm.push(("ColorTempFluorescent".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFluorescent",
            0x47,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x90 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsKelvin",
                0x48,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x9e, bo) {
        dm.push(("ColorTempKelvin".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempKelvin",
            0x4f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xa0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGGBLevelsFlash",
                0x50,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xae, bo) {
        dm.push(("ColorTempFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorTempFlash",
            0x57,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xb0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xbe, bo) {
        dm.push(("ColorTempUnknown2".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xc0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xce, bo) {
        dm.push(("ColorTempUnknown3".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xd0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xde, bo) {
        dm.push(("ColorTempUnknown4".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xe0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xee, bo) {
        dm.push(("ColorTempUnknown5".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0xf0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0xfe, bo) {
        dm.push(("ColorTempUnknown6".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x100 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x10e, bo) {
        dm.push(("ColorTempUnknown7".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x110 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x11e, bo) {
        dm.push(("ColorTempUnknown8".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x120 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x12e, bo) {
        dm.push(("ColorTempUnknown9".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x130 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x13e, bo) {
        dm.push(("ColorTempUnknown10".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x140 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x14e, bo) {
        dm.push(("ColorTempUnknown11".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x150 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x15e, bo) {
        dm.push(("ColorTempUnknown12".to_string(), Conv::Num(f64::from(v))));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match i16_at(data, 0x160 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    if let Some(v) = i16_at(data, 0x16e, bo) {
        dm.push(("ColorTempUnknown13".to_string(), Conv::Num(f64::from(v))));
    }
    tags
}

/// `Image::ExifTool::Canon::ColorCalib2` -- FORMAT int16s, FIRST_ENTRY 0.
fn canon_colorcalib2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0xa + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x14 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x1e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x28 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x32 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x3c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x46 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x50 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x5a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x64 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x6e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x78 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x82 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..5 {
            match i16_at(data, 0x8c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
    }
    tags
}

/// `Image::ExifTool::Canon::PSInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_psinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x0, bo) {
        dm.push(("ContrastStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastStandard", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x4, bo) {
        dm.push(("SharpnessStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SharpnessStandard", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x8, bo) {
        dm.push(("SaturationStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationStandard",
            0x8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xc, bo) {
        dm.push(("ColorToneStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorToneStandard", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x10, bo) {
        dm.push(("FilterEffectStandard".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x14, bo) {
        dm.push(("ToningEffectStandard".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x18, bo) {
        dm.push(("ContrastPortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastPortrait", 0x18, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x1c, bo) {
        dm.push(("SharpnessPortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessPortrait",
            0x1c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x20, bo) {
        dm.push(("SaturationPortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationPortrait",
            0x20,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x24, bo) {
        dm.push(("ColorTonePortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorTonePortrait",
            0x24,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x28, bo) {
        dm.push(("FilterEffectPortrait".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x2c, bo) {
        dm.push(("ToningEffectPortrait".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x30, bo) {
        dm.push(("ContrastLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ContrastLandscape",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x34, bo) {
        dm.push(("SharpnessLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessLandscape",
            0x34,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x38, bo) {
        dm.push(("SaturationLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationLandscape",
            0x38,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x3c, bo) {
        dm.push(("ColorToneLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorToneLandscape",
            0x3c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x40, bo) {
        dm.push(("FilterEffectLandscape".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x44, bo) {
        dm.push(("ToningEffectLandscape".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x48, bo) {
        dm.push(("ContrastNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastNeutral", 0x48, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x4c, bo) {
        dm.push(("SharpnessNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SharpnessNeutral", 0x4c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x50, bo) {
        dm.push(("SaturationNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationNeutral",
            0x50,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x54, bo) {
        dm.push(("ColorToneNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorToneNeutral", 0x54, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x58, bo) {
        dm.push(("FilterEffectNeutral".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x5c, bo) {
        dm.push(("ToningEffectNeutral".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x60, bo) {
        dm.push(("ContrastFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastFaithful", 0x60, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x64, bo) {
        dm.push(("SharpnessFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessFaithful",
            0x64,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x68, bo) {
        dm.push(("SaturationFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationFaithful",
            0x68,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x6c, bo) {
        dm.push(("ColorToneFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorToneFaithful",
            0x6c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x70, bo) {
        dm.push(("FilterEffectFaithful".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x74, bo) {
        dm.push(("ToningEffectFaithful".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x78, bo) {
        dm.push(("ContrastMonochrome".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ContrastMonochrome",
            0x78,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x7c, bo) {
        dm.push(("SharpnessMonochrome".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessMonochrome",
            0x7c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x80, bo) {
        dm.push(("SaturationMonochrome".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x84, bo) {
        dm.push(("ColorToneMonochrome".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x88, bo) {
        dm.push((
            "FilterEffectMonochrome".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Yellow".to_string(),
            2 => "Orange".to_string(),
            3 => "Red".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FilterEffectMonochrome",
            0x88,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x8c, bo) {
        dm.push((
            "ToningEffectMonochrome".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Sepia".to_string(),
            2 => "Blue".to_string(),
            3 => "Purple".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ToningEffectMonochrome",
            0x8c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x90, bo) {
        dm.push(("ContrastUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastUserDef1", 0x90, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x94, bo) {
        dm.push(("SharpnessUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessUserDef1",
            0x94,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x98, bo) {
        dm.push(("SaturationUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationUserDef1",
            0x98,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x9c, bo) {
        dm.push(("ColorToneUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorToneUserDef1",
            0x9c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xa0, bo) {
        dm.push(("FilterEffectUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Yellow".to_string(),
            2 => "Orange".to_string(),
            3 => "Red".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FilterEffectUserDef1",
            0xa0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xa4, bo) {
        dm.push(("ToningEffectUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Sepia".to_string(),
            2 => "Blue".to_string(),
            3 => "Purple".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ToningEffectUserDef1",
            0xa4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xa8, bo) {
        dm.push(("ContrastUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastUserDef2", 0xa8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0xac, bo) {
        dm.push(("SharpnessUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessUserDef2",
            0xac,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xb0, bo) {
        dm.push(("SaturationUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationUserDef2",
            0xb0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xb4, bo) {
        dm.push(("ColorToneUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorToneUserDef2",
            0xb4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xb8, bo) {
        dm.push(("FilterEffectUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Yellow".to_string(),
            2 => "Orange".to_string(),
            3 => "Red".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FilterEffectUserDef2",
            0xb8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xbc, bo) {
        dm.push(("ToningEffectUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Sepia".to_string(),
            2 => "Blue".to_string(),
            3 => "Purple".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ToningEffectUserDef2",
            0xbc,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xc0, bo) {
        dm.push(("ContrastUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastUserDef3", 0xc0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0xc4, bo) {
        dm.push(("SharpnessUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessUserDef3",
            0xc4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xc8, bo) {
        dm.push(("SaturationUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationUserDef3",
            0xc8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xcc, bo) {
        dm.push(("ColorToneUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorToneUserDef3",
            0xcc,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xd0, bo) {
        dm.push(("FilterEffectUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Yellow".to_string(),
            2 => "Orange".to_string(),
            3 => "Red".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FilterEffectUserDef3",
            0xd0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xd4, bo) {
        dm.push(("ToningEffectUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Sepia".to_string(),
            2 => "Blue".to_string(),
            3 => "Purple".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ToningEffectUserDef3",
            0xd4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xd8, bo) {
        dm.push(("UserDef1PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "UserDef1PictureStyle",
            0xd8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xda, bo) {
        dm.push(("UserDef2PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "UserDef2PictureStyle",
            0xda,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xdc, bo) {
        dm.push(("UserDef3PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "UserDef3PictureStyle",
            0xdc,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Canon::PSInfo2` -- FORMAT int8u, FIRST_ENTRY 0.
fn canon_psinfo2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Canon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i32_at(data, 0x0, bo) {
        dm.push(("ContrastStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastStandard", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x4, bo) {
        dm.push(("SharpnessStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SharpnessStandard", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x8, bo) {
        dm.push(("SaturationStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationStandard",
            0x8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xc, bo) {
        dm.push(("ColorToneStandard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorToneStandard", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x10, bo) {
        dm.push(("FilterEffectStandard".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x14, bo) {
        dm.push(("ToningEffectStandard".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x18, bo) {
        dm.push(("ContrastPortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastPortrait", 0x18, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x1c, bo) {
        dm.push(("SharpnessPortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessPortrait",
            0x1c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x20, bo) {
        dm.push(("SaturationPortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationPortrait",
            0x20,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x24, bo) {
        dm.push(("ColorTonePortrait".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorTonePortrait",
            0x24,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x28, bo) {
        dm.push(("FilterEffectPortrait".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x2c, bo) {
        dm.push(("ToningEffectPortrait".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x30, bo) {
        dm.push(("ContrastLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ContrastLandscape",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x34, bo) {
        dm.push(("SharpnessLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessLandscape",
            0x34,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x38, bo) {
        dm.push(("SaturationLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationLandscape",
            0x38,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x3c, bo) {
        dm.push(("ColorToneLandscape".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorToneLandscape",
            0x3c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x40, bo) {
        dm.push(("FilterEffectLandscape".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x44, bo) {
        dm.push(("ToningEffectLandscape".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x48, bo) {
        dm.push(("ContrastNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastNeutral", 0x48, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x4c, bo) {
        dm.push(("SharpnessNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SharpnessNeutral", 0x4c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x50, bo) {
        dm.push(("SaturationNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationNeutral",
            0x50,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x54, bo) {
        dm.push(("ColorToneNeutral".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorToneNeutral", 0x54, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x58, bo) {
        dm.push(("FilterEffectNeutral".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x5c, bo) {
        dm.push(("ToningEffectNeutral".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x60, bo) {
        dm.push(("ContrastFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastFaithful", 0x60, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x64, bo) {
        dm.push(("SharpnessFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessFaithful",
            0x64,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x68, bo) {
        dm.push(("SaturationFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationFaithful",
            0x68,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x6c, bo) {
        dm.push(("ColorToneFaithful".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorToneFaithful",
            0x6c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x70, bo) {
        dm.push(("FilterEffectFaithful".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x74, bo) {
        dm.push(("ToningEffectFaithful".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x78, bo) {
        dm.push(("ContrastMonochrome".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ContrastMonochrome",
            0x78,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x7c, bo) {
        dm.push(("SharpnessMonochrome".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessMonochrome",
            0x7c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x80, bo) {
        dm.push(("SaturationMonochrome".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x84, bo) {
        dm.push(("ColorToneMonochrome".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i32_at(data, 0x88, bo) {
        dm.push((
            "FilterEffectMonochrome".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Yellow".to_string(),
            2 => "Orange".to_string(),
            3 => "Red".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FilterEffectMonochrome",
            0x88,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x8c, bo) {
        dm.push((
            "ToningEffectMonochrome".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Sepia".to_string(),
            2 => "Blue".to_string(),
            3 => "Purple".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ToningEffectMonochrome",
            0x8c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x90, bo) {
        dm.push(("ContrastAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastAuto", 0x90, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x94, bo) {
        dm.push(("SharpnessAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SharpnessAuto", 0x94, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x98, bo) {
        dm.push(("SaturationAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SaturationAuto", 0x98, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0x9c, bo) {
        dm.push(("ColorToneAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorToneAuto", 0x9c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0xa0, bo) {
        dm.push(("FilterEffectAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Yellow".to_string(),
            2 => "Orange".to_string(),
            3 => "Red".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FilterEffectAuto", 0xa0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0xa4, bo) {
        dm.push(("ToningEffectAuto".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Sepia".to_string(),
            2 => "Blue".to_string(),
            3 => "Purple".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ToningEffectAuto", 0xa4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0xa8, bo) {
        dm.push(("ContrastUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastUserDef1", 0xa8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0xac, bo) {
        dm.push(("SharpnessUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessUserDef1",
            0xac,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xb0, bo) {
        dm.push(("SaturationUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationUserDef1",
            0xb0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xb4, bo) {
        dm.push(("ColorToneUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorToneUserDef1",
            0xb4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xb8, bo) {
        dm.push(("FilterEffectUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Yellow".to_string(),
            2 => "Orange".to_string(),
            3 => "Red".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FilterEffectUserDef1",
            0xb8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xbc, bo) {
        dm.push(("ToningEffectUserDef1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Sepia".to_string(),
            2 => "Blue".to_string(),
            3 => "Purple".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ToningEffectUserDef1",
            0xbc,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xc0, bo) {
        dm.push(("ContrastUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastUserDef2", 0xc0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0xc4, bo) {
        dm.push(("SharpnessUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessUserDef2",
            0xc4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xc8, bo) {
        dm.push(("SaturationUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationUserDef2",
            0xc8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xcc, bo) {
        dm.push(("ColorToneUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorToneUserDef2",
            0xcc,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xd0, bo) {
        dm.push(("FilterEffectUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Yellow".to_string(),
            2 => "Orange".to_string(),
            3 => "Red".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FilterEffectUserDef2",
            0xd0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xd4, bo) {
        dm.push(("ToningEffectUserDef2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Sepia".to_string(),
            2 => "Blue".to_string(),
            3 => "Purple".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ToningEffectUserDef2",
            0xd4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xd8, bo) {
        dm.push(("ContrastUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ContrastUserDef3", 0xd8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i32_at(data, 0xdc, bo) {
        dm.push(("SharpnessUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SharpnessUserDef3",
            0xdc,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xe0, bo) {
        dm.push(("SaturationUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SaturationUserDef3",
            0xe0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xe4, bo) {
        dm.push(("ColorToneUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorToneUserDef3",
            0xe4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xe8, bo) {
        dm.push(("FilterEffectUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Yellow".to_string(),
            2 => "Orange".to_string(),
            3 => "Red".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FilterEffectUserDef3",
            0xe8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0xec, bo) {
        dm.push(("ToningEffectUserDef3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            -559038737 => "n/a".to_string(),
            0 => "None".to_string(),
            1 => "Sepia".to_string(),
            2 => "Blue".to_string(),
            3 => "Purple".to_string(),
            4 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ToningEffectUserDef3",
            0xec,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xf0, bo) {
        dm.push(("UserDef1PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "UserDef1PictureStyle",
            0xf0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xf2, bo) {
        dm.push(("UserDef2PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "UserDef2PictureStyle",
            0xf2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xf4, bo) {
        dm.push(("UserDef3PictureStyle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            65 => "PC 1".to_string(),
            66 => "PC 2".to_string(),
            67 => "PC 3".to_string(),
            129 => "Standard".to_string(),
            130 => "Portrait".to_string(),
            131 => "Landscape".to_string(),
            132 => "Neutral".to_string(),
            133 => "Faithful".to_string(),
            134 => "Monochrome".to_string(),
            135 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "UserDef3PictureStyle",
            0xf4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::CanonCustom::PersonalFuncs` -- FORMAT int16u, FIRST_ENTRY 1.
fn canoncustom_personalfuncs(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "CanonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push((
            "PF0CustomFuncRegistration".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF0CustomFuncRegistration",
            0x1,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push((
            "PF1DisableShootingModes".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF1DisableShootingModes",
            0x2,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x6, bo) {
        dm.push((
            "PF2DisableMeteringModes".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF2DisableMeteringModes",
            0x3,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x8, bo) {
        dm.push((
            "PF3ManualExposureMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF3ManualExposureMetering",
            0x4,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xa, bo) {
        dm.push(("PF4ExposureTimeLimits".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF4ExposureTimeLimits",
            0x5,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xc, bo) {
        dm.push(("PF5ApertureLimits".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF5ApertureLimits",
            0x6,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xe, bo) {
        dm.push((
            "PF6PresetShootingModes".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF6PresetShootingModes",
            0x7,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x10, bo) {
        dm.push((
            "PF7BracketContinuousShoot".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF7BracketContinuousShoot",
            0x8,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x12, bo) {
        dm.push(("PF8SetBracketShots".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF8SetBracketShots",
            0x9,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x14, bo) {
        dm.push((
            "PF9ChangeBracketSequence".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF9ChangeBracketSequence",
            0xa,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x16, bo) {
        dm.push((
            "PF10RetainProgramShift".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF10RetainProgramShift",
            0xb,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x1c, bo) {
        dm.push(("PF13DrivePriority".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF13DrivePriority",
            0xe,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x1e, bo) {
        dm.push((
            "PF14DisableFocusSearch".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF14DisableFocusSearch",
            0xf,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x20, bo) {
        dm.push((
            "PF15DisableAFAssistBeam".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF15DisableAFAssistBeam",
            0x10,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x22, bo) {
        dm.push((
            "PF16AutoFocusPointShoot".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF16AutoFocusPointShoot",
            0x11,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x24, bo) {
        dm.push(("PF17DisableAFPointSel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF17DisableAFPointSel",
            0x12,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x26, bo) {
        dm.push((
            "PF18EnableAutoAFPointSel".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF18EnableAutoAFPointSel",
            0x13,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x28, bo) {
        dm.push((
            "PF19ContinuousShootSpeed".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF19ContinuousShootSpeed",
            0x14,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2a, bo) {
        dm.push((
            "PF20LimitContinousShots".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF20LimitContinousShots",
            0x15,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2c, bo) {
        dm.push((
            "PF21EnableQuietOperation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF21EnableQuietOperation",
            0x16,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x30, bo) {
        dm.push(("PF23SetTimerLengths".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF23SetTimerLengths",
            0x18,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x32, bo) {
        dm.push((
            "PF24LightLCDDuringBulb".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF24LightLCDDuringBulb",
            0x19,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x34, bo) {
        dm.push((
            "PF25DefaultClearSettings".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF25DefaultClearSettings",
            0x1a,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x36, bo) {
        dm.push(("PF26ShortenReleaseLag".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF26ShortenReleaseLag",
            0x1b,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x38, bo) {
        dm.push((
            "PF27ReverseDialRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF27ReverseDialRotation",
            0x1c,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x3a, bo) {
        dm.push((
            "PF28NoQuickDialExpComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF28NoQuickDialExpComp",
            0x1d,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x3c, bo) {
        dm.push((
            "PF29QuickDialSwitchOff".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF29QuickDialSwitchOff",
            0x1e,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x3e, bo) {
        dm.push(("PF30EnlargementMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF30EnlargementMode",
            0x1f,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x40, bo) {
        dm.push((
            "PF31OriginalDecisionData".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::CanonCustom::ConvertPfn($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "PF31OriginalDecisionData",
            0x20,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::CanonCustom::PersonalFuncValues` -- FORMAT int16u, FIRST_ENTRY 1.
fn canoncustom_personalfuncvalues(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "CanonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("PF1Value".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF1Value",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push(("PF2Value".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF2Value",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x6, bo) {
        dm.push(("PF3Value".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF3Value",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x8, bo) {
        dm.push(("PF4ExposureTimeMin".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val > 0 ? $val : 0", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(-Image::ExifTool::Canon::CanonEv($val*4)*log(2))*1000/8",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "PF4ExposureTimeMin",
                    0x4,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16_at(data, 0xa, bo) {
        dm.push(("PF4ExposureTimeMax".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val > 0 ? $val : 0", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(-Image::ExifTool::Canon::CanonEv($val*4)*log(2))*1000/8",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "PF4ExposureTimeMax",
                    0x5,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16_at(data, 0xc, bo) {
        dm.push(("PF5ApertureMin".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val > 0 ? $val : 0", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(Image::ExifTool::Canon::CanonEv($val*4-32)*log(2)/2)",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "PF5ApertureMin",
                    0x6,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16_at(data, 0xe, bo) {
        dm.push(("PF5ApertureMax".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val > 0 ? $val : 0", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with(
                "exp(Image::ExifTool::Canon::CanonEv($val*4-32)*log(2)/2)",
                &cv,
                &ctx,
            );
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.2g\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "PF5ApertureMax",
                    0x7,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u16_at(data, 0x10, bo) {
        dm.push(("PF8BracketShots".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF8BracketShots",
            0x8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x12, bo) {
        dm.push(("PF19ShootingSpeedLow".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF19ShootingSpeedLow",
            0x9,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x14, bo) {
        dm.push(("PF19ShootingSpeedHigh".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF19ShootingSpeedHigh",
            0xa,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x16, bo) {
        dm.push(("PF20MaxContinousShots".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF20MaxContinousShots",
            0xb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x18, bo) {
        dm.push(("PF23ShutterButtonTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF23ShutterButtonTime",
            0xc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x1a, bo) {
        dm.push(("PF23FELockTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF23FELockTime",
            0xd,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x1c, bo) {
        dm.push(("PF23PostReleaseTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF23PostReleaseTime",
            0xe,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x1e, bo) {
        dm.push(("PF25AEMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF25AEMode",
            0xf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x20, bo) {
        dm.push(("PF25MeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF25MeteringMode",
            0x10,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x22, bo) {
        dm.push(("PF25DriveMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF25DriveMode",
            0x11,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x24, bo) {
        dm.push(("PF25AFMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF25AFMode",
            0x12,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x26, bo) {
        dm.push(("PF25AFPointSel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF25AFPointSel",
            0x13,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x28, bo) {
        dm.push(("PF25ImageSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF25ImageSize",
            0x14,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2a, bo) {
        dm.push(("PF25WBMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF25WBMode",
            0x15,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2c, bo) {
        dm.push(("PF25Parameters".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF25Parameters",
            0x16,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2e, bo) {
        dm.push(("PF25ColorMatrix".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF25ColorMatrix",
            0x17,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x30, bo) {
        dm.push(("PF27Value".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PF27Value",
            0x18,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::CanonRaw::WhiteSample` -- FORMAT int16u, FIRST_ENTRY 1.
fn canonraw_whitesample(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "CanonRaw";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("WhiteSampleWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WhiteSampleWidth",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push(("WhiteSampleHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WhiteSampleHeight",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x6, bo) {
        dm.push(("WhiteSampleLeftBorder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WhiteSampleLeftBorder",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x8, bo) {
        dm.push(("WhiteSampleTopBorder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WhiteSampleTopBorder",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xa, bo) {
        dm.push(("WhiteSampleBits".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WhiteSampleBits",
            0x5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x6e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "BlackLevels",
                0x37,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::CanonVRD::DustInfo` -- FORMAT int32u, FIRST_ENTRY 0.
fn canonvrd_dustinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "CanonDR4";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push(("DustDeleteApplied".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "DustDeleteApplied",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::CanonVRD::DLOInfo` -- FORMAT int16s, FIRST_ENTRY 1.
/// Incomplete: a field of variable length moves every entry after
/// it, and this reads them where they would be without it.
fn canonvrd_dloinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "CanonVRD";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x8, bo) {
        dm.push(("DLOSettingApplied".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "DLOSettingApplied",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0xa, 10, true) {
        tags.push(mk(
            "DLOVersion",
            0x5,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Casio::FaceInfo1` -- FORMAT int8u, FIRST_ENTRY 0.
fn casio_faceinfo1(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Casio";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("FacesDetected".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FacesDetected",
                0x0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 1.0) {
        {
            let mut parts = Vec::new();
            for k in 0..2 {
                match u16_at(data, 0x1 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "FaceDetectFrameSize",
                    0x1,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 1.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0xd + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face1Position",
                    0xd,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 2.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x7c + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face2Position",
                    0x7c,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 3.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0xeb + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face3Position",
                    0xeb,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 4.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x15a + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face4Position",
                    0x15a,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 5.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x1c9 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face5Position",
                    0x1c9,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 6.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x238 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face6Position",
                    0x238,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 7.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x2a7 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face7Position",
                    0x2a7,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 8.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x316 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face8Position",
                    0x316,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 9.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x385 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face9Position",
                    0x385,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 10.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x3f4 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face10Position",
                    0x3f4,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Casio::FaceInfo2` -- FORMAT int8u, FIRST_ENTRY 0.
fn casio_faceinfo2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Casio";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x2) {
        dm.push(("FacesDetected".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FacesDetected",
                0x2,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 1.0) {
        {
            let mut parts = Vec::new();
            for k in 0..2 {
                match u16_at(data, 0x4 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "FaceDetectFrameSize",
                    0x4,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 1.0) {
        if let Some(v) = u8_at(data, 0x8) {
            dm.push(("FaceOrientation".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Horizontal (normal)".to_string(),
                1 => "Rotate 90 CW".to_string(),
                2 => "Rotate 270 CW".to_string(),
                3 => "Rotate 180".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("FaceOrientation", 0x8, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 1.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x18 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face1Position",
                    0x18,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 2.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x4c + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face2Position",
                    0x4c,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 3.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x80 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face3Position",
                    0x80,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 4.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0xb4 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face4Position",
                    0xb4,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 5.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0xe8 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face5Position",
                    0xe8,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 6.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x11c + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face6Position",
                    0x11c,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 7.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x150 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face7Position",
                    0x150,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 8.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x184 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face8Position",
                    0x184,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 9.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x1b8 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face9Position",
                    0x1b8,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 10.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x1ec + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face10Position",
                    0x1ec,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Casio::QVCI` -- FORMAT int8u, FIRST_ENTRY 0.
fn casio_qvci(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Casio";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x2c) {
        dm.push(("CasioQuality".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "Economy".to_string(),
            2 => "Normal".to_string(),
            3 => "Fine".to_string(),
            4 => "Super Fine".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("CasioQuality", 0x2c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x37) {
        dm.push(("FocalRange".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(text) = text_at(data, 0x4d, 20, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with(
            "$val=~tr/./:/; $val=~s/(\\d+:\\d+:\\d+):/$1 /; $val",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "DateTimeOriginal",
            0x4d,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x62, 7, true) {
        tags.push(mk(
            "ModelType",
            0x62,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x72, 9, true) {
        tags.push(mk(
            "ManufactureIndex",
            0x72,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x7c, 9, true) {
        tags.push(mk(
            "ManufactureCode",
            0x7c,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Casio::AVI` -- FORMAT int8u, FIRST_ENTRY 0.
fn casio_avi(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Casio";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, data.len().saturating_sub(0x0), true) {
        tags.push(mk(
            "Software",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::FLIR::GainDeadData` -- FORMAT int16u, FIRST_ENTRY 0.
/// Incomplete: a field of variable length moves every entry after
/// it, and this reads them where they would be without it.
fn flir_gaindeaddata(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "APP1";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("GainDeadMapByteOrder".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("ToggleByteOrder() if $val >= 0x0100; undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "GainDeadMapByteOrder",
                0x0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("GainDeadMapImageWidth".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "GainDeadMapImageWidth",
                0x1,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push((
            "GainDeadMapImageHeight".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "GainDeadMapImageHeight",
                0x2,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x20, bo) {
        dm.push(("GainDeadMapImage".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with(
            "my $copy = \\$$self{GainDeadMapImage}; \\$copy",
            &base,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "GainDeadMapImage",
                0x10,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::FLIR::CoarseData` -- FORMAT int16u, FIRST_ENTRY 0.
/// Incomplete: a field of variable length moves every entry after
/// it, and this reads them where they would be without it.
fn flir_coarsedata(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "APP1";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("CoarseMapByteOrder".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("ToggleByteOrder() if $val >= 0x0100; undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "CoarseMapByteOrder",
                0x0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("CoarseMapImageWidth".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "CoarseMapImageWidth",
                0x1,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push(("CoarseMapImageHeight".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "CoarseMapImageHeight",
                0x2,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x20, bo) {
        dm.push(("CoarseMapImage".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("my $copy = \\$$self{CoarseMapImage}; \\$copy", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "CoarseMapImage",
                0x10,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::FLIR::PaintData` -- FORMAT int16u, FIRST_ENTRY 0.
/// Incomplete: a field of variable length moves every entry after
/// it, and this reads them where they would be without it.
fn flir_paintdata(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "APP1";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("PaintByteOrder".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("ToggleByteOrder() if $val >= 0x0100; undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "PaintByteOrder",
                0x1,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xa, bo) {
        dm.push(("PaintImageWidth".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "PaintImageWidth",
                0x5,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xc, bo) {
        dm.push(("PaintImageHeight".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "PaintImageHeight",
                0x6,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x28, bo) {
        dm.push(("PaintImage".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("my $copy = \\$$self{PaintImage}; \\$copy", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "PaintImage",
                0x14,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::FLIR::SerialNums` -- FORMAT int8u, FIRST_ENTRY 0.
fn flir_serialnums(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x7e, 9, true) {
        tags.push(mk(
            "CameraSerialNumber",
            0x7e,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::FLIR::UnknownUUID` -- FORMAT float, FIRST_ENTRY 0.
fn flir_unknownuuid(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    tags
}

/// `Image::ExifTool::FLIR::GPS_UUID` -- FORMAT float, FIRST_ENTRY 0.
fn flir_gps_uuid(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Location";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = f32_at(data, 0x4, bo) {
        dm.push(("GPSLatitude".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "Image::ExifTool::GPS::ToDMS($self, $val, 1, \"N\")",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "GPSLatitude",
            0x1,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0x8, bo) {
        dm.push(("GPSLongitude".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "Image::ExifTool::GPS::ToDMS($self, $val, 1, \"E\")",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "GPSLongitude",
            0x2,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0xc, bo) {
        dm.push(("GPSAltitude".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("$val=int($val*100+0.5)/100;\"$val m\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "GPSAltitude",
            0x3,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::FLIR::AFF1` -- FORMAT int16u, FIRST_ENTRY 0.
fn flir_aff1(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "FLIR";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("RawDataByteOrder".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("ToggleByteOrder() if $val >= 0x0100; undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "RawDataByteOrder",
                0x0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("SensorWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SensorWidth",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push(("SensorHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SensorHeight",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::FLIR::AFF5` -- FORMAT int16u, FIRST_ENTRY 0.
fn flir_aff5(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "FLIR";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x24, bo) {
        dm.push(("RawDataByteOrder".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("ToggleByteOrder() if $val >= 0x0100; undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "RawDataByteOrder",
                0x12,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x26, bo) {
        dm.push(("SensorWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SensorWidth",
            0x13,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x28, bo) {
        dm.push(("SensorHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SensorHeight",
            0x14,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::FLIR::EmbeddedImage` -- FORMAT int16u, FIRST_ENTRY 0.
/// Incomplete: a field of variable length moves every entry after
/// it, and this reads them where they would be without it.
fn flir_embeddedimage(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "APP1";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push((
            "EmbeddedImageByteOrder".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("ToggleByteOrder() if $val >= 0x0100; undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "EmbeddedImageByteOrder",
                0x0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("EmbeddedImageWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "EmbeddedImageWidth",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push(("EmbeddedImageHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "EmbeddedImageHeight",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x20, 4, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "$val =~ /^\\x89PNG/s ? \"PNG\" : ($val =~ /^\\xff\\xd8\\xff/ ? \"JPG\" : \"DAT\")",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "EmbeddedImageType",
                0x10,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::FLIR::GPSInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn flir_gpsinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "APP1";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Location";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x0, bo) {
        dm.push(("GPSValid".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "No".to_string(),
                1 => "Yes".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("GPSValid", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(text) = text_at(data, 0x4, 4, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val eq \"\\0\\0\\0\\0\" ? undef : $val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("join \".\", split //, $val", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "GPSVersionID",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x8, 2, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("length($val) ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "GPSLatitudeRef",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0xa, 2, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("length($val) ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "GPSLongitudeRef",
                0xa,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "GPSValid").is_some_and(conv_expr::Val::truthy) {
        if let Some(v) = f64_at(data, 0x10, bo) {
            dm.push(("GPSLatitude".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "Image::ExifTool::GPS::ToDMS($self, $val, 1, \"N\")",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "GPSLatitude",
                0x10,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "GPSValid").is_some_and(conv_expr::Val::truthy) {
        if let Some(v) = f64_at(data, 0x18, bo) {
            dm.push(("GPSLongitude".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "Image::ExifTool::GPS::ToDMS($self, $val, 1, \"E\")",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "GPSLongitude",
                0x18,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "GPSValid").is_some_and(conv_expr::Val::truthy) {
        if let Some(v) = f32_at(data, 0x20, bo) {
            dm.push(("GPSAltitude".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.2f m\", $val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "GPSAltitude",
                0x20,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = f32_at(data, 0x40, bo) {
        dm.push(("GPSDOP".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val > 0 ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.2f\", $val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "GPSDOP",
                0x40,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x44, 2, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("length($val) ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "GPSSpeedRef",
                0x44,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x46, 2, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("length($val) ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "GPSTrackRef",
                0x46,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x48, 2, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("length($val) ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "GPSImgDirectionRef",
                0x48,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x4c) {
        dm.push(("GPSSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val < 0 ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "GPSSpeed",
                0x4c,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x50) {
        dm.push(("GPSTrack".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val < 0 ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "GPSTrack",
                0x50,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x54) {
        dm.push(("GPSImgDirection".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val < 0 ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "GPSImgDirection",
                0x54,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x58, 16, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("length($val) ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "GPSMapDatum",
                0x58,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::FLIR::Header` -- FORMAT int8u, FIRST_ENTRY 0.
fn flir_header(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "APP1";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x4, 16, true) {
        tags.push(mk(
            "CreatorSoftware",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::FLIR::MeterLink` -- FORMAT int8u, FIRST_ENTRY 0.
fn flir_meterlink(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "APP1";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x1a) {
        dm.push(("Reading1Units".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                13 => "C".to_string(),
                27 => "%".to_string(),
                29 => "Relative".to_string(),
                36 => "g/kg".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("Reading1Units", 0x1a, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x1c) {
        dm.push(("Reading1Description".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Humidity".to_string(),
                3 => "Moisture".to_string(),
                7 => "Dew Point".to_string(),
                8 => "Air Temperature".to_string(),
                9 => "IR Temperature".to_string(),
                11 => "Difference Temperature".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "Reading1Description",
                0x1c,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x20, 16, true) {
        tags.push(mk(
            "Reading1Device",
            0x20,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f64_at(data, 0x60, bo) {
        dm.push(("Reading1Value".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("
            return $val - 273.15 if $$self{Reading1Units} == 0x0d and $$self{Reading1Description} != 11;
            return $val *= 1000 if $$self{Reading1Units} == 0x24;
            return $val;
        ", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Reading1Value",
                0x60,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x7e) {
        dm.push(("Reading2Units".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                13 => "C".to_string(),
                27 => "%".to_string(),
                29 => "rel".to_string(),
                36 => "g/kg".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("Reading2Units", 0x7e, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x80) {
        dm.push(("Reading2Description".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Humidity".to_string(),
                3 => "Moisture".to_string(),
                7 => "Dew Point".to_string(),
                8 => "Air Temperature".to_string(),
                9 => "IR Temperature".to_string(),
                11 => "Difference Temperature".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "Reading2Description",
                0x80,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x84, 16, true) {
        tags.push(mk(
            "Reading2Device",
            0x84,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f64_at(data, 0xc4, bo) {
        dm.push(("Reading2Value".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("
            return $val - 273.15 if $$self{Reading2Units} == 0x0d and $$self{Reading2Description} != 11;
            return $val *= 1000 if $$self{Reading2Units} == 0x24;
            return $val;
        ", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Reading2Value",
                0xc4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xe2) {
        dm.push(("Reading3Units".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                13 => "C".to_string(),
                27 => "%".to_string(),
                29 => "rel".to_string(),
                36 => "g/kg".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("Reading3Units", 0xe2, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0xe4) {
        dm.push(("Reading3Description".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Humidity".to_string(),
                3 => "Moisture".to_string(),
                7 => "Dew Point".to_string(),
                8 => "Air Temperature".to_string(),
                9 => "IR Temperature".to_string(),
                11 => "Difference Temperature".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "Reading3Description",
                0xe4,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0xe8, 16, true) {
        tags.push(mk(
            "Reading3Device",
            0xe8,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f64_at(data, 0x128, bo) {
        dm.push(("Reading3Value".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("
            return $val - 273.15 if $$self{Reading3Units} == 0x0d and $$self{Reading3Description} != 11;
            return $val *= 1000 if $$self{Reading3Units} == 0x24;
            return $val;
        ", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Reading3Value",
                0x128,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x146) {
        dm.push(("Reading4Units".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                13 => "C".to_string(),
                27 => "%".to_string(),
                29 => "rel".to_string(),
                36 => "g/kg".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("Reading4Units", 0x146, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x148) {
        dm.push(("Reading4Description".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Humidity".to_string(),
                3 => "Moisture".to_string(),
                7 => "Dew Point".to_string(),
                8 => "Air Temperature".to_string(),
                9 => "IR Temperature".to_string(),
                11 => "Difference Temperature".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "Reading4Description",
                0x148,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x14c, 16, true) {
        tags.push(mk(
            "Reading4Device",
            0x14c,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f64_at(data, 0x18c, bo) {
        dm.push(("Reading4Value".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("
            return $val - 273.15 if $$self{Reading4Units} == 0x0d and $$self{Reading4Description} != 11;
            return $val *= 1000 if $$self{Reading4Units} == 0x24;
            return $val;
        ", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Reading4Value",
                0x18c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::FLIR::MoreInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn flir_moreinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x5, 6, true) {
        tags.push(mk(
            "LensModel",
            0x5,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0xb, bo) {
        dm.push(("UnknownTemperature1".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = f32_at(data, 0xf, bo) {
        dm.push(("UnknownTemperature2".to_string(), Conv::Num(f64::from(v))));
    }
    tags
}

/// `Image::ExifTool::FLIR::Params` -- FORMAT float, FIRST_ENTRY 0.
fn flir_params(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = f32_at(data, 0x4, bo) {
        dm.push((
            "ReflectedApparentTemperature".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 273.15", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f C\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ReflectedApparentTemperature",
                0x1,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = f32_at(data, 0x8, bo) {
        dm.push((
            "AtmosphericTemperature".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 273.15", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f C\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "AtmosphericTemperature",
                0x2,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = f32_at(data, 0xc, bo) {
        dm.push(("Emissivity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Emissivity",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0x10, bo) {
        dm.push(("ObjectDistance".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.2f m\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "ObjectDistance",
            0x4,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0x14, bo) {
        dm.push(("RelativeHumidity".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f %%\",$val*100)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "RelativeHumidity",
            0x5,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0x18, bo) {
        dm.push((
            "EstimatedAtmosphericTrans".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "EstimatedAtmosphericTrans",
            0x6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0x1c, bo) {
        dm.push(("IRWindowTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 273.15", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f C\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "IRWindowTemperature",
                0x7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = f32_at(data, 0x20, bo) {
        dm.push(("IRWindowTransmission".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "IRWindowTransmission",
            0x8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::FLIR::PiP` -- FORMAT int16s, FIRST_ENTRY 0.
fn flir_pip(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "APP1";
    const GRP1: &str = "FLIR";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = f32_at(data, 0x0, bo) {
        dm.push(("Real2IR".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Real2IR",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x4, bo) {
        dm.push(("OffsetX".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%+d\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "OffsetX",
            0x2,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x6, bo) {
        dm.push(("OffsetY".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%+d\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "OffsetY",
            0x3,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x8, bo) {
        dm.push(("PiPX1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PiPX1",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0xa, bo) {
        dm.push(("PiPX2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PiPX2",
            0x5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0xc, bo) {
        dm.push(("PiPY1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PiPY1",
            0x6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0xe, bo) {
        dm.push(("PiPY2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PiPY2",
            0x7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::FlashPix::PreviewInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn flashpix_previewinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "FlashPix";
    const GRP1: &str = "FlashPix";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0xd, bo) {
        dm.push(("PreviewImageWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreviewImageWidth",
            0xd,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x17, bo) {
        dm.push(("PreviewImageHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreviewImageHeight",
            0x17,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::FujiFilm::FFMV` -- FORMAT int8u, FIRST_ENTRY 0.
fn fujifilm_ffmv(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "FujiFilm";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 34, true) {
        tags.push(mk(
            "MovieStreamName",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::FujiFilm::RAFData` -- FORMAT int8u, FIRST_ENTRY 0.
fn fujifilm_rafdata(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "FujiFilm";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x0, bo) {
        dm.push(("FujiWidth".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val < 10000 ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$$self{FujiLayout} ? ($val / 2) : $val", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "RawImageWidth",
                    0x0,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if !(dm_val(dm, "FujiWidth").is_some_and(conv_expr::Val::truthy)) {
        if let Some(v) = u32_at(data, 0x4, bo) {
            dm.push(("FujiWidth".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val < 10000 ? $val : undef", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                let mut cv = base;
                let vc = conv_expr::eval_with("$$self{FujiLayout} ? ($val / 2) : $val", &cv, &ctx);
                if vc.as_ref() != Some(&Conv::Undef) {
                    if let Some(x) = vc {
                        cv = x;
                    }
                    let raw = Value::F64(cv.as_num());
                    tags.push(mk(
                        "RawImageWidth",
                        0x4,
                        cv.as_string(),
                        raw,
                        GRP0,
                        GRP1,
                        GRP2,
                        PRIO,
                    ));
                }
            }
        }
    }
    if !(!(dm_val(dm, "FujiWidth").is_some_and(conv_expr::Val::truthy))) {
        if let Some(v) = u32_at(data, 0x4, bo) {
            dm.push(("FujiHeight".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                let mut cv = base;
                let vc = conv_expr::eval_with("$$self{FujiLayout} ? ($val * 2) : $val", &cv, &ctx);
                if vc.as_ref() != Some(&Conv::Undef) {
                    if let Some(x) = vc {
                        cv = x;
                    }
                    let raw = Value::F64(cv.as_num());
                    tags.push(mk(
                        "RawImageHeight",
                        0x4,
                        cv.as_string(),
                        raw,
                        GRP0,
                        GRP1,
                        GRP2,
                        PRIO,
                    ));
                }
            }
        }
    }
    if !(dm_val(dm, "FujiWidth").is_some_and(conv_expr::Val::truthy)) {
        if let Some(v) = u32_at(data, 0x8, bo) {
            dm.push(("FujiWidth".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val < 10000 ? $val : undef", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                let mut cv = base;
                let vc = conv_expr::eval_with("$$self{FujiLayout} ? ($val / 2) : $val", &cv, &ctx);
                if vc.as_ref() != Some(&Conv::Undef) {
                    if let Some(x) = vc {
                        cv = x;
                    }
                    let raw = Value::F64(cv.as_num());
                    tags.push(mk(
                        "RawImageWidth",
                        0x8,
                        cv.as_string(),
                        raw,
                        GRP0,
                        GRP1,
                        GRP2,
                        PRIO,
                    ));
                }
            }
        }
    }
    if !(!(dm_val(dm, "FujiWidth").is_some_and(conv_expr::Val::truthy)))
        && !(dm_val(dm, "FujiHeight").is_some_and(conv_expr::Val::truthy))
    {
        if let Some(v) = u32_at(data, 0x8, bo) {
            dm.push(("FujiHeight".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                let mut cv = base;
                let vc = conv_expr::eval_with("$$self{FujiLayout} ? ($val * 2) : $val", &cv, &ctx);
                if vc.as_ref() != Some(&Conv::Undef) {
                    if let Some(x) = vc {
                        cv = x;
                    }
                    let raw = Value::F64(cv.as_num());
                    tags.push(mk(
                        "RawImageHeight",
                        0x8,
                        cv.as_string(),
                        raw,
                        GRP0,
                        GRP1,
                        GRP2,
                        PRIO,
                    ));
                }
            }
        }
    }
    if !(dm_val(dm, "FujiHeight").is_some_and(conv_expr::Val::truthy)) {
        if let Some(v) = u32_at(data, 0xc, bo) {
            dm.push(("RawImageHeight".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$$self{FujiLayout} ? ($val * 2) : $val", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "RawImageHeight",
                    0xc,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::FujiFilm::MOV` -- FORMAT int8u, FIRST_ENTRY 0.
fn fujifilm_mov(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "FujiFilm";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 24, true) {
        tags.push(mk(
            "Make",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x18, 16, true) {
        tags.push(mk(
            "Model",
            0x18,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x2e, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? 1 / $val : 0", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x2e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = rat64u_at(data, 0x32, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FNumber",
            0x32,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64s_at(data, 0x3a, bo) {
        dm.push(("ExposureCompensation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\", $val) : 0", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "ExposureCompensation",
            0x3a,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::H264::Camera1` -- FORMAT int8u, FIRST_ENTRY 0.
fn h264_camera1(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "H264";
    const GRP1: &str = "H264";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("ApertureSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            254 => "Closed".to_string(),
            255 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ApertureSetting", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0xf;
        dm.push(("Gain".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val - 1) * 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val==42 ? \"Out of range\" : \"$val dB\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk("Gain", 0x1, cv.as_string(), raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xf0) >> 4;
        dm.push(("ExposureProgram".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("$val == 15 ? undef : $val", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Program AE".to_string(),
                1 => "Gain".to_string(),
                2 => "Shutter speed priority AE".to_string(),
                3 => "Aperture-priority AE".to_string(),
                4 => "Manual".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ExposureProgram", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0xe0) >> 5;
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("$val == 7 ? undef : $val", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Auto".to_string(),
                1 => "Hold".to_string(),
                2 => "1-Push".to_string(),
                3 => "Daylight".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("WhiteBalance", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("Focus".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val == 0xff ? undef : $val", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            my $foc = ($val & 0x7e) / (($val & 0x01) ? 40 : 400);
            return(($val & 0x80 ? 'Manual' : 'Auto') . \" ($foc)\");
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "Focus",
                0x3,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::H264::Camera2` -- FORMAT int8u, FIRST_ENTRY 0.
fn h264_camera2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "H264";
    const GRP1: &str = "H264";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x1) {
        dm.push(("ImageStabilization".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            63 => "On (0x3f)".to_string(),
            191 => "Off (0xbf)".to_string(),
            255 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageStabilization",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::H264::FrameInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn h264_frameinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "H264";
    const GRP1: &str = "H264";
    const GRP2: &str = "Video";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("CaptureFrameRate".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "CaptureFrameRate",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        dm.push(("VideoFrameRate".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "VideoFrameRate",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::H264::MakeModel` -- FORMAT int16u, FIRST_ENTRY 0.
fn h264_makemodel(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "H264";
    const GRP1: &str = "H264";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("Make".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with(
            "($Image::ExifTool::H264::convMake{$val} || \"Unknown\"); $val",
            &base,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                259 => "Panasonic".to_string(),
                264 => "Sony".to_string(),
                4113 => "Canon".to_string(),
                4356 => "JVC".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("Make", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    tags
}

/// `Image::ExifTool::H264::RecInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn h264_recinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "H264";
    const GRP1: &str = "H264";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("RecordingMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            2 => "XP+".to_string(),
            4 => "SP".to_string(),
            5 => "LP".to_string(),
            6 => "FXP".to_string(),
            7 => "MXP".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RecordingMode", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::JPEG::Ocad` -- FORMAT int8u, FIRST_ENTRY 0.
fn jpeg_ocad(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "APP0";
    const GRP1: &str = "Ocad";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    tags
}

/// `Image::ExifTool::Kodak::Type9` -- FORMAT int8u, FIRST_ENTRY 0.
fn kodak_type9(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Kodak";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if MODEL_RE_15.is_match(make) {
        if let Some(v) = u16_at(data, 0xc, bo) {
            dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "FNumber",
                    0xc,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if !(MODEL_RE_15.is_match(make)) {
        if let Some(v) = u16_at(data, 0xc, bo) {
            dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 10", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "FNumber",
                    0xc,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x10, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1e6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x10,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x14, 20, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with("$val=~s{/}{:}g; $val", &cv, &ctx) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "DateTimeOriginal",
            0x14,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x34, bo) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ISO",
            0x34,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_15.is_match(make) {
        if let Some(text) = text_at(data, 0x57, 16, true) {
            tags.push(mk(
                "FirmwareVersion",
                0x57,
                text.clone(),
                Value::String(text),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Kodak::Type2` -- FORMAT int8u, FIRST_ENTRY 0.
fn kodak_type2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Kodak";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x8, 32, true) {
        tags.push(mk(
            "KodakMaker",
            0x8,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x28, 32, true) {
        tags.push(mk(
            "KodakModel",
            0x28,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x6c, bo) {
        dm.push(("KodakImageWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "KodakImageWidth",
            0x6c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x70, bo) {
        dm.push(("KodakImageHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "KodakImageHeight",
            0x70,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Kodak::Type3` -- FORMAT int8u, FIRST_ENTRY 0.
fn kodak_type3(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Kodak";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0xc, bo) {
        dm.push(("YearCreated".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "YearCreated",
            0xc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u8_at(data, 0xe + k * 1) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) =
                conv_expr::eval_with("sprintf(\"%.2d:%.2d\",split(\" \", $val))", &cv, &ctx)
            {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "MonthDayCreated",
                0xe,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u8_at(data, 0x10 + k * 1) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "sprintf(\"%2d:%.2d:%.2d.%.2d\",split(\" \", $val))",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "TimeCreated",
                0x10,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x1e, bo) {
        dm.push(("OpticalZoom".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "OpticalZoom",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x37) {
        dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Sharpness",
            0x37,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x38, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1e5", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x38,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x3c, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FNumber",
                0x3c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x4e, bo) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ISO",
            0x4e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Kodak::Type4` -- FORMAT int8u, FIRST_ENTRY 0.
fn kodak_type4(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Kodak";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x20, 12, true) {
        tags.push(mk(
            "OriginalFileName",
            0x20,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Kodak::Type5` -- FORMAT int8u, FIRST_ENTRY 0.
fn kodak_type5(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Kodak";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x14, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1e5", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x14,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1a) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "Daylight".to_string(),
            2 => "Flash".to_string(),
            3 => "Tungsten".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x1a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x1c, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FNumber",
                0x1c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x1e, bo) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ISO",
            0x1e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x20, bo) {
        dm.push(("OpticalZoom".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "OpticalZoom",
                0x20,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x22, bo) {
        dm.push(("DigitalZoom".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DigitalZoom",
                0x22,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x27) {
        dm.push(("FlashMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "On".to_string(),
            2 => "Off".to_string(),
            3 => "Red-Eye".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashMode", 0x27, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2a) {
        dm.push(("ImageRotated".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageRotated", 0x2a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2b) {
        dm.push(("Macro".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Macro", 0x2b, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Kodak::Type6` -- FORMAT int8u, FIRST_ENTRY 0.
fn kodak_type6(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Kodak";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x10, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1e5", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x10,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x14, bo) {
        dm.push(("ISOSetting".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u16_at(data, 0x18, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FNumber",
                0x18,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x1a, bo) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ISO",
            0x1a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x1c, bo) {
        dm.push(("OpticalZoom".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "OpticalZoom",
                0x1c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x1e, bo) {
        dm.push(("DigitalZoom".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DigitalZoom",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x22, bo) {
        dm.push(("Flash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Flash".to_string(),
            1 => "Fired".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Flash", 0x22, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Kodak::Type7` -- FORMAT int8u, FIRST_ENTRY 0.
fn kodak_type7(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Kodak";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 16, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with("$val=~s/\\s+$//; $val", &cv, &ctx) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        tags.push(mk(
            "SerialNumber",
            0x0,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Kodak::Processing` -- FORMAT int16u, FIRST_ENTRY 0.
fn kodak_processing(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Kodak";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x28 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "
            my @a = split ' ',$val;
            foreach (@a) {
                $_ = 2048 / $_ if $_;
            }
            return join ' ', @a;
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "WB_RGBLevels",
                0x14,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Kodak::Unknown` -- FORMAT int8u, FIRST_ENTRY 0.
fn kodak_unknown(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Kodak";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    tags
}

/// `Image::ExifTool::Kodak::MOV` -- FORMAT int8u, FIRST_ENTRY 0.
fn kodak_mov(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Kodak";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 21, true) {
        tags.push(mk(
            "Make",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x16, 42, true) {
        tags.push(mk(
            "Model",
            0x16,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x40, 8, true) {
        tags.push(mk(
            "ModelType",
            0x40,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x4e, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? 10 / $val : 0", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x4e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = rat64u_at(data, 0x52, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FNumber",
            0x52,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64s_at(data, 0x5a, bo) {
        dm.push(("ExposureCompensation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "ExposureCompensation",
            0x5a,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64u_at(data, 0x70, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f mm\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FocalLength",
            0x70,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Microsoft::Stitch` -- FORMAT float, FIRST_ENTRY 0.
fn microsoft_stitch(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Microsoft";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x0, bo) {
        dm.push((
            "PanoramicStitchVersion".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PanoramicStitchVersion",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push((
            "PanoramicStitchCameraMotion".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            2 => "Rigid Scale".to_string(),
            3 => "Affine".to_string(),
            4 => "3D Rotation".to_string(),
            5 => "Homography".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PanoramicStitchCameraMotion",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push((
            "PanoramicStitchMapType".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Perspective".to_string(),
            1 => "Horizontal Cylindrical".to_string(),
            2 => "Horizontal Spherical".to_string(),
            257 => "Vertical Cylindrical".to_string(),
            258 => "Vertical Spherical".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PanoramicStitchMapType",
            0x2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0xc, bo) {
        dm.push(("PanoramicStitchTheta0".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PanoramicStitchTheta0",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0x10, bo) {
        dm.push(("PanoramicStitchTheta1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PanoramicStitchTheta1",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0x14, bo) {
        dm.push(("PanoramicStitchPhi0".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PanoramicStitchPhi0",
            0x5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0x18, bo) {
        dm.push(("PanoramicStitchPhi1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PanoramicStitchPhi1",
            0x6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Minolta::CameraSettings7D` -- FORMAT int16u, FIRST_ENTRY 0.
fn minolta_camerasettings7d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Minolta";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("ExposureMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Program".to_string(),
            1 => "Aperture Priority".to_string(),
            2 => "Shutter Priority".to_string(),
            3 => "Manual".to_string(),
            4 => "Auto".to_string(),
            5 => "Program-shift A".to_string(),
            6 => "Program-shift S".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureMode", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push(("MinoltaImageSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Large".to_string(),
            1 => "Medium".to_string(),
            2 => "Small".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MinoltaImageSize", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x6, bo) {
        dm.push(("MinoltaQuality".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "RAW".to_string(),
            16 => "Fine".to_string(),
            32 => "Normal".to_string(),
            34 => "RAW+JPEG".to_string(),
            48 => "Economy".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MinoltaQuality", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x8, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Shade".to_string(),
            3 => "Cloudy".to_string(),
            4 => "Tungsten".to_string(),
            5 => "Fluorescent".to_string(),
            256 => "Kelvin".to_string(),
            512 => "Manual".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x1c, bo) {
        dm.push(("FocusMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AF-S".to_string(),
            1 => "AF-C".to_string(),
            3 => "Manual".to_string(),
            4 => "AF-A".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusMode", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x20, bo) {
        dm.push(("AFPoints".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Center".to_string(),
            1 => "Top".to_string(),
            2 => "Top-right".to_string(),
            3 => "Right".to_string(),
            4 => "Bottom-right".to_string(),
            5 => "Bottom".to_string(),
            6 => "Bottom-left".to_string(),
            7 => "Left".to_string(),
            8 => "Top-left".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPoints", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x2a, bo) {
        dm.push(("Flash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Flash", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x2c, bo) {
        dm.push(("FlashMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Normal".to_string(),
            1 => "Red-eye reduction".to_string(),
            2 => "Rear flash sync".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashMode", 0x16, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x38, bo) {
        dm.push(("ISOSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOSetting", 0x1c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0x3c, bo) {
        dm.push(("ExposureCompensation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 24", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureCompensation",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x4a, bo) {
        dm.push(("ColorSpace".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Natural sRGB".to_string(),
            1 => "Natural+ sRGB".to_string(),
            4 => "Adobe RGB".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorSpace", 0x25, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x4c, bo) {
        dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Sharpness",
                0x26,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x4e, bo) {
        dm.push(("Contrast".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Contrast",
                0x27,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x50, bo) {
        dm.push(("Saturation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Saturation",
                0x28,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x5a, bo) {
        dm.push(("FreeMemoryCardImages".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FreeMemoryCardImages",
            0x2d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x7e, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val * 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ColorTemperature",
                0x3f,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x80, bo) {
        dm.push(("HueAdjustment".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "HueAdjustment",
                0x40,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x8c, bo) {
        dm.push(("Rotation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            72 => "Horizontal (normal)".to_string(),
            76 => "Rotate 90 CW".to_string(),
            82 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Rotation", 0x46, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x8e, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (($val-8)/16)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FNumber",
                0x47,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x90, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** ((48-$val)/8)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x48,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x94, bo) {
        dm.push(("FreeMemoryCardImages".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FreeMemoryCardImages",
            0x4a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xbc, bo) {
        dm.push(("ImageNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ImageNumber",
                0x5e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xc0, bo) {
        dm.push(("NoiseReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoiseReduction", 0x60, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xc4, bo) {
        dm.push(("ImageNumber2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ImageNumber2",
                0x62,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xe2, bo) {
        dm.push(("ImageStabilization".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageStabilization",
            0x71,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xea, bo) {
        dm.push(("ZoneMatchingOn".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ZoneMatchingOn", 0x75, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Minolta::CameraInfoA100` -- FORMAT int8u, FIRST_ENTRY 0.
fn minolta_camerainfoa100(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Minolta";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x1) {
        dm.push(("AFSensorActive".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Top-right".to_string(),
            1 => "Bottom-right".to_string(),
            2 => "Bottom".to_string(),
            3 => "Middle Horizontal".to_string(),
            4 => "Center Vertical".to_string(),
            5 => "Top".to_string(),
            6 => "Top-left".to_string(),
            7 => "Bottom-left".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFSensorActive", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        dm.push(("AFStatusActiveSensor".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusActiveSensor",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("AFStatusTop-right".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusTop-right",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        dm.push(("AFStatusBottom-right".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusBottom-right",
            0x6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        dm.push(("AFStatusBottom".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusBottom",
            0x8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        dm.push((
            "AFStatusMiddleHorizontal".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusMiddleHorizontal",
            0xa,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc) {
        dm.push((
            "AFStatusCenterVertical".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusCenterVertical",
            0xc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe) {
        dm.push(("AFStatusTop".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusTop",
            0xe,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        dm.push(("AFStatusTop-left".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusTop-left",
            0x10,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        dm.push(("AFStatusBottom-left".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusBottom-left",
            0x12,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        dm.push(("FocusLocked".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Manual Focus".to_string(),
            4 => "No".to_string(),
            16 => "Continuous Focus".to_string(),
            64 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusLocked", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push(("AFPoint".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Center".to_string(),
            2 => "Top".to_string(),
            3 => "Top-right".to_string(),
            4 => "Right".to_string(),
            5 => "Bottom-right".to_string(),
            6 => "Bottom".to_string(),
            7 => "Bottom-left".to_string(),
            8 => "Left".to_string(),
            9 => "Top-left".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPoint", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x16) {
        dm.push(("AFMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "DMF".to_string(),
            1 => "AF-S".to_string(),
            2 => "AF-C".to_string(),
            3 => "AF-A".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFMode", 0x16, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2d) {
        dm.push(("AFStatusLeft".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusLeft",
            0x2d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        dm.push((
            "AFStatusCenterHorizontal".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusCenterHorizontal",
            0x2f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x31) {
        dm.push(("AFStatusRight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFStatusRight",
            0x31,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x33) {
        dm.push(("AFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Wide".to_string(),
            1 => "Local".to_string(),
            2 => "Spot".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAreaMode", 0x33, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Minolta::WBInfoA100` -- FORMAT int8u, FIRST_ENTRY 0.
fn minolta_wbinfoa100(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Minolta";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0xe) {
        dm.push(("DriveMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Self-timer 10 sec".to_string(),
            1 => "Continuous".to_string(),
            2 => "Single-frame Exposure Bracketing".to_string(),
            3 => "Continuous Exposure Bracketing".to_string(),
            4 => "Self-Timer 2 sec".to_string(),
            5 => "Single Frame".to_string(),
            8 => "White Balance Bracketing Low".to_string(),
            9 => "White Balance Bracketing High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DriveMode", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x10) {
        dm.push(("Rotation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 270 CW".to_string(),
            2 => "Rotate 90 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Rotation", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        dm.push((
            "ImageStabilizationSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageStabilizationSetting",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push((
            "DynamicRangeOptimizerMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Standard".to_string(),
            2 => "Advanced".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DynamicRangeOptimizerMode",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2a) {
        dm.push((
            "ExposureCompensationMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Ambient and Flash".to_string(),
            1 => "Ambient Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureCompensationMode",
            0x2a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b) {
        dm.push(("WBBracketShotNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WBBracketShotNumber",
            0x2b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2c) {
        dm.push((
            "WhiteBalanceBracketing".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Low".to_string(),
            2 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "WhiteBalanceBracketing",
            0x2c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2d) {
        dm.push((
            "ExposureBracketShotNumber".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ExposureBracketShotNumber",
            0x2d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x31, bo) {
        dm.push(("FlashFunction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No flash".to_string(),
            768 => "Built-in flash".to_string(),
            4613 => "Manual".to_string(),
            4622 => "Strobe".to_string(),
            4750 => "Fill flash, Pre-flash TTL".to_string(),
            4782 => "Bounce flash".to_string(),
            5134 => "Rear sync, ADI".to_string(),
            5262 => "Fill flash, ADI".to_string(),
            5504 => "Wireless".to_string(),
            6030 => "HSS".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashFunction", 0x31, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x34, bo) {
        dm.push(("ExposureMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Program".to_string(),
            1 => "Aperture Priority".to_string(),
            2 => "Shutter Priority".to_string(),
            3 => "Manual".to_string(),
            4 => "Auto".to_string(),
            5 => "Program Shift A".to_string(),
            6 => "Program Shift S".to_string(),
            4115 => "Portrait".to_string(),
            4131 => "Sports".to_string(),
            4147 => "Sunset".to_string(),
            4163 => "Night View/Portrait".to_string(),
            4179 => "Landscape".to_string(),
            4227 => "Macro".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureMode", 0x34, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x36, bo) {
        dm.push(("ColorMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Vivid".to_string(),
            2 => "Portrait".to_string(),
            3 => "Landscape".to_string(),
            4 => "Sunset".to_string(),
            5 => "Night View".to_string(),
            7 => "B&W".to_string(),
            8 => "Adobe RGB".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorMode", 0x36, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x38, bo) {
        dm.push(("AverageLV".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val-106)/8", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "AverageLV",
                0x38,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x3c) {
        dm.push(("FrameNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FrameNumber",
            0x3c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x96 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGBLevels",
                0x96,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0xae + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_GBRGLevels",
                0xae,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0xc0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RedLevelsTungsten",
                0xc0,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0xce + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_BlueLevelsTungsten",
                0xce,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0xdc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RedLevelsDaylight",
                0xdc,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0xea + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_BlueLevelsDaylight",
                0xea,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0xf8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RedLevelsCloudy",
                0xf8,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0x106 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_BlueLevelsCloudy",
                0x106,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0x114 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RedLevelsFlash",
                0x114,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0x122 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_BlueLevelsFlash",
                0x122,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0x14c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RedLevelsFluorescent",
                0x14c,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0x15a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_BlueLevelsFluorescent",
                0x15a,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0x168 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RedLevelsShade",
                0x168,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0x176 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_BlueLevelsShade",
                0x176,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x188, bo) {
        dm.push(("WB_RedLevel6500K".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WB_RedLevel6500K",
            0x188,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x18a, bo) {
        dm.push(("WB_BlueLevel6500K".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WB_BlueLevel6500K",
            0x18a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x18c, bo) {
        dm.push(("WB_RedLevelCustom".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WB_RedLevelCustom",
            0x18c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x18e, bo) {
        dm.push(("WB_BlueLevelCustom".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WB_BlueLevelCustom",
            0x18e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x198, bo) {
        dm.push(("WB_RedLevel3500K".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WB_RedLevel3500K",
            0x198,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x19a, bo) {
        dm.push(("WB_BlueLevel3500K".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WB_BlueLevel3500K",
            0x19a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..75 {
            match u16_at(data, 0x1be + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RedLevelsKelvin",
                0x1be,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..75 {
            match u16_at(data, 0x254 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_BlueLevelsKelvin",
                0x254,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x304 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevelsFlash",
                0x304,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x308 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevelsCoolWhiteF",
                0x308,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x3e8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevelsTungsten",
                0x3e8,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x3ec + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevelsDaylight",
                0x3ec,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x3f0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevelsCloudy",
                0x3f0,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x3f4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevelsFlash",
                0x3f4,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0x3fc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RedLevelsFluorescent",
                0x3fc,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u16_at(data, 0x40a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_BlueLevelsFluorescent",
                0x40a,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x418 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevelsShade",
                0x418,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x420 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevels6500K",
                0x420,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x424 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevelsCustom",
                0x424,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x430 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevels3500K",
                0x430,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x528 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevelsDaylight",
                0x528,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x546 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGBLevels",
                0x546,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..40 {
            match u8_at(data, 0x628 + k * 1) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "AEMeteringSegments",
                0x628,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x690) {
        dm.push(("MeasuredLV".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val-106)/8", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "MeasuredLV",
                0x690,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x691) {
        dm.push(("BrightnessValue".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val-106)/8", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "BrightnessValue",
                0x691,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x104c, 9600, false) {
        tags.push(mk(
            "TiffMeteringImage",
            0x104c,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x49b8) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? 2 ** (6 - $val/8) : 0", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val ? Image::ExifTool::Exif::PrintExposureTime($val) : \"Bulb\"",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x49b8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x49ba) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (($val-48)/8) * 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val + 0.5)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ISO",
                0x49ba,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x49bb) {
        dm.push(("FocusDistance".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2**(($val-126)/16)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val > 266 ? \"inf\" : sprintf(\"%.2f m\", $val)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "FocusDistance",
                0x49bb,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16rev_at(data, 0x49bd, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Minolta AF 28-85mm F3.5-4.5 New".to_string(),
            1 => "Minolta AF 80-200mm F2.8 HS-APO G".to_string(),
            2 => "Minolta AF 28-70mm F2.8 G".to_string(),
            3 => "Minolta AF 28-80mm F4-5.6".to_string(),
            4 => "Minolta AF 85mm F1.4G".to_string(),
            5 => "Minolta AF 35-70mm F3.5-4.5 [II]".to_string(),
            6 => "Minolta AF 24-85mm F3.5-4.5 [New]".to_string(),
            7 => "Minolta AF 100-300mm F4.5-5.6 APO [New] or 100-400mm or Sigma Lens".to_string(),
            8 => "Minolta AF 70-210mm F4.5-5.6 [II]".to_string(),
            9 => "Minolta AF 50mm F3.5 Macro".to_string(),
            10 => "Minolta AF 28-105mm F3.5-4.5 [New]".to_string(),
            11 => "Minolta AF 300mm F4 HS-APO G".to_string(),
            12 => "Minolta AF 100mm F2.8 Soft Focus".to_string(),
            13 => "Minolta AF 75-300mm F4.5-5.6 (New or II)".to_string(),
            14 => "Minolta AF 100-400mm F4.5-6.7 APO".to_string(),
            15 => "Minolta AF 400mm F4.5 HS-APO G".to_string(),
            16 => "Minolta AF 17-35mm F3.5 G".to_string(),
            17 => "Minolta AF 20-35mm F3.5-4.5".to_string(),
            18 => "Minolta AF 28-80mm F3.5-5.6 II".to_string(),
            19 => "Minolta AF 35mm F1.4 G".to_string(),
            20 => "Minolta/Sony 135mm F2.8 [T4.5] STF".to_string(),
            22 => "Minolta AF 35-80mm F4-5.6 II".to_string(),
            23 => "Minolta AF 200mm F4 Macro APO G".to_string(),
            24 => "Minolta/Sony AF 24-105mm F3.5-4.5 (D) or Sigma or Tamron Lens".to_string(),
            25 => "Minolta AF 100-300mm F4.5-5.6 APO (D) or Sigma Lens".to_string(),
            27 => "Minolta AF 85mm F1.4 G (D)".to_string(),
            28 => "Minolta/Sony AF 100mm F2.8 Macro (D) or Tamron Lens".to_string(),
            29 => "Minolta/Sony AF 75-300mm F4.5-5.6 (D)".to_string(),
            30 => "Minolta AF 28-80mm F3.5-5.6 (D) or Sigma Lens".to_string(),
            31 => "Minolta/Sony AF 50mm F2.8 Macro (D) or F3.5".to_string(),
            32 => "Minolta/Sony AF 300mm F2.8 G or 1.5x Teleconverter".to_string(),
            33 => "Minolta/Sony AF 70-200mm F2.8 G".to_string(),
            35 => "Minolta AF 85mm F1.4 G (D) Limited".to_string(),
            36 => "Minolta AF 28-100mm F3.5-5.6 (D)".to_string(),
            38 => "Minolta AF 17-35mm F2.8-4 (D)".to_string(),
            39 => "Minolta AF 28-75mm F2.8 (D)".to_string(),
            40 => "Minolta/Sony AF DT 18-70mm F3.5-5.6 (D)".to_string(),
            41 => "Minolta/Sony AF DT 11-18mm F4.5-5.6 (D) or Tamron Lens".to_string(),
            42 => "Minolta/Sony AF DT 18-200mm F3.5-6.3 (D)".to_string(),
            43 => "Sony 35mm F1.4 G (SAL35F14G)".to_string(),
            44 => "Sony 50mm F1.4 (SAL50F14)".to_string(),
            45 => "Carl Zeiss Planar T* 85mm F1.4 ZA (SAL85F14Z)".to_string(),
            46 => "Carl Zeiss Vario-Sonnar T* DT 16-80mm F3.5-4.5 ZA (SAL1680Z)".to_string(),
            47 => "Carl Zeiss Sonnar T* 135mm F1.8 ZA (SAL135F18Z)".to_string(),
            48 => "Carl Zeiss Vario-Sonnar T* 24-70mm F2.8 ZA SSM (SAL2470Z) or Other Lens"
                .to_string(),
            49 => "Sony DT 55-200mm F4-5.6 (SAL55200)".to_string(),
            50 => "Sony DT 18-250mm F3.5-6.3 (SAL18250)".to_string(),
            51 => "Sony DT 16-105mm F3.5-5.6 (SAL16105)".to_string(),
            52 => "Sony 70-300mm F4.5-5.6 G SSM (SAL70300G) or G SSM II or Tamron Lens".to_string(),
            53 => "Sony 70-400mm F4-5.6 G SSM (SAL70400G)".to_string(),
            54 => {
                "Carl Zeiss Vario-Sonnar T* 16-35mm F2.8 ZA SSM (SAL1635Z) or ZA SSM II".to_string()
            }
            55 => "Sony DT 18-55mm F3.5-5.6 SAM (SAL1855) or SAM II".to_string(),
            56 => "Sony DT 55-200mm F4-5.6 SAM (SAL55200-2)".to_string(),
            57 => "Sony DT 50mm F1.8 SAM (SAL50F18) or Tamron Lens or Commlite CM-EF-NEX adapter"
                .to_string(),
            58 => "Sony DT 30mm F2.8 Macro SAM (SAL30M28)".to_string(),
            59 => "Sony 28-75mm F2.8 SAM (SAL2875)".to_string(),
            60 => "Carl Zeiss Distagon T* 24mm F2 ZA SSM (SAL24F20Z)".to_string(),
            61 => "Sony 85mm F2.8 SAM (SAL85F28)".to_string(),
            62 => "Sony DT 35mm F1.8 SAM (SAL35F18)".to_string(),
            63 => "Sony DT 16-50mm F2.8 SSM (SAL1650)".to_string(),
            64 => "Sony 500mm F4 G SSM (SAL500F40G)".to_string(),
            65 => "Sony DT 18-135mm F3.5-5.6 SAM (SAL18135)".to_string(),
            66 => "Sony 300mm F2.8 G SSM II (SAL300F28G2)".to_string(),
            67 => "Sony 70-200mm F2.8 G SSM II (SAL70200G2)".to_string(),
            68 => "Sony DT 55-300mm F4.5-5.6 SAM (SAL55300)".to_string(),
            69 => "Sony 70-400mm F4-5.6 G SSM II (SAL70400G2)".to_string(),
            70 => "Carl Zeiss Planar T* 50mm F1.4 ZA SSM (SAL50F14Z)".to_string(),
            128 => "Tamron or Sigma Lens (128)".to_string(),
            129 => "Tamron Lens (129)".to_string(),
            131 => "Tamron 20-40mm F2.7-3.5 SP Aspherical IF".to_string(),
            135 => "Vivitar 28-210mm F3.5-5.6".to_string(),
            136 => "Tokina EMZ M100 AF 100mm F3.5".to_string(),
            137 => "Cosina 70-210mm F2.8-4 AF".to_string(),
            138 => "Soligor 19-35mm F3.5-4.5".to_string(),
            139 => "Tokina AF 28-300mm F4-6.3".to_string(),
            142 => "Cosina AF 70-300mm F4.5-5.6 MC".to_string(),
            146 => "Voigtlander Macro APO-Lanthar 125mm F2.5 SL".to_string(),
            194 => "Tamron SP AF 17-50mm F2.8 XR Di II LD Aspherical [IF]".to_string(),
            202 => "Tamron SP AF 70-200mm F2.8 Di LD [IF] Macro".to_string(),
            203 => "Tamron SP 70-200mm F2.8 Di USD".to_string(),
            204 => "Tamron SP 24-70mm F2.8 Di USD".to_string(),
            212 => "Tamron 28-300mm F3.5-6.3 Di PZD".to_string(),
            213 => "Tamron 16-300mm F3.5-6.3 Di II PZD Macro".to_string(),
            214 => "Tamron SP 150-600mm F5-6.3 Di USD".to_string(),
            215 => "Tamron SP 15-30mm F2.8 Di USD".to_string(),
            216 => "Tamron SP 45mm F1.8 Di USD".to_string(),
            217 => "Tamron SP 35mm F1.8 Di USD".to_string(),
            218 => "Tamron SP 90mm F2.8 Di Macro 1:1 USD (F017)".to_string(),
            220 => "Tamron SP 150-600mm F5-6.3 Di USD G2".to_string(),
            224 => "Tamron SP 90mm F2.8 Di Macro 1:1 USD (F004)".to_string(),
            255 => "Tamron Lens (255)".to_string(),
            18688 => "Sigma MC-11 SA-E Mount Converter with not-supported Sigma lens".to_string(),
            25501 => "Minolta AF 50mm F1.7".to_string(),
            25511 => "Minolta AF 35-70mm F4 or Other Lens".to_string(),
            25521 => "Minolta AF 28-85mm F3.5-4.5 or Other Lens".to_string(),
            25531 => "Minolta AF 28-135mm F4-4.5 or Other Lens".to_string(),
            25541 => "Minolta AF 35-105mm F3.5-4.5".to_string(),
            25551 => "Minolta AF 70-210mm F4 Macro or Sigma Lens".to_string(),
            25561 => "Minolta AF 135mm F2.8".to_string(),
            25571 => "Minolta/Sony AF 28mm F2.8".to_string(),
            25581 => "Minolta AF 24-50mm F4".to_string(),
            25601 => "Minolta AF 100-200mm F4.5".to_string(),
            25611 => "Minolta AF 75-300mm F4.5-5.6 or Sigma Lens".to_string(),
            25621 => "Minolta AF 50mm F1.4 [New]".to_string(),
            25631 => "Minolta AF 300mm F2.8 APO or Sigma Lens".to_string(),
            25641 => "Minolta AF 50mm F2.8 Macro or Sigma Lens".to_string(),
            25651 => "Minolta AF 600mm F4 APO".to_string(),
            25661 => "Minolta AF 24mm F2.8 or Sigma Lens".to_string(),
            25721 => "Minolta/Sony AF 500mm F8 Reflex".to_string(),
            25781 => "Minolta/Sony AF 16mm F2.8 Fisheye or Sigma Lens".to_string(),
            25791 => "Minolta/Sony AF 20mm F2.8 or Tokina Lens".to_string(),
            25811 => "Minolta AF 100mm F2.8 Macro [New] or Sigma or Tamron Lens".to_string(),
            25851 => "Beroflex 35-135mm F3.5-4.5".to_string(),
            25858 => "Minolta AF 35-105mm F3.5-4.5 New or Tamron Lens".to_string(),
            25881 => "Minolta AF 70-210mm F3.5-4.5".to_string(),
            25891 => "Minolta AF 80-200mm F2.8 APO or Tokina Lens".to_string(),
            25901 => {
                "Minolta AF 200mm F2.8 G APO + Minolta AF 1.4x APO or Other Lens + 1.4x".to_string()
            }
            25911 => "Minolta AF 35mm F1.4".to_string(),
            25921 => "Minolta AF 85mm F1.4 G (D)".to_string(),
            25931 => "Minolta AF 200mm F2.8 APO".to_string(),
            25941 => "Minolta AF 3x-1x F1.7-2.8 Macro".to_string(),
            25961 => "Minolta AF 28mm F2".to_string(),
            25971 => "Minolta AF 35mm F2 [New]".to_string(),
            25981 => "Minolta AF 100mm F2".to_string(),
            26011 => {
                "Minolta AF 200mm F2.8 G APO + Minolta AF 2x APO or Other Lens + 2x".to_string()
            }
            26041 => "Minolta AF 80-200mm F4.5-5.6".to_string(),
            26051 => "Minolta AF 35-80mm F4-5.6".to_string(),
            26061 => "Minolta AF 100-300mm F4.5-5.6".to_string(),
            26071 => "Minolta AF 35-80mm F4-5.6".to_string(),
            26081 => "Minolta AF 300mm F2.8 HS-APO G".to_string(),
            26091 => "Minolta AF 600mm F4 HS-APO G".to_string(),
            26121 => "Minolta AF 200mm F2.8 HS-APO G".to_string(),
            26131 => "Minolta AF 50mm F1.7 New".to_string(),
            26151 => "Minolta AF 28-105mm F3.5-4.5 xi".to_string(),
            26161 => "Minolta AF 35-200mm F4.5-5.6 xi".to_string(),
            26181 => "Minolta AF 28-80mm F4-5.6 xi".to_string(),
            26191 => "Minolta AF 80-200mm F4.5-5.6 xi".to_string(),
            26201 => "Minolta AF 28-70mm F2.8 G".to_string(),
            26211 => "Minolta AF 100-300mm F4.5-5.6 xi".to_string(),
            26241 => "Minolta AF 35-80mm F4-5.6 Power Zoom".to_string(),
            26281 => "Minolta AF 80-200mm F2.8 HS-APO G".to_string(),
            26291 => "Minolta AF 85mm F1.4 New".to_string(),
            26311 => "Minolta AF 100-300mm F4.5-5.6 APO".to_string(),
            26321 => "Minolta AF 24-50mm F4 New".to_string(),
            26381 => "Minolta AF 50mm F2.8 Macro New".to_string(),
            26391 => "Minolta AF 100mm F2.8 Macro".to_string(),
            26411 => "Minolta/Sony AF 20mm F2.8 New".to_string(),
            26421 => "Minolta AF 24mm F2.8 New".to_string(),
            26441 => "Minolta AF 100-400mm F4.5-6.7 APO".to_string(),
            26621 => "Minolta AF 50mm F1.4 New".to_string(),
            26671 => "Minolta AF 35mm F2 New".to_string(),
            26681 => "Minolta AF 28mm F2 New".to_string(),
            26721 => "Minolta AF 24-105mm F3.5-4.5 (D)".to_string(),
            30464 => "Metabones Canon EF Speed Booster".to_string(),
            45671 => "Tokina 70-210mm F4-5.6".to_string(),
            45681 => "Tokina AF 35-200mm F4-5.6 Zoom SD".to_string(),
            45701 => "Tamron AF 35-135mm F3.5-4.5".to_string(),
            45711 => "Vivitar 70-210mm F4.5-5.6".to_string(),
            45741 => "2x Teleconverter or Tamron or Tokina Lens".to_string(),
            45751 => "1.4x Teleconverter".to_string(),
            45851 => "Tamron SP AF 300mm F2.8 LD IF".to_string(),
            45861 => "Tamron SP AF 35-105mm F2.8 LD Aspherical IF".to_string(),
            45871 => "Tamron AF 70-210mm F2.8 SP LD".to_string(),
            48128 => "Metabones Canon EF Speed Booster Ultra".to_string(),
            61184 => "Canon EF Adapter".to_string(),
            65280 => "Sigma 16mm F2.8 Filtermatic Fisheye".to_string(),
            65535 => "E-Mount, T-Mount, Other Lens or no lens".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensType", 0x49bd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i8_at(data, 0x49c0) {
        dm.push(("ExposureCompensation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 8", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ExposureCompensation",
                0x49c0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x49c1) {
        dm.push(("FlashExposureComp".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 8", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FlashExposureComp",
                0x49c1,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x49c2) {
        dm.push(("ImageStabilization".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageStabilization",
            0x49c2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x49c3) {
        dm.push(("BrightnessValue".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val-106)/8", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "BrightnessValue",
                0x49c3,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x49c5) {
        dm.push(("MaxAperture".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (($val-8)/16)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "MaxAperture",
                0x49c5,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x49c7) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (($val-8)/16)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FNumber",
                0x49c7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x49dc, 12, true) {
        tags.push(mk(
            "InternalSerialNumber",
            0x49dc,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Minolta::MOV1` -- FORMAT int8u, FIRST_ENTRY 0.
fn minolta_mov1(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Minolta";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 32, true) {
        tags.push(mk(
            "Make",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x20, 8, true) {
        tags.push(mk(
            "ModelType",
            0x20,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x2e, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? 10 / $val : 0", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x2e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = rat64u_at(data, 0x32, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FNumber",
            0x32,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64s_at(data, 0x3a, bo) {
        dm.push(("ExposureCompensation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "ExposureCompensation",
            0x3a,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64u_at(data, 0x50, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f mm\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FocalLength",
            0x50,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Minolta::MOV2` -- FORMAT int8u, FIRST_ENTRY 0.
fn minolta_mov2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Minolta";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 32, true) {
        tags.push(mk(
            "Make",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x18, 8, true) {
        tags.push(mk(
            "ModelType",
            0x18,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x26, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? 10 / $val : 0", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x26,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = rat64u_at(data, 0x2a, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FNumber",
            0x2a,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64s_at(data, 0x32, bo) {
        dm.push(("ExposureCompensation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "ExposureCompensation",
            0x32,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64u_at(data, 0x48, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f mm\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FocalLength",
            0x48,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Minolta::CameraSettings5D` -- FORMAT int16u, FIRST_ENTRY 0.
fn minolta_camerasettings5d(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Minolta";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x14, bo) {
        dm.push(("ExposureMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Program".to_string(),
            1 => "Aperture Priority".to_string(),
            2 => "Shutter Priority".to_string(),
            3 => "Manual".to_string(),
            4 => "Auto?".to_string(),
            4131 => "Connected Copying?".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureMode", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x18, bo) {
        dm.push(("MinoltaImageSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Large".to_string(),
            1 => "Medium".to_string(),
            2 => "Small".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MinoltaImageSize", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x1a, bo) {
        dm.push(("MinoltaQuality".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "RAW".to_string(),
            16 => "Fine".to_string(),
            32 => "Normal".to_string(),
            34 => "RAW+JPEG".to_string(),
            48 => "Economy".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MinoltaQuality", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x1c, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Shade".to_string(),
            4 => "Tungsten".to_string(),
            5 => "Fluorescent".to_string(),
            6 => "Flash".to_string(),
            256 => "Kelvin".to_string(),
            512 => "Manual".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x3e, bo) {
        dm.push(("Flash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Did not fire".to_string(),
            1 => "Fired".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Flash", 0x1f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x40, bo) {
        dm.push(("FlashMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Normal".to_string(),
            1 => "Red-eye reduction".to_string(),
            2 => "Rear flash sync".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashMode", 0x20, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x4a, bo) {
        dm.push(("MeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Multi-segment".to_string(),
            1 => "Center-weighted average".to_string(),
            2 => "Spot".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MeteringMode", 0x25, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x4c, bo) {
        dm.push(("ISOSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            8 => "200 (Zone Matching High)".to_string(),
            10 => "80 (Zone Matching Low)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOSetting", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x5e, bo) {
        dm.push(("ColorSpace".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Natural sRGB".to_string(),
            1 => "Natural+ sRGB".to_string(),
            2 => "Monochrome".to_string(),
            4 => "Adobe RGB (ICC)".to_string(),
            5 => "Adobe RGB".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorSpace", 0x2f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x60, bo) {
        dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Sharpness",
                0x30,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x62, bo) {
        dm.push(("Contrast".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Contrast",
                0x31,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x64, bo) {
        dm.push(("Saturation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Saturation",
                0x32,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x6a, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** ((48-$val)/8)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x35,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x6c, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (($val-8)/16)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FNumber",
                0x36,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x6e, bo) {
        dm.push(("FreeMemoryCardImages".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FreeMemoryCardImages",
            0x37,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x92, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val * 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ColorTemperature",
                0x49,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x94, bo) {
        dm.push(("HueAdjustment".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "HueAdjustment",
                0x4a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xa0, bo) {
        dm.push(("Rotation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            72 => "Horizontal (normal)".to_string(),
            76 => "Rotate 90 CW".to_string(),
            82 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Rotation", 0x50, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xa6, bo) {
        dm.push(("ExposureCompensation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100 - 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureCompensation",
                0x53,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xa8, bo) {
        dm.push(("FreeMemoryCardImages".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FreeMemoryCardImages",
            0x54,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xca, bo) {
        dm.push(("Rotation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 90 CW".to_string(),
            2 => "Rotate 270 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Rotation", 0x65, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0xdc, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val * 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ColorTemperature",
                0x6e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xe2, bo) {
        dm.push(("PictureFinish".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Natural".to_string(),
            1 => "Natural+".to_string(),
            2 => "Portrait".to_string(),
            3 => "Wind Scene".to_string(),
            4 => "Evening Scene".to_string(),
            5 => "Night Scene".to_string(),
            6 => "Night Portrait".to_string(),
            7 => "Monochrome".to_string(),
            8 => "Adobe RGB".to_string(),
            9 => "Adobe RGB (ICC)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureFinish", 0x71, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x15c, bo) {
        dm.push(("ImageNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ImageNumber",
                0xae,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x160, bo) {
        dm.push(("NoiseReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoiseReduction", 0xb0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x17a, bo) {
        dm.push(("ImageStabilization".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageStabilization",
            0xbd,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Minolta::CameraSettingsA100` -- FORMAT int16u, FIRST_ENTRY 0.
fn minolta_camerasettingsa100(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Minolta";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("ExposureMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Program".to_string(),
            1 => "Aperture Priority".to_string(),
            2 => "Shutter Priority".to_string(),
            3 => "Manual".to_string(),
            4 => "Auto".to_string(),
            5 => "Program Shift A".to_string(),
            6 => "Program Shift S".to_string(),
            4115 => "Portrait".to_string(),
            4131 => "Sports".to_string(),
            4147 => "Sunset".to_string(),
            4163 => "Night View/Portrait".to_string(),
            4179 => "Landscape".to_string(),
            4227 => "Macro".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureMode", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push((
            "ExposureCompensationSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100 - 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ExposureCompensationSetting",
                0x1,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xa, bo) {
        dm.push(("HighSpeedSync".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("HighSpeedSync", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xc, bo) {
        dm.push(("ShutterSpeedSetting".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? 2 ** (6 - $val/8) : 0", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val ? Image::ExifTool::Exif::PrintExposureTime($val) : \"Bulb\"",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "ShutterSpeedSetting",
                0x6,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xe, bo) {
        dm.push(("ApertureSetting".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (($val/8 - 1) / 2)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintFNumber($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ApertureSetting",
                0x7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x10, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? 2 ** (6 - $val/8) : 0", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val ? Image::ExifTool::Exif::PrintExposureTime($val) : \"Bulb\"",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x12, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (($val/8 - 1) / 2)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintFNumber($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FNumber",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x14, bo) {
        dm.push(("DriveMode2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Self-timer 10 sec".to_string(),
            1 => "Continuous".to_string(),
            4 => "Self-timer 2 sec".to_string(),
            5 => "Single Frame".to_string(),
            8 => "White Balance Bracketing Low".to_string(),
            9 => "White Balance Bracketing High".to_string(),
            770 => "Single-frame Bracketing Low".to_string(),
            771 => "Continous Bracketing Low".to_string(),
            1794 => "Single-frame Bracketing High".to_string(),
            1795 => "Continuous Bracketing High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DriveMode2", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x16, bo) {
        dm.push(("WhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Shade".to_string(),
            4 => "Tungsten".to_string(),
            5 => "Fluorescent".to_string(),
            6 => "Flash".to_string(),
            256 => "Kelvin".to_string(),
            512 => "Manual".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance", 0xb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x18, bo) {
        dm.push(("FocusMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AF-S".to_string(),
            1 => "AF-C".to_string(),
            4 => "AF-A".to_string(),
            5 => "Manual".to_string(),
            6 => "DMF".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusMode", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x1a, bo) {
        dm.push(("AFPointSelected".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "Center".to_string(),
            2 => "Top".to_string(),
            3 => "Top-right".to_string(),
            4 => "Right".to_string(),
            5 => "Bottom-right".to_string(),
            6 => "Bottom".to_string(),
            7 => "Bottom-left".to_string(),
            8 => "Left".to_string(),
            9 => "Top-left".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPointSelected", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x1c, bo) {
        dm.push(("AFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Wide".to_string(),
            1 => "Local".to_string(),
            2 => "Spot".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAreaMode", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x1e, bo) {
        dm.push(("FlashMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            2 => "Rear Sync".to_string(),
            3 => "Wireless".to_string(),
            4 => "Fill Flash".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashMode", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x20, bo) {
        dm.push(("FlashExposureCompSet".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100 - 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "FlashExposureCompSet",
                0x10,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x24, bo) {
        dm.push(("MeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Multi-segment".to_string(),
            1 => "Center-weighted average".to_string(),
            2 => "Spot".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MeteringMode", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x26, bo) {
        dm.push(("ISOSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            174 => "80 (Zone Matching Low)".to_string(),
            184 => "200 (Zone Matching High)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOSetting", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x28, bo) {
        dm.push(("ZoneMatchingMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Standard".to_string(),
            2 => "Advanced".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ZoneMatchingMode", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x2a, bo) {
        dm.push(("DynamicRangeOptimizer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Standard".to_string(),
            2 => "Advanced".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DynamicRangeOptimizer",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2c, bo) {
        dm.push(("ColorMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Vivid".to_string(),
            2 => "Portrait".to_string(),
            3 => "Landscape".to_string(),
            4 => "Sunset".to_string(),
            5 => "Night Scene".to_string(),
            7 => "B&W".to_string(),
            8 => "Adobe RGB".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorMode", 0x16, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x2e, bo) {
        dm.push(("ColorSpace".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "sRGB".to_string(),
            2 => "B&W".to_string(),
            5 => "Adobe RGB".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorSpace", 0x17, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x30, bo) {
        dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Sharpness",
                0x18,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x32, bo) {
        dm.push(("Contrast".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Contrast",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x34, bo) {
        dm.push(("Saturation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "Saturation",
                0x1a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x38, bo) {
        dm.push(("FlashMetering".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "ADI (Advanced Distance Integration)".to_string(),
            1 => "Pre-flash TTL".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashMetering", 0x1c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x3a, bo) {
        dm.push((
            "PrioritySetupShutterRelease".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AF".to_string(),
            1 => "Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PrioritySetupShutterRelease",
            0x1d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x3c, bo) {
        dm.push(("DriveMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Single Frame".to_string(),
            1 => "Continuous".to_string(),
            2 => "Self-timer".to_string(),
            3 => "Continuous Bracketing".to_string(),
            4 => "Single-Frame Bracketing".to_string(),
            5 => "White Balance Bracketing".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DriveMode", 0x1e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x3e, bo) {
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "10 s".to_string(),
            4 => "2 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x1f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x40, bo) {
        dm.push(("ContinuousBracketing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            771 => "Low".to_string(),
            1795 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ContinuousBracketing",
            0x20,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x42, bo) {
        dm.push(("SingleFrameBracketing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            770 => "Low".to_string(),
            1794 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SingleFrameBracketing",
            0x21,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x44, bo) {
        dm.push((
            "WhiteBalanceBracketing".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            8 => "Low".to_string(),
            9 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "WhiteBalanceBracketing",
            0x22,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x46, bo) {
        dm.push(("WhiteBalanceSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Preset".to_string(),
            2 => "Custom".to_string(),
            3 => "Color Temperature/Color Filter".to_string(),
            32769 => "Preset".to_string(),
            32770 => "Custom".to_string(),
            32771 => "Color Temperature/Color Filter".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "WhiteBalanceSetting",
            0x23,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x48, bo) {
        dm.push(("PresetWhiteBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "Daylight".to_string(),
            2 => "Cloudy".to_string(),
            3 => "Shade".to_string(),
            4 => "Tungsten".to_string(),
            5 => "Fluorescent".to_string(),
            6 => "Flash".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PresetWhiteBalance",
            0x24,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4a, bo) {
        dm.push((
            "ColorTemperatureSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Temperature".to_string(),
            2 => "Color Filter".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ColorTemperatureSetting",
            0x25,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4c, bo) {
        dm.push(("CustomWBSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Setup".to_string(),
            1 => "Recall".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("CustomWBSetting", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x4e, bo) {
        dm.push((
            "DynamicRangeOptimizerSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Standard".to_string(),
            2 => "Advanced".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DynamicRangeOptimizerSetting",
            0x27,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x64, bo) {
        dm.push(("FreeMemoryCardImages".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FreeMemoryCardImages",
            0x32,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x68, bo) {
        dm.push(("CustomWBRedLevel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "CustomWBRedLevel",
            0x34,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x6a, bo) {
        dm.push(("CustomWBGreenLevel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "CustomWBGreenLevel",
            0x35,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x6c, bo) {
        dm.push(("CustomWBBlueLevel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "CustomWBBlueLevel",
            0x36,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x6e, bo) {
        dm.push(("CustomWBError".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "OK".to_string(),
            1 => "Error".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("CustomWBError", 0x37, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0x70, bo) {
        dm.push(("WhiteBalanceFineTune".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WhiteBalanceFineTune",
            0x38,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x72, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val * 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ColorTemperature",
                0x39,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x74, bo) {
        dm.push((
            "ColorCompensationFilter".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorCompensationFilter",
            0x3a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x76, bo) {
        dm.push(("SonyImageSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Medium".to_string(),
            2 => "Small".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SonyImageSize", 0x3b, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x78, bo) {
        dm.push(("SonyQuality".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "RAW".to_string(),
            32 => "Fine".to_string(),
            34 => "RAW + JPEG".to_string(),
            48 => "Standard".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SonyQuality", 0x3c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x7a, bo) {
        dm.push(("InstantPlaybackTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val s\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "InstantPlaybackTime",
            0x3d,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x7c, bo) {
        dm.push(("InstantPlaybackSetup".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Image and Information".to_string(),
            1 => "Image Only".to_string(),
            3 => "Image and Histogram".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "InstantPlaybackSetup",
            0x3e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x7e, bo) {
        dm.push(("NoiseReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoiseReduction", 0x3f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x80, bo) {
        dm.push(("EyeStartAF".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("EyeStartAF", 0x40, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x82, bo) {
        dm.push(("RedEyeReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RedEyeReduction", 0x41, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x84, bo) {
        dm.push(("FlashDefault".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Fill Flash".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashDefault", 0x42, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x86, bo) {
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0 - +".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketOrder", 0x43, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x88, bo) {
        dm.push(("FocusHoldButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus Hold".to_string(),
            1 => "DOF Preview".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusHoldButton", 0x44, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x8a, bo) {
        dm.push(("AELButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Hold".to_string(),
            1 => "Toggle".to_string(),
            2 => "Spot Hold".to_string(),
            3 => "Spot Toggle".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELButton", 0x45, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x8c, bo) {
        dm.push(("ControlDialSet".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Shutter Speed".to_string(),
            1 => "Aperture".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ControlDialSet", 0x46, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x8e, bo) {
        dm.push((
            "ExposureCompensationMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Ambient and Flash".to_string(),
            1 => "Ambient Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureCompensationMode",
            0x47,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x90, bo) {
        dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssist", 0x48, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x92, bo) {
        dm.push(("CardShutterLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("CardShutterLock", 0x49, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x94, bo) {
        dm.push(("LensShutterLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensShutterLock", 0x4a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x96, bo) {
        dm.push(("AFAreaIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.3 s".to_string(),
            1 => "0.6 s".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFAreaIllumination",
            0x4b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x98, bo) {
        dm.push(("MonitorDisplayOff".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Automatic".to_string(),
            1 => "Manual".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MonitorDisplayOff",
            0x4c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x9a, bo) {
        dm.push(("RecordDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto Rotate".to_string(),
            1 => "Horizontal".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RecordDisplay", 0x4d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x9c, bo) {
        dm.push(("PlayDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto Rotate".to_string(),
            1 => "Manual Rotate".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PlayDisplay", 0x4e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xa0, bo) {
        dm.push(("ExposureIndicator".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Not Indicated".to_string(),
            1 => "Under Scale".to_string(),
            119 => "Bottom of Scale".to_string(),
            120 => "-2.0".to_string(),
            121 => "-1.7".to_string(),
            122 => "-1.5".to_string(),
            123 => "-1.3".to_string(),
            124 => "-1.0".to_string(),
            125 => "-0.7".to_string(),
            126 => "-0.5".to_string(),
            127 => "-0.3".to_string(),
            128 => "0".to_string(),
            129 => "+0.3".to_string(),
            130 => "+0.5".to_string(),
            131 => "+0.7".to_string(),
            132 => "+1.0".to_string(),
            133 => "+1.3".to_string(),
            134 => "+1.5".to_string(),
            135 => "+1.7".to_string(),
            136 => "+2.0".to_string(),
            253 => "Top of Scale".to_string(),
            254 => "Over Scale".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureIndicator",
            0x50,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xa2, bo) {
        dm.push(("AELExposureIndicator".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Not Indicated".to_string(),
            1 => "Under Scale".to_string(),
            119 => "Bottom of Scale".to_string(),
            120 => "-2.0".to_string(),
            121 => "-1.7".to_string(),
            122 => "-1.5".to_string(),
            123 => "-1.3".to_string(),
            124 => "-1.0".to_string(),
            125 => "-0.7".to_string(),
            126 => "-0.5".to_string(),
            127 => "-0.3".to_string(),
            128 => "0".to_string(),
            129 => "+0.3".to_string(),
            130 => "+0.5".to_string(),
            131 => "+0.7".to_string(),
            132 => "+1.0".to_string(),
            133 => "+1.3".to_string(),
            134 => "+1.5".to_string(),
            135 => "+1.7".to_string(),
            136 => "+2.0".to_string(),
            253 => "Top of Scale".to_string(),
            254 => "Over Scale".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AELExposureIndicator",
            0x51,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xa4, bo) {
        dm.push((
            "ExposureBracketingIndicatorLast".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Not Indicated".to_string(),
            1 => "Under Scale".to_string(),
            119 => "Bottom of Scale".to_string(),
            120 => "-2.0".to_string(),
            121 => "-1.7".to_string(),
            122 => "-1.5".to_string(),
            123 => "-1.3".to_string(),
            124 => "-1.0".to_string(),
            125 => "-0.7".to_string(),
            126 => "-0.5".to_string(),
            127 => "-0.3".to_string(),
            128 => "0".to_string(),
            129 => "+0.3".to_string(),
            130 => "+0.5".to_string(),
            131 => "+0.7".to_string(),
            132 => "+1.0".to_string(),
            133 => "+1.3".to_string(),
            134 => "+1.5".to_string(),
            135 => "+1.7".to_string(),
            136 => "+2.0".to_string(),
            253 => "Top of Scale".to_string(),
            254 => "Over Scale".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureBracketingIndicatorLast",
            0x52,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xa6, bo) {
        dm.push((
            "MeteringOffScaleIndicator".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Within Range".to_string(),
            1 => "Under/Over Range".to_string(),
            255 => "Out of Range".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MeteringOffScaleIndicator",
            0x53,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xa8, bo) {
        dm.push((
            "FlashExposureIndicator".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Not Indicated".to_string(),
            1 => "Under Scale".to_string(),
            119 => "Bottom of Scale".to_string(),
            120 => "-2.0".to_string(),
            121 => "-1.7".to_string(),
            122 => "-1.5".to_string(),
            123 => "-1.3".to_string(),
            124 => "-1.0".to_string(),
            125 => "-0.7".to_string(),
            126 => "-0.5".to_string(),
            127 => "-0.3".to_string(),
            128 => "0".to_string(),
            129 => "+0.3".to_string(),
            130 => "+0.5".to_string(),
            131 => "+0.7".to_string(),
            132 => "+1.0".to_string(),
            133 => "+1.3".to_string(),
            134 => "+1.5".to_string(),
            135 => "+1.7".to_string(),
            136 => "+2.0".to_string(),
            253 => "Top of Scale".to_string(),
            254 => "Over Scale".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashExposureIndicator",
            0x54,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xaa, bo) {
        dm.push((
            "FlashExposureIndicatorNext".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Not Indicated".to_string(),
            1 => "Under Scale".to_string(),
            119 => "Bottom of Scale".to_string(),
            120 => "-2.0".to_string(),
            121 => "-1.7".to_string(),
            122 => "-1.5".to_string(),
            123 => "-1.3".to_string(),
            124 => "-1.0".to_string(),
            125 => "-0.7".to_string(),
            126 => "-0.5".to_string(),
            127 => "-0.3".to_string(),
            128 => "0".to_string(),
            129 => "+0.3".to_string(),
            130 => "+0.5".to_string(),
            131 => "+0.7".to_string(),
            132 => "+1.0".to_string(),
            133 => "+1.3".to_string(),
            134 => "+1.5".to_string(),
            135 => "+1.7".to_string(),
            136 => "+2.0".to_string(),
            253 => "Top of Scale".to_string(),
            254 => "Over Scale".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashExposureIndicatorNext",
            0x55,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xac, bo) {
        dm.push((
            "FlashExposureIndicatorLast".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Not Indicated".to_string(),
            1 => "Under Scale".to_string(),
            119 => "Bottom of Scale".to_string(),
            120 => "-2.0".to_string(),
            121 => "-1.7".to_string(),
            122 => "-1.5".to_string(),
            123 => "-1.3".to_string(),
            124 => "-1.0".to_string(),
            125 => "-0.7".to_string(),
            126 => "-0.5".to_string(),
            127 => "-0.3".to_string(),
            128 => "0".to_string(),
            129 => "+0.3".to_string(),
            130 => "+0.5".to_string(),
            131 => "+0.7".to_string(),
            132 => "+1.0".to_string(),
            133 => "+1.3".to_string(),
            134 => "+1.5".to_string(),
            135 => "+1.7".to_string(),
            136 => "+2.0".to_string(),
            253 => "Top of Scale".to_string(),
            254 => "Over Scale".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashExposureIndicatorLast",
            0x56,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xae, bo) {
        dm.push(("ImageStabilization".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageStabilization",
            0x57,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xb0, bo) {
        dm.push(("FocusModeSwitch".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AF".to_string(),
            1 => "MF".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusModeSwitch", 0x58, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xb2, bo) {
        dm.push(("FlashType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Built-in".to_string(),
            2 => "External".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashType", 0x59, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xb4, bo) {
        dm.push(("Rotation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal (normal)".to_string(),
            1 => "Rotate 270 CW".to_string(),
            2 => "Rotate 90 CW".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Rotation", 0x5a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xb6, bo) {
        dm.push(("AELock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELock", 0x5b, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0xbc, bo) {
        dm.push(("ColorTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val * 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ColorTemperature",
                0x5e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xbe, bo) {
        dm.push((
            "ColorCompensationFilter".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorCompensationFilter",
            0x5f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xc0, bo) {
        dm.push(("BatteryState".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "Very Low".to_string(),
            4 => "Low".to_string(),
            5 => "Half Full".to_string(),
            6 => "Sufficient Power Remaining".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("BatteryState", 0x60, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Minolta::ISInfoA100` -- FORMAT int8u, FIRST_ENTRY 0.
fn minolta_isinfoa100(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Minolta";
    const GRP2: &str = "Camera";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("ImageStabilization".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            10116 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageStabilization",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::LensDataUnknown` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_lensdataunknown(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "LensDataVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD40` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod40(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x246, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x246,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x24a) {
        let v = (v & 0x8) >> 3;
        dm.push(("VibrationReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VibrationReduction",
            0x24a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x2d9..0x2d9 + 12) {
        tags.extend(nikoncustom_settingsd40(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD80` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod80(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x24a, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x24a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x24e) {
        let v = v & 0x7;
        dm.push(("Rotation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal".to_string(),
            1 => "Rotate 270 CW".to_string(),
            2 => "Rotate 90 CW".to_string(),
            3 => "Rotate 180".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Rotation", 0x24e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x24e) {
        let v = (v & 0x18) >> 3;
        dm.push(("VibrationReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            3 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VibrationReduction",
            0x24e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x24e) {
        let v = (v & 0xe0) >> 5;
        dm.push(("FlashFired".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "Image::ExifTool::DecodeBits($val, { 1 => 'Internal', 2 => 'External' })",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "FlashFired",
            0x24e,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2c4) {
        let v = (v & 0xf0) >> 4;
        dm.push(("NikonImageSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Large (10.0 M)".to_string(),
            1 => "Medium (5.6 M)".to_string(),
            2 => "Small (2.5 M)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NikonImageSize", 0x2c4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2c4) {
        let v = v & 0xf;
        dm.push(("ImageQuality".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "NEF (RAW)".to_string(),
            1 => "JPEG Fine".to_string(),
            2 => "JPEG Normal".to_string(),
            3 => "JPEG Basic".to_string(),
            4 => "NEF (RAW) + JPEG Fine".to_string(),
            5 => "NEF (RAW) + JPEG Normal".to_string(),
            6 => "NEF (RAW) + JPEG Basic".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageQuality", 0x2c4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(sub) = data.get(0x2ec..0x2ec + 17) {
        tags.extend(nikoncustom_settingsd80(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD90` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod90(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b5) {
        dm.push(("ISO2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/12-5)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val + 0.5)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ISO2",
                0x2b5,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x2d5, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x2d5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x374..0x374 + 36) {
        tags.extend(nikoncustom_settingsd90(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD3a` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod3a(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x256) {
        dm.push(("ISO2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/12-5)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val + 0.5)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ISO2",
                0x256,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x276, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x276,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2d3) {
        let v = (v & 0x18) >> 3;
        dm.push(("NikonImageSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Large".to_string(),
            1 => "Medium".to_string(),
            2 => "Small".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NikonImageSize", 0x2d3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2d3) {
        let v = v & 0x7;
        dm.push(("ImageQuality".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "NEF (RAW) + JPEG Fine".to_string(),
            1 => "NEF (RAW) + JPEG Norm".to_string(),
            2 => "NEF (RAW) + JPEG Basic".to_string(),
            3 => "NEF (RAW)".to_string(),
            4 => "TIF (RGB)".to_string(),
            5 => "JPEG Fine".to_string(),
            6 => "JPEG Normal".to_string(),
            7 => "JPEG Basic".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageQuality", 0x2d3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(sub) = data.get(0x301..0x301 + 24) {
        tags.extend(nikoncustom_settingsd3(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD3b` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod3b(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x10) {
        dm.push(("ImageArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "FX (36x24)".to_string(),
            1 => "DX (24x16)".to_string(),
            2 => "5:4 (30x24)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageArea", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x25d) {
        dm.push(("ISO2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/12-5)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val + 0.5)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ISO2",
                0x25d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x28a) {
        dm.push((
            "PreFlashReturnStrength".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreFlashReturnStrength",
            0x28a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2dc) {
        let v = (v & 0x18) >> 3;
        dm.push(("NikonImageSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Large".to_string(),
            1 => "Medium".to_string(),
            2 => "Small".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NikonImageSize", 0x2dc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2dc) {
        let v = v & 0x7;
        dm.push(("ImageQuality".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "NEF (RAW) + JPEG Fine".to_string(),
            1 => "NEF (RAW) + JPEG Norm".to_string(),
            2 => "NEF (RAW) + JPEG Basic".to_string(),
            3 => "NEF (RAW)".to_string(),
            4 => "TIF (RGB)".to_string(),
            5 => "JPEG Fine".to_string(),
            6 => "JPEG Normal".to_string(),
            7 => "JPEG Basic".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageQuality", 0x2dc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(sub) = data.get(0x30a..0x30a + 24) {
        tags.extend(nikoncustom_settingsd3(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD3X` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod3x(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x25d) {
        dm.push(("ISO2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/12-5)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val + 0.5)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ISO2",
                0x25d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x280, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x280,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x30b..0x30b + 24) {
        tags.extend(nikoncustom_settingsd3(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD3S` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod3s(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        dm.push(("ImageArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "FX (36x24)".to_string(),
            1 => "DX (24x16)".to_string(),
            2 => "5:4 (30x24)".to_string(),
            3 => "1.2x (30x20)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageArea", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x221) {
        dm.push(("ISO2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/12-5)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val + 0.5)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ISO2",
                0x221,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x242, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x242,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x29f) {
        let v = (v & 0x40) >> 6;
        dm.push(("JPGCompression".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Size Priority".to_string(),
            1 => "Optimal Quality".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("JPGCompression", 0x29f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(sub) = data.get(0x2ce..0x2ce + 27) {
        tags.extend(nikoncustom_settingsd3(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD300a` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod300a(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x25c) {
        dm.push(("ISO2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/12-5)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val + 0.5)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ISO2",
                0x25c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x279, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x279,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2d1, bo) {
        dm.push(("AFFineTuneAdj".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0".to_string(),
            58 => "+1".to_string(),
            59 => "+2".to_string(),
            60 => "+4".to_string(),
            61 => "+8".to_string(),
            62 => "+16".to_string(),
            194 => "-16".to_string(),
            195 => "-8".to_string(),
            196 => "-4".to_string(),
            197 => "-2".to_string(),
            198 => "-1".to_string(),
            4158 => "+17".to_string(),
            4290 => "-17".to_string(),
            8253 => "+9".to_string(),
            8254 => "+18".to_string(),
            8386 => "-18".to_string(),
            8387 => "-9".to_string(),
            12350 => "+19".to_string(),
            12482 => "-19".to_string(),
            16444 => "+5".to_string(),
            16445 => "+10".to_string(),
            16446 => "+20".to_string(),
            16578 => "-20".to_string(),
            16579 => "-10".to_string(),
            16580 => "-5".to_string(),
            24637 => "+11".to_string(),
            24771 => "-11".to_string(),
            32827 => "+3".to_string(),
            32828 => "+6".to_string(),
            32829 => "+12".to_string(),
            32963 => "-12".to_string(),
            32964 => "-6".to_string(),
            32965 => "-3".to_string(),
            41021 => "+13".to_string(),
            41155 => "-13".to_string(),
            49212 => "+7".to_string(),
            49213 => "+14".to_string(),
            49347 => "-14".to_string(),
            49348 => "-7".to_string(),
            57405 => "+15".to_string(),
            57539 => "-15".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFFineTuneAdj", 0x2d1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(sub) = data.get(0x316..0x316 + 24) {
        tags.extend(nikoncustom_settingsd3(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD300b` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod300b(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x265) {
        dm.push(("ISO2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/12-5)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val + 0.5)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ISO2",
                0x265,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x284, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x284,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() == "1.10B") {
        if let Some(v) = u16_at(data, 0x2dc, bo) {
            dm.push(("AFFineTuneAdj".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "0".to_string(),
                2110 => "+11".to_string(),
                2242 => "-11".to_string(),
                8252 => "+3".to_string(),
                8253 => "+6".to_string(),
                8254 => "+12".to_string(),
                8386 => "-12".to_string(),
                8387 => "-6".to_string(),
                8388 => "-3".to_string(),
                14398 => "+13".to_string(),
                14530 => "-13".to_string(),
                20541 => "+7".to_string(),
                20542 => "+14".to_string(),
                20674 => "-14".to_string(),
                20675 => "-7".to_string(),
                26686 => "+15".to_string(),
                26818 => "-15".to_string(),
                32826 => "+1".to_string(),
                32827 => "+2".to_string(),
                32828 => "+4".to_string(),
                32829 => "+8".to_string(),
                32830 => "+16".to_string(),
                32962 => "-16".to_string(),
                32963 => "-8".to_string(),
                32964 => "-4".to_string(),
                32965 => "-2".to_string(),
                32966 => "-1".to_string(),
                38974 => "+17".to_string(),
                39106 => "-17".to_string(),
                45117 => "+9".to_string(),
                45118 => "+18".to_string(),
                45250 => "-18".to_string(),
                45251 => "-9".to_string(),
                51262 => "+19".to_string(),
                51394 => "-19".to_string(),
                57404 => "+5".to_string(),
                57405 => "+10".to_string(),
                57406 => "+20".to_string(),
                57538 => "-20".to_string(),
                57539 => "-10".to_string(),
                57540 => "-5".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFFineTuneAdj", 0x2dc, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() == "1.10B")) {
        if let Some(v) = u16_at(data, 0x2dc, bo) {
            dm.push(("AFFineTuneAdj".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "0".to_string(),
                1086 => "+13".to_string(),
                1218 => "-13".to_string(),
                6205 => "+7".to_string(),
                6206 => "+14".to_string(),
                6338 => "-14".to_string(),
                6339 => "-7".to_string(),
                11326 => "+15".to_string(),
                11458 => "-15".to_string(),
                16442 => "+1".to_string(),
                16443 => "+2".to_string(),
                16444 => "+4".to_string(),
                16445 => "+8".to_string(),
                16446 => "+16".to_string(),
                16578 => "-16".to_string(),
                16579 => "-8".to_string(),
                16580 => "-4".to_string(),
                16581 => "-2".to_string(),
                16582 => "-1".to_string(),
                21566 => "+17".to_string(),
                21698 => "-17".to_string(),
                26685 => "+9".to_string(),
                26686 => "+18".to_string(),
                26818 => "-18".to_string(),
                26819 => "-9".to_string(),
                31806 => "+19".to_string(),
                31938 => "-19".to_string(),
                36924 => "+5".to_string(),
                36925 => "+10".to_string(),
                36926 => "+20".to_string(),
                37058 => "-20".to_string(),
                37059 => "-10".to_string(),
                37060 => "-5".to_string(),
                47165 => "+11".to_string(),
                47299 => "-11".to_string(),
                57403 => "+3".to_string(),
                57404 => "+6".to_string(),
                57405 => "+12".to_string(),
                57539 => "-12".to_string(),
                57540 => "-6".to_string(),
                57541 => "-3".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFFineTuneAdj", 0x2dc, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(sub) = data.get(0x322..0x322 + 24) {
        tags.extend(nikoncustom_settingsd3(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD300S` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod300s(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x265) {
        dm.push(("ISO2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/12-5)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val + 0.5)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ISO2",
                0x265,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x286, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x286,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x324..0x324 + 24) {
        tags.extend(nikoncustom_settingsd3(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD700` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod700(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x265) {
        dm.push(("ISO2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/12-5)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val + 0.5)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ISO2",
                0x265,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x287, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x287,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x324..0x324 + 48) {
        tags.extend(nikoncustom_settingsd700(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD780` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod780(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = u32_at(data, 0x9c, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_orientationinfo(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD7500` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod7500(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = u32_at(data, 0xa0, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_orientationinfo(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD800` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod800(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4c0) {
        dm.push((
            "RepeatingFlashOutputExternal".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val/6)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "RepeatingFlashOutputExternal",
                0x4c0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x4c2) {
        dm.push((
            "RepeatingFlashRateExternal".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val Hz\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "RepeatingFlashRateExternal",
                0x4c2,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x4c3) {
        dm.push((
            "RepeatingFlashCountExternal".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "RepeatingFlashCountExternal",
                0x4c3,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x4d2) {
        dm.push(("FlashExposureComp2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("-$val/6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FlashExposureComp2",
                0x4d2,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x4da) {
        dm.push((
            "RepeatingFlashRateBuilt-in".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val Hz\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "RepeatingFlashRateBuilt-in",
                0x4da,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x4db) {
        dm.push((
            "RepeatingFlashCountBuilt-in".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val || undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "RepeatingFlashCountBuilt-in",
                0x4db,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x51c) {
        dm.push(("SequenceNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SequenceNumber",
            0x51c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x5fb, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x5fb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x6ec..0x6ec + 48) {
        tags.extend(nikoncustom_settingsd800(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD810` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod810(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = u32_at(data, 0x10, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_settingsinfod810(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0x24, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_bracketinginfod810(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0x38, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_isoautoinfod810(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0x40, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikoncustom_settingsd810(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0x84, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_orientationinfo(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD850` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod850(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = u32_at(data, 0x10, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_menusettingsd850(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0x4c, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_moresettingsd850(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0x58, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikoncustom_settingsd850(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0xa0, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_orientationinfo(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD5000` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod5000(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b5) {
        dm.push(("ISO2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("100*exp(($val/12-5)*log(2))", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val + 0.5)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ISO2",
                0x2b5,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x2d6, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x2d6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x378..0x378 + 34) {
        tags.extend(nikoncustom_settingsd5000(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD5100` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod5100(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x321, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x321,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x407..0x407 + 34) {
        tags.extend(nikoncustom_settingsd5100(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD5200` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod5200(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xbd8, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0xbd8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0xcd5..0xcd5 + 34) {
        tags.extend(nikoncustom_settingsd5200(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD7000` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod7000(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x320, bo) {
        dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShutterCount",
            0x320,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x404..0x404 + 48) {
        tags.extend(nikoncustom_settingsd7000(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD4` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod4(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x751..0x751 + 56) {
        tags.extend(nikoncustom_settingsd4(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD4S` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod4s(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1d0) {
        let v = v & 0x3;
        dm.push(("SecondarySlotFunction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Overflow".to_string(),
            2 => "Backup".to_string(),
            3 => "NEF Primary + JPG Secondary".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SecondarySlotFunction",
            0x1d0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x174c) {
        let v = v & 0xff;
        dm.push(("AEBracketingSteps".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE Bracketing Disabled".to_string(),
            32 => "AE Bracketing Disabled".to_string(),
            48 => "AE Bracketing Disabled".to_string(),
            64 => "AE Bracketing Disabled".to_string(),
            80 => "AE Bracketing Disabled".to_string(),
            129 => "+3F0.3".to_string(),
            130 => "-3F0.3".to_string(),
            131 => "+2F0.3".to_string(),
            132 => "-2F0.3".to_string(),
            133 => "3F0.3".to_string(),
            134 => "5F0.3".to_string(),
            135 => "7F0.3".to_string(),
            136 => "9F0.3".to_string(),
            145 => "+3F0.5".to_string(),
            146 => "-3F0.5".to_string(),
            147 => "+2F0.5".to_string(),
            148 => "-2F0.5".to_string(),
            149 => "3F0.5".to_string(),
            150 => "5F0.5".to_string(),
            151 => "7F0.5".to_string(),
            152 => "9F0.5".to_string(),
            161 => "+3F0.7".to_string(),
            162 => "-3F0.7".to_string(),
            163 => "+2F0.7".to_string(),
            164 => "-2F0.7".to_string(),
            165 => "3F0.7".to_string(),
            166 => "5F0.7".to_string(),
            167 => "7F0.7".to_string(),
            168 => "9F0.7".to_string(),
            177 => "+3F1".to_string(),
            178 => "-3F1".to_string(),
            179 => "+2F1".to_string(),
            180 => "-2F1".to_string(),
            181 => "3F1".to_string(),
            182 => "5F1".to_string(),
            183 => "7F1".to_string(),
            184 => "9F1".to_string(),
            193 => "+3F2".to_string(),
            194 => "-3F2".to_string(),
            195 => "+2F2".to_string(),
            196 => "-2F2".to_string(),
            197 => "3F2".to_string(),
            198 => "5F2".to_string(),
            209 => "+3F3".to_string(),
            210 => "-3F3".to_string(),
            211 => "+2F3".to_string(),
            212 => "-2F3".to_string(),
            213 => "3F3".to_string(),
            214 => "5F3".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AEBracketingSteps",
            0x174c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_val(dm, "FILE_TYPE").is_some_and(|v| v.as_string().as_str() != "TIFF") {
        if let Some(v) = u8_at(data, 0x174d) {
            let v = v & 0xff;
            dm.push(("WBBracketingSteps".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "WB Bracketing Disabled".to_string(),
                1 => "b3F 1".to_string(),
                2 => "A3F 1".to_string(),
                3 => "b2F 1".to_string(),
                4 => "A2F 1".to_string(),
                5 => "3F 1".to_string(),
                6 => "5F 1".to_string(),
                7 => "7F 1".to_string(),
                8 => "9F 1".to_string(),
                16 => "0F 2".to_string(),
                17 => "b3F 2".to_string(),
                18 => "A3F 2".to_string(),
                19 => "b2F 2".to_string(),
                20 => "A2F 2".to_string(),
                21 => "3F 2".to_string(),
                22 => "5F 2".to_string(),
                23 => "7F 2".to_string(),
                24 => "9F 2".to_string(),
                32 => "0F 3".to_string(),
                33 => "b3F 3".to_string(),
                34 => "A3F 3".to_string(),
                35 => "b2F 3".to_string(),
                36 => "A2F 3".to_string(),
                37 => "3F 3".to_string(),
                38 => "5F 3".to_string(),
                39 => "7F 3".to_string(),
                40 => "9F 3".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "WBBracketingSteps",
                0x174d,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x184d) {
        let v = v & 0xff;
        dm.push(("ReleaseMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Single Frame".to_string(),
            1 => "Continuous High Speed".to_string(),
            3 => "Continuous Low Speed".to_string(),
            4 => "Timer".to_string(),
            32 => "Mirror-Up".to_string(),
            64 => "Quiet".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReleaseMode", 0x184d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x18ea) {
        let v = v & 0x3f;
        dm.push(("ISOAutoShutterTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/4000 s".to_string(),
            1 => "1/3200 s".to_string(),
            2 => "1/2500 s".to_string(),
            3 => "1/2000 s".to_string(),
            4 => "1/1600 s".to_string(),
            5 => "1/1250 s".to_string(),
            6 => "1/1000 s".to_string(),
            7 => "1/800 s".to_string(),
            8 => "1/640 s".to_string(),
            9 => "1/500 s".to_string(),
            10 => "1/400 s".to_string(),
            11 => "1/320 s".to_string(),
            12 => "1/250 s".to_string(),
            13 => "1/200 s".to_string(),
            14 => "1/160 s".to_string(),
            15 => "1/125 s".to_string(),
            16 => "1/100 s".to_string(),
            17 => "1/80 s".to_string(),
            18 => "1/60 s".to_string(),
            19 => "1/50 s".to_string(),
            20 => "1/40 s".to_string(),
            21 => "1/30 s".to_string(),
            22 => "1/15 s".to_string(),
            23 => "1/8 s".to_string(),
            24 => "1/4 s".to_string(),
            25 => "1/2 s".to_string(),
            26 => "1 s".to_string(),
            27 => "2 s".to_string(),
            28 => "4 s".to_string(),
            29 => "8 s".to_string(),
            30 => "15 s".to_string(),
            31 => "30 s".to_string(),
            32 => "Auto (Slowest)".to_string(),
            33 => "Auto (Slower)".to_string(),
            34 => "Auto".to_string(),
            35 => "Auto (Faster)".to_string(),
            36 => "Auto (Fastest)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ISOAutoShutterTime",
            0x18ea,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x18eb) {
        let v = v & 0xff;
        dm.push(("ISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            36 => "ISO 200".to_string(),
            38 => "ISO 250".to_string(),
            39 => "ISO 280".to_string(),
            40 => "ISO 320".to_string(),
            42 => "ISO 400".to_string(),
            44 => "ISO 500".to_string(),
            45 => "ISO 560".to_string(),
            46 => "ISO 640".to_string(),
            48 => "ISO 800".to_string(),
            50 => "ISO 1000".to_string(),
            51 => "ISO 1100".to_string(),
            52 => "ISO 1250".to_string(),
            54 => "ISO 1600".to_string(),
            56 => "ISO 2000".to_string(),
            57 => "ISO 2200".to_string(),
            58 => "ISO 2500".to_string(),
            60 => "ISO 3200".to_string(),
            62 => "ISO 4000".to_string(),
            63 => "ISO 4500".to_string(),
            64 => "ISO 5000".to_string(),
            66 => "ISO 6400".to_string(),
            68 => "ISO 8000".to_string(),
            69 => "ISO 9000".to_string(),
            70 => "ISO 10000".to_string(),
            72 => "ISO 12800".to_string(),
            74 => "ISO 16000".to_string(),
            75 => "ISO 18000".to_string(),
            76 => "ISO 20000".to_string(),
            78 => "ISO 25600".to_string(),
            80 => "ISO 32000".to_string(),
            81 => "ISO 36000".to_string(),
            82 => "ISO 40000".to_string(),
            84 => "ISO 51200".to_string(),
            86 => "ISO Hi 0.3".to_string(),
            87 => "ISO Hi 0.5".to_string(),
            88 => "ISO Hi 0.7".to_string(),
            90 => "ISO Hi 1.0".to_string(),
            96 => "ISO Hi 2.0".to_string(),
            102 => "ISO Hi 3.0".to_string(),
            108 => "ISO Hi 4.0".to_string(),
            114 => "ISO Hi 5.0".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOAutoHiLimit", 0x18eb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3693) {
        let v = (v & 0x30) >> 4;
        dm.push(("Rotation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal".to_string(),
            1 => "Rotate 270 CW".to_string(),
            2 => "Rotate 90 CW".to_string(),
            3 => "Rotate 180".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Rotation", 0x3693, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(sub) = data.get(0x350b..0x350b + 12) {
        tags.extend(nikon_orientationinfo(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD500` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod500(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(sub) = u32_at(data, 0x10, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_rotationinfod500(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0x14, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_jpginfod500(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0x2c, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_bracketinginfod500(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0x50, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_shootingmenud500(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0x58, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_customsettingsd500(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0xa0, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_orientationinfo(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0xa8, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_otherinfod500(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD6` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod6(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 8, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x24, bo) {
        dm.push(("NumberOffsets".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "NumberOffsets",
            0x24,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = u32_at(data, 0x30, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_seqinfod6(sub, make, model, bo, file_type, format, dm));
    }
    if let Some(sub) = u32_at(data, 0x9c, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_orientationinfo(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0xa4, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_intervalinfod6(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoD610` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfod610(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        tags.push(mk(
            "FirmwareVersion",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x7cf..0x7cf + 48) {
        tags.extend(nikoncustom_settingsd610(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoZ6III` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfoz6iii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 8, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0xe, 8, true) {
        tags.push(mk(
            "FirmwareVersion2",
            0xe,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x18, 8, true) {
        tags.push(mk(
            "FirmwareVersion3",
            0x18,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x24, bo) {
        dm.push(("NumberOffsets".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "NumberOffsets",
            0x24,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96"))
    {
        if let Some(sub) = u32_at(data, 0x88, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_orientationinfo(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if (MODEL_RE_16.is_match(model)
        && (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
            && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() < "02.00")))
    {
        if let Some(sub) = u32_at(data, 0x90, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_menusettingsz6iii(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoZ7II` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfoz7ii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 8, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0xe, 8, true) {
        tags.push(mk(
            "FirmwareVersion2",
            0xe,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x18, 8, true) {
        tags.push(mk(
            "FirmwareVersion3",
            0x18,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x24, bo) {
        dm.push(("NumberOffsets".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "NumberOffsets",
            0x24,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = u32_at(data, 0x30, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_intervalinfoz7ii(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = u32_at(data, 0x38, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_portraitinfoz7ii(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if MODEL_RE_17.is_match(model) {
        if let Some(sub) = u32_at(data, 0x88, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_orientationinfo(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if MODEL_RE_18.is_match(model) {
        if let Some(sub) = u32_at(data, 0x98, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_orientationinfo(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if let Some(sub) = u32_at(data, 0xa0, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_menuinfoz7ii(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoZ8` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfoz8(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 8, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0xe, 8, true) {
        tags.push(mk(
            "FirmwareVersion2",
            0xe,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x18, 8, true) {
        tags.push(mk(
            "FirmwareVersion3",
            0x18,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x24, bo) {
        dm.push(("NumberOffsets".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "NumberOffsets",
            0x24,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = u32_at(data, 0x30, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_seqinfoz9(sub, make, model, bo, file_type, format, dm));
    }
    if (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() >= "02.00"))
    {
        if let Some(sub) = u32_at(data, 0x80, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_autocaptureinfo(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96"))
    {
        if let Some(sub) = u32_at(data, 0x84, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_orientationinfo(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if let Some(sub) = u32_at(data, 0x8c, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_menuinfoz8(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfoZ9` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfoz9(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "ShotInfoVersion",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x4, 8, true) {
        dm.push(("FirmwareVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0xe, 8, true) {
        tags.push(mk(
            "FirmwareVersion2",
            0xe,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x18, 8, true) {
        tags.push(mk(
            "FirmwareVersion3",
            0x18,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x24, bo) {
        dm.push(("NumberOffsets".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "NumberOffsets",
            0x24,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = u32_at(data, 0x30, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_seqinfoz9(sub, make, model, bo, file_type, format, dm));
    }
    if (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() >= "03.01"))
    {
        if let Some(sub) = u32_at(data, 0x58, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_offset13infoz9(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() >= "04.00"))
    {
        if let Some(sub) = u32_at(data, 0x80, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_autocaptureinfo(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96"))
    {
        if let Some(sub) = u32_at(data, 0x84, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_orientationinfo(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if let Some(sub) = u32_at(data, 0x8c, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_menuinfoz9(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShotInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shotinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        dm.push(("ShotInfoVersion".to_string(), Conv::Str(text.clone())));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val; $val =~ /^\\d+$/ ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "ShotInfoVersion",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x4, 5, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val =~ /^\\d\\.\\d+.$/ ? $val : undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_19.is_match(model) {
        if let Some(v) = u8_at(data, 0x10) {
            dm.push(("DistortionControl".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "DistortionControl",
                0x10,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "ShotInfoVersion").is_some_and(|v| v.as_string().as_str() == "0204") {
        if let Some(v) = u8_at(data, 0x66) {
            dm.push(("VR_0x66".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if dm_val(dm, "ShotInfoVersion").is_some_and(|v| v.as_string().as_str() == "0204") {
        if let Some(v) = u32_at(data, 0x6a, bo) {
            dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ShutterCount",
                0x6a,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "ShotInfoVersion").is_some_and(|v| v.as_string().as_str() == "0204") {
        if let Some(v) = u32_at(data, 0x6e, bo) {
            dm.push(("DeletedImageCount".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "DeletedImageCount",
                0x6e,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "ShotInfoVersion").is_some_and(|v| v.as_string().as_str() == "0207") {
        if let Some(v) = u8_at(data, 0x75) {
            dm.push(("VibrationReduction".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On (1)".to_string(),
                2 => "On (2)".to_string(),
                3 => "On (3)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "VibrationReduction",
                0x75,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "ShotInfoVersion").is_some_and(|v| v.as_string().as_str() == "0204") {
        if let Some(v) = u8_at(data, 0x82) {
            dm.push(("VibrationReduction".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "VibrationReduction",
                0x82,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "ShotInfoVersion").is_some_and(|v| v.as_string().as_str() == "0205") {
        if let Some(text) = text_at(data, 0x157, 2, false) {
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(text);
            if let Some(x) = conv_expr::eval_with("unpack(\"n\", $val)", &cv, &ctx) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "ShutterCount",
                0x157,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "ShotInfoVersion").is_some_and(|v| v.as_string().as_str() == "0205") {
        if let Some(v) = u8_at(data, 0x1ae) {
            dm.push(("VibrationReduction".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "n/a".to_string(),
                12 => "Off".to_string(),
                15 => "On".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "VibrationReduction",
                0x1ae,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "ShotInfoVersion").is_some_and(|v| v.as_string().as_str() == "0211") {
        if let Some(v) = u32_at(data, 0x24d, bo) {
            dm.push(("ShutterCount".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ShutterCount",
                0x24d,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::OrientationInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_orientationinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = fix32u_at(data, 0x0, bo) {
        dm.push(("RollAngle".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val <= 180 ? $val : $val - 360", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\", $val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "RollAngle",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = fix32u_at(data, 0x4, bo) {
        dm.push(("PitchAngle".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val <= 180 ? $val : $val - 360", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\", $val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "PitchAngle",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = fix32u_at(data, 0x8, bo) {
        dm.push(("YawAngle".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val <= 180 ? $val : $val - 360", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\", $val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "YawAngle",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::SettingsInfoD810` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_settingsinfod810(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x13c) {
        let v = v & 0x3;
        dm.push(("SecondarySlotFunction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Overflow".to_string(),
            2 => "Backup".to_string(),
            3 => "NEF Primary + JPG Secondary".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SecondarySlotFunction",
            0x13c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::BracketingInfoD810` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_bracketinginfod810(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0xf) {
        let v = v & 0xff;
        dm.push(("AEBracketingSteps".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE Bracketing Disabled".to_string(),
            32 => "AE Bracketing Disabled".to_string(),
            48 => "AE Bracketing Disabled".to_string(),
            64 => "AE Bracketing Disabled".to_string(),
            80 => "AE Bracketing Disabled".to_string(),
            129 => "+3F0.3".to_string(),
            130 => "-3F0.3".to_string(),
            131 => "+2F0.3".to_string(),
            132 => "-2F0.3".to_string(),
            133 => "3F0.3".to_string(),
            134 => "5F0.3".to_string(),
            135 => "7F0.3".to_string(),
            136 => "9F0.3".to_string(),
            145 => "+3F0.5".to_string(),
            146 => "-3F0.5".to_string(),
            147 => "+2F0.5".to_string(),
            148 => "-2F0.5".to_string(),
            149 => "3F0.5".to_string(),
            150 => "5F0.5".to_string(),
            151 => "7F0.5".to_string(),
            152 => "9F0.5".to_string(),
            161 => "+3F0.7".to_string(),
            162 => "-3F0.7".to_string(),
            163 => "+2F0.7".to_string(),
            164 => "-2F0.7".to_string(),
            165 => "3F0.7".to_string(),
            166 => "5F0.7".to_string(),
            167 => "7F0.7".to_string(),
            168 => "9F0.7".to_string(),
            177 => "+3F1".to_string(),
            178 => "-3F1".to_string(),
            179 => "+2F1".to_string(),
            180 => "-2F1".to_string(),
            181 => "3F1".to_string(),
            182 => "5F1".to_string(),
            183 => "7F1".to_string(),
            184 => "9F1".to_string(),
            193 => "+3F2".to_string(),
            194 => "-3F2".to_string(),
            195 => "+2F2".to_string(),
            196 => "-2F2".to_string(),
            197 => "3F2".to_string(),
            198 => "5F2".to_string(),
            209 => "+3F3".to_string(),
            210 => "-3F3".to_string(),
            211 => "+2F3".to_string(),
            212 => "-2F3".to_string(),
            213 => "3F3".to_string(),
            214 => "5F3".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AEBracketingSteps", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if dm_val(dm, "FILE_TYPE").is_some_and(|v| v.as_string().as_str() != "TIFF") {
        if let Some(v) = u8_at(data, 0x10) {
            let v = v & 0xff;
            dm.push(("WBBracketingSteps".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "WB Bracketing Disabled".to_string(),
                1 => "b3F 1".to_string(),
                2 => "A3F 1".to_string(),
                3 => "b2F 1".to_string(),
                4 => "A2F 1".to_string(),
                5 => "3F 1".to_string(),
                6 => "5F 1".to_string(),
                7 => "7F 1".to_string(),
                8 => "9F 1".to_string(),
                16 => "0F 2".to_string(),
                17 => "b3F 2".to_string(),
                18 => "A3F 2".to_string(),
                19 => "b2F 2".to_string(),
                20 => "A2F 2".to_string(),
                21 => "3F 2".to_string(),
                22 => "5F 2".to_string(),
                23 => "7F 2".to_string(),
                24 => "9F 2".to_string(),
                32 => "0F 3".to_string(),
                33 => "b3F 3".to_string(),
                34 => "A3F 3".to_string(),
                35 => "b2F 3".to_string(),
                36 => "A2F 3".to_string(),
                37 => "3F 3".to_string(),
                38 => "5F 3".to_string(),
                39 => "7F 3".to_string(),
                40 => "9F 3".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "WBBracketingSteps",
                0x10,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = v & 0x3;
        dm.push(("NikonMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Matrix".to_string(),
            1 => "Center".to_string(),
            2 => "Spot".to_string(),
            3 => "Highlight".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "NikonMeteringMode",
            0x17,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ISOAutoInfoD810` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_isoautoinfod810(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x4) {
        let v = v & 0x3f;
        dm.push(("ISOAutoShutterTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/4000 s".to_string(),
            1 => "1/3200 s".to_string(),
            2 => "1/2500 s".to_string(),
            3 => "1/2000 s".to_string(),
            4 => "1/1600 s".to_string(),
            5 => "1/1250 s".to_string(),
            6 => "1/1000 s".to_string(),
            7 => "1/800 s".to_string(),
            8 => "1/640 s".to_string(),
            9 => "1/500 s".to_string(),
            10 => "1/400 s".to_string(),
            11 => "1/320 s".to_string(),
            12 => "1/250 s".to_string(),
            13 => "1/200 s".to_string(),
            14 => "1/160 s".to_string(),
            15 => "1/125 s".to_string(),
            16 => "1/100 s".to_string(),
            17 => "1/80 s".to_string(),
            18 => "1/60 s".to_string(),
            19 => "1/50 s".to_string(),
            20 => "1/40 s".to_string(),
            21 => "1/30 s".to_string(),
            22 => "1/15 s".to_string(),
            23 => "1/8 s".to_string(),
            24 => "1/4 s".to_string(),
            25 => "1/2 s".to_string(),
            26 => "1 s".to_string(),
            27 => "2 s".to_string(),
            28 => "4 s".to_string(),
            29 => "8 s".to_string(),
            30 => "15 s".to_string(),
            31 => "30 s".to_string(),
            32 => "Auto (Slowest)".to_string(),
            33 => "Auto (Slower)".to_string(),
            34 => "Auto".to_string(),
            35 => "Auto (Faster)".to_string(),
            36 => "Auto (Fastest)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ISOAutoShutterTime",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = v & 0xff;
        dm.push(("ISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            36 => "ISO 200".to_string(),
            38 => "ISO 250".to_string(),
            39 => "ISO 280".to_string(),
            40 => "ISO 320".to_string(),
            42 => "ISO 400".to_string(),
            44 => "ISO 500".to_string(),
            45 => "ISO 560".to_string(),
            46 => "ISO 640".to_string(),
            48 => "ISO 800".to_string(),
            50 => "ISO 1000".to_string(),
            51 => "ISO 1100".to_string(),
            52 => "ISO 1250".to_string(),
            54 => "ISO 1600".to_string(),
            56 => "ISO 2000".to_string(),
            57 => "ISO 2200".to_string(),
            58 => "ISO 2500".to_string(),
            60 => "ISO 3200".to_string(),
            62 => "ISO 4000".to_string(),
            63 => "ISO 4500".to_string(),
            64 => "ISO 5000".to_string(),
            66 => "ISO 6400".to_string(),
            68 => "ISO 8000".to_string(),
            69 => "ISO 9000".to_string(),
            70 => "ISO 10000".to_string(),
            72 => "ISO 12800".to_string(),
            74 => "ISO 16000".to_string(),
            75 => "ISO 18000".to_string(),
            76 => "ISO 20000".to_string(),
            78 => "ISO 25600".to_string(),
            80 => "ISO 32000".to_string(),
            81 => "ISO 36000".to_string(),
            82 => "ISO 40000".to_string(),
            84 => "ISO 51200".to_string(),
            86 => "ISO Hi 0.3".to_string(),
            87 => "ISO Hi 0.5".to_string(),
            88 => "ISO Hi 0.7".to_string(),
            90 => "ISO Hi 1.0".to_string(),
            96 => "ISO Hi 2.0".to_string(),
            102 => "ISO Hi 3.0".to_string(),
            108 => "ISO Hi 4.0".to_string(),
            114 => "ISO Hi 5.0".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOAutoHiLimit", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuSettingsD850` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menusettingsd850(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x6dd) {
        let v = v & 0x7;
        dm.push((
            "PhotoShootingMenuBankImageArea".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "FX (36x24)".to_string(),
            1 => "DX (24x16)".to_string(),
            2 => "5:4 (30x24)".to_string(),
            3 => "1.2x (30x20)".to_string(),
            4 => "1:1 (24x24)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PhotoShootingMenuBankImageArea",
            0x6dd,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::MoreSettingsD850` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_moresettingsd850(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if dm_val(dm, "FILE_TYPE").is_some_and(|v| v.as_string().as_str() == "JPEG") {
        if let Some(v) = u8_at(data, 0x24) {
            let v = v & 0x3;
            dm.push(("PhotoShootingMenuBank".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "A".to_string(),
                1 => "B".to_string(),
                2 => "C".to_string(),
                3 => "D".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "PhotoShootingMenuBank",
                0x24,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x25) {
        let v = (v & 0x80) >> 7;
        dm.push(("PrimarySlot".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "XQD Card".to_string(),
            1 => "SD Card".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PrimarySlot", 0x25, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Nikon::RotationInfoD500` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_rotationinfod500(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x1a) {
        let v = v & 0x3;
        dm.push(("Rotation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Horizontal".to_string(),
            1 => "Rotate 270 CW".to_string(),
            2 => "Rotate 90 CW".to_string(),
            3 => "Rotate 180".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Rotation", 0x1a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if (dm_val(dm, "Model").is_some_and(|v| v.as_string().as_str() == "NIKON D5")
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() >= "1.40"))
    {
        if let Some(v) = u8_at(data, 0x20) {
            dm.push(("Interval".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 0 ? sprintf(\"%.0f\", $val) : \"\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "Interval",
                0x20,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "Model").is_some_and(|v| v.as_string().as_str() == "NIKON D5")
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() >= "1.40"))
    {
        if let Some(v) = u8_at(data, 0x24) {
            dm.push(("IntervalFrame".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val > 0 ? sprintf(\"%.0f\", $val) : \"\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "IntervalFrame",
                0x24,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x532) {
        let v = v & 0x1;
        dm.push((
            "FlickerReductionIndicator".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlickerReductionIndicator",
            0x532,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::JPGInfoD500` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_jpginfod500(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x24) {
        let v = v & 0x1;
        dm.push(("JPGCompression".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Size Priority".to_string(),
            1 => "Optimal Quality".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("JPGCompression", 0x24, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Nikon::BracketingInfoD500` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_bracketinginfod500(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if dm_val(dm, "FILE_TYPE").is_some_and(|v| v.as_string().as_str() != "TIFF") {
        if let Some(v) = u8_at(data, 0xf) {
            let v = v & 0xff;
            dm.push(("AEBracketingSteps".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "AE Bracketing Disabled".to_string(),
                32 => "AE Bracketing Disabled".to_string(),
                48 => "AE Bracketing Disabled".to_string(),
                64 => "AE Bracketing Disabled".to_string(),
                80 => "AE Bracketing Disabled".to_string(),
                129 => "+3F0.3".to_string(),
                130 => "-3F0.3".to_string(),
                131 => "+2F0.3".to_string(),
                132 => "-2F0.3".to_string(),
                133 => "3F0.3".to_string(),
                134 => "5F0.3".to_string(),
                135 => "7F0.3".to_string(),
                136 => "9F0.3".to_string(),
                145 => "+3F0.5".to_string(),
                146 => "-3F0.5".to_string(),
                147 => "+2F0.5".to_string(),
                148 => "-2F0.5".to_string(),
                149 => "3F0.5".to_string(),
                150 => "5F0.5".to_string(),
                151 => "7F0.5".to_string(),
                152 => "9F0.5".to_string(),
                161 => "+3F0.7".to_string(),
                162 => "-3F0.7".to_string(),
                163 => "+2F0.7".to_string(),
                164 => "-2F0.7".to_string(),
                165 => "3F0.7".to_string(),
                166 => "5F0.7".to_string(),
                167 => "7F0.7".to_string(),
                168 => "9F0.7".to_string(),
                177 => "+3F1".to_string(),
                178 => "-3F1".to_string(),
                179 => "+2F1".to_string(),
                180 => "-2F1".to_string(),
                181 => "3F1".to_string(),
                182 => "5F1".to_string(),
                183 => "7F1".to_string(),
                184 => "9F1".to_string(),
                193 => "+3F2".to_string(),
                194 => "-3F2".to_string(),
                195 => "+2F2".to_string(),
                196 => "-2F2".to_string(),
                197 => "3F2".to_string(),
                198 => "5F2".to_string(),
                209 => "+3F3".to_string(),
                210 => "-3F3".to_string(),
                211 => "+2F3".to_string(),
                212 => "-2F3".to_string(),
                213 => "3F3".to_string(),
                214 => "5F3".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AEBracketingSteps", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_val(dm, "FILE_TYPE").is_some_and(|v| v.as_string().as_str() != "TIFF") {
        if let Some(v) = u8_at(data, 0x10) {
            let v = v & 0xff;
            dm.push(("WBBracketingSteps".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "WB Bracketing Disabled".to_string(),
                1 => "b3F 1".to_string(),
                2 => "A3F 1".to_string(),
                3 => "b2F 1".to_string(),
                4 => "A2F 1".to_string(),
                5 => "3F 1".to_string(),
                6 => "5F 1".to_string(),
                7 => "7F 1".to_string(),
                8 => "9F 1".to_string(),
                16 => "0F 2".to_string(),
                17 => "b3F 2".to_string(),
                18 => "A3F 2".to_string(),
                19 => "b2F 2".to_string(),
                20 => "A2F 2".to_string(),
                21 => "3F 2".to_string(),
                22 => "5F 2".to_string(),
                23 => "7F 2".to_string(),
                24 => "9F 2".to_string(),
                32 => "0F 3".to_string(),
                33 => "b3F 3".to_string(),
                34 => "A3F 3".to_string(),
                35 => "b2F 3".to_string(),
                36 => "A2F 3".to_string(),
                37 => "3F 3".to_string(),
                38 => "5F 3".to_string(),
                39 => "7F 3".to_string(),
                40 => "9F 3".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "WBBracketingSteps",
                0x10,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = (v & 0xf0) >> 4;
        dm.push(("ADLBracketingStep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Low".to_string(),
            2 => "Normal".to_string(),
            3 => "High".to_string(),
            4 => "Extra High".to_string(),
            8 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ADLBracketingStep",
            0x17,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x18) {
        let v = v & 0xf;
        dm.push(("ADLBracketingType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "2 Shots".to_string(),
            2 => "3 Shots".to_string(),
            3 => "4 Shots".to_string(),
            4 => "5 Shots".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ADLBracketingType",
            0x18,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::ShootingMenuD500` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_shootingmenud500(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x3;
        dm.push(("PhotoShootingMenuBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PhotoShootingMenuBank",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_20.is_match(model) {
        if let Some(v) = u8_at(data, 0x2) {
            let v = (v & 0x80) >> 7;
            dm.push(("PrimarySlot".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "XQD Card".to_string(),
                1 => "SD Card".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("PrimarySlot", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = v & 0x3f;
        dm.push(("ISOAutoShutterTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/4000 s".to_string(),
            1 => "1/3200 s".to_string(),
            2 => "1/2500 s".to_string(),
            3 => "1/2000 s".to_string(),
            4 => "1/1600 s".to_string(),
            5 => "1/1250 s".to_string(),
            6 => "1/1000 s".to_string(),
            7 => "1/800 s".to_string(),
            8 => "1/640 s".to_string(),
            9 => "1/500 s".to_string(),
            10 => "1/400 s".to_string(),
            11 => "1/320 s".to_string(),
            12 => "1/250 s".to_string(),
            13 => "1/200 s".to_string(),
            14 => "1/160 s".to_string(),
            15 => "1/125 s".to_string(),
            16 => "1/100 s".to_string(),
            17 => "1/80 s".to_string(),
            18 => "1/60 s".to_string(),
            19 => "1/50 s".to_string(),
            20 => "1/40 s".to_string(),
            21 => "1/30 s".to_string(),
            22 => "1/15 s".to_string(),
            23 => "1/8 s".to_string(),
            24 => "1/4 s".to_string(),
            25 => "1/2 s".to_string(),
            26 => "1 s".to_string(),
            27 => "2 s".to_string(),
            28 => "4 s".to_string(),
            29 => "8 s".to_string(),
            30 => "15 s".to_string(),
            31 => "30 s".to_string(),
            32 => "Auto (Slowest)".to_string(),
            33 => "Auto (Slower)".to_string(),
            34 => "Auto".to_string(),
            35 => "Auto (Faster)".to_string(),
            36 => "Auto (Fastest)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ISOAutoShutterTime",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = v & 0xff;
        dm.push(("ISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            36 => "ISO 200".to_string(),
            38 => "ISO 250".to_string(),
            39 => "ISO 280".to_string(),
            40 => "ISO 320".to_string(),
            42 => "ISO 400".to_string(),
            44 => "ISO 500".to_string(),
            45 => "ISO 560".to_string(),
            46 => "ISO 640".to_string(),
            48 => "ISO 800".to_string(),
            50 => "ISO 1000".to_string(),
            51 => "ISO 1100".to_string(),
            52 => "ISO 1250".to_string(),
            54 => "ISO 1600".to_string(),
            56 => "ISO 2000".to_string(),
            57 => "ISO 2200".to_string(),
            58 => "ISO 2500".to_string(),
            60 => "ISO 3200".to_string(),
            62 => "ISO 4000".to_string(),
            63 => "ISO 4500".to_string(),
            64 => "ISO 5000".to_string(),
            66 => "ISO 6400".to_string(),
            68 => "ISO 8000".to_string(),
            69 => "ISO 9000".to_string(),
            70 => "ISO 10000".to_string(),
            72 => "ISO 12800".to_string(),
            74 => "ISO 16000".to_string(),
            75 => "ISO 18000".to_string(),
            76 => "ISO 20000".to_string(),
            78 => "ISO 25600".to_string(),
            80 => "ISO 32000".to_string(),
            81 => "ISO 36000".to_string(),
            82 => "ISO 40000".to_string(),
            84 => "ISO 51200".to_string(),
            86 => "ISO Hi 0.3".to_string(),
            87 => "ISO Hi 0.5".to_string(),
            88 => "ISO Hi 0.7".to_string(),
            90 => "ISO Hi 1.0".to_string(),
            96 => "ISO Hi 2.0".to_string(),
            102 => "ISO Hi 3.0".to_string(),
            108 => "ISO Hi 4.0".to_string(),
            114 => "ISO Hi 5.0".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOAutoHiLimit", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x20) >> 5;
        dm.push(("FlickerReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Enable".to_string(),
            1 => "Disable".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlickerReduction", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = v & 0x7;
        dm.push((
            "PhotoShootingMenuBankImageArea".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "FX (36x24)".to_string(),
            1 => "DX (24x16)".to_string(),
            2 => "5:4 (30x24)".to_string(),
            3 => "1.2x (30x20)".to_string(),
            4 => "1.3x (18x12)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PhotoShootingMenuBankImageArea",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::CustomSettingsD500` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_customsettingsd500(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if MODEL_RE_21.is_match(model) {
        if let Some(sub) = data.get(0x0..0x0 + 90) {
            tags.extend(nikoncustom_settingsd5(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if !(MODEL_RE_21.is_match(model)) {
        if let Some(sub) = data.get(0x0..0x0 + 90) {
            tags.extend(nikoncustom_settingsd500(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::OtherInfoD500` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_otherinfod500(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if MODEL_RE_20.is_match(model) {
        if let Some(v) = u8_at(data, 0x214) {
            let v = v & 0x3;
            dm.push(("NikonMeteringMode".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Matrix".to_string(),
                1 => "Center".to_string(),
                2 => "Spot".to_string(),
                3 => "Highlight".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "NikonMeteringMode",
                0x214,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::SeqInfoD6` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_seqinfod6(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x24, bo) {
        dm.push(("IntervalShooting".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("
            return 'Off' if $val == 0 ;
            my $i = sprintf(\"Interval %.0f of %.0f\",$val, $$self{IntervalShootingIntervals}||0);    #something like \"Interval 1 of 3\"
            my $f = ($$self{IntervalShootingShotsPerInterval}||0) > 1 ? sprintf(\" Frame %.0f of %.0f\",$$self{IntervalFrame}||0, $$self{IntervalShootingShotsPerInterval}||0): '' ;  #something like \"Frame 1 of 3\" or blank
            return \"On: $i$f\"

        ", &cv, &ctx) { cv = x; }
            tags.push(mk(
                "IntervalShooting",
                0x24,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0) {
        if let Some(v) = u16_at(data, 0x28, bo) {
            dm.push(("IntervalFrame".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "IntervalFrame",
                    0x28,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x2b) {
        dm.push(("ImageArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "FX (36x24)".to_string(),
            1 => "DX (24x16)".to_string(),
            2 => "5:4 (30x24)".to_string(),
            3 => "1.2x (30x20)".to_string(),
            4 => "1:1 (24x24)".to_string(),
            6 => "16:9".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageArea", 0x2b, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Nikon::IntervalInfoD6` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_intervalinfod6(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0) {
        if let Some(v) = u32_at(data, 0x17c, bo) {
            dm.push((
                "IntervalShootingIntervals".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "Intervals",
                    0x17c,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0) {
        if let Some(v) = u32_at(data, 0x180, bo) {
            dm.push((
                "IntervalShootingShotsPerInterval".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "ShotsPerInterval",
                    0x180,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0) {
        if let Some(v) = u8_at(data, 0x184) {
            dm.push((
                "IntervalExposureSmoothing".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "IntervalExposureSmoothing",
                0x184,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0) {
        if let Some(v) = u8_at(data, 0x186) {
            dm.push(("IntervalPriority".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "IntervalPriority",
                0x186,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1a8) {
        dm.push(("FocusShiftNumberShots".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusShiftNumberShots",
            0x1a8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1ac) {
        dm.push(("FocusShiftStepWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusShiftStepWidth",
            0x1ac,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b0) {
        dm.push(("FocusShiftInterval".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "$val == 1? \"1 Second\" : sprintf(\"%.0f Seconds\",$val)",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "FocusShiftInterval",
            0x1b0,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b4) {
        dm.push((
            "FocusShiftExposureLock".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusShiftExposureLock",
            0x1b4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x20e) {
        dm.push((
            "DiffractionCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DiffractionCompensation",
            0x20e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x214) {
        dm.push(("FlashControlMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Auto External Flash".to_string(),
                2 => "GN (distance priority)".to_string(),
                3 => "Manual".to_string(),
                4 => "Repeating Flash".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlMode",
                0x214,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x21a) {
            dm.push(("FlashGNDistance".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v >= 3.0) {
        if let Some(v) = u8_at(data, 0x21e) {
            dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x228) {
        dm.push(("FlashRemoteControl".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x22c) {
        dm.push((
            "FlashGroupOptionsMasterMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Manual".to_string(),
                2 => "Auto".to_string(),
                3 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashMasterControlMode",
                0x22c,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashGroupOptionsMasterMode").is_some_and(|v| v != 3.0) {
        if let Some(v) = i8_at(data, 0x22e) {
            dm.push((
                "FlashMasterCompensation".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if dm_get(dm, "FlashGroupOptionsMasterMode").is_some_and(|v| v == 1.0) {
        if let Some(v) = u8_at(data, 0x232) {
            dm.push(("FlashMasterOutput".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x234) {
        dm.push(("FlashWirelessOption".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2ca) {
        dm.push(("MovieType".to_string(), Conv::Num(f64::from(v))));
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuSettingsZ6III` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menusettingsz6iii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x5a) {
        dm.push(("SingleFrame".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SingleFrame",
                0x5a,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x5c) {
        dm.push(("ReleaseMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("$$self{SingleFrame} == 0 ? 5 : $val", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Continuous Low".to_string(),
                1 => "Continuous High".to_string(),
                2 => "Continuous High (Extended)".to_string(),
                4 => "Timer".to_string(),
                5 => "Single Frame".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ReleaseMode", 0x5c, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u32_at(data, 0xa0, bo) {
        dm.push(("IntervalDurationHours".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "IntervalDurationHours",
            0xa0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xa4, bo) {
        dm.push((
            "IntervalDurationMinutes".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "IntervalDurationMinutes",
            0xa4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xa8, bo) {
        dm.push((
            "IntervalDurationSeconds".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "IntervalDurationSeconds",
            0xa8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xb0, bo) {
        dm.push((
            "IntervalShootingIntervals".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "Intervals",
                0xb0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0xb4, bo) {
        dm.push((
            "IntervalShootingShotsPerInterval".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ShotsPerInterval",
                0xb4,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xb8) {
        dm.push((
            "IntervalExposureSmoothing".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "IntervalExposureSmoothing",
            0xb8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xba) {
        dm.push(("IntervalPriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("IntervalPriority", 0xba, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xdc) {
        dm.push(("FocusShiftNumberShots".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusShiftNumberShots",
            0xdc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe0) {
        dm.push(("FocusShiftStepWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusShiftStepWidth",
            0xe0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe4) {
        dm.push(("FocusShiftInterval".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "$val == 1? \"1 Second\" : sprintf(\"%.0f Seconds\",$val)",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "FocusShiftInterval",
            0xe4,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe8) {
        dm.push((
            "FocusShiftExposureLock".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusShiftExposureLock",
            0xe8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x142) {
        dm.push((
            "DiffractionCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DiffractionCompensation",
            0x142,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x143) {
        dm.push(("AutoDistortionControl".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoDistortionControl",
            0x143,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x146) {
        dm.push(("NikonMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Matrix".to_string(),
            1 => "Center".to_string(),
            2 => "Spot".to_string(),
            3 => "Highlight".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "NikonMeteringMode",
            0x146,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x148) {
        dm.push(("FlashControlMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Auto External Flash".to_string(),
                2 => "GN (distance priority)".to_string(),
                3 => "Manual".to_string(),
                4 => "Repeating Flash".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlMode",
                0x148,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x14e) {
            dm.push(("FlashGNDistance".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v >= 3.0) {
        if let Some(v) = u8_at(data, 0x152) {
            dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x15a) {
        dm.push(("FlashWirelessOption".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x15c) {
        dm.push(("FlashRemoteControl".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x160) {
        dm.push((
            "FlashGroupOptionsMasterMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Manual".to_string(),
                2 => "Auto".to_string(),
                3 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashMasterControlMode",
                0x160,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashGroupOptionsMasterMode").is_some_and(|v| v != 3.0) {
        if let Some(v) = i8_at(data, 0x162) {
            dm.push((
                "FlashMasterCompensation".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if dm_get(dm, "FlashGroupOptionsMasterMode").is_some_and(|v| v == 1.0) {
        if let Some(v) = u8_at(data, 0x166) {
            dm.push(("FlashMasterOutput".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x168) {
        dm.push(("SingleFrame".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SingleFrame",
                0x168,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x16c) {
        dm.push(("HighFrameRate".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "CH".to_string(),
            2 => "CH*".to_string(),
            3 => "C30".to_string(),
            4 => "C120".to_string(),
            5 => "C60".to_string(),
            6 => "C15".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("HighFrameRate", 0x16c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1bc) {
        dm.push(("MultipleExposureMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                2 => "On (Series)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "MultipleExposureMode",
                0x1bc,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "MultipleExposureMode").is_some_and(|v| v != 0.0) {
        if let Some(v) = u8_at(data, 0x1be) {
            dm.push(("MultiExposureShots".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "MultiExposureShots",
                0x1be,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1dc, bo) {
        dm.push(("IntervalDurationHours".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "IntervalDurationHours",
            0x1dc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x1e0, bo) {
        dm.push((
            "IntervalDurationMinutes".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "IntervalDurationMinutes",
            0x1e0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x1e4, bo) {
        dm.push((
            "IntervalDurationSeconds".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "IntervalDurationSeconds",
            0x1e4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x1ec, bo) {
        dm.push((
            "IntervalShootingIntervals".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "Intervals",
                0x1ec,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x1f0, bo) {
        dm.push((
            "IntervalShootingShotsPerInterval".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ShotsPerInterval",
                0x1f0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1f4) {
        dm.push((
            "IntervalExposureSmoothing".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "IntervalExposureSmoothing",
            0x1f4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f6) {
        dm.push(("IntervalPriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "IntervalPriority",
            0x1f6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f6) {
        dm.push(("MovieFrameSize".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1f8) {
        dm.push(("MovieFrameRate".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1fa) {
        dm.push(("MovieSlowMotion".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1fe) {
        dm.push(("MovieType".to_string(), Conv::Num(f64::from(v))));
    }
    if MODEL_RE_22.is_match(model) {
        if let Some(v) = u8_at(data, 0x204) {
            dm.push((
                "MovieISOAutoManualMode".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "MovieISOAutoManualMode",
                0x204,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x218) {
        dm.push(("FocusShiftNumberShots".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusShiftNumberShots",
            0x218,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x21c) {
        dm.push(("FocusShiftStepWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusShiftStepWidth",
            0x21c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x220) {
        dm.push(("FocusShiftInterval".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "$val == 1? \"1 Second\" : sprintf(\"%.0f Seconds\",$val)",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "FocusShiftInterval",
            0x220,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x224) {
        dm.push((
            "FocusShiftExposureLock".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusShiftExposureLock",
            0x224,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x238) {
        dm.push(("MovieActiveD-Lighting".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x23c) {
        dm.push((
            "MovieHighISONoiseReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x23e) {
        dm.push(("MovieVignetteControl".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x240) {
        dm.push((
            "MovieVignetteControlSameAsPhoto".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x241) {
        dm.push((
            "MovieDiffractionCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x242) {
        dm.push((
            "MovieAutoDistortionControl".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x248) {
        dm.push(("MovieFocusMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x24e) {
        dm.push((
            "MovieVibrationReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x24f) {
        dm.push((
            "MovieVibrationReductionSameAsPhoto".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x288) {
        dm.push(("AutoISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoISO", 0x288, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x28a, bo) {
        dm.push(("ISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2ce) {
        dm.push((
            "DiffractionCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DiffractionCompensation",
            0x2ce,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2cf) {
        dm.push(("AutoDistortionControl".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoDistortionControl",
            0x2cf,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2d0) {
        dm.push((
            "FlickerReductionShooting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlickerReductionShooting",
            0x2d0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2d2) {
        dm.push(("NikonMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Matrix".to_string(),
            1 => "Center".to_string(),
            2 => "Spot".to_string(),
            3 => "Highlight".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "NikonMeteringMode",
            0x2d2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2d4) {
        dm.push(("FlashControlMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Auto External Flash".to_string(),
                2 => "GN (distance priority)".to_string(),
                3 => "Manual".to_string(),
                4 => "Repeating Flash".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlMode",
                0x2d4,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x2da) {
            dm.push(("FlashGNDistance".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v >= 3.0) {
        if let Some(v) = u8_at(data, 0x2de) {
            dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x2e6) {
        dm.push(("FlashWirelessOption".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2e8) {
        dm.push(("FlashRemoteControl".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2ec) {
        dm.push((
            "FlashGroupOptionsMasterMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Manual".to_string(),
                2 => "Auto".to_string(),
                3 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashMasterControlMode",
                0x2ec,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashGroupOptionsMasterMode").is_some_and(|v| v != 3.0) {
        if let Some(v) = i8_at(data, 0x2ee) {
            dm.push((
                "FlashMasterCompensation".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if dm_get(dm, "FlashGroupOptionsMasterMode").is_some_and(|v| v == 1.0) {
        if let Some(v) = u8_at(data, 0x2f2) {
            dm.push(("FlashMasterOutput".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x340) {
        dm.push(("AFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Pinpoint".to_string(),
                1 => "Single".to_string(),
                2 => "Dynamic".to_string(),
                3 => "Wide (S)".to_string(),
                4 => "Wide (L)".to_string(),
                5 => "3D".to_string(),
                6 => "Auto".to_string(),
                11 => "Subject Tracking".to_string(),
                12 => "Wide (C1)".to_string(),
                13 => "Wide (C2)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFAreaMode", 0x340, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x342) {
        dm.push(("VRMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Normal".to_string(),
            2 => "Sport".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("VRMode", 0x342, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x346) {
        dm.push(("BracketSet".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "AE/Flash".to_string(),
                1 => "AE".to_string(),
                2 => "Flash".to_string(),
                3 => "White Balance".to_string(),
                4 => "Active-D Lighting".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("BracketSet", 0x346, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "BracketSet").is_some_and(|v| v < 3.0) {
        if let Some(v) = u8_at(data, 0x348) {
            dm.push(("BracketProgram".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Disabled".to_string(),
                2 => "2F".to_string(),
                3 => "3F".to_string(),
                4 => "4F".to_string(),
                5 => "5F".to_string(),
                7 => "7F".to_string(),
                9 => "9F".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("BracketProgram", 0x348, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "BracketSet").is_some_and(|v| v < 3.0) {
        if let Some(v) = u8_at(data, 0x34a) {
            dm.push(("BracketIncrement".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "0.3".to_string(),
                1 => "0.5".to_string(),
                2 => "0.7".to_string(),
                3 => "1.0".to_string(),
                4 => "2.0".to_string(),
                5 => "3.0".to_string(),
                6 => "1.3".to_string(),
                7 => "1.5".to_string(),
                8 => "1.7".to_string(),
                9 => "2.3".to_string(),
                10 => "2.5".to_string(),
                11 => "2.7".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "BracketIncrement",
                0x34a,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x354) {
        dm.push(("HDR".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                2 => "On (Series)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("HDR", 0x354, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x35a) {
        dm.push(("SecondarySlotFunction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Overflow".to_string(),
            1 => "Backup".to_string(),
            2 => "NEF Primary + JPG Secondary".to_string(),
            3 => "JPG Primary + JPG Secondary".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SecondarySlotFunction",
            0x35a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if (dm_val(dm, "HDMIBitDepth").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "HDMIBitDepth").is_some_and(|v| v == 2.0))
    {
        if let Some(v) = u8_at(data, 0x35a) {
            dm.push(("HDMIOutputNLog".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if dm_val(dm, "HDR").is_some_and(|v| v.as_string().as_str() != "0") {
        if let Some(v) = u8_at(data, 0x360) {
            dm.push(("HDRLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Auto".to_string(),
                1 => "Extra High".to_string(),
                2 => "High".to_string(),
                3 => "Normal".to_string(),
                4 => "Low".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("HDRLevel", 0x360, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x364) {
        dm.push(("Slot2JpgSize".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x36e) {
        dm.push(("SubjectDetection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Auto".to_string(),
            2 => "People".to_string(),
            3 => "Animals".to_string(),
            4 => "Vehicles".to_string(),
            5 => "Birds".to_string(),
            6 => "Airplanes".to_string(),
            7 => "Faces".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubjectDetection",
            0x36e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_get(dm, "AFAreaMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x370) {
            dm.push(("DynamicAFAreaSize".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                let s = match base.as_num() as i64 {
                    0 => "Small".to_string(),
                    1 => "Medium".to_string(),
                    2 => "Large".to_string(),
                    other => other.to_string(),
                };
                #[allow(clippy::cast_possible_truncation)]
                let raw = Value::I32(base.as_num() as i32);
                tags.push(mk(
                    "DynamicAFAreaSize",
                    0x370,
                    s,
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x374) {
        dm.push(("ToneMap".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x378) {
        dm.push((
            "PortraitImpressionBalance".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Mode 1".to_string(),
            2 => "Mode 2".to_string(),
            3 => "Mode 3".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PortraitImpressionBalance",
            0x378,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x386) {
        dm.push((
            "HighFrequencyFlickerReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x388) {
        dm.push(("PixelShiftShooting".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                2 => "On (Series)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "PixelShiftShooting",
                0x388,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "PixelShiftShooting").is_some_and(|v| v > 0.0) {
        if let Some(v) = u8_at(data, 0x38a) {
            dm.push(("PixelShiftNumberShots".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "4".to_string(),
                1 => "8".to_string(),
                2 => "16".to_string(),
                3 => "32".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "PixelShiftNumberShots",
                0x38a,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "PixelShiftShooting").is_some_and(|v| v > 0.0) {
        if let Some(v) = u8_at(data, 0x38c) {
            dm.push(("PixelShiftDelay".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val == 0? \"No Delay\" : sprintf(\"%.0f sec\",$val)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "PixelShiftDelay",
                0x38c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "PixelShiftShooting").is_some_and(|v| v > 0.0) {
        if let Some(v) = u8_at(data, 0x38e) {
            dm.push(("PixelShiftInterval".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val == 0? \"No Delay\" : sprintf(\"%.0f sec\",$val)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "PixelShiftInterval",
                0x38e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x3ea) {
        dm.push((
            "SubjectDetectionAreaMF".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "All".to_string(),
            2 => "Wide (L)".to_string(),
            3 => "Wide (S)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubjectDetectionAreaMF",
            0x3ea,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3ec) {
        dm.push(("LinkVRToFocusPoint".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x416) {
        dm.push(("MovieSlowMotion".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x41a) {
        dm.push(("MovieType".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                1 => "H.264 8-bit (MP4)".to_string(),
                2 => "H.265 8-bit (MOV)".to_string(),
                3 => "H.265 10-bit (MOV)".to_string(),
                4 => "ProRes 422 HQ 10-bit (MOV)".to_string(),
                5 => "ProRes RAW HQ 12-bit (MOV)".to_string(),
                6 => "NRAW 12-bit (NEV)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("MovieType", 0x41a, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x48a) {
        dm.push(("MovieFrameSize".to_string(), Conv::Num(f64::from(v))));
    }
    if dm_get(dm, "MovieType").is_some_and(|v| v != 1.0) {
        if let Some(v) = u8_at(data, 0x48c) {
            dm.push(("MovieFrameRate".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x8fc) {
        dm.push(("Language".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x8fe) {
        dm.push(("TimeZone".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "+10:00 (Sydney)".to_string(),
            5 => "+09:00 (Tokyo)".to_string(),
            6 => "+08:00 (Beijing, Honk Kong, Sinapore)".to_string(),
            10 => "+05:45 (Kathmandu)".to_string(),
            11 => "+05:30 (New Dehli)".to_string(),
            12 => "+05:00 (Islamabad)".to_string(),
            13 => "+04:30 (Kabul)".to_string(),
            14 => "+04:00 (Abu Dhabi)".to_string(),
            15 => "+03:30 (Tehran)".to_string(),
            16 => "+03:00 (Moscow, Nairobi)".to_string(),
            17 => "+02:00 (Athens, Helsinki)".to_string(),
            18 => "+01:00 (Madrid, Paris, Berlin)".to_string(),
            19 => "+00:00 (London)".to_string(),
            20 => "-01:00 (Azores)".to_string(),
            21 => "-02:00 (Fernando de Noronha)".to_string(),
            22 => "-03:00 (Buenos Aires, Sao Paulo)".to_string(),
            23 => "-03:30 (Newfoundland)".to_string(),
            24 => "-04:00 (Manaus, Caracas)".to_string(),
            25 => "-05:00 (New York, Toronto, Lima)".to_string(),
            26 => "-06:00 (Chicago, Mexico City)".to_string(),
            27 => "-07:00 (Denver)".to_string(),
            28 => "-08:00 (Los Angeles, Vancouver)".to_string(),
            29 => "-09:00 (Anchorage)".to_string(),
            30 => "-10:00 (Hawaii)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("TimeZone", 0x8fe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x904) {
        dm.push(("MonitorBrightness".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x98c) {
        dm.push(("EmptySlotRelease".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x992) {
        dm.push(("EnergySavingMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x9ac) {
        dm.push(("USBPowerDelivery".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x9b0) {
        dm.push(("SaveFocusPosition".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x9b7) {
        dm.push(("SilentPhotography".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x9c0) {
        dm.push(("AirplaneMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(sub) = data.get(0x4e7..0x4e7 + 700) {
        tags.extend(nikoncustom_settingsz6iii(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::IntervalInfoZ7II` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_intervalinfoz7ii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x24, bo) {
        dm.push(("IntervalShooting".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("
            return 'Off' if $val == 0 ;
            my $i = sprintf(\"Interval %.0f of %.0f\",$val, $$self{IntervalShootingIntervals}||0); # something like \"Interval 1 of 3\"
            my $f = ($$self{IntervalShootingShotsPerInterval}||0) > 1 ? sprintf(\" Frame %.0f of %.0f\",$$self{IntervalFrame}||0, $$self{IntervalShootingShotsPerInterval}||0): '' ;  # something like \"Frame 1 of 3\" or blank
            return \"On: $i$f\"

        ", &cv, &ctx) { cv = x; }
            tags.push(mk(
                "IntervalShooting",
                0x24,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0) {
        if let Some(v) = u16_at(data, 0x28, bo) {
            dm.push(("IntervalFrame".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "IntervalFrame",
                    0x28,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x2b) {
        dm.push(("ImageArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "FX (36x24)".to_string(),
            1 => "DX (24x16)".to_string(),
            2 => "5:4 (30x24)".to_string(),
            3 => "1.2x (30x20)".to_string(),
            4 => "1:1 (24x24)".to_string(),
            6 => "16:9".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageArea", 0x2b, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Nikon::PortraitInfoZ7II` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_portraitinfoz7ii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() >= "01.30") {
        {
            let mut parts = Vec::new();
            for k in 0..2 {
                match u8_at(data, 0xa0 + k * 1) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                let ctx = Ctx {
                    make,
                    model,
                    file_type,
                    dm,
                };
                let mut cv = Conv::Str(s.clone());
                let raw = Value::String(cv.as_string());
                if let Some(x) = conv_expr::eval_with("
            return 'Off' if $val eq '0 0' or $val eq '255 255';
            my @v = split ' ', $val;
            my $brightness = $v[1]==128 ? 'Brightness: Neutral' : sprintf('Brightness: %+.1f',($v[1]-128)/4);
            my $color = $v[0]==128 ? 'Color: Neutral' : sprintf('%s: %.1f', $v[0]>128 ? 'Yellow' : 'Magenta', abs($v[0]-128)/4);

            return \"$color $brightness\"
        ", &cv, &ctx) { cv = x; }
                tags.push(mk(
                    "PortraitImpressionBalance",
                    0xa0,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuInfoZ7II` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menuinfoz7ii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(sub) = u32_at(data, 0x10, bo).and_then(|at| data.get(at as usize..)) {
        tags.extend(nikon_menusettingsz7ii(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::SeqInfoZ9` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_seqinfoz9(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96"))
    {
        if let Some(v) = u8_at(data, 0x20) {
            dm.push(("FocusShiftShooting".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                let mut cv = base;
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("
            return 'Off' if $val == 0 ;
            my $i = sprintf(\"Frame %.0f of %.0f\",$val, $$self{FocusShiftNumberShots}); # something like Frame 1 of 100\"
            if ($$self{PixelShiftActive} and $$self{PixelShiftActive} eq 1) {$i = sprintf(\"Frame %.0f\",$val);}   #for the Z8 fw3 with PixelShift Enabled, the frame count is correct, but the frame total needs to be multiplied by the number of PixelShift frames (which I cannot find)
            return \"On: $i\"
        ", &cv, &ctx) { cv = x; }
                tags.push(mk(
                    "FocusShiftShooting",
                    0x20,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96"))
    {
        if let Some(v) = u16_at(data, 0x28, bo) {
            dm.push(("IntervalShooting".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                let mut cv = base;
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("
            return 'Off' if $val == 0 ;
            my $i = sprintf(\"Interval %.0f of %.0f\",$val, $$self{IntervalShootingIntervals}||0); # something like \"Interval 1 of 3\"
            my $f = ($$self{IntervalShootingShotsPerInterval}||0) > 1 ? sprintf(\" Frame %.0f of %.0f\",$$self{IntervalFrame}||0, $$self{IntervalShootingShotsPerInterval}||0): '' ;  # something like \"Frame 1 of 3\" or blank
            return \"On: $i$f\"

        ", &cv, &ctx) { cv = x; }
                tags.push(mk(
                    "IntervalShooting",
                    0x28,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u16_at(data, 0x2a, bo) {
            dm.push(("IntervalFrame".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "IntervalFrame",
                    0x2a,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::AutoCaptureInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_autocaptureinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("AutoCapturedFrame".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "No".to_string(),
                5 => "Yes".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AutoCapturedFrame", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if (dm_val(dm, "AutoCapturedFrame").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "AutoCapturedFrame").is_some_and(|v| v.as_string().as_str() != "0"))
    {
        if let Some(v) = u8_at(data, 0x1) {
            dm.push(("AutoCaptureCriteria".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "AutoCaptureCriteria",
                0x1,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "AutoCapturedFrame").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "AutoCapturedFrame").is_some_and(|v| v.as_string().as_str() != "0"))
    {
        if let Some(v) = u8_at(data, 0x37) {
            dm.push((
                "AutoCaptureRecordingTime".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1 Sec".to_string(),
                1 => "3 Sec".to_string(),
                2 => "5 Sec".to_string(),
                4 => "30 Sec".to_string(),
                5 => "No Limit".to_string(),
                6 => "2 Sec".to_string(),
                7 => "10 Sec".to_string(),
                8 => "20 Sec".to_string(),
                9 => "1 Min".to_string(),
                10 => "3 Min".to_string(),
                11 => "5 Min".to_string(),
                12 => "10 Min".to_string(),
                13 => "30 Min".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "AutoCaptureRecordingTime",
                0x37,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "AutoCapturedFrame").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "AutoCapturedFrame").is_some_and(|v| v.as_string().as_str() != "0"))
    {
        if let Some(v) = u8_at(data, 0x38) {
            dm.push(("AutoCaptureWaitTime".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "No Wait".to_string(),
                1 => "10 Sec".to_string(),
                2 => "30 Sec".to_string(),
                3 => "1 Min".to_string(),
                4 => "5 Min".to_string(),
                5 => "10 Min".to_string(),
                6 => "30 Min".to_string(),
                7 => "1 Sec".to_string(),
                8 => "2 Sec".to_string(),
                9 => "3 Sec".to_string(),
                10 => "5 Sec".to_string(),
                11 => "20 Sec".to_string(),
                12 => "3 Min".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "AutoCaptureWaitTime",
                0x38,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "AutoCapturedFrame").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "AutoCapturedFrame").is_some_and(|v| v.as_string().as_str() != "0"))
    {
        if let Some(v) = u8_at(data, 0x4a) {
            dm.push((
                "AutoCaptureDistanceFar".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f m\", $val/10)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "AutoCaptureDistanceFar",
                0x4a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "AutoCapturedFrame").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "AutoCapturedFrame").is_some_and(|v| v.as_string().as_str() != "0"))
    {
        if let Some(v) = u8_at(data, 0x4e) {
            dm.push((
                "AutoCaptureDistanceNear".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f m\", $val/10)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "AutoCaptureDistanceNear",
                0x4e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "AutoCapturedFrame").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "AutoCapturedFrame").is_some_and(|v| v.as_string().as_str() != "0"))
    {
        if let Some(v) = u8_at(data, 0x5f) {
            dm.push((
                "AutoCaptureCriteriaMotionDirection".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "AutoCaptureCriteriaMotionDirection",
                0x5f,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "AutoCapturedFrame").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "AutoCapturedFrame").is_some_and(|v| v.as_string().as_str() != "0"))
    {
        if let Some(v) = u8_at(data, 0x63) {
            dm.push((
                "AutoCaptureCriteriaMotionSpeed".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "AutoCaptureCriteriaMotionSpeed",
                0x63,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "AutoCapturedFrame").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "AutoCapturedFrame").is_some_and(|v| v.as_string().as_str() != "0"))
    {
        if let Some(v) = u8_at(data, 0x64) {
            dm.push((
                "AutoCaptureCriteriaMotionSize".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "AutoCaptureCriteriaMotionSize",
                0x64,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "AutoCapturedFrame").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "AutoCapturedFrame").is_some_and(|v| v.as_string().as_str() != "0"))
    {
        if let Some(v) = u8_at(data, 0x69) {
            dm.push((
                "AutoCaptureCriteriaSubjectSize".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "AutoCaptureCriteriaSubjectSize",
                0x69,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "AutoCapturedFrame").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "AutoCapturedFrame").is_some_and(|v| v.as_string().as_str() != "0"))
    {
        if let Some(v) = u8_at(data, 0x6a) {
            dm.push((
                "AutoCaptureCriteriaSubjectType".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let vc = conv_expr::eval_with("$val + 1", &base, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                let base = vc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                let s = match base.as_num() as i64 {
                    0 => "Off".to_string(),
                    1 => "Auto".to_string(),
                    2 => "People".to_string(),
                    3 => "Animals".to_string(),
                    4 => "Vehicles".to_string(),
                    5 => "Birds".to_string(),
                    6 => "Airplanes".to_string(),
                    7 => "Faces".to_string(),
                    other => other.to_string(),
                };
                #[allow(clippy::cast_possible_truncation)]
                let raw = Value::I32(base.as_num() as i32);
                tags.push(mk(
                    "AutoCaptureCriteriaSubjectType",
                    0x6a,
                    s,
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuInfoZ8` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menuinfoz8(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() < "02.00"))
    {
        if let Some(sub) = u32_at(data, 0x10, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_menusettingsz8v1(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if !(dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() < "02.00"))
        && (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
            && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() >= "02.00"))
    {
        if let Some(sub) = u32_at(data, 0x10, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_menusettingsz8v2(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::Offset13InfoZ9` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_offset13infoz9(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96"))
    {
        if let Some(v) = i8_at(data, 0xbe8) {
            dm.push((
                "AFAreaInitialXPosition".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("
            my $imageArea = $$self{ImageArea};
            my $afAreaMode = $$self{VALUE}{AFAreaMode};
            my $dynamicAFAreaSize = ( defined $$self{DynamicAFAreaSize} ? $$self{DynamicAFAreaSize} : 0 );

            my $FX = 0;            #image size 8256 x 5504
            my $DX = 1;            #image size 5392 x 3592
            my $WideScreen = 4;    #16:9 image area, image size 8256x4640
            my $OneToOne = 8;      #1:1 image area, image size 5504x5504

            my $Single = 1;
            my $Dynamic = 2;
            my $WideS = 3;
            my $WideL = 4;
            my $ThreeD = 5;
            my $Auto = 6;
            my $WideC1 = 12;

            my $DynamicS = 0;
            my $DynamicM = 1;
            my $DynamicL = 2;

            my $start = 502;           #FX, 16:9 & 1:1 formats
            my $increment = 259;       #FX & 16:9 formats

            $start = $start + 5 * $increment if $imageArea == $OneToOne;  # need to provide additional offset for the cropped horizontal pixels in 1:1 (19 vs 29 horizontal focus positions)
            $start = $start - $increment if $val < 49 and ($imageArea == $FX or $imageArea == $WideScreen);   #calculations for the left side of the frames are offset by 1 position from the right side
            $start = $start - $increment if $imageArea == $OneToOne and $afAreaMode == $Auto;

            if ($imageArea == $DX) {    # DX results are in FX coordinate system to match reporting of ($AFAreaXPosition , $AFAreaYPosition)
                $start = 636;
                $increment = 388;
                if ( $afAreaMode == $WideS ) {  #Wide S focus box width is an unusual size
                    $start = 591;
                    $increment = 393;
                }
                $start = $start - $increment if $afAreaMode == $Auto ;
            }

            my $divisor = 3.99;     #subtract .01 to ensure $val of 2n+2 rounds up
            $divisor = 4.01 if $val >= 50;        #...but round up on the right side of the frame
            $divisor = 6 if $imageArea == $DX or $imageArea == $OneToOne;

            my $roundedValOverDivisor = sprintf(\"%.0f\", $val / $divisor);   #round to nearest int

            my $focusBoxWidth = $$self{AFAreaInitialWidth}  ;     #wider focus boxes (e.g., DynM, DynL and some Wide C1/C2) will start and end closer to the center of the frame
            $focusBoxWidth = int($focusBoxWidth * 2 / 3) if $imageArea == $DX or $imageArea == $OneToOne ;

            my $skipPositions = int($focusBoxWidth / 2);   #jump over half the width of the focus box

            my $result =  $start + $increment * ($roundedValOverDivisor + $skipPositions  - 1 ) ;

            return $result;
        ", &cv, &ctx) { cv = x; }
            tags.push(mk(
                "AFAreaInitialXPosition",
                0xbe8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96"))
    {
        if let Some(v) = i8_at(data, 0xbe9) {
            dm.push((
                "AFAreaInitialYPosition".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("
            my $imageArea = $$self{ImageArea};
            my $afAreaMode = $$self{VALUE}{AFAreaMode};

            my $FX = 0;            #image size 8256 x 5504
            my $DX = 1;            #image size 5392 x 3592
            my $WideScreen = 4;    #16:9 image area, image size 8256x4640
            my $OneToOne = 8;      #1:1 image area, image size 5504x5504

            my $Single = 1;
            my $Dynamic = 2;
            my $WideS = 3;
            my $WideL = 4;
            my $ThreeD = 5;
            my $Auto = 6;
            my $WideC1 = 12;

            my $DynamicS = 0;
            my $DynamicM = 1;
            my $DynamicL = 2;

            my $start = 424;           #FX, 16:9 & 1:1 formats
            my $increment = 291;       #FX, & 16:9 formats
            $start = $start + $increment if $imageArea == $WideScreen and $val > 0;

            if ($imageArea == $DX) {    # DX results are in FX coordinate system to match reporting of ($AFAreaXPosition , $AFAreaYPosition)
                $start = 572;
                $increment = 436;
                if ( $afAreaMode == $WideS ) {  #Wide S focus box is a strange size
                    $start = 542;
                    $increment = 442;
                }
            }

            my $divisor = 6.67;
            $divisor = 10.01 if $imageArea == $DX ;   #extra .01 to ensure $val of 10*n+5 rounds down
            $divisor = 8.01 if $imageArea == $WideScreen ;

            my $roundedValOverDivisor = sprintf(\"%.0f\", $val / $divisor);   #round to nearest int

            my $focusBoxHeight = $$self{AFAreaInitialHeight}  ;    #wider focus boxes (e.g., DynM, DynL and some Wide C1/C2) will start and end closer to the center of the frame
            $focusBoxHeight = int($focusBoxHeight * 2 / 3) if $imageArea == $DX ;

            my $skipPositions = int($focusBoxHeight / 2);   #jump over half the height of the focus box

            my $result =  $start + $increment * ($roundedValOverDivisor + $skipPositions  - 1 ) ;
            return $result;
        ", &cv, &ctx) { cv = x; }
            tags.push(mk(
                "AFAreaInitialYPosition",
                0xbe9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96"))
    {
        if let Some(v) = u8_at(data, 0xbea) {
            dm.push(("AFAreaInitialWidth".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("1 + int ($val / 4)", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "AFAreaInitialWidth",
                    0xbea,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96"))
    {
        if let Some(v) = u8_at(data, 0xbeb) {
            dm.push(("AFAreaInitialHeight".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("1 + int ($val / 7) ", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "AFAreaInitialHeight",
                    0xbeb,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuInfoZ9` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menuinfoz9(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() < "03.00"))
    {
        if let Some(sub) = u32_at(data, 0x10, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_menusettingsz9(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if !(dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() < "03.00"))
        && (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
            && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() < "04.00"))
    {
        if let Some(sub) = u32_at(data, 0x10, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_menusettingsz9v3(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if !(dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() < "03.00"))
        && !(dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
            && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() < "04.00"))
    {
        if let Some(sub) = u32_at(data, 0x10, bo).and_then(|at| data.get(at as usize..)) {
            tags.extend(nikon_menusettingsz9v4(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuSettingsZ7II` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menusettingsz7ii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x5a) {
        dm.push(("SingleFrame".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "SingleFrame",
                0x5a,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x5c) {
        dm.push(("ReleaseMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("$$self{SingleFrame} == 0 ? 5 : $val", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Continuous Low".to_string(),
                1 => "Continuous High".to_string(),
                2 => "Continuous High (Extended)".to_string(),
                4 => "Timer".to_string(),
                5 => "Single Frame".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ReleaseMode", 0x5c, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u32_at(data, 0xa0, bo) {
        dm.push(("IntervalDurationHours".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "IntervalDurationHours",
            0xa0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xa4, bo) {
        dm.push((
            "IntervalDurationMinutes".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "IntervalDurationMinutes",
            0xa4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xa8, bo) {
        dm.push((
            "IntervalDurationSeconds".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "IntervalDurationSeconds",
            0xa8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xb0, bo) {
        dm.push((
            "IntervalShootingIntervals".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "Intervals",
                0xb0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0xb4, bo) {
        dm.push((
            "IntervalShootingShotsPerInterval".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "ShotsPerInterval",
                0xb4,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xb8) {
        dm.push((
            "IntervalExposureSmoothing".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "IntervalExposureSmoothing",
            0xb8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xba) {
        dm.push(("IntervalPriority".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("IntervalPriority", 0xba, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xdc) {
        dm.push(("FocusShiftNumberShots".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusShiftNumberShots",
            0xdc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe0) {
        dm.push(("FocusShiftStepWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusShiftStepWidth",
            0xe0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe4) {
        dm.push(("FocusShiftInterval".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "$val == 1? \"1 Second\" : sprintf(\"%.0f Seconds\",$val)",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "FocusShiftInterval",
            0xe4,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe8) {
        dm.push((
            "FocusShiftExposureLock".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusShiftExposureLock",
            0xe8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x142) {
        dm.push((
            "DiffractionCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DiffractionCompensation",
            0x142,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x143) {
        dm.push(("AutoDistortionControl".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoDistortionControl",
            0x143,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x146) {
        dm.push(("NikonMeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Matrix".to_string(),
            1 => "Center".to_string(),
            2 => "Spot".to_string(),
            3 => "Highlight".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "NikonMeteringMode",
            0x146,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x148) {
        dm.push(("FlashControlMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Auto External Flash".to_string(),
                2 => "GN (distance priority)".to_string(),
                3 => "Manual".to_string(),
                4 => "Repeating Flash".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlMode",
                0x148,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x14e) {
            dm.push(("FlashGNDistance".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v >= 3.0) {
        if let Some(v) = u8_at(data, 0x152) {
            dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x15a) {
        dm.push(("FlashWirelessOption".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x15c) {
        dm.push(("FlashRemoteControl".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x160) {
        dm.push((
            "FlashGroupOptionsMasterMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Manual".to_string(),
                2 => "Auto".to_string(),
                3 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashMasterControlMode",
                0x160,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashGroupOptionsMasterMode").is_some_and(|v| v != 3.0) {
        if let Some(v) = i8_at(data, 0x162) {
            dm.push((
                "FlashMasterCompensation".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if dm_get(dm, "FlashGroupOptionsMasterMode").is_some_and(|v| v == 1.0) {
        if let Some(v) = u8_at(data, 0x166) {
            dm.push(("FlashMasterOutput".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x1f6) {
        dm.push(("MovieFrameSize".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1f8) {
        dm.push(("MovieFrameRate".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1fa) {
        dm.push(("MovieSlowMotion".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1fe) {
        dm.push(("MovieType".to_string(), Conv::Num(f64::from(v))));
    }
    if MODEL_RE_22.is_match(model) {
        if let Some(v) = u8_at(data, 0x204) {
            dm.push((
                "MovieISOAutoManualMode".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "MovieISOAutoManualMode",
                0x204,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x238) {
        dm.push(("MovieActiveD-Lighting".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x23c) {
        dm.push((
            "MovieHighISONoiseReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x23e) {
        dm.push(("MovieVignetteControl".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x240) {
        dm.push((
            "MovieVignetteControlSameAsPhoto".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x241) {
        dm.push((
            "MovieDiffractionCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x242) {
        dm.push((
            "MovieAutoDistortionControl".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x248) {
        dm.push(("MovieFocusMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x24e) {
        dm.push((
            "MovieVibrationReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x24f) {
        dm.push((
            "MovieVibrationReductionSameAsPhoto".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if (dm_val(dm, "HDMIBitDepth").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "HDMIBitDepth").is_some_and(|v| v == 2.0))
    {
        if let Some(v) = u8_at(data, 0x35a) {
            dm.push(("HDMIOutputNLog".to_string(), Conv::Num(f64::from(v))));
        }
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuSettingsZ8v1` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menusettingsz8v1(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x694) {
        dm.push(("TimeZone".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "+10:00 (Sydney)".to_string(),
            5 => "+09:00 (Tokyo)".to_string(),
            6 => "+08:00 (Beijing, Honk Kong, Sinapore)".to_string(),
            10 => "+05:45 (Kathmandu)".to_string(),
            11 => "+05:30 (New Dehli)".to_string(),
            12 => "+05:00 (Islamabad)".to_string(),
            13 => "+04:30 (Kabul)".to_string(),
            14 => "+04:00 (Abu Dhabi)".to_string(),
            15 => "+03:30 (Tehran)".to_string(),
            16 => "+03:00 (Moscow, Nairobi)".to_string(),
            17 => "+02:00 (Athens, Helsinki)".to_string(),
            18 => "+01:00 (Madrid, Paris, Berlin)".to_string(),
            19 => "+00:00 (London)".to_string(),
            20 => "-01:00 (Azores)".to_string(),
            21 => "-02:00 (Fernando de Noronha)".to_string(),
            22 => "-03:00 (Buenos Aires, Sao Paulo)".to_string(),
            23 => "-03:30 (Newfoundland)".to_string(),
            24 => "-04:00 (Manaus, Caracas)".to_string(),
            25 => "-05:00 (New York, Toronto, Lima)".to_string(),
            26 => "-06:00 (Chicago, Mexico City)".to_string(),
            27 => "-07:00 (Denver)".to_string(),
            28 => "-08:00 (Los Angeles, Vancouver)".to_string(),
            29 => "-09:00 (Anchorage)".to_string(),
            30 => "-10:00 (Hawaii)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("TimeZone", 0x694, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x69a) {
        dm.push(("MonitorBrightness".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x6a2) {
        dm.push(("Language".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x6b0) {
        dm.push(("AFFineTune".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u16_at(data, 0x6b4, bo) {
        dm.push((
            "NonCPULens1FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6b6, bo) {
        dm.push((
            "NonCPULens2FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6b8, bo) {
        dm.push((
            "NonCPULens3FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6ba, bo) {
        dm.push((
            "NonCPULens4FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6bc, bo) {
        dm.push((
            "NonCPULens5FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6be, bo) {
        dm.push((
            "NonCPULens6FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6c0, bo) {
        dm.push((
            "NonCPULens7FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6c2, bo) {
        dm.push((
            "NonCPULens8FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6c4, bo) {
        dm.push((
            "NonCPULens9FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6c6, bo) {
        dm.push((
            "NonCPULens10FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6c8, bo) {
        dm.push((
            "NonCPULens11FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6ca, bo) {
        dm.push((
            "NonCPULens12FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6cc, bo) {
        dm.push((
            "NonCPULens13FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6ce, bo) {
        dm.push((
            "NonCPULens14FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6d0, bo) {
        dm.push((
            "NonCPULens15FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6d2, bo) {
        dm.push((
            "NonCPULens16FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6d4, bo) {
        dm.push((
            "NonCPULens17FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6d6, bo) {
        dm.push((
            "NonCPULens18FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6d8, bo) {
        dm.push((
            "NonCPULens19FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6da, bo) {
        dm.push((
            "NonCPULens20FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6dc, bo) {
        dm.push((
            "NonCPULens1MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6de, bo) {
        dm.push((
            "NonCPULens2MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6e0, bo) {
        dm.push((
            "NonCPULens3MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6e2, bo) {
        dm.push((
            "NonCPULens4MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6e4, bo) {
        dm.push((
            "NonCPULens5MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6e6, bo) {
        dm.push((
            "NonCPULens6MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6e8, bo) {
        dm.push((
            "NonCPULens7MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6ea, bo) {
        dm.push((
            "NonCPULens8MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6ec, bo) {
        dm.push((
            "NonCPULens9MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6ee, bo) {
        dm.push((
            "NonCPULens10MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6f0, bo) {
        dm.push((
            "NonCPULens11MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6f2, bo) {
        dm.push((
            "NonCPULens12MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6f4, bo) {
        dm.push((
            "NonCPULens13MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6f6, bo) {
        dm.push((
            "NonCPULens14MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6f8, bo) {
        dm.push((
            "NonCPULens15MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6fa, bo) {
        dm.push((
            "NonCPULens16MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6fc, bo) {
        dm.push((
            "NonCPULens17MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6fe, bo) {
        dm.push((
            "NonCPULens18MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x700, bo) {
        dm.push((
            "NonCPULens19MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x702, bo) {
        dm.push((
            "NonCPULens20MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x710) {
        dm.push(("HDMIOutputResolution".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "4320p".to_string(),
            2 => "2160p".to_string(),
            3 => "1080p".to_string(),
            4 => "1080i".to_string(),
            5 => "720p".to_string(),
            6 => "576p".to_string(),
            7 => "480p".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HDMIOutputResolution",
            0x710,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x722) {
        dm.push(("AirplaneMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x723) {
        dm.push(("EmptySlotRelease".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x746) {
        dm.push(("EnergySavingMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x762) {
        dm.push(("USBPowerDelivery".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x76b) {
        dm.push(("SensorShield".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(sub) = data.get(0x0..0x0 + 943) {
        tags.extend(nikon_menusettingsz8(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = data.get(0x3af..0x3af + 730) {
        tags.extend(nikoncustom_settingsz8(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuSettingsZ8v2` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menusettingsz8v2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x6a2) {
        dm.push(("Language".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x6a4) {
        dm.push(("TimeZone".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "+10:00 (Sydney)".to_string(),
            5 => "+09:00 (Tokyo)".to_string(),
            6 => "+08:00 (Beijing, Honk Kong, Sinapore)".to_string(),
            10 => "+05:45 (Kathmandu)".to_string(),
            11 => "+05:30 (New Dehli)".to_string(),
            12 => "+05:00 (Islamabad)".to_string(),
            13 => "+04:30 (Kabul)".to_string(),
            14 => "+04:00 (Abu Dhabi)".to_string(),
            15 => "+03:30 (Tehran)".to_string(),
            16 => "+03:00 (Moscow, Nairobi)".to_string(),
            17 => "+02:00 (Athens, Helsinki)".to_string(),
            18 => "+01:00 (Madrid, Paris, Berlin)".to_string(),
            19 => "+00:00 (London)".to_string(),
            20 => "-01:00 (Azores)".to_string(),
            21 => "-02:00 (Fernando de Noronha)".to_string(),
            22 => "-03:00 (Buenos Aires, Sao Paulo)".to_string(),
            23 => "-03:30 (Newfoundland)".to_string(),
            24 => "-04:00 (Manaus, Caracas)".to_string(),
            25 => "-05:00 (New York, Toronto, Lima)".to_string(),
            26 => "-06:00 (Chicago, Mexico City)".to_string(),
            27 => "-07:00 (Denver)".to_string(),
            28 => "-08:00 (Los Angeles, Vancouver)".to_string(),
            29 => "-09:00 (Anchorage)".to_string(),
            30 => "-10:00 (Hawaii)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("TimeZone", 0x6a4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6aa) {
        dm.push(("MonitorBrightness".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x6c0) {
        dm.push(("AFFineTune".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u16_at(data, 0x6c4, bo) {
        dm.push((
            "NonCPULens1FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6c6, bo) {
        dm.push((
            "NonCPULens2FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6c8, bo) {
        dm.push((
            "NonCPULens3FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6ca, bo) {
        dm.push((
            "NonCPULens4FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6cc, bo) {
        dm.push((
            "NonCPULens5FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6ce, bo) {
        dm.push((
            "NonCPULens6FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6d0, bo) {
        dm.push((
            "NonCPULens7FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6d2, bo) {
        dm.push((
            "NonCPULens8FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6d4, bo) {
        dm.push((
            "NonCPULens9FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6d6, bo) {
        dm.push((
            "NonCPULens10FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6d8, bo) {
        dm.push((
            "NonCPULens11FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6da, bo) {
        dm.push((
            "NonCPULens12FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6dc, bo) {
        dm.push((
            "NonCPULens13FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6de, bo) {
        dm.push((
            "NonCPULens14FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6e0, bo) {
        dm.push((
            "NonCPULens15FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6e2, bo) {
        dm.push((
            "NonCPULens16FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6e4, bo) {
        dm.push((
            "NonCPULens17FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6e6, bo) {
        dm.push((
            "NonCPULens18FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6e8, bo) {
        dm.push((
            "NonCPULens19FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x6ea, bo) {
        dm.push((
            "NonCPULens20FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x714, bo) {
        dm.push((
            "NonCPULens1MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x718, bo) {
        dm.push((
            "NonCPULens2MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x71c, bo) {
        dm.push((
            "NonCPULens3MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x720, bo) {
        dm.push((
            "NonCPULens4MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x724, bo) {
        dm.push((
            "NonCPULens5MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x728, bo) {
        dm.push((
            "NonCPULens6MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x72c, bo) {
        dm.push((
            "NonCPULens7MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x730, bo) {
        dm.push((
            "NonCPULens8MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x734, bo) {
        dm.push((
            "NonCPULens9MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x738, bo) {
        dm.push((
            "NonCPULens10MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x73c, bo) {
        dm.push((
            "NonCPULens11MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x740, bo) {
        dm.push((
            "NonCPULens12MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x744, bo) {
        dm.push((
            "NonCPULens13MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x748, bo) {
        dm.push((
            "NonCPULens14MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x74c, bo) {
        dm.push((
            "NonCPULens15MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x750, bo) {
        dm.push((
            "NonCPULens16MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x754, bo) {
        dm.push((
            "NonCPULens17MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x758, bo) {
        dm.push((
            "NonCPULens18MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x75c, bo) {
        dm.push((
            "NonCPULens19MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u32_at(data, 0x760, bo) {
        dm.push((
            "NonCPULens20MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x770) {
        dm.push(("HDMIOutputResolution".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "4320p".to_string(),
            2 => "2160p".to_string(),
            3 => "1080p".to_string(),
            4 => "1080i".to_string(),
            5 => "720p".to_string(),
            6 => "576p".to_string(),
            7 => "480p".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HDMIOutputResolution",
            0x770,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x782) {
        dm.push(("AirplaneMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x783) {
        dm.push(("EmptySlotRelease".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x7a6) {
        dm.push(("EnergySavingMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x7c2) {
        dm.push(("USBPowerDelivery".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x7cb) {
        dm.push(("SensorShield".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x7fe) {
        dm.push(("PixelShiftShooting".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                2 => "On (Series)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "PixelShiftShooting",
                0x7fe,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "PixelShiftShooting").is_some_and(|v| v > 0.0) {
        if let Some(v) = u8_at(data, 0x800) {
            dm.push(("PixelShiftNumberShots".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "4".to_string(),
                1 => "8".to_string(),
                2 => "16".to_string(),
                3 => "32".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "PixelShiftNumberShots",
                0x800,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "PixelShiftShooting").is_some_and(|v| v > 0.0) {
        if let Some(v) = u8_at(data, 0x802) {
            dm.push(("PixelShiftDelay".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "1 s".to_string(),
                2 => "2 s".to_string(),
                3 => "3 s".to_string(),
                4 => "5 s".to_string(),
                5 => "10 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("PixelShiftDelay", 0x802, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x804) {
        dm.push(("PlaybackButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PlaybackButton",
            0x804,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x806) {
        dm.push(("WBButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WBButton",
            0x806,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x808) {
        dm.push(("BracketButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "BracketButton",
            0x808,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x80a) {
        dm.push((
            "LensFunc1ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LensFunc1ButtonPlaybackMode",
            0x80a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x80c) {
        dm.push((
            "LensFunc2ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LensFunc2ButtonPlaybackMode",
            0x80c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x80e) {
        dm.push((
            "PlaybackButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PlaybackButtonPlaybackMode",
            0x80e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x810) {
        dm.push((
            "BracketButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "BracketButtonPlaybackMode",
            0x810,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x89e) {
        dm.push(("MaximumApertureLV".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x8a0) {
        dm.push(("ReleaseModeButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ReleaseModeButton",
            0x8a0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8a8) {
        dm.push((
            "ReleaseModeButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ReleaseModeButtonPlaybackMode",
            0x8a8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x0..0x0 + 943) {
        tags.extend(nikon_menusettingsz8(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = data.get(0x3af..0x3af + 755) {
        tags.extend(nikoncustom_settingsz8(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuSettingsZ9` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menusettingsz9(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x8c) {
        dm.push(("MultipleExposureMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                2 => "On (Series)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "MultipleExposureMode",
                0x8c,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "MultipleExposureMode").is_some_and(|v| v != 0.0) {
        if let Some(v) = u8_at(data, 0x8e) {
            dm.push(("MultiExposureShots".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "MultiExposureShots",
                0x8e,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u32_at(data, 0xbc, bo) {
            dm.push((
                "IntervalShootingIntervals".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "Intervals",
                    0xbc,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u32_at(data, 0xc0, bo) {
            dm.push((
                "IntervalShootingShotsPerInterval".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "ShotsPerInterval",
                    0xc0,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xe8) {
            dm.push(("FocusShiftNumberShots".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "FocusShiftNumberShots",
                    0xe8,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xec) {
            dm.push(("FocusShiftStepWidth".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FocusShiftStepWidth",
                0xec,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xf0) {
            dm.push(("FocusShiftInterval".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val == 1? \"1 Second\" : sprintf(\"%.0f Seconds\",$val)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "FocusShiftInterval",
                0xf0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xf4) {
            dm.push((
                "FocusShiftExposureLock".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x112) {
        dm.push(("PhotoShootingMenuBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PhotoShootingMenuBank",
            0x112,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x114) {
        dm.push(("ExtendedMenuBanks".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExtendedMenuBanks",
            0x114,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x134) {
        dm.push(("ImageArea".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "FX".to_string(),
                1 => "DX".to_string(),
                4 => "16:9".to_string(),
                8 => "1:1".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "PhotoShootingMenuBankImageArea",
                0x134,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x142) {
        dm.push(("AutoISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoISO", 0x142, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x144, bo) {
        dm.push(("ISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u16_at(data, 0x146, bo) {
        dm.push(("ISOAutoFlashLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i16_at(data, 0x14e, bo) {
        dm.push(("ISOAutoShutterTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("$val / 8", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                -15 => "Auto".to_string(),
                -12 => "15 s".to_string(),
                -9 => "8 s".to_string(),
                -6 => "4 s".to_string(),
                -3 => "2 s".to_string(),
                0 => "1 s".to_string(),
                1 => "1/1.3 s".to_string(),
                2 => "1/1.6 s".to_string(),
                3 => "1/2 s".to_string(),
                4 => "1/2.5 s".to_string(),
                5 => "1/12000 s".to_string(),
                6 => "1/4 s".to_string(),
                7 => "1/5 s".to_string(),
                8 => "1/6 s".to_string(),
                9 => "1/8 s".to_string(),
                10 => "1/10 s".to_string(),
                11 => "1/13 s".to_string(),
                12 => "1/15 s".to_string(),
                13 => "1/20 s".to_string(),
                14 => "1/25 s".to_string(),
                15 => "1/30 s".to_string(),
                16 => "1/40 s".to_string(),
                17 => "1/50 s".to_string(),
                18 => "1/60 s".to_string(),
                19 => "1/80 s".to_string(),
                20 => "1/100 s".to_string(),
                21 => "1/120 s".to_string(),
                22 => "1/160 s".to_string(),
                23 => "1/200 s".to_string(),
                24 => "1/250 s".to_string(),
                25 => "1/320 s".to_string(),
                26 => "1/400 s".to_string(),
                27 => "1/500 s".to_string(),
                28 => "1/640 s".to_string(),
                29 => "1/800 s".to_string(),
                30 => "1/1000 s".to_string(),
                31 => "1/1250 s".to_string(),
                32 => "1/1600 s".to_string(),
                33 => "1/2000 s".to_string(),
                34 => "1/2500 s".to_string(),
                35 => "1/3200 s".to_string(),
                36 => "1/4000 s".to_string(),
                37 => "1/5000 s".to_string(),
                38 => "1/6400 s".to_string(),
                39 => "1/8000 s".to_string(),
                40 => "1/10000 s".to_string(),
                41 => "1/13000 s".to_string(),
                42 => "1/16000 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "ISOAutoShutterTime",
                0x14e,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1a0) {
        dm.push(("MovieVignetteControl".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1a2) {
        dm.push((
            "DiffractionCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DiffractionCompensation",
            0x1a2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1a4) {
        dm.push((
            "FlickerReductionShooting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlickerReductionShooting",
            0x1a4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1a8) {
        dm.push(("FlashControlMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Auto External Flash".to_string(),
                2 => "GN (distance priority)".to_string(),
                3 => "Manual".to_string(),
                4 => "Repeating Flash".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlMode",
                0x1a8,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x1aa) {
        dm.push((
            "FlashMasterCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x1ae) {
            dm.push(("FlashGNDistance".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v >= 3.0) {
        if let Some(v) = u8_at(data, 0x1b2) {
            dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x1bc) {
        dm.push(("FlashRemoteControl".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1c8) {
        dm.push(("FlashWirelessOption".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x210) {
        dm.push(("AFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Pinpoint".to_string(),
                1 => "Single".to_string(),
                2 => "Dynamic".to_string(),
                3 => "Wide (S)".to_string(),
                4 => "Wide (L)".to_string(),
                5 => "3D".to_string(),
                6 => "Auto".to_string(),
                11 => "Subject Tracking".to_string(),
                12 => "Wide (C1)".to_string(),
                13 => "Wide (C2)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFAreaMode", 0x210, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x212) {
        dm.push(("VRMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Normal".to_string(),
            2 => "Sport".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("VRMode", 0x212, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x216) {
        dm.push(("BracketSet".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "AE/Flash".to_string(),
                1 => "AE".to_string(),
                2 => "Flash".to_string(),
                3 => "White Balance".to_string(),
                4 => "Active-D Lighting".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("BracketSet", 0x216, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "BracketSet").is_some_and(|v| v < 3.0) {
        if let Some(v) = u8_at(data, 0x218) {
            dm.push(("BracketProgram".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Disabled".to_string(),
                2 => "2F".to_string(),
                3 => "3F".to_string(),
                4 => "4F".to_string(),
                5 => "5F".to_string(),
                7 => "7F".to_string(),
                9 => "9F".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("BracketProgram", 0x218, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "BracketSet").is_some_and(|v| v < 3.0) {
        if let Some(v) = u8_at(data, 0x21a) {
            dm.push(("BracketIncrement".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "0.3".to_string(),
                1 => "0.5".to_string(),
                2 => "0.7".to_string(),
                3 => "1.0".to_string(),
                4 => "2.0".to_string(),
                5 => "3.0".to_string(),
                6 => "1.3".to_string(),
                7 => "1.5".to_string(),
                8 => "1.7".to_string(),
                9 => "2.3".to_string(),
                10 => "2.5".to_string(),
                11 => "2.7".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "BracketIncrement",
                0x21a,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x22c) {
        dm.push(("SecondarySlotFunction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Overflow".to_string(),
            1 => "Backup".to_string(),
            2 => "NEF Primary + JPG Secondary".to_string(),
            3 => "JPG Primary + JPG Secondary".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SecondarySlotFunction",
            0x22c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x23c) {
        dm.push(("DXCropAlert".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DXCropAlert", 0x23c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x23e) {
        dm.push(("SubjectDetection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Auto".to_string(),
            2 => "People".to_string(),
            3 => "Animals".to_string(),
            4 => "Vehicles".to_string(),
            5 => "Birds".to_string(),
            6 => "Airplanes".to_string(),
            7 => "Faces".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubjectDetection",
            0x23e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_get(dm, "AFAreaMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x240) {
            dm.push(("DynamicAFAreaSize".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                let s = match base.as_num() as i64 {
                    0 => "Small".to_string(),
                    1 => "Medium".to_string(),
                    2 => "Large".to_string(),
                    other => other.to_string(),
                };
                #[allow(clippy::cast_possible_truncation)]
                let raw = Value::I32(base.as_num() as i32);
                tags.push(mk(
                    "DynamicAFAreaSize",
                    0x240,
                    s,
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x25c) {
        let v = v & 0x1;
        dm.push(("MovieImageArea".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x266) {
        dm.push(("MovieType".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u16_at(data, 0x268, bo) {
        dm.push(("MovieISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x26a) {
        dm.push((
            "MovieISOAutoControlManualMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x26c, bo) {
        dm.push((
            "MovieISOAutoManualMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x2b8) {
        dm.push(("MovieActiveD-Lighting".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2ba) {
        dm.push((
            "MovieHighISONoiseReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x2c0) {
        dm.push(("MovieFlickerReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "50Hz".to_string(),
            2 => "60Hz".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieFlickerReduction",
            0x2c0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2c2) {
        dm.push(("MovieMeteringMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2c4) {
        dm.push(("MovieFocusMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2c6) {
        dm.push(("MovieAFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Pinpoint".to_string(),
            1 => "Single".to_string(),
            2 => "Dynamic".to_string(),
            3 => "Wide (S)".to_string(),
            4 => "Wide (L)".to_string(),
            5 => "3D".to_string(),
            6 => "Auto".to_string(),
            11 => "Subject Tracking".to_string(),
            12 => "Wide (C1)".to_string(),
            13 => "Wide (C2)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MovieAFAreaMode", 0x2c6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2c8) {
        dm.push(("MovieVRMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2cc) {
        dm.push(("MovieElectronicVR".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2ce) {
        dm.push(("MovieSoundRecording".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2d0) {
        dm.push(("MicrophoneSensitivity".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2d2) {
        dm.push(("MicrophoneAttenuator".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2d4) {
        dm.push((
            "MicrophoneFrequencyResponse".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x2d6) {
        dm.push(("WindNoiseReduction".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2ec) {
        dm.push(("MovieToneMap".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2f2) {
        dm.push(("MovieFrameSize".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2f4) {
        dm.push(("MovieFrameRate".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2fa) {
        dm.push(("MicrophoneJackPower".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2fb) {
        dm.push(("MovieDXCropAlert".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2fc) {
        dm.push(("MovieSubjectDetection".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x592) {
        dm.push(("Language".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x594) {
        dm.push(("TimeZone".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "+10:00 (Sydney)".to_string(),
            5 => "+09:00 (Tokyo)".to_string(),
            6 => "+08:00 (Beijing, Honk Kong, Sinapore)".to_string(),
            10 => "+05:45 (Kathmandu)".to_string(),
            11 => "+05:30 (New Dehli)".to_string(),
            12 => "+05:00 (Islamabad)".to_string(),
            13 => "+04:30 (Kabul)".to_string(),
            14 => "+04:00 (Abu Dhabi)".to_string(),
            15 => "+03:30 (Tehran)".to_string(),
            16 => "+03:00 (Moscow, Nairobi)".to_string(),
            17 => "+02:00 (Athens, Helsinki)".to_string(),
            18 => "+01:00 (Madrid, Paris, Berlin)".to_string(),
            19 => "+00:00 (London)".to_string(),
            20 => "-01:00 (Azores)".to_string(),
            21 => "-02:00 (Fernando de Noronha)".to_string(),
            22 => "-03:00 (Buenos Aires, Sao Paulo)".to_string(),
            23 => "-03:30 (Newfoundland)".to_string(),
            24 => "-04:00 (Manaus, Caracas)".to_string(),
            25 => "-05:00 (New York, Toronto, Lima)".to_string(),
            26 => "-06:00 (Chicago, Mexico City)".to_string(),
            27 => "-07:00 (Denver)".to_string(),
            28 => "-08:00 (Los Angeles, Vancouver)".to_string(),
            29 => "-09:00 (Anchorage)".to_string(),
            30 => "-10:00 (Hawaii)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("TimeZone", 0x594, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x59a) {
        dm.push(("MonitorBrightness".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x5b0) {
        dm.push(("AFFineTune".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x610) {
        dm.push(("HDMIOutputResolution".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "4320p".to_string(),
            2 => "2160p".to_string(),
            3 => "1080p".to_string(),
            4 => "1080i".to_string(),
            5 => "720p".to_string(),
            6 => "576p".to_string(),
            7 => "480p".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HDMIOutputResolution",
            0x610,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x61d) {
        dm.push((
            "SetClockFromLocationData".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x624) {
        dm.push(("AirplaneMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x625) {
        dm.push(("EmptySlotRelease".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x648) {
        dm.push(("EnergySavingMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x660) {
        dm.push(("RecordLocationData".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x664) {
        dm.push(("USBPowerDelivery".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x66d) {
        dm.push(("SensorShield".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(sub) = data.get(0x31f..0x31f + 608) {
        tags.extend(nikoncustom_settingsz9(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuSettingsZ9v3` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menusettingsz9v3(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x48) {
        dm.push(("HighFrameRate".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "CH".to_string(),
            2 => "CH*".to_string(),
            3 => "C30".to_string(),
            4 => "C120".to_string(),
            5 => "C60".to_string(),
            6 => "C15".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("HighFrameRate", 0x48, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x9a) {
        dm.push(("MultipleExposureMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                2 => "On (Series)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "MultipleExposureMode",
                0x9a,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "MultipleExposureMode").is_some_and(|v| v != 0.0) {
        if let Some(v) = u8_at(data, 0x9c) {
            dm.push(("MultiExposureShots".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "MultiExposureShots",
                0x9c,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u32_at(data, 0xcc, bo) {
            dm.push((
                "IntervalShootingIntervals".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "Intervals",
                    0xcc,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u32_at(data, 0xd0, bo) {
            dm.push((
                "IntervalShootingShotsPerInterval".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "ShotsPerInterval",
                    0xd0,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xf8) {
            dm.push(("FocusShiftNumberShots".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "FocusShiftNumberShots",
                    0xf8,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xfc) {
            dm.push(("FocusShiftStepWidth".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FocusShiftStepWidth",
                0xfc,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0x100) {
            dm.push(("FocusShiftInterval".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val == 1? \"1 Second\" : sprintf(\"%.0f Seconds\",$val)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "FocusShiftInterval",
                0x100,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0x104) {
            dm.push((
                "FocusShiftExposureLock".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x122) {
        dm.push(("PhotoShootingMenuBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PhotoShootingMenuBank",
            0x122,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x124) {
        dm.push(("ExtendedMenuBanks".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExtendedMenuBanks",
            0x124,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x148) {
        dm.push(("ImageArea".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "FX".to_string(),
                1 => "DX".to_string(),
                4 => "16:9".to_string(),
                8 => "1:1".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "PhotoShootingMenuBankImageArea",
                0x148,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x156) {
        dm.push(("AutoISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoISO", 0x156, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x158, bo) {
        dm.push(("ISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u16_at(data, 0x15a, bo) {
        dm.push(("ISOAutoFlashLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i16_at(data, 0x162, bo) {
        dm.push(("ISOAutoShutterTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("$val / 8", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                -15 => "Auto".to_string(),
                -12 => "15 s".to_string(),
                -9 => "8 s".to_string(),
                -6 => "4 s".to_string(),
                -3 => "2 s".to_string(),
                0 => "1 s".to_string(),
                1 => "1/1.3 s".to_string(),
                2 => "1/1.6 s".to_string(),
                3 => "1/2 s".to_string(),
                4 => "1/2.5 s".to_string(),
                5 => "1/12000 s".to_string(),
                6 => "1/4 s".to_string(),
                7 => "1/5 s".to_string(),
                8 => "1/6 s".to_string(),
                9 => "1/8 s".to_string(),
                10 => "1/10 s".to_string(),
                11 => "1/13 s".to_string(),
                12 => "1/15 s".to_string(),
                13 => "1/20 s".to_string(),
                14 => "1/25 s".to_string(),
                15 => "1/30 s".to_string(),
                16 => "1/40 s".to_string(),
                17 => "1/50 s".to_string(),
                18 => "1/60 s".to_string(),
                19 => "1/80 s".to_string(),
                20 => "1/100 s".to_string(),
                21 => "1/120 s".to_string(),
                22 => "1/160 s".to_string(),
                23 => "1/200 s".to_string(),
                24 => "1/250 s".to_string(),
                25 => "1/320 s".to_string(),
                26 => "1/400 s".to_string(),
                27 => "1/500 s".to_string(),
                28 => "1/640 s".to_string(),
                29 => "1/800 s".to_string(),
                30 => "1/1000 s".to_string(),
                31 => "1/1250 s".to_string(),
                32 => "1/1600 s".to_string(),
                33 => "1/2000 s".to_string(),
                34 => "1/2500 s".to_string(),
                35 => "1/3200 s".to_string(),
                36 => "1/4000 s".to_string(),
                37 => "1/5000 s".to_string(),
                38 => "1/6400 s".to_string(),
                39 => "1/8000 s".to_string(),
                40 => "1/10000 s".to_string(),
                41 => "1/13000 s".to_string(),
                42 => "1/16000 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "ISOAutoShutterTime",
                0x162,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1b4) {
        dm.push(("MovieVignetteControl".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1b6) {
        dm.push((
            "DiffractionCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DiffractionCompensation",
            0x1b6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b8) {
        dm.push((
            "FlickerReductionShooting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlickerReductionShooting",
            0x1b8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1bc) {
        dm.push(("FlashControlMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Auto External Flash".to_string(),
                2 => "GN (distance priority)".to_string(),
                3 => "Manual".to_string(),
                4 => "Repeating Flash".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlMode",
                0x1bc,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x1be) {
        dm.push((
            "FlashMasterCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x1c2) {
            dm.push(("FlashGNDistance".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v >= 3.0) {
        if let Some(v) = u8_at(data, 0x1c6) {
            dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x224) {
        dm.push(("AFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Pinpoint".to_string(),
                1 => "Single".to_string(),
                2 => "Dynamic".to_string(),
                3 => "Wide (S)".to_string(),
                4 => "Wide (L)".to_string(),
                5 => "3D".to_string(),
                6 => "Auto".to_string(),
                11 => "Subject Tracking".to_string(),
                12 => "Wide (C1)".to_string(),
                13 => "Wide (C2)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFAreaMode", 0x224, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x226) {
        dm.push(("VRMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Normal".to_string(),
            2 => "Sport".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("VRMode", 0x226, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x22a) {
        dm.push(("BracketSet".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "AE/Flash".to_string(),
                1 => "AE".to_string(),
                2 => "Flash".to_string(),
                3 => "White Balance".to_string(),
                4 => "Active-D Lighting".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("BracketSet", 0x22a, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "BracketSet").is_some_and(|v| v < 3.0) {
        if let Some(v) = u8_at(data, 0x22c) {
            dm.push(("BracketProgram".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Disabled".to_string(),
                2 => "2F".to_string(),
                3 => "3F".to_string(),
                4 => "4F".to_string(),
                5 => "5F".to_string(),
                7 => "7F".to_string(),
                9 => "9F".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("BracketProgram", 0x22c, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "BracketSet").is_some_and(|v| v < 3.0) {
        if let Some(v) = u8_at(data, 0x22e) {
            dm.push(("BracketIncrement".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "0.3".to_string(),
                1 => "0.5".to_string(),
                2 => "0.7".to_string(),
                3 => "1.0".to_string(),
                4 => "2.0".to_string(),
                5 => "3.0".to_string(),
                6 => "1.3".to_string(),
                7 => "1.5".to_string(),
                8 => "1.7".to_string(),
                9 => "2.3".to_string(),
                10 => "2.5".to_string(),
                11 => "2.7".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "BracketIncrement",
                0x22e,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x240) {
        dm.push(("SecondarySlotFunction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Overflow".to_string(),
            1 => "Backup".to_string(),
            2 => "NEF Primary + JPG Secondary".to_string(),
            3 => "JPG Primary + JPG Secondary".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SecondarySlotFunction",
            0x240,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x250) {
        dm.push(("DXCropAlert".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DXCropAlert", 0x250, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x252) {
        dm.push(("SubjectDetection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Auto".to_string(),
            2 => "People".to_string(),
            3 => "Animals".to_string(),
            4 => "Vehicles".to_string(),
            5 => "Birds".to_string(),
            6 => "Airplanes".to_string(),
            7 => "Faces".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubjectDetection",
            0x252,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_get(dm, "AFAreaMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x254) {
            dm.push(("DynamicAFAreaSize".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                let s = match base.as_num() as i64 {
                    0 => "Small".to_string(),
                    1 => "Medium".to_string(),
                    2 => "Large".to_string(),
                    other => other.to_string(),
                };
                #[allow(clippy::cast_possible_truncation)]
                let raw = Value::I32(base.as_num() as i32);
                tags.push(mk(
                    "DynamicAFAreaSize",
                    0x254,
                    s,
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x27c) {
        dm.push((
            "HighFrequencyFlickerReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x286) {
        let v = v & 0x1;
        dm.push(("MovieImageArea".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x290) {
        dm.push(("MovieType".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u16_at(data, 0x292, bo) {
        dm.push(("MovieISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x294) {
        dm.push((
            "MovieISOAutoControlManualMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x296, bo) {
        dm.push((
            "MovieISOAutoManualMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x2e0) {
        dm.push(("MovieActiveD-Lighting".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2e2) {
        dm.push((
            "MovieHighISONoiseReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x2e8) {
        dm.push(("MovieFlickerReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "50Hz".to_string(),
            2 => "60Hz".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieFlickerReduction",
            0x2e8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2ea) {
        dm.push(("MovieMeteringMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2ec) {
        dm.push(("MovieFocusMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2ee) {
        dm.push(("MovieAFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Pinpoint".to_string(),
            1 => "Single".to_string(),
            2 => "Dynamic".to_string(),
            3 => "Wide (S)".to_string(),
            4 => "Wide (L)".to_string(),
            5 => "3D".to_string(),
            6 => "Auto".to_string(),
            11 => "Subject Tracking".to_string(),
            12 => "Wide (C1)".to_string(),
            13 => "Wide (C2)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MovieAFAreaMode", 0x2ee, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2f0) {
        dm.push(("MovieVRMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2f4) {
        dm.push(("MovieElectronicVR".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2f6) {
        dm.push(("MovieSoundRecording".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2f8) {
        dm.push(("MicrophoneSensitivity".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2fa) {
        dm.push(("MicrophoneAttenuator".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2fc) {
        dm.push((
            "MicrophoneFrequencyResponse".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x2fe) {
        dm.push(("WindNoiseReduction".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x314) {
        dm.push(("MovieToneMap".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x31a) {
        dm.push(("MovieFrameSize".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x31c) {
        dm.push(("MovieFrameRate".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x322) {
        dm.push(("MicrophoneJackPower".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x323) {
        dm.push(("MovieDXCropAlert".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x324) {
        dm.push(("MovieSubjectDetection".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x32c) {
        dm.push(("MovieHighResZoom".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x5c2) {
        dm.push(("Language".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x5c4) {
        dm.push(("TimeZone".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "+10:00 (Sydney)".to_string(),
            5 => "+09:00 (Tokyo)".to_string(),
            6 => "+08:00 (Beijing, Honk Kong, Sinapore)".to_string(),
            10 => "+05:45 (Kathmandu)".to_string(),
            11 => "+05:30 (New Dehli)".to_string(),
            12 => "+05:00 (Islamabad)".to_string(),
            13 => "+04:30 (Kabul)".to_string(),
            14 => "+04:00 (Abu Dhabi)".to_string(),
            15 => "+03:30 (Tehran)".to_string(),
            16 => "+03:00 (Moscow, Nairobi)".to_string(),
            17 => "+02:00 (Athens, Helsinki)".to_string(),
            18 => "+01:00 (Madrid, Paris, Berlin)".to_string(),
            19 => "+00:00 (London)".to_string(),
            20 => "-01:00 (Azores)".to_string(),
            21 => "-02:00 (Fernando de Noronha)".to_string(),
            22 => "-03:00 (Buenos Aires, Sao Paulo)".to_string(),
            23 => "-03:30 (Newfoundland)".to_string(),
            24 => "-04:00 (Manaus, Caracas)".to_string(),
            25 => "-05:00 (New York, Toronto, Lima)".to_string(),
            26 => "-06:00 (Chicago, Mexico City)".to_string(),
            27 => "-07:00 (Denver)".to_string(),
            28 => "-08:00 (Los Angeles, Vancouver)".to_string(),
            29 => "-09:00 (Anchorage)".to_string(),
            30 => "-10:00 (Hawaii)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("TimeZone", 0x5c4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5ca) {
        dm.push(("MonitorBrightness".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x5e0) {
        dm.push(("AFFineTune".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x640) {
        dm.push(("HDMIOutputResolution".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "4320p".to_string(),
            2 => "2160p".to_string(),
            3 => "1080p".to_string(),
            4 => "1080i".to_string(),
            5 => "720p".to_string(),
            6 => "576p".to_string(),
            7 => "480p".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HDMIOutputResolution",
            0x640,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x64d) {
        dm.push((
            "SetClockFromLocationData".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x654) {
        dm.push(("AirplaneMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x655) {
        dm.push(("EmptySlotRelease".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x678) {
        dm.push(("EnergySavingMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x690) {
        dm.push(("RecordLocationData".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x694) {
        dm.push(("USBPowerDelivery".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x69d) {
        dm.push(("SensorShield".to_string(), Conv::Num(f64::from(v))));
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0x6da) {
            dm.push(("FocusShiftAutoReset".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x712) {
        dm.push(("PreReleaseBurstLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "0.3 Sec".to_string(),
            2 => "0.5 Sec".to_string(),
            3 => "1 Sec".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PreReleaseBurstLength",
            0x712,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x714) {
        dm.push((
            "PostReleaseBurstLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 Sec".to_string(),
            1 => "2 Sec".to_string(),
            2 => "3 Sec".to_string(),
            3 => "Max".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PostReleaseBurstLength",
            0x714,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x34f..0x34f + 608) {
        tags.extend(nikoncustom_settingsz9(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuSettingsZ9v4` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menusettingsz9v4(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x48) {
        dm.push(("HighFrameRate".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "CH".to_string(),
            2 => "CH*".to_string(),
            3 => "C30".to_string(),
            4 => "C120".to_string(),
            5 => "C60".to_string(),
            6 => "C15".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("HighFrameRate", 0x48, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x9a) {
        dm.push(("MultipleExposureMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                2 => "On (Series)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "MultipleExposureMode",
                0x9a,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "MultipleExposureMode").is_some_and(|v| v != 0.0) {
        if let Some(v) = u8_at(data, 0x9c) {
            dm.push(("MultiExposureShots".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "MultiExposureShots",
                0x9c,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u32_at(data, 0xcc, bo) {
            dm.push((
                "IntervalShootingIntervals".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "Intervals",
                    0xcc,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u32_at(data, 0xd0, bo) {
            dm.push((
                "IntervalShootingShotsPerInterval".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "ShotsPerInterval",
                    0xd0,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xf8) {
            dm.push(("FocusShiftNumberShots".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "FocusShiftNumberShots",
                    0xf8,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xfc) {
            dm.push(("FocusShiftStepWidth".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FocusShiftStepWidth",
                0xfc,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0x100) {
            dm.push(("FocusShiftInterval".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val == 1? \"1 Second\" : sprintf(\"%.0f Seconds\",$val)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "FocusShiftInterval",
                0x100,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0x104) {
            dm.push((
                "FocusShiftExposureLock".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x122) {
        dm.push(("PhotoShootingMenuBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PhotoShootingMenuBank",
            0x122,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x124) {
        dm.push(("ExtendedMenuBanks".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExtendedMenuBanks",
            0x124,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x148) {
        dm.push(("ImageArea".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "FX".to_string(),
                1 => "DX".to_string(),
                4 => "16:9".to_string(),
                8 => "1:1".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "PhotoShootingMenuBankImageArea",
                0x148,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x156) {
        dm.push(("AutoISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoISO", 0x156, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x158, bo) {
        dm.push(("ISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u16_at(data, 0x15a, bo) {
        dm.push(("ISOAutoFlashLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i16_at(data, 0x162, bo) {
        dm.push(("ISOAutoShutterTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("$val / 8", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                -15 => "Auto".to_string(),
                -12 => "15 s".to_string(),
                -9 => "8 s".to_string(),
                -6 => "4 s".to_string(),
                -3 => "2 s".to_string(),
                0 => "1 s".to_string(),
                1 => "1/1.3 s".to_string(),
                2 => "1/1.6 s".to_string(),
                3 => "1/2 s".to_string(),
                4 => "1/2.5 s".to_string(),
                5 => "1/12000 s".to_string(),
                6 => "1/4 s".to_string(),
                7 => "1/5 s".to_string(),
                8 => "1/6 s".to_string(),
                9 => "1/8 s".to_string(),
                10 => "1/10 s".to_string(),
                11 => "1/13 s".to_string(),
                12 => "1/15 s".to_string(),
                13 => "1/20 s".to_string(),
                14 => "1/25 s".to_string(),
                15 => "1/30 s".to_string(),
                16 => "1/40 s".to_string(),
                17 => "1/50 s".to_string(),
                18 => "1/60 s".to_string(),
                19 => "1/80 s".to_string(),
                20 => "1/100 s".to_string(),
                21 => "1/120 s".to_string(),
                22 => "1/160 s".to_string(),
                23 => "1/200 s".to_string(),
                24 => "1/250 s".to_string(),
                25 => "1/320 s".to_string(),
                26 => "1/400 s".to_string(),
                27 => "1/500 s".to_string(),
                28 => "1/640 s".to_string(),
                29 => "1/800 s".to_string(),
                30 => "1/1000 s".to_string(),
                31 => "1/1250 s".to_string(),
                32 => "1/1600 s".to_string(),
                33 => "1/2000 s".to_string(),
                34 => "1/2500 s".to_string(),
                35 => "1/3200 s".to_string(),
                36 => "1/4000 s".to_string(),
                37 => "1/5000 s".to_string(),
                38 => "1/6400 s".to_string(),
                39 => "1/8000 s".to_string(),
                40 => "1/10000 s".to_string(),
                41 => "1/13000 s".to_string(),
                42 => "1/16000 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "ISOAutoShutterTime",
                0x162,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1b4) {
        dm.push(("MovieVignetteControl".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1b6) {
        dm.push((
            "DiffractionCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DiffractionCompensation",
            0x1b6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b8) {
        dm.push((
            "FlickerReductionShooting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlickerReductionShooting",
            0x1b8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1bc) {
        dm.push(("FlashControlMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Auto External Flash".to_string(),
                2 => "GN (distance priority)".to_string(),
                3 => "Manual".to_string(),
                4 => "Repeating Flash".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlMode",
                0x1bc,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x1be) {
        dm.push((
            "FlashMasterCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x1c2) {
            dm.push(("FlashGNDistance".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if dm_get(dm, "FlashControlMode").is_some_and(|v| v >= 3.0) {
        if let Some(v) = u8_at(data, 0x1c6) {
            dm.push(("FlashOutput".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x224) {
        dm.push(("AFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Pinpoint".to_string(),
                1 => "Single".to_string(),
                2 => "Dynamic".to_string(),
                3 => "Wide (S)".to_string(),
                4 => "Wide (L)".to_string(),
                5 => "3D".to_string(),
                6 => "Auto".to_string(),
                11 => "Subject Tracking".to_string(),
                12 => "Wide (C1)".to_string(),
                13 => "Wide (C2)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFAreaMode", 0x224, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x226) {
        dm.push(("VRMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Normal".to_string(),
            2 => "Sport".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("VRMode", 0x226, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x22a) {
        dm.push(("BracketSet".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "AE/Flash".to_string(),
                1 => "AE".to_string(),
                2 => "Flash".to_string(),
                3 => "White Balance".to_string(),
                4 => "Active-D Lighting".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("BracketSet", 0x22a, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "BracketSet").is_some_and(|v| v < 3.0) {
        if let Some(v) = u8_at(data, 0x22c) {
            dm.push(("BracketProgram".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Disabled".to_string(),
                2 => "2F".to_string(),
                3 => "3F".to_string(),
                4 => "4F".to_string(),
                5 => "5F".to_string(),
                7 => "7F".to_string(),
                9 => "9F".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("BracketProgram", 0x22c, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "BracketSet").is_some_and(|v| v < 3.0) {
        if let Some(v) = u8_at(data, 0x22e) {
            dm.push(("BracketIncrement".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "0.3".to_string(),
                1 => "0.5".to_string(),
                2 => "0.7".to_string(),
                3 => "1.0".to_string(),
                4 => "2.0".to_string(),
                5 => "3.0".to_string(),
                6 => "1.3".to_string(),
                7 => "1.5".to_string(),
                8 => "1.7".to_string(),
                9 => "2.3".to_string(),
                10 => "2.5".to_string(),
                11 => "2.7".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "BracketIncrement",
                0x22e,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x23a) {
        dm.push(("HDR".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                2 => "On (Series)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("HDR", 0x23a, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x240) {
        dm.push(("SecondarySlotFunction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Overflow".to_string(),
            1 => "Backup".to_string(),
            2 => "NEF Primary + JPG Secondary".to_string(),
            3 => "JPG Primary + JPG Secondary".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SecondarySlotFunction",
            0x240,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_val(dm, "HDR").is_some_and(|v| v.as_string().as_str() != "0") {
        if let Some(v) = u8_at(data, 0x246) {
            dm.push(("HDRLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Auto".to_string(),
                1 => "Extra High".to_string(),
                2 => "High".to_string(),
                3 => "Normal".to_string(),
                4 => "Low".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("HDRLevel", 0x246, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x24a) {
        dm.push(("Slot2JpgSize".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x250) {
        dm.push(("DXCropAlert".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DXCropAlert", 0x250, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x252) {
        dm.push(("SubjectDetection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Auto".to_string(),
            2 => "People".to_string(),
            3 => "Animals".to_string(),
            4 => "Vehicles".to_string(),
            5 => "Birds".to_string(),
            6 => "Airplanes".to_string(),
            7 => "Faces".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubjectDetection",
            0x252,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_get(dm, "AFAreaMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x254) {
            dm.push(("DynamicAFAreaSize".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                let s = match base.as_num() as i64 {
                    0 => "Small".to_string(),
                    1 => "Medium".to_string(),
                    2 => "Large".to_string(),
                    other => other.to_string(),
                };
                #[allow(clippy::cast_possible_truncation)]
                let raw = Value::I32(base.as_num() as i32);
                tags.push(mk(
                    "DynamicAFAreaSize",
                    0x254,
                    s,
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x27c) {
        dm.push((
            "HighFrequencyFlickerReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x286) {
        let v = v & 0x1;
        dm.push(("MovieImageArea".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x290) {
        dm.push(("MovieType".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u16_at(data, 0x292, bo) {
        dm.push(("MovieISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x294) {
        dm.push((
            "MovieISOAutoControlManualMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x296, bo) {
        dm.push((
            "MovieISOAutoManualMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x2e0) {
        dm.push(("MovieActiveD-Lighting".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2e2) {
        dm.push((
            "MovieHighISONoiseReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x2e8) {
        dm.push(("MovieFlickerReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "50Hz".to_string(),
            2 => "60Hz".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieFlickerReduction",
            0x2e8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2ea) {
        dm.push(("MovieMeteringMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2ec) {
        dm.push(("MovieFocusMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2ee) {
        dm.push(("MovieAFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Pinpoint".to_string(),
            1 => "Single".to_string(),
            2 => "Dynamic".to_string(),
            3 => "Wide (S)".to_string(),
            4 => "Wide (L)".to_string(),
            5 => "3D".to_string(),
            6 => "Auto".to_string(),
            11 => "Subject Tracking".to_string(),
            12 => "Wide (C1)".to_string(),
            13 => "Wide (C2)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MovieAFAreaMode", 0x2ee, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2f0) {
        dm.push(("MovieVRMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2f4) {
        dm.push(("MovieElectronicVR".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2f6) {
        dm.push(("MovieSoundRecording".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2f8) {
        dm.push(("MicrophoneSensitivity".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2fa) {
        dm.push(("MicrophoneAttenuator".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2fc) {
        dm.push((
            "MicrophoneFrequencyResponse".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x2fe) {
        dm.push(("WindNoiseReduction".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x314) {
        dm.push(("MovieToneMap".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x31a) {
        dm.push(("MovieFrameSize".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x31c) {
        dm.push(("MovieFrameRate".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x322) {
        dm.push(("MicrophoneJackPower".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x323) {
        dm.push(("MovieDXCropAlert".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x324) {
        dm.push(("MovieSubjectDetection".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x32c) {
        dm.push(("MovieHighResZoom".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x5da) {
        dm.push(("Language".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x5dc) {
        dm.push(("TimeZone".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "+10:00 (Sydney)".to_string(),
            5 => "+09:00 (Tokyo)".to_string(),
            6 => "+08:00 (Beijing, Honk Kong, Sinapore)".to_string(),
            10 => "+05:45 (Kathmandu)".to_string(),
            11 => "+05:30 (New Dehli)".to_string(),
            12 => "+05:00 (Islamabad)".to_string(),
            13 => "+04:30 (Kabul)".to_string(),
            14 => "+04:00 (Abu Dhabi)".to_string(),
            15 => "+03:30 (Tehran)".to_string(),
            16 => "+03:00 (Moscow, Nairobi)".to_string(),
            17 => "+02:00 (Athens, Helsinki)".to_string(),
            18 => "+01:00 (Madrid, Paris, Berlin)".to_string(),
            19 => "+00:00 (London)".to_string(),
            20 => "-01:00 (Azores)".to_string(),
            21 => "-02:00 (Fernando de Noronha)".to_string(),
            22 => "-03:00 (Buenos Aires, Sao Paulo)".to_string(),
            23 => "-03:30 (Newfoundland)".to_string(),
            24 => "-04:00 (Manaus, Caracas)".to_string(),
            25 => "-05:00 (New York, Toronto, Lima)".to_string(),
            26 => "-06:00 (Chicago, Mexico City)".to_string(),
            27 => "-07:00 (Denver)".to_string(),
            28 => "-08:00 (Los Angeles, Vancouver)".to_string(),
            29 => "-09:00 (Anchorage)".to_string(),
            30 => "-10:00 (Hawaii)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("TimeZone", 0x5dc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5e2) {
        dm.push(("MonitorBrightness".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x5f8) {
        dm.push(("AFFineTune".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i16_at(data, 0x5fc, bo) {
        dm.push((
            "NonCPULens1FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x600, bo) {
        dm.push((
            "NonCPULens2FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x604, bo) {
        dm.push((
            "NonCPULens3FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x608, bo) {
        dm.push((
            "NonCPULens4FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x60c, bo) {
        dm.push((
            "NonCPULens5FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x610, bo) {
        dm.push((
            "NonCPULens6FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x614, bo) {
        dm.push((
            "NonCPULens7FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x618, bo) {
        dm.push((
            "NonCPULens8FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x61c, bo) {
        dm.push((
            "NonCPULens9FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x620, bo) {
        dm.push((
            "NonCPULens10FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x624, bo) {
        dm.push((
            "NonCPULens11FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x628, bo) {
        dm.push((
            "NonCPULens12FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x62c, bo) {
        dm.push((
            "NonCPULens13FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x630, bo) {
        dm.push((
            "NonCPULens14FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x634, bo) {
        dm.push((
            "NonCPULens15FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x638, bo) {
        dm.push((
            "NonCPULens16FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x63c, bo) {
        dm.push((
            "NonCPULens17FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x640, bo) {
        dm.push((
            "NonCPULens18FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x644, bo) {
        dm.push((
            "NonCPULens19FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x648, bo) {
        dm.push((
            "NonCPULens20FocalLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x64c, bo) {
        dm.push((
            "NonCPULens1MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x650, bo) {
        dm.push((
            "NonCPULens2MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x654, bo) {
        dm.push((
            "NonCPULens3MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x658, bo) {
        dm.push((
            "NonCPULens4MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x65c, bo) {
        dm.push((
            "NonCPULens5MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x660, bo) {
        dm.push((
            "NonCPULens6MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x664, bo) {
        dm.push((
            "NonCPULens7MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x668, bo) {
        dm.push((
            "NonCPULens8MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x66c, bo) {
        dm.push((
            "NonCPULens9MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x670, bo) {
        dm.push((
            "NonCPULens10MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x674, bo) {
        dm.push((
            "NonCPULens11MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x678, bo) {
        dm.push((
            "NonCPULens12MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x67c, bo) {
        dm.push((
            "NonCPULens13MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x680, bo) {
        dm.push((
            "NonCPULens14MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x684, bo) {
        dm.push((
            "NonCPULens15MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x688, bo) {
        dm.push((
            "NonCPULens16MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x68c, bo) {
        dm.push((
            "NonCPULens17MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x690, bo) {
        dm.push((
            "NonCPULens18MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x694, bo) {
        dm.push((
            "NonCPULens19MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = i16_at(data, 0x698, bo) {
        dm.push((
            "NonCPULens20MaxAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x6a8) {
        dm.push(("HDMIOutputResolution".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "4320p".to_string(),
            2 => "2160p".to_string(),
            3 => "1080p".to_string(),
            4 => "1080i".to_string(),
            5 => "720p".to_string(),
            6 => "576p".to_string(),
            7 => "480p".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HDMIOutputResolution",
            0x6a8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6b5) {
        dm.push((
            "SetClockFromLocationData".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x6bc) {
        dm.push(("AirplaneMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x6bd) {
        dm.push(("EmptySlotRelease".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x6e0) {
        dm.push(("EnergySavingMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x6f8) {
        dm.push(("RecordLocationData".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x6fc) {
        dm.push(("USBPowerDelivery".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x705) {
        dm.push(("SensorShield".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x746) {
        dm.push(("AutoCapturePreset".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1".to_string(),
            1 => "2".to_string(),
            2 => "3".to_string(),
            3 => "4".to_string(),
            4 => "5".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoCapturePreset",
            0x746,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0x748) {
            dm.push(("FocusShiftAutoReset".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if let Some(v) = u8_at(data, 0x782) {
        dm.push(("PreReleaseBurstLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "0.3 Sec".to_string(),
            2 => "0.5 Sec".to_string(),
            3 => "1 Sec".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PreReleaseBurstLength",
            0x782,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x784) {
        dm.push((
            "PostReleaseBurstLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 Sec".to_string(),
            1 => "2 Sec".to_string(),
            2 => "3 Sec".to_string(),
            3 => "Max".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PostReleaseBurstLength",
            0x784,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x792) {
        dm.push(("VerticalISOButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "VerticalISOButton",
            0x792,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x794) {
        dm.push((
            "ExposureCompensationButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ExposureCompensationButton",
            0x794,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x796) {
        dm.push(("ISOButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ISOButton",
            0x796,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7d2) {
        dm.push((
            "ViewModeShowEffectsOfSettings".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x7d4) {
        dm.push(("DispButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "DispButton",
            0x7d4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = fix32u_at(data, 0x800, bo) {
        dm.push(("ExposureDelay".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "$val ? sprintf(\"%.1f sec\",$val/1000) : \"Off\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "ExposureDelay",
            0x800,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() >= "05.00"))
    {
        if let Some(v) = u8_at(data, 0x804) {
            dm.push((
                "CommandDialFrameAdvanceZoom".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() >= "05.00"))
    {
        if let Some(v) = u8_at(data, 0x806) {
            dm.push((
                "SubCommandDialFrameAdvanceZoom".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x808) {
        dm.push(("PlaybackButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PlaybackButton",
            0x808,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x80a) {
        dm.push(("WBButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WBButton",
            0x80a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x80c) {
        dm.push(("BracketButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "BracketButton",
            0x80c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x80e) {
        dm.push(("FlashModeButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FlashModeButton",
            0x80e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x810) {
        dm.push((
            "LensFunc1ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LensFunc1ButtonPlaybackMode",
            0x810,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x812) {
        dm.push((
            "LensFunc2ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LensFunc2ButtonPlaybackMode",
            0x812,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x814) {
        dm.push((
            "PlaybackButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PlaybackButtonPlaybackMode",
            0x814,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x816) {
        dm.push((
            "BracketButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "BracketButtonPlaybackMode",
            0x816,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x818) {
        dm.push((
            "FlashModeButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FlashModeButtonPlaybackMode",
            0x818,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x34f..0x34f + 632) {
        tags.extend(nikoncustom_settingsz9v4(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Nikon::MenuSettingsZ8` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikon_menusettingsz8(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nikon";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x48) {
        dm.push(("HighFrameRate".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "CH".to_string(),
            2 => "CH*".to_string(),
            3 => "C30".to_string(),
            4 => "C120".to_string(),
            5 => "C60".to_string(),
            6 => "C15".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("HighFrameRate", 0x48, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x98) {
        dm.push(("MultipleExposureMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                2 => "On (Series)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "MultipleExposureMode",
                0x98,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "MultipleExposureMode").is_some_and(|v| v != 0.0) {
        if let Some(v) = u8_at(data, 0x9a) {
            dm.push(("MultiExposureShots".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "MultiExposureShots",
                0x9a,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u32_at(data, 0xb8, bo) {
            dm.push(("IntervalDurationHours".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "IntervalDurationHours",
                0xb8,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u32_at(data, 0xbc, bo) {
            dm.push((
                "IntervalDurationMinutes".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "IntervalDurationMinutes",
                0xbc,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u32_at(data, 0xc0, bo) {
            dm.push((
                "IntervalDurationSeconds".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "IntervalDurationSeconds",
                0xc0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u32_at(data, 0xc8, bo) {
            dm.push((
                "IntervalShootingIntervals".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "Intervals",
                    0xc8,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u32_at(data, 0xcc, bo) {
            dm.push((
                "IntervalShootingShotsPerInterval".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "ShotsPerInterval",
                    0xcc,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xd0) {
            dm.push((
                "IntervalExposureSmoothing".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "IntervalExposureSmoothing",
                0xd0,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "IntervalShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xd2) {
            dm.push(("IntervalPriority".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("IntervalPriority", 0xd2, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xf4) {
            dm.push(("FocusShiftNumberShots".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                tags.push(mk(
                    "FocusShiftNumberShots",
                    0xf4,
                    base.as_string(),
                    Value::I32(base.as_num() as i32),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xf8) {
            dm.push(("FocusShiftStepWidth".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FocusShiftStepWidth",
                0xf8,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0xfc) {
            dm.push(("FocusShiftInterval".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "$val == 1? \"1 Second\" : sprintf(\"%.0f Seconds\",$val)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "FocusShiftInterval",
                0xfc,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if (dm_val(dm, "ShutterMode").is_some_and(conv_expr::Val::truthy)
        && (dm_val(dm, "ShutterMode").is_some_and(|v| v.as_string().as_str() != "96")
            && dm_get(dm, "FocusShiftShooting").is_some_and(|v| v > 0.0)))
    {
        if let Some(v) = u8_at(data, 0x100) {
            dm.push((
                "FocusShiftExposureLock".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x11e) {
        dm.push(("PhotoShootingMenuBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PhotoShootingMenuBank",
            0x11e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x120) {
        dm.push(("ExtendedMenuBanks".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExtendedMenuBanks",
            0x120,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x144) {
        dm.push((
            "PhotoShootingMenuBankImageArea".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "FX".to_string(),
            1 => "DX".to_string(),
            4 => "16:9".to_string(),
            8 => "1:1".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PhotoShootingMenuBankImageArea",
            0x144,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x152) {
        dm.push(("AutoISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoISO", 0x152, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x154, bo) {
        dm.push(("ISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u16_at(data, 0x156, bo) {
        dm.push(("ISOAutoFlashLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i16_at(data, 0x15e, bo) {
        dm.push(("ISOAutoShutterTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("$val / 8", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                -15 => "Auto".to_string(),
                -12 => "15 s".to_string(),
                -9 => "8 s".to_string(),
                -6 => "4 s".to_string(),
                -3 => "2 s".to_string(),
                0 => "1 s".to_string(),
                1 => "1/1.3 s".to_string(),
                2 => "1/1.6 s".to_string(),
                3 => "1/2 s".to_string(),
                4 => "1/2.5 s".to_string(),
                5 => "1/12000 s".to_string(),
                6 => "1/4 s".to_string(),
                7 => "1/5 s".to_string(),
                8 => "1/6 s".to_string(),
                9 => "1/8 s".to_string(),
                10 => "1/10 s".to_string(),
                11 => "1/13 s".to_string(),
                12 => "1/15 s".to_string(),
                13 => "1/20 s".to_string(),
                14 => "1/25 s".to_string(),
                15 => "1/30 s".to_string(),
                16 => "1/40 s".to_string(),
                17 => "1/50 s".to_string(),
                18 => "1/60 s".to_string(),
                19 => "1/80 s".to_string(),
                20 => "1/100 s".to_string(),
                21 => "1/120 s".to_string(),
                22 => "1/160 s".to_string(),
                23 => "1/200 s".to_string(),
                24 => "1/250 s".to_string(),
                25 => "1/320 s".to_string(),
                26 => "1/400 s".to_string(),
                27 => "1/500 s".to_string(),
                28 => "1/640 s".to_string(),
                29 => "1/800 s".to_string(),
                30 => "1/1000 s".to_string(),
                31 => "1/1250 s".to_string(),
                32 => "1/1600 s".to_string(),
                33 => "1/2000 s".to_string(),
                34 => "1/2500 s".to_string(),
                35 => "1/3200 s".to_string(),
                36 => "1/4000 s".to_string(),
                37 => "1/5000 s".to_string(),
                38 => "1/6400 s".to_string(),
                39 => "1/8000 s".to_string(),
                40 => "1/10000 s".to_string(),
                41 => "1/13000 s".to_string(),
                42 => "1/16000 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "ISOAutoShutterTime",
                0x15e,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1b0) {
        dm.push(("MovieVignetteControl".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1b2) {
        dm.push((
            "DiffractionCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DiffractionCompensation",
            0x1b2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b4) {
        dm.push((
            "FlickerReductionShooting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlickerReductionShooting",
            0x1b4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b8) {
        dm.push(("FlashControlMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Auto External Flash".to_string(),
                2 => "GN (distance priority)".to_string(),
                3 => "Manual".to_string(),
                4 => "Repeating Flash".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlMode",
                0x1b8,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x224) {
        dm.push(("AFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Pinpoint".to_string(),
                1 => "Single".to_string(),
                2 => "Dynamic".to_string(),
                3 => "Wide (S)".to_string(),
                4 => "Wide (L)".to_string(),
                5 => "3D".to_string(),
                6 => "Auto".to_string(),
                11 => "Subject Tracking".to_string(),
                12 => "Wide (C1)".to_string(),
                13 => "Wide (C2)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFAreaMode", 0x224, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x226) {
        dm.push(("VRMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Normal".to_string(),
            2 => "Sport".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("VRMode", 0x226, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x22a) {
        dm.push(("BracketSet".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "AE/Flash".to_string(),
                1 => "AE".to_string(),
                2 => "Flash".to_string(),
                3 => "White Balance".to_string(),
                4 => "Active-D Lighting".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("BracketSet", 0x22a, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "BracketSet").is_some_and(|v| v < 3.0) {
        if let Some(v) = u8_at(data, 0x22c) {
            dm.push(("BracketProgram".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Disabled".to_string(),
                2 => "2F".to_string(),
                3 => "3F".to_string(),
                4 => "4F".to_string(),
                5 => "5F".to_string(),
                7 => "7F".to_string(),
                9 => "9F".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("BracketProgram", 0x22c, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if dm_get(dm, "BracketSet").is_some_and(|v| v < 3.0) {
        if let Some(v) = u8_at(data, 0x22e) {
            dm.push(("BracketIncrement".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "0.3".to_string(),
                1 => "0.5".to_string(),
                2 => "0.7".to_string(),
                3 => "1.0".to_string(),
                4 => "2.0".to_string(),
                5 => "3.0".to_string(),
                6 => "1.3".to_string(),
                7 => "1.5".to_string(),
                8 => "1.7".to_string(),
                9 => "2.3".to_string(),
                10 => "2.5".to_string(),
                11 => "2.7".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "BracketIncrement",
                0x22e,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x23a) {
        dm.push(("HDR".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                2 => "On (Series)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("HDR", 0x23a, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x240) {
        dm.push(("SecondarySlotFunction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Overflow".to_string(),
            1 => "Backup".to_string(),
            2 => "NEF Primary + JPG Secondary".to_string(),
            3 => "JPG Primary + JPG Secondary".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SecondarySlotFunction",
            0x240,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_val(dm, "HDR").is_some_and(|v| v.as_string().as_str() != "0") {
        if let Some(v) = u8_at(data, 0x246) {
            dm.push(("HDRLevel".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Auto".to_string(),
                1 => "Extra High".to_string(),
                2 => "High".to_string(),
                3 => "Normal".to_string(),
                4 => "Low".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("HDRLevel", 0x246, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x24a) {
        dm.push(("Slot2JpgSize".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x250) {
        dm.push(("DXCropAlert".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DXCropAlert", 0x250, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x252) {
        dm.push(("SubjectDetection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Auto".to_string(),
            2 => "People".to_string(),
            3 => "Animals".to_string(),
            4 => "Vehicles".to_string(),
            5 => "Birds".to_string(),
            6 => "Airplanes".to_string(),
            7 => "Faces".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubjectDetection",
            0x252,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if dm_get(dm, "AFAreaMode").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x254) {
            dm.push(("DynamicAFAreaSize".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with("$val", &base, &ctx);
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                let s = match base.as_num() as i64 {
                    0 => "Small".to_string(),
                    1 => "Medium".to_string(),
                    2 => "Large".to_string(),
                    other => other.to_string(),
                };
                #[allow(clippy::cast_possible_truncation)]
                let raw = Value::I32(base.as_num() as i32);
                tags.push(mk(
                    "DynamicAFAreaSize",
                    0x254,
                    s,
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x26a) {
        dm.push(("ToneMap".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x26e) {
        dm.push((
            "PortraitImpressionBalance".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Mode 1".to_string(),
            2 => "Mode 2".to_string(),
            3 => "Mode 3".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PortraitImpressionBalance",
            0x26e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x27c) {
        dm.push((
            "HighFrequencyFlickerReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "HighFrequencyFlickerReduction",
            0x27c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2da) {
        let v = v & 0x1;
        dm.push(("MovieImageArea".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2e4) {
        dm.push(("MovieType".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u16_at(data, 0x2e6, bo) {
        dm.push(("MovieISOAutoHiLimit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2e8) {
        dm.push((
            "MovieISOAutoControlManualMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u16_at(data, 0x2ea, bo) {
        dm.push((
            "MovieISOAutoManualMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x334) {
        dm.push(("MovieActiveD-Lighting".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x336) {
        dm.push((
            "MovieHighISONoiseReduction".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x33c) {
        dm.push(("MovieFlickerReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "50Hz".to_string(),
            2 => "60Hz".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieFlickerReduction",
            0x33c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x33e) {
        dm.push(("MovieMeteringMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x340) {
        dm.push(("MovieFocusMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x342) {
        dm.push(("MovieAFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Pinpoint".to_string(),
            1 => "Single".to_string(),
            2 => "Dynamic".to_string(),
            3 => "Wide (S)".to_string(),
            4 => "Wide (L)".to_string(),
            5 => "3D".to_string(),
            6 => "Auto".to_string(),
            11 => "Subject Tracking".to_string(),
            12 => "Wide (C1)".to_string(),
            13 => "Wide (C2)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MovieAFAreaMode", 0x342, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x344) {
        dm.push(("MovieVRMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x348) {
        dm.push(("MovieElectronicVR".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x34a) {
        dm.push(("MovieSoundRecording".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x34c) {
        dm.push(("MicrophoneSensitivity".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x34e) {
        dm.push(("MicrophoneAttenuator".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x350) {
        dm.push((
            "MicrophoneFrequencyResponse".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x352) {
        dm.push(("WindNoiseReduction".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x372) {
        dm.push(("MovieFrameSize".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x374) {
        dm.push(("MovieFrameRate".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x376) {
        dm.push(("MicrophoneJackPower".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x377) {
        dm.push(("MovieDXCropAlert".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x378) {
        dm.push(("MovieSubjectDetection".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x380) {
        dm.push(("MovieHighResZoom".to_string(), Conv::Num(f64::from(v))));
    }
    tags
}

/// `Image::ExifTool::NikonCapture::DLightingHQ` -- FORMAT int32u, FIRST_ENTRY 0.
fn nikoncapture_dlightinghq(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCapture";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x0, bo) {
        dm.push(("D-LightingHQShadow".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "D-LightingHQShadow",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push(("D-LightingHQHighlight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "D-LightingHQHighlight",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push((
            "D-LightingHQColorBoost".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "D-LightingHQColorBoost",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCapture::DLightingHS` -- FORMAT int32u, FIRST_ENTRY 0.
fn nikoncapture_dlightinghs(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCapture";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x0, bo) {
        dm.push((
            "D-LightingHSAdjustment".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "D-LightingHSAdjustment",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push((
            "D-LightingHSColorBoost".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "D-LightingHSColorBoost",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCapture::HighlightData` -- FORMAT int8s, FIRST_ENTRY 0.
fn nikoncapture_highlightdata(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCapture";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i8_at(data, 0x0) {
        dm.push(("ShadowProtection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ShadowProtection",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x1) {
        dm.push(("SaturationAdj".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SaturationAdj",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x6) {
        dm.push(("HighlightProtection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "HighlightProtection",
            0x6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCapture::PictureCtrl` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncapture_picturectrl(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCapture";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("PictureControlActive".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PictureControlActive",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x13, 16, true) {
        tags.push(mk(
            "PictureControlMode",
            0x13,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2a) {
        dm.push(("QuickAdjust".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "QuickAdjust",
                0x2a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2b) {
        dm.push(("SharpeningAdj".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? $val - 128 : \"Auto\"", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "SharpeningAdj",
                0x2b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2c) {
        dm.push(("ContrastAdj".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? $val - 128 : \"Auto\"", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ContrastAdj",
                0x2c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2d) {
        dm.push(("BrightnessAdj".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? $val - 128 : \"Auto\"", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "BrightnessAdj",
                0x2d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2e) {
        dm.push(("SaturationAdj".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? $val - 128 : \"Auto\"", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "SaturationAdj",
                0x2e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2f) {
        dm.push(("HueAdj".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 128", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "HueAdj",
                0x2f,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::NikonCapture::UnsharpData` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncapture_unsharpdata(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCapture";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("UnsharpCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "UnsharpCount",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x13, bo) {
        dm.push(("Unsharp1Color".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "RGB".to_string(),
            1 => "Red".to_string(),
            2 => "Green".to_string(),
            3 => "Blue".to_string(),
            4 => "Yellow".to_string(),
            5 => "Magenta".to_string(),
            6 => "Cyan".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Unsharp1Color", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x17, bo) {
        dm.push(("Unsharp1Intensity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp1Intensity",
            0x17,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x19, bo) {
        dm.push(("Unsharp1HaloWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp1HaloWidth",
            0x19,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push(("Unsharp1Threshold".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp1Threshold",
            0x1b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2e, bo) {
        dm.push(("Unsharp2Color".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "RGB".to_string(),
            1 => "Red".to_string(),
            2 => "Green".to_string(),
            3 => "Blue".to_string(),
            4 => "Yellow".to_string(),
            5 => "Magenta".to_string(),
            6 => "Cyan".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Unsharp2Color", 0x2e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x32, bo) {
        dm.push(("Unsharp2Intensity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp2Intensity",
            0x32,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x34, bo) {
        dm.push(("Unsharp2HaloWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp2HaloWidth",
            0x34,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x36) {
        dm.push(("Unsharp2Threshold".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp2Threshold",
            0x36,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x49, bo) {
        dm.push(("Unsharp3Color".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "RGB".to_string(),
            1 => "Red".to_string(),
            2 => "Green".to_string(),
            3 => "Blue".to_string(),
            4 => "Yellow".to_string(),
            5 => "Magenta".to_string(),
            6 => "Cyan".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Unsharp3Color", 0x49, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x4d, bo) {
        dm.push(("Unsharp3Intensity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp3Intensity",
            0x4d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4f, bo) {
        dm.push(("Unsharp3HaloWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp3HaloWidth",
            0x4f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x51) {
        dm.push(("Unsharp3Threshold".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp3Threshold",
            0x51,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x64, bo) {
        dm.push(("Unsharp4Color".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "RGB".to_string(),
            1 => "Red".to_string(),
            2 => "Green".to_string(),
            3 => "Blue".to_string(),
            4 => "Yellow".to_string(),
            5 => "Magenta".to_string(),
            6 => "Cyan".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Unsharp4Color", 0x64, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x68, bo) {
        dm.push(("Unsharp4Intensity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp4Intensity",
            0x68,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x6a, bo) {
        dm.push(("Unsharp4HaloWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp4HaloWidth",
            0x6a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6c) {
        dm.push(("Unsharp4Threshold".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Unsharp4Threshold",
            0x6c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCapture::WBAdjData` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncapture_wbadjdata(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCapture";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = f64_at(data, 0x0, bo) {
        dm.push(("WBAdjRedBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WBAdjRedBalance",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f64_at(data, 0x8, bo) {
        dm.push(("WBAdjBlueBalance".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WBAdjBlueBalance",
            0x8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        dm.push(("WBAdjMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "Use Gray Point".to_string(),
            2 => "Recorded Value".to_string(),
            3 => "Use Temperature".to_string(),
            4 => "Calculate Automatically".to_string(),
            5 => "Auto2".to_string(),
            6 => "Underwater".to_string(),
            7 => "Auto1".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBAdjMode", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x14, bo) {
        dm.push(("WBAdjLighting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            256 => "Incandescent".to_string(),
            512 => "Daylight (direct sunlight)".to_string(),
            513 => "Daylight (shade)".to_string(),
            514 => "Daylight (cloudy)".to_string(),
            768 => "Standard Fluorescent (warm white)".to_string(),
            769 => "Standard Fluorescent (3700K)".to_string(),
            770 => "Standard Fluorescent (cool white)".to_string(),
            771 => "Standard Fluorescent (5000K)".to_string(),
            772 => "Standard Fluorescent (daylight)".to_string(),
            773 => "Standard Fluorescent (high temperature mercury vapor)".to_string(),
            1024 => "High Color Rendering Fluorescent (warm white)".to_string(),
            1025 => "High Color Rendering Fluorescent (3700K)".to_string(),
            1026 => "High Color Rendering Fluorescent (cool white)".to_string(),
            1027 => "High Color Rendering Fluorescent (5000K)".to_string(),
            1028 => "High Color Rendering Fluorescent (daylight)".to_string(),
            1280 => "Flash".to_string(),
            1281 => "Flash (FL-G1 filter)".to_string(),
            1282 => "Flash (FL-G2 filter)".to_string(),
            1283 => "Flash (TN-A1 filter)".to_string(),
            1284 => "Flash (TN-A2 filter)".to_string(),
            1536 => "Sodium Vapor Lamps".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBAdjLighting", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x18, bo) {
        dm.push(("WBAdjTemperature".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WBAdjTemperature",
            0x18,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i32_at(data, 0x25, bo) {
        dm.push(("WBAdjTint".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WBAdjTint",
            0x25,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCapture::Brightness` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncapture_brightness(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCapture";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = f64_at(data, 0x0, bo) {
        dm.push(("BrightnessAdj".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val * 50", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "BrightnessAdj",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x8) {
        dm.push(("EnhanceDarkTones".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("EnhanceDarkTones", 0x8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::NikonCapture::ColorBoost` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncapture_colorboost(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCapture";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("ColorBoostType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Nature".to_string(),
            1 => "People".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorBoostType", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u32_at(data, 0x1, bo) {
        dm.push(("ColorBoostLevel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorBoostLevel",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCapture::Exposure` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncapture_exposure(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCapture";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x0, bo) {
        dm.push(("ExposureAdj".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 100", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "ExposureAdj",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = f64_at(data, 0x12, bo) {
        dm.push(("ExposureAdj2".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.4f\", $val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "ExposureAdj2",
            0x12,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x24) {
        dm.push(("ActiveD-Lighting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ActiveD-Lighting", 0x24, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x25) {
        dm.push(("ActiveD-LightingMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unchanged".to_string(),
            1 => "Off".to_string(),
            2 => "Low".to_string(),
            3 => "Normal".to_string(),
            4 => "High".to_string(),
            6 => "Extra High".to_string(),
            7 => "Extra High 1".to_string(),
            8 => "Extra High 2".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ActiveD-LightingMode",
            0x25,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD40` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd40(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x80) >> 7;
        dm.push(("Beep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Beep", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x40) >> 6;
        dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssist", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x20) >> 5;
        dm.push(("NoMemoryCard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release Locked".to_string(),
            1 => "Enable Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoMemoryCard", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x10) >> 4;
        dm.push(("ImageReview".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageReview", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x80) >> 7;
        dm.push(("AutoISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoISO", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x30) >> 4;
        dm.push(("AutoISOMax".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AutoISOMax",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x7;
        dm.push((
            "AutoISOMinShutterSpeed".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/125 s".to_string(),
            1 => "1/60 s".to_string(),
            2 => "1/30 s".to_string(),
            3 => "1/15 s".to_string(),
            4 => "1/8 s".to_string(),
            5 => "1/4 s".to_string(),
            6 => "1/2 s".to_string(),
            7 => "1 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoISOMinShutterSpeed",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = v & 0x7;
        dm.push(("ImageReviewTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "8 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageReviewTime", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0xe0) >> 5;
        dm.push(("MonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "8 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MonitorOffTime", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x1c) >> 2;
        dm.push(("MeteringTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "8 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "30 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MeteringTime", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = v & 0x3;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0xc0) >> 6;
        dm.push(("RemoteOnDuration".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 min".to_string(),
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RemoteOnDuration", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0xe) >> 1;
        dm.push(("AELockButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE/AF Lock".to_string(),
            1 => "AE Lock Only".to_string(),
            2 => "AF Lock Only".to_string(),
            3 => "AE Lock (hold)".to_string(),
            4 => "AF-ON".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELockButton", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = v & 0x1;
        dm.push(("AELock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELock", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x70) >> 4;
        dm.push(("ShootingModeSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Single Frame".to_string(),
            1 => "Continuous".to_string(),
            2 => "Self-timer".to_string(),
            3 => "Delayed Remote".to_string(),
            4 => "Quick-response Remote".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingModeSetting",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = v & 0x7;
        dm.push(("TimerFunctionButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Shooting Mode".to_string(),
            1 => "Image Quality/Size".to_string(),
            2 => "ISO".to_string(),
            3 => "White Balance".to_string(),
            4 => "Self-timer".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "TimerFunctionButton",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = v & 0x3;
        dm.push(("Metering".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Matrix".to_string(),
            1 => "Center-weighted".to_string(),
            2 => "Spot".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Metering", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = (v & 0x10) >> 4;
        dm.push(("InternalFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Manual".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("InternalFlash", 0x8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = v & 0x7;
        dm.push(("ManualFlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "ManualFlashOutput",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x9) {
        dm.push(("FlashLevel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("sprintf(\"%+.1f\",$val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FlashLevel",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0xc0) >> 6;
        dm.push(("FocusModeSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Manual".to_string(),
            1 => "AF-S".to_string(),
            2 => "AF-C".to_string(),
            3 => "AF-A".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusModeSetting", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0x30) >> 4;
        dm.push(("AFAreaModeSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Single Area".to_string(),
            1 => "Dynamic Area".to_string(),
            2 => "Closest Subject".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAreaModeSetting", 0xb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD810` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd810(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x8) >> 3;
        dm.push(("LightSwitch".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "LCD Backlight".to_string(),
            1 => "LCD Backlight and Shooting Information".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LightSwitch", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x3;
        dm.push(("CustomSettingsBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsBank",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Release + Focus".to_string(),
            2 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x20) >> 5;
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus".to_string(),
            1 => "Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-SPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x10) >> 4;
        dm.push(("AFPointSelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "51 Points".to_string(),
            1 => "11 Points".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPointSelection", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x7;
        dm.push(("FocusTrackingLockOn".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "1 (Short)".to_string(),
            2 => "2".to_string(),
            3 => "3 (Normal)".to_string(),
            4 => "4".to_string(),
            5 => "5 (Long)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusTrackingLockOn",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x80) >> 7;
        dm.push(("AFActivation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Shutter/AF-On".to_string(),
            1 => "AF-On Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFActivation", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x8) >> 3;
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Wrap".to_string(),
            1 => "Wrap".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusPointWrap", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x6) >> 1;
        dm.push(("AFPointBrightness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "On".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPointBrightness", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = v & 0x1;
        dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssist", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x40) >> 6;
        dm.push(("BatteryOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "MB-D12 First".to_string(),
            1 => "Camera Battery First".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("BatteryOrder", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = v & 0x3;
        dm.push(("MB-D12BatteryType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "LR6 (AA alkaline)".to_string(),
            1 => "HR6 (AA Ni-MH)".to_string(),
            2 => "FR6 (AA lithium)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MB-D12BatteryType", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x40) >> 6;
        dm.push(("Pitch".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "High".to_string(),
            1 => "Low".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Pitch", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x20) >> 5;
        dm.push(("NoMemoryCard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release Locked".to_string(),
            1 => "Enable Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoMemoryCard", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0xc) >> 2;
        dm.push(("ISODisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Show ISO/Easy ISO".to_string(),
            1 => "Show ISO Sensitivity".to_string(),
            3 => "Show Frame Count".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISODisplay", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x2) >> 1;
        dm.push(("GridDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GridDisplay", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0xc0) >> 6;
        dm.push(("ShootingInfoDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Not Set".to_string(),
            1 => "Auto".to_string(),
            2 => "Manual (dark on light)".to_string(),
            3 => "Manual (light on dark)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoDisplay",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x20) >> 5;
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LCDIllumination", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x8) >> 3;
        dm.push((
            "ElectronicFront-CurtainShutter".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ElectronicFront-CurtainShutter",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x4) >> 2;
        dm.push(("ScreenTips".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ScreenTips", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = v & 0x3;
        dm.push(("Beep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Low".to_string(),
            2 => "Medium".to_string(),
            3 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Beep", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x80) >> 7;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReverseIndicators", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x18) >> 3;
        dm.push((
            "CommandDialsReverseRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Shutter Speed & Aperture".to_string(),
            2 => "Exposure Compensation".to_string(),
            3 => "Exposure Compensation, Shutter Speed & Aperture".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsReverseRotation",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = v & 0x3;
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (auto reset)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xc0) >> 6;
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x30) >> 4;
        dm.push(("ISOStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOStepSize", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xc) >> 2;
        dm.push(("ExposureCompStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureCompStepSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "8 mm".to_string(),
            1 => "12 mm".to_string(),
            2 => "15 mm".to_string(),
            3 => "20 mm".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0xc0) >> 6;
        dm.push((
            "MultiSelectorShootMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Select Center Focus Point (Reset)".to_string(),
            1 => "Highlight Active Focus Point".to_string(),
            2 => "Preset Focus Point (Pre)".to_string(),
            3 => "Not Used (None)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorShootMode",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0x30) >> 4;
        dm.push((
            "MultiSelectorPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Thumbnail On/Off".to_string(),
            1 => "View Histograms".to_string(),
            2 => "Zoom On/Off".to_string(),
            3 => "Choose Folder".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorPlaybackMode",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = v & 0x1;
        dm.push(("MultiSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Do Nothing".to_string(),
            1 => "Reset Meter-off Delay".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MultiSelector", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0xc0) >> 6;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0xb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = v & 0xf;
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "CLModeShootingSpeed",
            0xb,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MaxContinuousRelease",
            0xc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0xe0) >> 5;
        dm.push(("AutoBracketSet".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE & Flash".to_string(),
            1 => "AE Only".to_string(),
            2 => "Flash Only".to_string(),
            3 => "WB Bracketing".to_string(),
            4 => "Active D-Lighting".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketSet", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0x10) >> 4;
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0,-,+".to_string(),
            1 => "-,0,+".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketOrder", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0xc) >> 2;
        dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Flash/Speed".to_string(),
            1 => "Flash/Speed/Aperture".to_string(),
            2 => "Flash/Aperture".to_string(),
            3 => "Flash Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketModeM", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xe) {
        let v = v & 0x1f;
        dm.push(("FuncButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            21 => "Disable Synchronized Release".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FuncButton", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = v & 0x1f;
        dm.push(("PreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            21 => "Disable Synchronized Release".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PreviewButton", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = v & 0x7;
        dm.push(("AssignBktButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto Bracketing".to_string(),
            1 => "Multiple Exposure".to_string(),
            2 => "HDR (high dynamic range)".to_string(),
            3 => "None".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AssignBktButton", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = v & 0x1f;
        dm.push(("AELockButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            21 => "Disable Synchronized Release".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELockButton", 0x11, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CommandDialsChangeMainSub".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Autofocus Off, Exposure Off".to_string(),
            1 => "Autofocus Off, Exposure On".to_string(),
            2 => "Autofocus Off, Exposure On (Mode A)".to_string(),
            4 => "Autofocus On, Exposure Off".to_string(),
            5 => "Autofocus On, Exposure On".to_string(),
            6 => "Autofocus On, Exposure On (Mode A)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsChangeMainSub",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x18) >> 3;
        dm.push((
            "CommandDialsMenuAndPlayback".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            2 => "On (Image Review Excluded)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsMenuAndPlayback",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x4) >> 2;
        dm.push((
            "CommandDialsApertureSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Sub-command Dial".to_string(),
            1 => "Aperture Ring".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsApertureSetting",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = v & 0x1;
        dm.push((
            "ReleaseButtonToUseDial".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ReleaseButtonToUseDial",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0xf0) >> 4;
        dm.push(("StandbyTimer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "6 s".to_string(),
            3 => "10 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            9 => "30 min".to_string(),
            10 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("StandbyTimer", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0xc0) >> 6;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0x30) >> 4;
        dm.push(("SelfTimerShotInterval".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.5 s".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SelfTimerShotInterval",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = v & 0xf;
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x14,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "ImageReviewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageReviewMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "LiveViewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            4 => "20 min".to_string(),
            5 => "30 min".to_string(),
            6 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0xe0) >> 5;
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            2 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            2 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = (v & 0xf0) >> 4;
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/320 s (auto FP)".to_string(),
            2 => "1/250 s (auto FP)".to_string(),
            3 => "1/250 s".to_string(),
            5 => "1/200 s".to_string(),
            6 => "1/160 s".to_string(),
            7 => "1/125 s".to_string(),
            8 => "1/100 s".to_string(),
            9 => "1/80 s".to_string(),
            10 => "1/60 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashSyncSpeed", 0x17, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = v & 0xf;
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/60 s".to_string(),
            1 => "1/30 s".to_string(),
            2 => "1/15 s".to_string(),
            3 => "1/8 s".to_string(),
            4 => "1/4 s".to_string(),
            5 => "1/2 s".to_string(),
            6 => "1 s".to_string(),
            7 => "2 s".to_string(),
            8 => "4 s".to_string(),
            9 => "8 s".to_string(),
            10 => "15 s".to_string(),
            11 => "30 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashShutterSpeed",
            0x17,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x18) {
        let v = (v & 0xc0) >> 6;
        dm.push(("FlashControlBuiltin".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Manual".to_string(),
                2 => "Repeating Flash".to_string(),
                3 => "Commander Mode".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlBuilt-in",
                0x18,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = (v & 0x20) >> 5;
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x1f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x24) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x24,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x25) {
        let v = (v & 0xc0) >> 6;
        dm.push(("MultiSelectorLiveView".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Reset".to_string(),
            1 => "Zoom".to_string(),
            3 => "Not Used".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorLiveView",
            0x25,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x80) >> 7;
        dm.push(("ShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ShutterSpeedLock", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x40) >> 6;
        dm.push(("ApertureLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ApertureLock", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x20) >> 5;
        dm.push(("MovieShutterButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Take Photo".to_string(),
            1 => "Record Movies".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieShutterButton",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x4) >> 2;
        dm.push(("FlashExposureCompArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Entire frame".to_string(),
            1 => "Background only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashExposureCompArea",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x28) {
        let v = v & 0xf;
        dm.push((
            "MovieAELockButtonAssignment".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            5 => "AE/AF Lock".to_string(),
            6 => "AE Lock Only".to_string(),
            7 => "AE Lock (hold)".to_string(),
            8 => "AF Lock Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieAELockButtonAssignment",
            0x28,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x29) {
        let v = (v & 0x70) >> 4;
        dm.push(("MovieFunctionButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Power Aperture (open)".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieFunctionButton",
            0x29,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x29) {
        let v = v & 0x7;
        dm.push(("MoviePreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            2 => "Power Aperture (open)".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MoviePreviewButton",
            0x29,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2a) {
        let v = v & 0xf;
        dm.push(("FuncButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (FX/DX/5:4)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FuncButtonPlusDials",
            0x2a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b) {
        let v = v & 0xf;
        dm.push((
            "PreviewButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (FX/DX/5:4)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PreviewButtonPlusDials",
            0x2b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2c) {
        let v = v & 0xf;
        dm.push(("AELockButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (FX/DX/5:4)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AELockButtonPlusDials",
            0x2c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2d) {
        let v = v & 0xf;
        dm.push((
            "AssignMovieRecordButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (FX/DX/5:4)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            9 => "White Balance".to_string(),
            10 => "ISO Sensitivity".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieRecordButton",
            0x2d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2e) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptHighlightWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptHighlightWeighted",
                0x2e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x80) >> 7;
        dm.push(("DynamicAreaAFDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DynamicAreaAFDisplay",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x40) >> 6;
        dm.push(("AFPointIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On During Manual Focusing".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointIllumination",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x18) >> 3;
        dm.push(("StoreByOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Focus Point".to_string(),
            2 => "Focus Point and AF-area mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "StoreByOrientation",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x4) >> 2;
        dm.push((
            "GroupAreaAFIllumination".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Squares".to_string(),
            1 => "Dots".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "GroupAreaAFIllumination",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = (v & 0x80) >> 7;
        dm.push(("MatrixMetering".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Face Detection On".to_string(),
            1 => "Face Detection Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MatrixMetering", 0x30, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = (v & 0x30) >> 4;
        dm.push(("LiveViewButtonOptions".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Enable".to_string(),
            2 => "Disable".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewButtonOptions",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = v & 0x3;
        dm.push(("AFModeRestrictions".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Restrictions".to_string(),
            1 => "AF-C".to_string(),
            2 => "AF-S".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFModeRestrictions",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x31) {
        let v = (v & 0x7e) >> 1;
        dm.push((
            "LimitAFAreaModeSelection".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto-area".to_string(),
            1 => "Group-area".to_string(),
            2 => "3D-tracking".to_string(),
            3 => "Dynamic area (51 points)".to_string(),
            4 => "Dynamic area (21 points)".to_string(),
            5 => "Dynamic area (9 points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LimitAFAreaModeSelection",
            0x31,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x32) {
        let v = v & 0x7;
        dm.push(("AF-OnForMB-D12".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE/AF Lock".to_string(),
            1 => "AE Lock Only".to_string(),
            2 => "AF Lock Only".to_string(),
            3 => "AE Lock (hold)".to_string(),
            4 => "AE Lock (reset)".to_string(),
            5 => "AF-On".to_string(),
            6 => "FV Lock".to_string(),
            7 => "Same As Fn Button".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AF-OnForMB-D12", 0x32, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x33) {
        let v = v & 0x1f;
        dm.push(("AssignRemoteFnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            16 => "+NEF(RAW)".to_string(),
            25 => "Live View".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignRemoteFnButton",
            0x33,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x34) {
        let v = v & 0x3f;
        dm.push((
            "LensFocusFunctionButtons".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            7 => "AF Lock Only".to_string(),
            21 => "Disable Synchronized Release".to_string(),
            22 => "Remote Release Only".to_string(),
            24 => "Preset focus Point".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            32 => "AF-Area Mode:  Single-point AF".to_string(),
            33 => "AF-Area Mode: Dynamic-area AF (9 points)".to_string(),
            34 => "AF-Area Mode: Dynamic-area AF (21 points)".to_string(),
            35 => "AF-Area Mode: Dynamic-area AF (51 points)".to_string(),
            36 => "AF-Area Mode: Group-area AF".to_string(),
            37 => "AF-Area Mode: Auto area AF".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LensFocusFunctionButtons",
            0x34,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD850` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd850(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x3;
        dm.push(("CustomSettingsBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsBank",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Release + Focus".to_string(),
            2 => "Focus".to_string(),
            3 => "Focus + Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x20) >> 5;
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus".to_string(),
            1 => "Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-SPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x10) >> 4;
        dm.push(("AFPointSelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "55 Points".to_string(),
            1 => "15 Points".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPointSelection", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x8) >> 3;
        dm.push((
            "Three-DTrackingFaceDetection".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "Three-DTrackingFaceDetection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x7;
        dm.push(("BlockShotAFResponse".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "1 (Quick)".to_string(),
            2 => "2".to_string(),
            3 => "3 (Normal)".to_string(),
            4 => "4".to_string(),
            5 => "5 (Delay)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "BlockShotAFResponse",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x8) >> 3;
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Wrap".to_string(),
            1 => "Wrap".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusPointWrap", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x6) >> 1;
        dm.push(("AFPointBrightness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "On".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPointBrightness", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x8) >> 3;
        dm.push(("ISODisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Show ISO Sensitivity".to_string(),
            1 => "Show Frame Count".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISODisplay", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x2) >> 1;
        dm.push(("GridDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GridDisplay", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x20) >> 5;
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LCDIllumination", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x8) >> 3;
        dm.push((
            "ElectronicFront-CurtainShutter".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ElectronicFront-CurtainShutter",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x80) >> 7;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReverseIndicators", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x18) >> 3;
        dm.push((
            "CommandDialsReverseRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Shutter Speed & Aperture".to_string(),
            2 => "Exposure Compensation".to_string(),
            3 => "Exposure Compensation, Shutter Speed & Aperture".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsReverseRotation",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = v & 0x3;
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (Auto Reset)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xc0) >> 6;
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x30) >> 4;
        dm.push(("ISOStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOStepSize", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xc) >> 2;
        dm.push(("ExposureCompStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureCompStepSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "8 mm".to_string(),
            1 => "12 mm".to_string(),
            2 => "15 mm".to_string(),
            3 => "20 mm".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "MultiSelectorShootMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Select Center Focus Point (Reset)".to_string(),
            2 => "Preset Focus Point (Pre)".to_string(),
            3 => "Highlight Active Focus Point".to_string(),
            4 => "Not Used (None)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorShootMode",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0xc) >> 2;
        dm.push((
            "MultiSelectorPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Thumbnail On/Off".to_string(),
            1 => "View Histograms".to_string(),
            2 => "Zoom On/Off".to_string(),
            3 => "Choose Folder".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorPlaybackMode",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = v & 0x1;
        dm.push(("MultiSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Do Nothing".to_string(),
            1 => "Reset Meter-off Delay".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MultiSelector", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0xe0) >> 5;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "0.2 s".to_string(),
            2 => "0.5 s".to_string(),
            3 => "1 s".to_string(),
            4 => "2 s".to_string(),
            5 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0xb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = v & 0xf;
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "CLModeShootingSpeed",
            0xb,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MaxContinuousRelease",
            0xc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0x10) >> 4;
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0,-,+".to_string(),
            1 => "-,0,+".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketOrder", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0xc) >> 2;
        dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Flash/Speed".to_string(),
            1 => "Flash/Speed/Aperture".to_string(),
            2 => "Flash/Aperture".to_string(),
            3 => "Flash Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketModeM", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xe) {
        let v = v & 0x3f;
        dm.push(("Func1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 153 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 153 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            56 => "AF-Area Mode (Dynamic Area 9 Points)".to_string(),
            57 => "AF-Area Mode + AF-On (Dynamic Area 9 Points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Func1Button", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = v & 0x3f;
        dm.push(("PreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 153 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 153 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            56 => "AF-Area Mode (Dynamic Area 9 Points)".to_string(),
            57 => "AF-Area Mode + AF-On (Dynamic Area 9 Points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PreviewButton", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = v & 0x7;
        dm.push(("AssignBktButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto Bracketing".to_string(),
            1 => "Multiple Exposure".to_string(),
            2 => "HDR (high dynamic range)".to_string(),
            3 => "None".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AssignBktButton", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CommandDialsChangeMainSub".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Autofocus Off, Exposure Off".to_string(),
            1 => "Autofocus Off, Exposure On".to_string(),
            2 => "Autofocus Off, Exposure On (Mode A)".to_string(),
            4 => "Autofocus On, Exposure Off".to_string(),
            5 => "Autofocus On, Exposure On".to_string(),
            6 => "Autofocus On, Exposure On (Mode A)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsChangeMainSub",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x18) >> 3;
        dm.push((
            "CommandDialsMenuAndPlayback".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            2 => "On (Image Review Excluded)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsMenuAndPlayback",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x4) >> 2;
        dm.push((
            "CommandDialsApertureSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Sub-command Dial".to_string(),
            1 => "Aperture Ring".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsApertureSetting",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = v & 0x1;
        dm.push((
            "ReleaseButtonToUseDial".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ReleaseButtonToUseDial",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0xf0) >> 4;
        dm.push(("StandbyTimer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "6 s".to_string(),
            3 => "10 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            9 => "30 min".to_string(),
            10 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("StandbyTimer", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0xc0) >> 6;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0x30) >> 4;
        dm.push(("SelfTimerShotInterval".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.5 s".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SelfTimerShotInterval",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = v & 0xf;
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x14,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "ImageReviewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageReviewMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "LiveViewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            4 => "20 min".to_string(),
            5 => "30 min".to_string(),
            6 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0xe0) >> 5;
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            2 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            2 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = (v & 0xf0) >> 4;
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            2 => "1/250 s (auto FP)".to_string(),
            3 => "1/250 s".to_string(),
            5 => "1/200 s".to_string(),
            6 => "1/160 s".to_string(),
            7 => "1/125 s".to_string(),
            8 => "1/100 s".to_string(),
            9 => "1/80 s".to_string(),
            10 => "1/60 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashSyncSpeed", 0x17, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = v & 0xf;
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/60 s".to_string(),
            1 => "1/30 s".to_string(),
            2 => "1/15 s".to_string(),
            3 => "1/8 s".to_string(),
            4 => "1/4 s".to_string(),
            5 => "1/2 s".to_string(),
            6 => "1 s".to_string(),
            7 => "2 s".to_string(),
            8 => "4 s".to_string(),
            9 => "8 s".to_string(),
            10 => "15 s".to_string(),
            11 => "30 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashShutterSpeed",
            0x17,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = (v & 0x20) >> 5;
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x1f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x24) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x24,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x25) {
        let v = (v & 0xc0) >> 6;
        dm.push(("MultiSelectorLiveView".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Reset".to_string(),
            1 => "Zoom".to_string(),
            3 => "Not Used".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorLiveView",
            0x25,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x80) >> 7;
        dm.push(("ShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ShutterSpeedLock", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x40) >> 6;
        dm.push(("ApertureLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ApertureLock", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x10) >> 4;
        dm.push(("MovieShutterButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Take Photo".to_string(),
            1 => "Record Movies".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieShutterButton",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x4) >> 2;
        dm.push(("FlashExposureCompArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Entire Frame".to_string(),
            1 => "Background Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashExposureCompArea",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "AutoFlashISOSensitivity".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Subject and Background".to_string(),
            1 => "Subject Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoFlashISOSensitivity",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x29) {
        let v = (v & 0xf0) >> 4;
        dm.push(("MovieFunc1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            2 => "Power Aperture (close)".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            11 => "Exposure Compensation -".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MovieFunc1Button", 0x29, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x29) {
        let v = v & 0xf;
        dm.push(("MoviePreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Power Aperture (open)".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            10 => "Exposure Compensation +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MoviePreviewButton",
            0x29,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2a) {
        let v = v & 0xf;
        dm.push(("Func1ButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "Func1ButtonPlusDials",
            0x2a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b) {
        let v = v & 0xf;
        dm.push((
            "PreviewButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PreviewButtonPlusDials",
            0x2b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2d) {
        let v = v & 0xf;
        dm.push((
            "AssignMovieRecordButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            11 => "Exposure Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieRecordButtonPlusDials",
            0x2d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2e) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptHighlightWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptHighlightWeighted",
                0x2e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x80) >> 7;
        dm.push(("DynamicAreaAFDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DynamicAreaAFDisplay",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x40) >> 6;
        dm.push(("AFPointIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On During Manual Focusing".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointIllumination",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x18) >> 3;
        dm.push(("StoreByOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Focus Point".to_string(),
            2 => "Focus Point and AF-area Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "StoreByOrientation",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = (v & 0x80) >> 7;
        dm.push(("MatrixMetering".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Face Detection On".to_string(),
            1 => "Face Detection Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MatrixMetering", 0x30, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = (v & 0x30) >> 4;
        dm.push(("LiveViewButtonOptions".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Enable".to_string(),
            1 => "Enable (Standby Timer Active)".to_string(),
            2 => "Disable".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewButtonOptions",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = v & 0x3;
        dm.push(("AFModeRestrictions".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Restrictions".to_string(),
            1 => "AF-C".to_string(),
            2 => "AF-S".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFModeRestrictions",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x31) {
        let v = (v & 0x7e) >> 1;
        dm.push((
            "LimitAFAreaModeSelection".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto-area".to_string(),
            1 => "Group-area".to_string(),
            2 => "3D-tracking".to_string(),
            3 => "Dynamic area (153 points)".to_string(),
            4 => "Dynamic area (72 points)".to_string(),
            5 => "Dynamic area (25 points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LimitAFAreaModeSelection",
            0x31,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x34) {
        let v = v & 0x3f;
        dm.push((
            "LensFocusFunctionButtons".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            24 => "Preset Focus Point".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 153 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 153 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            56 => "AF-Area Mode (Dynamic Area 9 Points)".to_string(),
            57 => "AF-Area Mode + AF-On (Dynamic Area 9 Points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LensFocusFunctionButtons",
            0x34,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x42) {
        let v = v & 0xff;
        dm.push(("VerticalMultiSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Same as Multi-Selector with Info(U/D) & Playback(R/L)".to_string(),
            8 => "Same as Multi-Selector with Info(R/L) & Playback(U/D)".to_string(),
            128 => "Focus Point Selection".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VerticalMultiSelector",
            0x42,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x43) {
        let v = v & 0x3f;
        dm.push((
            "AssignMB-D18FuncButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            18 => "Reset Focus Point".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            22 => "Remote Release Only".to_string(),
            23 => "Preset Focus Point".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 153 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 153 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            54 => "Highlight Active Focus Point".to_string(),
            56 => "AF-Area Mode (Dynamic Area 9 Points)".to_string(),
            57 => "AF-Area Mode + AF-On (Dynamic Area 9 Points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMB-D18FuncButton",
            0x43,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x44) {
        let v = v & 0xf;
        dm.push((
            "AssignMB-D18FuncButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            10 => "ISO Sensitivity".to_string(),
            11 => "Exposure Mode".to_string(),
            12 => "Exposure Compensation".to_string(),
            13 => "Metering Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMB-D18FuncButtonPlusDials",
            0x44,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x46) {
        let v = v & 0x3f;
        dm.push(("AF-OnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 153 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 153 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            56 => "AF-Area Mode (Dynamic Area 9 Points)".to_string(),
            57 => "AF-Area Mode + AF-On (Dynamic Area 9 Points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AF-OnButton", 0x46, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x47) {
        let v = (v & 0x80) >> 7;
        dm.push(("SubSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus Point Selection".to_string(),
            1 => "Same as MultiSelector".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SubSelector", 0x47, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x48) {
        let v = v & 0x3f;
        dm.push(("SubSelectorCenter".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            18 => "Reset Focus Point".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            22 => "Remote Release Only".to_string(),
            23 => "Preset Focus Point".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 153 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 153 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            54 => "Highlight Active Focus Point".to_string(),
            56 => "AF-Area Mode (Dynamic Area 9 Points)".to_string(),
            57 => "AF-Area Mode + AF-On (Dynamic Area 9 Points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubSelectorCenter",
            0x48,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x49) {
        let v = v & 0xf;
        dm.push(("SubSelectorPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubSelectorPlusDials",
            0x49,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4a) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "AssignMovieSubselector".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            5 => "AE/AF Lock".to_string(),
            6 => "AE Lock (Only)".to_string(),
            7 => "AE Lock (Hold)".to_string(),
            8 => "AF Lock (Only)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieSubselector",
            0x4a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4b) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "AssignMovieFunc1ButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieFunc1ButtonPlusDials",
            0x4b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4b) {
        let v = v & 0x1;
        dm.push((
            "AssignMoviePreviewButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMoviePreviewButtonPlusDials",
            0x4b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4c) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "AssignMovieSubselectorPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieSubselectorPlusDials",
            0x4c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4d) {
        let v = (v & 0x80) >> 7;
        dm.push(("SyncReleaseMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Sync".to_string(),
            1 => "Sync".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SyncReleaseMode", 0x4d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4d) {
        let v = (v & 0x40) >> 6;
        dm.push((
            "ContinuousModeLiveView".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ContinuousModeLiveView",
            0x4d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "Three-DTrackingWatchArea".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Wide".to_string(),
            1 => "Normal".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "Three-DTrackingWatchArea",
            0x4e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = (v & 0x60) >> 5;
        dm.push(("SubjectMotion".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Steady".to_string(),
            1 => "Middle".to_string(),
            2 => "Erratic".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SubjectMotion", 0x4e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = (v & 0x8) >> 3;
        dm.push(("AFActivation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Shutter/AF-On".to_string(),
            1 => "AF-On Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFActivation", 0x4e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = v & 0x3;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On (Half Press)".to_string(),
            2 => "On (Burst Mode)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x4e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4f) {
        let v = v & 0x7f;
        dm.push((
            "AssignMB-D18AF-OnButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 153 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 153 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            56 => "AF-Area Mode (Dynamic Area 9 Points)".to_string(),
            57 => "AF-Area Mode + AF-On (Dynamic Area 9 Points)".to_string(),
            100 => "Same as Camera AF-On Button".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMB-D18AF-OnButton",
            0x4f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x50) {
        let v = v & 0x3f;
        dm.push(("Func2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            15 => "My Menu Top Item".to_string(),
            20 => "My Menu".to_string(),
            55 => "Rating".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Func2Button", 0x50, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x52) {
        let v = (v & 0x70) >> 4;
        dm.push((
            "AssignMovieFunc2Button".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieFunc2Button",
            0x52,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsZ6III` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsz6iii(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Release + Focus".to_string(),
            3 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x3,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AF-SPrioritySelection",
            0x5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        dm.push(("BlockShotAFResponse".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "1 (Quick)".to_string(),
            2 => "2".to_string(),
            3 => "3 (Normal)".to_string(),
            4 => "4".to_string(),
            5 => "5 (Delayed)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "BlockShotAFResponse",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb) {
        dm.push(("AFPointSel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFPointSel",
            0xb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd) {
        dm.push(("StoreByOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Focus Point".to_string(),
            2 => "Focus Point and AF-area mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "StoreByOrientation",
            0xd,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf) {
        dm.push(("AFActivation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFActivation",
            0xf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        dm.push((
            "AF-OnOutOfFocusRelease".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        dm.push((
            "LimitAF-AreaModeSelPinpoint".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        dm.push((
            "LimitAF-AreaModeSelWideAF_S".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        dm.push((
            "LimitAF-AreaModeSelWideAF_L".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push((
            "LimitAFAreaModeSelAuto".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x17) {
        dm.push((
            "ManualFocusPointIllumination".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x18) {
        dm.push(("DynamicAreaAFAssist".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1a) {
        dm.push(("AF-AssistIlluminator".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-AssistIlluminator",
            0x1a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push((
            "ManualFocusRingInAFMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ManualFocusRingInAFMode",
            0x1b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1d) {
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x1d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (auto reset)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x1f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x21) {
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Small".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x21,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x23) {
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x23,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x25) {
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x25,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x27) {
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x27,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x29) {
        dm.push((
            "FineTuneOptHighlightWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptHighlightWeighted",
                0x29,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2b) {
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On (Half Press)".to_string(),
            2 => "On (Burst Mode)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x2b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2d) {
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x2d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x31) {
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x31,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x35) {
        dm.push(("SelfTimerShotInterval".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.5 s".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SelfTimerShotInterval",
            0x35,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x37) {
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x37,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x39) {
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x39,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3b) {
        dm.push((
            "ImageReviewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageReviewMonitorOffTime",
            0x3b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3d) {
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x3d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3f) {
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CLModeShootingSpeed",
                0x3f,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x41, bo) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val eq -1 ?  'No Limit' : $val ) ", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "MaxContinuousRelease",
                0x41,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x45) {
        dm.push(("SyncReleaseMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x4b) {
        dm.push((
            "LimitSelectableImageAreaDX".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x4c) {
        dm.push((
            "LimitSelectableImageArea1To1".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x4d) {
        dm.push((
            "LimitSelectableImageArea16To9".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FileNumberSequence",
            0x4e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x51) {
        dm.push(("FocusPeakingLevel".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x53) {
        dm.push((
            "FocusPeakingHighlightColor".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x55) {
        dm.push(("ContinuousModeDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ContinuousModeDisplay",
            0x55,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x57) {
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("($val-144)/8", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1/60 s".to_string(),
                1 => "1/80 s".to_string(),
                2 => "1/100 s".to_string(),
                3 => "1/125 s".to_string(),
                4 => "1/160 s".to_string(),
                5 => "1/200 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("FlashSyncSpeed", 0x57, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x59) {
        dm.push(("HighSpeedSync".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("HighSpeedSync", 0x59, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5b) {
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with(
            "my $t = ($val - 16) % 24; $t ? $val / 24  : 2 + ($val - 16) / 24",
            &base,
            &ctx,
        );
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1 s".to_string(),
                1 => "1/2 s".to_string(),
                2 => "1/4 s".to_string(),
                3 => "1/8 s".to_string(),
                4 => "1/15 s".to_string(),
                5 => "1/30 s".to_string(),
                6 => "1/60 s".to_string(),
                7 => "30 s".to_string(),
                8 => "15 s".to_string(),
                9 => "8 s".to_string(),
                10 => "4 s".to_string(),
                11 => "2 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashShutterSpeed",
                0x5b,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x5d) {
        dm.push(("FlashExposureCompArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FlashExposureCompArea",
            0x5d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5f) {
        dm.push((
            "AutoFlashISOSensitivity".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AutoFlashISOSensitivity",
            0x5f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x61) {
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x61, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x63) {
        dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Flash/Speed".to_string(),
            1 => "Flash/Speed/Aperture".to_string(),
            2 => "Flash/Aperture".to_string(),
            3 => "Flash Only".to_string(),
            4 => "Flash/ISO".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketModeM", 0x63, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x65) {
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AutoBracketOrder",
            0x65,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x67) {
        dm.push(("Func1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            3 => "FV Lock".to_string(),
            4 => "AE/AF Lock".to_string(),
            5 => "AE Lock Only".to_string(),
            6 => "AE Lock (reset on release)".to_string(),
            7 => "AE Lock (hold)".to_string(),
            8 => "AF Lock Only".to_string(),
            9 => "AF-On".to_string(),
            10 => "Flash Disable/Enable".to_string(),
            11 => "Bracketing Burst".to_string(),
            12 => "+NEF(RAW)".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Synchronized Release".to_string(),
            20 => "My Menu".to_string(),
            21 => "My Menu Top Item".to_string(),
            22 => "Playback".to_string(),
            23 => "Rating".to_string(),
            24 => "Protect".to_string(),
            25 => "Zoom".to_string(),
            26 => "Focus Peaking".to_string(),
            27 => "Flash Mode/Compensation".to_string(),
            28 => "Image Area".to_string(),
            30 => "Non-CPU Lens".to_string(),
            31 => "Active-D Lighting".to_string(),
            32 => "Exposure Delay Mode".to_string(),
            33 => "1 Stop Speed/Aperture".to_string(),
            34 => "White Balance".to_string(),
            35 => "Metering".to_string(),
            36 => "Auto Bracketing".to_string(),
            37 => "Multiple Exposure".to_string(),
            38 => "HDR Overlay".to_string(),
            39 => "Picture Control".to_string(),
            40 => "Quality".to_string(),
            41 => "Focus Mode/AF AreaMode".to_string(),
            42 => "Select Center Focus Point".to_string(),
            44 => "Record Movie".to_string(),
            45 => "Thumbnail On/Off".to_string(),
            46 => "View Histograms".to_string(),
            47 => "Choose Folder".to_string(),
            48 => "Power Aperture (Open)".to_string(),
            49 => "Power Aperture (Close)".to_string(),
            52 => "Microphone Sensitivity".to_string(),
            53 => "Release Mode".to_string(),
            57 => "Preset Focus Point".to_string(),
            58 => "AE/AWB Lock (hold)".to_string(),
            59 => "AF-AreaMode".to_string(),
            60 => "AF-AreaMode + AF-On".to_string(),
            61 => "Recall Shooting Functions".to_string(),
            64 => "Filtered Playback".to_string(),
            65 => "Same as AF-On".to_string(),
            66 => "Voice Memo".to_string(),
            70 => "Photo Shooting Bank".to_string(),
            71 => "ISO".to_string(),
            72 => "Shooting Mode".to_string(),
            73 => "Exposure Compensation".to_string(),
            76 => "Silent Mode".to_string(),
            78 => "LiveView Information".to_string(),
            79 => "AWB Lock (hold)".to_string(),
            80 => "Grid Display".to_string(),
            81 => "Starlight View".to_string(),
            82 => "Select To Send (PC)".to_string(),
            83 => "Select To Send (FTP)".to_string(),
            84 => "Pattern Tone Range".to_string(),
            85 => "Control Lock".to_string(),
            86 => "Save Focus Position".to_string(),
            87 => "Recall Focus Position".to_string(),
            88 => "Recall Shooting Functions (Hold)".to_string(),
            89 => "Set Picture Control (HLG)".to_string(),
            90 => "Skin Softening".to_string(),
            91 => "Portrait Impression Balance".to_string(),
            97 => "High Frequency Flicker Reduction".to_string(),
            98 => "Switch FX/DX".to_string(),
            99 => "View Mode (Photo LV)".to_string(),
            100 => "Photo Flicker Reduction".to_string(),
            101 => "Filtered Playback (Select Criteria)".to_string(),
            103 => "Start Series Playback".to_string(),
            104 => "View Assist".to_string(),
            105 => "Hi-Res Zoom+".to_string(),
            106 => "Hi-Res Zoom-".to_string(),
            108 => "Override Other Cameras".to_string(),
            109 => "DISP - Cycle Information Display (shooting)".to_string(),
            110 => "DISP - Cycle Information Display (playback)".to_string(),
            111 => "Resume Shooting".to_string(),
            112 => "Switch Eyes".to_string(),
            113 => "Power Zoom +".to_string(),
            114 => "Power Zoom -".to_string(),
            115 => "Delete".to_string(),
            116 => "Pixel Shift Shooting".to_string(),
            117 => "Cycle AF-area Mode".to_string(),
            118 => "Pixel Shift Shooting".to_string(),
            119 => "Move Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Func1Button", 0x67, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x77) {
        dm.push(("Func2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            3 => "FV Lock".to_string(),
            4 => "AE/AF Lock".to_string(),
            5 => "AE Lock Only".to_string(),
            6 => "AE Lock (reset on release)".to_string(),
            7 => "AE Lock (hold)".to_string(),
            8 => "AF Lock Only".to_string(),
            9 => "AF-On".to_string(),
            10 => "Flash Disable/Enable".to_string(),
            11 => "Bracketing Burst".to_string(),
            12 => "+NEF(RAW)".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Synchronized Release".to_string(),
            20 => "My Menu".to_string(),
            21 => "My Menu Top Item".to_string(),
            22 => "Playback".to_string(),
            23 => "Rating".to_string(),
            24 => "Protect".to_string(),
            25 => "Zoom".to_string(),
            26 => "Focus Peaking".to_string(),
            27 => "Flash Mode/Compensation".to_string(),
            28 => "Image Area".to_string(),
            30 => "Non-CPU Lens".to_string(),
            31 => "Active-D Lighting".to_string(),
            32 => "Exposure Delay Mode".to_string(),
            33 => "1 Stop Speed/Aperture".to_string(),
            34 => "White Balance".to_string(),
            35 => "Metering".to_string(),
            36 => "Auto Bracketing".to_string(),
            37 => "Multiple Exposure".to_string(),
            38 => "HDR Overlay".to_string(),
            39 => "Picture Control".to_string(),
            40 => "Quality".to_string(),
            41 => "Focus Mode/AF AreaMode".to_string(),
            42 => "Select Center Focus Point".to_string(),
            44 => "Record Movie".to_string(),
            45 => "Thumbnail On/Off".to_string(),
            46 => "View Histograms".to_string(),
            47 => "Choose Folder".to_string(),
            48 => "Power Aperture (Open)".to_string(),
            49 => "Power Aperture (Close)".to_string(),
            52 => "Microphone Sensitivity".to_string(),
            53 => "Release Mode".to_string(),
            57 => "Preset Focus Point".to_string(),
            58 => "AE/AWB Lock (hold)".to_string(),
            59 => "AF-AreaMode".to_string(),
            60 => "AF-AreaMode + AF-On".to_string(),
            61 => "Recall Shooting Functions".to_string(),
            64 => "Filtered Playback".to_string(),
            65 => "Same as AF-On".to_string(),
            66 => "Voice Memo".to_string(),
            70 => "Photo Shooting Bank".to_string(),
            71 => "ISO".to_string(),
            72 => "Shooting Mode".to_string(),
            73 => "Exposure Compensation".to_string(),
            76 => "Silent Mode".to_string(),
            78 => "LiveView Information".to_string(),
            79 => "AWB Lock (hold)".to_string(),
            80 => "Grid Display".to_string(),
            81 => "Starlight View".to_string(),
            82 => "Select To Send (PC)".to_string(),
            83 => "Select To Send (FTP)".to_string(),
            84 => "Pattern Tone Range".to_string(),
            85 => "Control Lock".to_string(),
            86 => "Save Focus Position".to_string(),
            87 => "Recall Focus Position".to_string(),
            88 => "Recall Shooting Functions (Hold)".to_string(),
            89 => "Set Picture Control (HLG)".to_string(),
            90 => "Skin Softening".to_string(),
            91 => "Portrait Impression Balance".to_string(),
            97 => "High Frequency Flicker Reduction".to_string(),
            98 => "Switch FX/DX".to_string(),
            99 => "View Mode (Photo LV)".to_string(),
            100 => "Photo Flicker Reduction".to_string(),
            101 => "Filtered Playback (Select Criteria)".to_string(),
            103 => "Start Series Playback".to_string(),
            104 => "View Assist".to_string(),
            105 => "Hi-Res Zoom+".to_string(),
            106 => "Hi-Res Zoom-".to_string(),
            108 => "Override Other Cameras".to_string(),
            109 => "DISP - Cycle Information Display (shooting)".to_string(),
            110 => "DISP - Cycle Information Display (playback)".to_string(),
            111 => "Resume Shooting".to_string(),
            112 => "Switch Eyes".to_string(),
            113 => "Power Zoom +".to_string(),
            114 => "Power Zoom -".to_string(),
            115 => "Delete".to_string(),
            116 => "Pixel Shift Shooting".to_string(),
            117 => "Cycle AF-area Mode".to_string(),
            118 => "Pixel Shift Shooting".to_string(),
            119 => "Move Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Func2Button", 0x77, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x87) {
        dm.push(("AFOnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            3 => "FV Lock".to_string(),
            4 => "AE/AF Lock".to_string(),
            5 => "AE Lock Only".to_string(),
            6 => "AE Lock (reset on release)".to_string(),
            7 => "AE Lock (hold)".to_string(),
            8 => "AF Lock Only".to_string(),
            9 => "AF-On".to_string(),
            10 => "Flash Disable/Enable".to_string(),
            11 => "Bracketing Burst".to_string(),
            12 => "+NEF(RAW)".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Synchronized Release".to_string(),
            20 => "My Menu".to_string(),
            21 => "My Menu Top Item".to_string(),
            22 => "Playback".to_string(),
            23 => "Rating".to_string(),
            24 => "Protect".to_string(),
            25 => "Zoom".to_string(),
            26 => "Focus Peaking".to_string(),
            27 => "Flash Mode/Compensation".to_string(),
            28 => "Image Area".to_string(),
            30 => "Non-CPU Lens".to_string(),
            31 => "Active-D Lighting".to_string(),
            32 => "Exposure Delay Mode".to_string(),
            33 => "1 Stop Speed/Aperture".to_string(),
            34 => "White Balance".to_string(),
            35 => "Metering".to_string(),
            36 => "Auto Bracketing".to_string(),
            37 => "Multiple Exposure".to_string(),
            38 => "HDR Overlay".to_string(),
            39 => "Picture Control".to_string(),
            40 => "Quality".to_string(),
            41 => "Focus Mode/AF AreaMode".to_string(),
            42 => "Select Center Focus Point".to_string(),
            44 => "Record Movie".to_string(),
            45 => "Thumbnail On/Off".to_string(),
            46 => "View Histograms".to_string(),
            47 => "Choose Folder".to_string(),
            48 => "Power Aperture (Open)".to_string(),
            49 => "Power Aperture (Close)".to_string(),
            52 => "Microphone Sensitivity".to_string(),
            53 => "Release Mode".to_string(),
            57 => "Preset Focus Point".to_string(),
            58 => "AE/AWB Lock (hold)".to_string(),
            59 => "AF-AreaMode".to_string(),
            60 => "AF-AreaMode + AF-On".to_string(),
            61 => "Recall Shooting Functions".to_string(),
            64 => "Filtered Playback".to_string(),
            65 => "Same as AF-On".to_string(),
            66 => "Voice Memo".to_string(),
            70 => "Photo Shooting Bank".to_string(),
            71 => "ISO".to_string(),
            72 => "Shooting Mode".to_string(),
            73 => "Exposure Compensation".to_string(),
            76 => "Silent Mode".to_string(),
            78 => "LiveView Information".to_string(),
            79 => "AWB Lock (hold)".to_string(),
            80 => "Grid Display".to_string(),
            81 => "Starlight View".to_string(),
            82 => "Select To Send (PC)".to_string(),
            83 => "Select To Send (FTP)".to_string(),
            84 => "Pattern Tone Range".to_string(),
            85 => "Control Lock".to_string(),
            86 => "Save Focus Position".to_string(),
            87 => "Recall Focus Position".to_string(),
            88 => "Recall Shooting Functions (Hold)".to_string(),
            89 => "Set Picture Control (HLG)".to_string(),
            90 => "Skin Softening".to_string(),
            91 => "Portrait Impression Balance".to_string(),
            97 => "High Frequency Flicker Reduction".to_string(),
            98 => "Switch FX/DX".to_string(),
            99 => "View Mode (Photo LV)".to_string(),
            100 => "Photo Flicker Reduction".to_string(),
            101 => "Filtered Playback (Select Criteria)".to_string(),
            103 => "Start Series Playback".to_string(),
            104 => "View Assist".to_string(),
            105 => "Hi-Res Zoom+".to_string(),
            106 => "Hi-Res Zoom-".to_string(),
            108 => "Override Other Cameras".to_string(),
            109 => "DISP - Cycle Information Display (shooting)".to_string(),
            110 => "DISP - Cycle Information Display (playback)".to_string(),
            111 => "Resume Shooting".to_string(),
            112 => "Switch Eyes".to_string(),
            113 => "Power Zoom +".to_string(),
            114 => "Power Zoom -".to_string(),
            115 => "Delete".to_string(),
            116 => "Pixel Shift Shooting".to_string(),
            117 => "Cycle AF-area Mode".to_string(),
            118 => "Pixel Shift Shooting".to_string(),
            119 => "Move Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFOnButton", 0x87, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x93) {
        dm.push(("SubSelector".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x9f) {
        dm.push((
            "AssignMovieRecordButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0xa3) {
        dm.push(("LensFunc1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            3 => "FV Lock".to_string(),
            4 => "AE/AF Lock".to_string(),
            5 => "AE Lock Only".to_string(),
            6 => "AE Lock (reset on release)".to_string(),
            7 => "AE Lock (hold)".to_string(),
            8 => "AF Lock Only".to_string(),
            9 => "AF-On".to_string(),
            10 => "Flash Disable/Enable".to_string(),
            11 => "Bracketing Burst".to_string(),
            12 => "+NEF(RAW)".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Synchronized Release".to_string(),
            20 => "My Menu".to_string(),
            21 => "My Menu Top Item".to_string(),
            22 => "Playback".to_string(),
            23 => "Rating".to_string(),
            24 => "Protect".to_string(),
            25 => "Zoom".to_string(),
            26 => "Focus Peaking".to_string(),
            27 => "Flash Mode/Compensation".to_string(),
            28 => "Image Area".to_string(),
            30 => "Non-CPU Lens".to_string(),
            31 => "Active-D Lighting".to_string(),
            32 => "Exposure Delay Mode".to_string(),
            33 => "1 Stop Speed/Aperture".to_string(),
            34 => "White Balance".to_string(),
            35 => "Metering".to_string(),
            36 => "Auto Bracketing".to_string(),
            37 => "Multiple Exposure".to_string(),
            38 => "HDR Overlay".to_string(),
            39 => "Picture Control".to_string(),
            40 => "Quality".to_string(),
            41 => "Focus Mode/AF AreaMode".to_string(),
            42 => "Select Center Focus Point".to_string(),
            44 => "Record Movie".to_string(),
            45 => "Thumbnail On/Off".to_string(),
            46 => "View Histograms".to_string(),
            47 => "Choose Folder".to_string(),
            48 => "Power Aperture (Open)".to_string(),
            49 => "Power Aperture (Close)".to_string(),
            52 => "Microphone Sensitivity".to_string(),
            53 => "Release Mode".to_string(),
            57 => "Preset Focus Point".to_string(),
            58 => "AE/AWB Lock (hold)".to_string(),
            59 => "AF-AreaMode".to_string(),
            60 => "AF-AreaMode + AF-On".to_string(),
            61 => "Recall Shooting Functions".to_string(),
            64 => "Filtered Playback".to_string(),
            65 => "Same as AF-On".to_string(),
            66 => "Voice Memo".to_string(),
            70 => "Photo Shooting Bank".to_string(),
            71 => "ISO".to_string(),
            72 => "Shooting Mode".to_string(),
            73 => "Exposure Compensation".to_string(),
            76 => "Silent Mode".to_string(),
            78 => "LiveView Information".to_string(),
            79 => "AWB Lock (hold)".to_string(),
            80 => "Grid Display".to_string(),
            81 => "Starlight View".to_string(),
            82 => "Select To Send (PC)".to_string(),
            83 => "Select To Send (FTP)".to_string(),
            84 => "Pattern Tone Range".to_string(),
            85 => "Control Lock".to_string(),
            86 => "Save Focus Position".to_string(),
            87 => "Recall Focus Position".to_string(),
            88 => "Recall Shooting Functions (Hold)".to_string(),
            89 => "Set Picture Control (HLG)".to_string(),
            90 => "Skin Softening".to_string(),
            91 => "Portrait Impression Balance".to_string(),
            97 => "High Frequency Flicker Reduction".to_string(),
            98 => "Switch FX/DX".to_string(),
            99 => "View Mode (Photo LV)".to_string(),
            100 => "Photo Flicker Reduction".to_string(),
            101 => "Filtered Playback (Select Criteria)".to_string(),
            103 => "Start Series Playback".to_string(),
            104 => "View Assist".to_string(),
            105 => "Hi-Res Zoom+".to_string(),
            106 => "Hi-Res Zoom-".to_string(),
            108 => "Override Other Cameras".to_string(),
            109 => "DISP - Cycle Information Display (shooting)".to_string(),
            110 => "DISP - Cycle Information Display (playback)".to_string(),
            111 => "Resume Shooting".to_string(),
            112 => "Switch Eyes".to_string(),
            113 => "Power Zoom +".to_string(),
            114 => "Power Zoom -".to_string(),
            115 => "Delete".to_string(),
            116 => "Pixel Shift Shooting".to_string(),
            117 => "Cycle AF-area Mode".to_string(),
            118 => "Pixel Shift Shooting".to_string(),
            119 => "Move Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensFunc1Button", 0xa3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xab) {
        dm.push(("LensFunc2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            3 => "FV Lock".to_string(),
            4 => "AE/AF Lock".to_string(),
            5 => "AE Lock Only".to_string(),
            6 => "AE Lock (reset on release)".to_string(),
            7 => "AE Lock (hold)".to_string(),
            8 => "AF Lock Only".to_string(),
            9 => "AF-On".to_string(),
            10 => "Flash Disable/Enable".to_string(),
            11 => "Bracketing Burst".to_string(),
            12 => "+NEF(RAW)".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Synchronized Release".to_string(),
            20 => "My Menu".to_string(),
            21 => "My Menu Top Item".to_string(),
            22 => "Playback".to_string(),
            23 => "Rating".to_string(),
            24 => "Protect".to_string(),
            25 => "Zoom".to_string(),
            26 => "Focus Peaking".to_string(),
            27 => "Flash Mode/Compensation".to_string(),
            28 => "Image Area".to_string(),
            30 => "Non-CPU Lens".to_string(),
            31 => "Active-D Lighting".to_string(),
            32 => "Exposure Delay Mode".to_string(),
            33 => "1 Stop Speed/Aperture".to_string(),
            34 => "White Balance".to_string(),
            35 => "Metering".to_string(),
            36 => "Auto Bracketing".to_string(),
            37 => "Multiple Exposure".to_string(),
            38 => "HDR Overlay".to_string(),
            39 => "Picture Control".to_string(),
            40 => "Quality".to_string(),
            41 => "Focus Mode/AF AreaMode".to_string(),
            42 => "Select Center Focus Point".to_string(),
            44 => "Record Movie".to_string(),
            45 => "Thumbnail On/Off".to_string(),
            46 => "View Histograms".to_string(),
            47 => "Choose Folder".to_string(),
            48 => "Power Aperture (Open)".to_string(),
            49 => "Power Aperture (Close)".to_string(),
            52 => "Microphone Sensitivity".to_string(),
            53 => "Release Mode".to_string(),
            57 => "Preset Focus Point".to_string(),
            58 => "AE/AWB Lock (hold)".to_string(),
            59 => "AF-AreaMode".to_string(),
            60 => "AF-AreaMode + AF-On".to_string(),
            61 => "Recall Shooting Functions".to_string(),
            64 => "Filtered Playback".to_string(),
            65 => "Same as AF-On".to_string(),
            66 => "Voice Memo".to_string(),
            70 => "Photo Shooting Bank".to_string(),
            71 => "ISO".to_string(),
            72 => "Shooting Mode".to_string(),
            73 => "Exposure Compensation".to_string(),
            76 => "Silent Mode".to_string(),
            78 => "LiveView Information".to_string(),
            79 => "AWB Lock (hold)".to_string(),
            80 => "Grid Display".to_string(),
            81 => "Starlight View".to_string(),
            82 => "Select To Send (PC)".to_string(),
            83 => "Select To Send (FTP)".to_string(),
            84 => "Pattern Tone Range".to_string(),
            85 => "Control Lock".to_string(),
            86 => "Save Focus Position".to_string(),
            87 => "Recall Focus Position".to_string(),
            88 => "Recall Shooting Functions (Hold)".to_string(),
            89 => "Set Picture Control (HLG)".to_string(),
            90 => "Skin Softening".to_string(),
            91 => "Portrait Impression Balance".to_string(),
            97 => "High Frequency Flicker Reduction".to_string(),
            98 => "Switch FX/DX".to_string(),
            99 => "View Mode (Photo LV)".to_string(),
            100 => "Photo Flicker Reduction".to_string(),
            101 => "Filtered Playback (Select Criteria)".to_string(),
            103 => "Start Series Playback".to_string(),
            104 => "View Assist".to_string(),
            105 => "Hi-Res Zoom+".to_string(),
            106 => "Hi-Res Zoom-".to_string(),
            108 => "Override Other Cameras".to_string(),
            109 => "DISP - Cycle Information Display (shooting)".to_string(),
            110 => "DISP - Cycle Information Display (playback)".to_string(),
            111 => "Resume Shooting".to_string(),
            112 => "Switch Eyes".to_string(),
            113 => "Power Zoom +".to_string(),
            114 => "Power Zoom -".to_string(),
            115 => "Delete".to_string(),
            116 => "Pixel Shift Shooting".to_string(),
            117 => "Cycle AF-area Mode".to_string(),
            118 => "Pixel Shift Shooting".to_string(),
            119 => "Move Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensFunc2Button", 0xab, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb1) {
        dm.push(("LensControlRing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None (Disabled)".to_string(),
            1 => "Focus (M/A)".to_string(),
            2 => "ISO Sensitivity".to_string(),
            3 => "Exposure Compensation".to_string(),
            4 => "Aperture".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensControlRing", 0xb1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb3) {
        dm.push((
            "MultiSelectorShootMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            3 => "FV Lock".to_string(),
            4 => "AE/AF Lock".to_string(),
            5 => "AE Lock Only".to_string(),
            6 => "AE Lock (reset on release)".to_string(),
            7 => "AE Lock (hold)".to_string(),
            8 => "AF Lock Only".to_string(),
            9 => "AF-On".to_string(),
            10 => "Flash Disable/Enable".to_string(),
            11 => "Bracketing Burst".to_string(),
            12 => "+NEF(RAW)".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Synchronized Release".to_string(),
            20 => "My Menu".to_string(),
            21 => "My Menu Top Item".to_string(),
            22 => "Playback".to_string(),
            23 => "Rating".to_string(),
            24 => "Protect".to_string(),
            25 => "Zoom".to_string(),
            26 => "Focus Peaking".to_string(),
            27 => "Flash Mode/Compensation".to_string(),
            28 => "Image Area".to_string(),
            30 => "Non-CPU Lens".to_string(),
            31 => "Active-D Lighting".to_string(),
            32 => "Exposure Delay Mode".to_string(),
            33 => "1 Stop Speed/Aperture".to_string(),
            34 => "White Balance".to_string(),
            35 => "Metering".to_string(),
            36 => "Auto Bracketing".to_string(),
            37 => "Multiple Exposure".to_string(),
            38 => "HDR Overlay".to_string(),
            39 => "Picture Control".to_string(),
            40 => "Quality".to_string(),
            41 => "Focus Mode/AF AreaMode".to_string(),
            42 => "Select Center Focus Point".to_string(),
            44 => "Record Movie".to_string(),
            45 => "Thumbnail On/Off".to_string(),
            46 => "View Histograms".to_string(),
            47 => "Choose Folder".to_string(),
            48 => "Power Aperture (Open)".to_string(),
            49 => "Power Aperture (Close)".to_string(),
            52 => "Microphone Sensitivity".to_string(),
            53 => "Release Mode".to_string(),
            57 => "Preset Focus Point".to_string(),
            58 => "AE/AWB Lock (hold)".to_string(),
            59 => "AF-AreaMode".to_string(),
            60 => "AF-AreaMode + AF-On".to_string(),
            61 => "Recall Shooting Functions".to_string(),
            64 => "Filtered Playback".to_string(),
            65 => "Same as AF-On".to_string(),
            66 => "Voice Memo".to_string(),
            70 => "Photo Shooting Bank".to_string(),
            71 => "ISO".to_string(),
            72 => "Shooting Mode".to_string(),
            73 => "Exposure Compensation".to_string(),
            76 => "Silent Mode".to_string(),
            78 => "LiveView Information".to_string(),
            79 => "AWB Lock (hold)".to_string(),
            80 => "Grid Display".to_string(),
            81 => "Starlight View".to_string(),
            82 => "Select To Send (PC)".to_string(),
            83 => "Select To Send (FTP)".to_string(),
            84 => "Pattern Tone Range".to_string(),
            85 => "Control Lock".to_string(),
            86 => "Save Focus Position".to_string(),
            87 => "Recall Focus Position".to_string(),
            88 => "Recall Shooting Functions (Hold)".to_string(),
            89 => "Set Picture Control (HLG)".to_string(),
            90 => "Skin Softening".to_string(),
            91 => "Portrait Impression Balance".to_string(),
            97 => "High Frequency Flicker Reduction".to_string(),
            98 => "Switch FX/DX".to_string(),
            99 => "View Mode (Photo LV)".to_string(),
            100 => "Photo Flicker Reduction".to_string(),
            101 => "Filtered Playback (Select Criteria)".to_string(),
            103 => "Start Series Playback".to_string(),
            104 => "View Assist".to_string(),
            105 => "Hi-Res Zoom+".to_string(),
            106 => "Hi-Res Zoom-".to_string(),
            108 => "Override Other Cameras".to_string(),
            109 => "DISP - Cycle Information Display (shooting)".to_string(),
            110 => "DISP - Cycle Information Display (playback)".to_string(),
            111 => "Resume Shooting".to_string(),
            112 => "Switch Eyes".to_string(),
            113 => "Power Zoom +".to_string(),
            114 => "Power Zoom -".to_string(),
            115 => "Delete".to_string(),
            116 => "Pixel Shift Shooting".to_string(),
            117 => "Cycle AF-area Mode".to_string(),
            118 => "Pixel Shift Shooting".to_string(),
            119 => "Move Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorShootMode",
            0xb3,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb7) {
        dm.push((
            "MultiSelectorPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            3 => "FV Lock".to_string(),
            4 => "AE/AF Lock".to_string(),
            5 => "AE Lock Only".to_string(),
            6 => "AE Lock (reset on release)".to_string(),
            7 => "AE Lock (hold)".to_string(),
            8 => "AF Lock Only".to_string(),
            9 => "AF-On".to_string(),
            10 => "Flash Disable/Enable".to_string(),
            11 => "Bracketing Burst".to_string(),
            12 => "+NEF(RAW)".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Synchronized Release".to_string(),
            20 => "My Menu".to_string(),
            21 => "My Menu Top Item".to_string(),
            22 => "Playback".to_string(),
            23 => "Rating".to_string(),
            24 => "Protect".to_string(),
            25 => "Zoom".to_string(),
            26 => "Focus Peaking".to_string(),
            27 => "Flash Mode/Compensation".to_string(),
            28 => "Image Area".to_string(),
            30 => "Non-CPU Lens".to_string(),
            31 => "Active-D Lighting".to_string(),
            32 => "Exposure Delay Mode".to_string(),
            33 => "1 Stop Speed/Aperture".to_string(),
            34 => "White Balance".to_string(),
            35 => "Metering".to_string(),
            36 => "Auto Bracketing".to_string(),
            37 => "Multiple Exposure".to_string(),
            38 => "HDR Overlay".to_string(),
            39 => "Picture Control".to_string(),
            40 => "Quality".to_string(),
            41 => "Focus Mode/AF AreaMode".to_string(),
            42 => "Select Center Focus Point".to_string(),
            44 => "Record Movie".to_string(),
            45 => "Thumbnail On/Off".to_string(),
            46 => "View Histograms".to_string(),
            47 => "Choose Folder".to_string(),
            48 => "Power Aperture (Open)".to_string(),
            49 => "Power Aperture (Close)".to_string(),
            52 => "Microphone Sensitivity".to_string(),
            53 => "Release Mode".to_string(),
            57 => "Preset Focus Point".to_string(),
            58 => "AE/AWB Lock (hold)".to_string(),
            59 => "AF-AreaMode".to_string(),
            60 => "AF-AreaMode + AF-On".to_string(),
            61 => "Recall Shooting Functions".to_string(),
            64 => "Filtered Playback".to_string(),
            65 => "Same as AF-On".to_string(),
            66 => "Voice Memo".to_string(),
            70 => "Photo Shooting Bank".to_string(),
            71 => "ISO".to_string(),
            72 => "Shooting Mode".to_string(),
            73 => "Exposure Compensation".to_string(),
            76 => "Silent Mode".to_string(),
            78 => "LiveView Information".to_string(),
            79 => "AWB Lock (hold)".to_string(),
            80 => "Grid Display".to_string(),
            81 => "Starlight View".to_string(),
            82 => "Select To Send (PC)".to_string(),
            83 => "Select To Send (FTP)".to_string(),
            84 => "Pattern Tone Range".to_string(),
            85 => "Control Lock".to_string(),
            86 => "Save Focus Position".to_string(),
            87 => "Recall Focus Position".to_string(),
            88 => "Recall Shooting Functions (Hold)".to_string(),
            89 => "Set Picture Control (HLG)".to_string(),
            90 => "Skin Softening".to_string(),
            91 => "Portrait Impression Balance".to_string(),
            97 => "High Frequency Flicker Reduction".to_string(),
            98 => "Switch FX/DX".to_string(),
            99 => "View Mode (Photo LV)".to_string(),
            100 => "Photo Flicker Reduction".to_string(),
            101 => "Filtered Playback (Select Criteria)".to_string(),
            103 => "Start Series Playback".to_string(),
            104 => "View Assist".to_string(),
            105 => "Hi-Res Zoom+".to_string(),
            106 => "Hi-Res Zoom-".to_string(),
            108 => "Override Other Cameras".to_string(),
            109 => "DISP - Cycle Information Display (shooting)".to_string(),
            110 => "DISP - Cycle Information Display (playback)".to_string(),
            111 => "Resume Shooting".to_string(),
            112 => "Switch Eyes".to_string(),
            113 => "Power Zoom +".to_string(),
            114 => "Power Zoom -".to_string(),
            115 => "Delete".to_string(),
            116 => "Pixel Shift Shooting".to_string(),
            117 => "Cycle AF-area Mode".to_string(),
            118 => "Pixel Shift Shooting".to_string(),
            119 => "Move Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorPlaybackMode",
            0xb7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xbb) {
        dm.push(("ShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ShutterSpeedLock", 0xbb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xbc) {
        dm.push(("ApertureLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ApertureLock", 0xbc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xbd) {
        dm.push((
            "CmdDialsReverseRotExposureComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "CmdDialsReverseRotExposureComp",
                0xbd,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CmdDialsReverseRotExposureComp").is_some_and(|v| v == 0.0) {
        if let Some(v) = u8_at(data, 0xbe) {
            dm.push((
                "CmdDialsReverseRotation".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "No".to_string(),
                1 => "Shutter Speed & Aperture".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CmdDialsReverseRotation",
                0xbe,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(dm_get(dm, "CmdDialsReverseRotExposureComp").is_some_and(|v| v == 0.0)) {
        if let Some(v) = u8_at(data, 0xbe) {
            dm.push((
                "CmdDialsReverseRotation".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Exposure Compensation".to_string(),
                1 => "Exposure Compensation, Shutter Speed & Aperture".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CmdDialsReverseRotation",
                0xbe,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xc3) {
        dm.push(("UseDialWithoutHold".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xc5) {
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xc7) {
        dm.push(("MovieFunc1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            3 => "FV Lock".to_string(),
            4 => "AE/AF Lock".to_string(),
            5 => "AE Lock Only".to_string(),
            6 => "AE Lock (reset on release)".to_string(),
            7 => "AE Lock (hold)".to_string(),
            8 => "AF Lock Only".to_string(),
            9 => "AF-On".to_string(),
            10 => "Flash Disable/Enable".to_string(),
            11 => "Bracketing Burst".to_string(),
            12 => "+NEF(RAW)".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Synchronized Release".to_string(),
            20 => "My Menu".to_string(),
            21 => "My Menu Top Item".to_string(),
            22 => "Playback".to_string(),
            23 => "Rating".to_string(),
            24 => "Protect".to_string(),
            25 => "Zoom".to_string(),
            26 => "Focus Peaking".to_string(),
            27 => "Flash Mode/Compensation".to_string(),
            28 => "Image Area".to_string(),
            30 => "Non-CPU Lens".to_string(),
            31 => "Active-D Lighting".to_string(),
            32 => "Exposure Delay Mode".to_string(),
            33 => "1 Stop Speed/Aperture".to_string(),
            34 => "White Balance".to_string(),
            35 => "Metering".to_string(),
            36 => "Auto Bracketing".to_string(),
            37 => "Multiple Exposure".to_string(),
            38 => "HDR Overlay".to_string(),
            39 => "Picture Control".to_string(),
            40 => "Quality".to_string(),
            41 => "Focus Mode/AF AreaMode".to_string(),
            42 => "Select Center Focus Point".to_string(),
            44 => "Record Movie".to_string(),
            45 => "Thumbnail On/Off".to_string(),
            46 => "View Histograms".to_string(),
            47 => "Choose Folder".to_string(),
            48 => "Power Aperture (Open)".to_string(),
            49 => "Power Aperture (Close)".to_string(),
            52 => "Microphone Sensitivity".to_string(),
            53 => "Release Mode".to_string(),
            57 => "Preset Focus Point".to_string(),
            58 => "AE/AWB Lock (hold)".to_string(),
            59 => "AF-AreaMode".to_string(),
            60 => "AF-AreaMode + AF-On".to_string(),
            61 => "Recall Shooting Functions".to_string(),
            64 => "Filtered Playback".to_string(),
            65 => "Same as AF-On".to_string(),
            66 => "Voice Memo".to_string(),
            70 => "Photo Shooting Bank".to_string(),
            71 => "ISO".to_string(),
            72 => "Shooting Mode".to_string(),
            73 => "Exposure Compensation".to_string(),
            76 => "Silent Mode".to_string(),
            78 => "LiveView Information".to_string(),
            79 => "AWB Lock (hold)".to_string(),
            80 => "Grid Display".to_string(),
            81 => "Starlight View".to_string(),
            82 => "Select To Send (PC)".to_string(),
            83 => "Select To Send (FTP)".to_string(),
            84 => "Pattern Tone Range".to_string(),
            85 => "Control Lock".to_string(),
            86 => "Save Focus Position".to_string(),
            87 => "Recall Focus Position".to_string(),
            88 => "Recall Shooting Functions (Hold)".to_string(),
            89 => "Set Picture Control (HLG)".to_string(),
            90 => "Skin Softening".to_string(),
            91 => "Portrait Impression Balance".to_string(),
            97 => "High Frequency Flicker Reduction".to_string(),
            98 => "Switch FX/DX".to_string(),
            99 => "View Mode (Photo LV)".to_string(),
            100 => "Photo Flicker Reduction".to_string(),
            101 => "Filtered Playback (Select Criteria)".to_string(),
            103 => "Start Series Playback".to_string(),
            104 => "View Assist".to_string(),
            105 => "Hi-Res Zoom+".to_string(),
            106 => "Hi-Res Zoom-".to_string(),
            108 => "Override Other Cameras".to_string(),
            109 => "DISP - Cycle Information Display (shooting)".to_string(),
            110 => "DISP - Cycle Information Display (playback)".to_string(),
            111 => "Resume Shooting".to_string(),
            112 => "Switch Eyes".to_string(),
            113 => "Power Zoom +".to_string(),
            114 => "Power Zoom -".to_string(),
            115 => "Delete".to_string(),
            116 => "Pixel Shift Shooting".to_string(),
            117 => "Cycle AF-area Mode".to_string(),
            118 => "Pixel Shift Shooting".to_string(),
            119 => "Move Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MovieFunc1Button", 0xc7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xcd) {
        dm.push(("MovieFunc2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            3 => "FV Lock".to_string(),
            4 => "AE/AF Lock".to_string(),
            5 => "AE Lock Only".to_string(),
            6 => "AE Lock (reset on release)".to_string(),
            7 => "AE Lock (hold)".to_string(),
            8 => "AF Lock Only".to_string(),
            9 => "AF-On".to_string(),
            10 => "Flash Disable/Enable".to_string(),
            11 => "Bracketing Burst".to_string(),
            12 => "+NEF(RAW)".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Synchronized Release".to_string(),
            20 => "My Menu".to_string(),
            21 => "My Menu Top Item".to_string(),
            22 => "Playback".to_string(),
            23 => "Rating".to_string(),
            24 => "Protect".to_string(),
            25 => "Zoom".to_string(),
            26 => "Focus Peaking".to_string(),
            27 => "Flash Mode/Compensation".to_string(),
            28 => "Image Area".to_string(),
            30 => "Non-CPU Lens".to_string(),
            31 => "Active-D Lighting".to_string(),
            32 => "Exposure Delay Mode".to_string(),
            33 => "1 Stop Speed/Aperture".to_string(),
            34 => "White Balance".to_string(),
            35 => "Metering".to_string(),
            36 => "Auto Bracketing".to_string(),
            37 => "Multiple Exposure".to_string(),
            38 => "HDR Overlay".to_string(),
            39 => "Picture Control".to_string(),
            40 => "Quality".to_string(),
            41 => "Focus Mode/AF AreaMode".to_string(),
            42 => "Select Center Focus Point".to_string(),
            44 => "Record Movie".to_string(),
            45 => "Thumbnail On/Off".to_string(),
            46 => "View Histograms".to_string(),
            47 => "Choose Folder".to_string(),
            48 => "Power Aperture (Open)".to_string(),
            49 => "Power Aperture (Close)".to_string(),
            52 => "Microphone Sensitivity".to_string(),
            53 => "Release Mode".to_string(),
            57 => "Preset Focus Point".to_string(),
            58 => "AE/AWB Lock (hold)".to_string(),
            59 => "AF-AreaMode".to_string(),
            60 => "AF-AreaMode + AF-On".to_string(),
            61 => "Recall Shooting Functions".to_string(),
            64 => "Filtered Playback".to_string(),
            65 => "Same as AF-On".to_string(),
            66 => "Voice Memo".to_string(),
            70 => "Photo Shooting Bank".to_string(),
            71 => "ISO".to_string(),
            72 => "Shooting Mode".to_string(),
            73 => "Exposure Compensation".to_string(),
            76 => "Silent Mode".to_string(),
            78 => "LiveView Information".to_string(),
            79 => "AWB Lock (hold)".to_string(),
            80 => "Grid Display".to_string(),
            81 => "Starlight View".to_string(),
            82 => "Select To Send (PC)".to_string(),
            83 => "Select To Send (FTP)".to_string(),
            84 => "Pattern Tone Range".to_string(),
            85 => "Control Lock".to_string(),
            86 => "Save Focus Position".to_string(),
            87 => "Recall Focus Position".to_string(),
            88 => "Recall Shooting Functions (Hold)".to_string(),
            89 => "Set Picture Control (HLG)".to_string(),
            90 => "Skin Softening".to_string(),
            91 => "Portrait Impression Balance".to_string(),
            97 => "High Frequency Flicker Reduction".to_string(),
            98 => "Switch FX/DX".to_string(),
            99 => "View Mode (Photo LV)".to_string(),
            100 => "Photo Flicker Reduction".to_string(),
            101 => "Filtered Playback (Select Criteria)".to_string(),
            103 => "Start Series Playback".to_string(),
            104 => "View Assist".to_string(),
            105 => "Hi-Res Zoom+".to_string(),
            106 => "Hi-Res Zoom-".to_string(),
            108 => "Override Other Cameras".to_string(),
            109 => "DISP - Cycle Information Display (shooting)".to_string(),
            110 => "DISP - Cycle Information Display (playback)".to_string(),
            111 => "Resume Shooting".to_string(),
            112 => "Switch Eyes".to_string(),
            113 => "Power Zoom +".to_string(),
            114 => "Power Zoom -".to_string(),
            115 => "Delete".to_string(),
            116 => "Pixel Shift Shooting".to_string(),
            117 => "Cycle AF-area Mode".to_string(),
            118 => "Pixel Shift Shooting".to_string(),
            119 => "Move Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MovieFunc2Button", 0xcd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd3) {
        dm.push(("MovieAF-OnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            3 => "FV Lock".to_string(),
            4 => "AE/AF Lock".to_string(),
            5 => "AE Lock Only".to_string(),
            6 => "AE Lock (reset on release)".to_string(),
            7 => "AE Lock (hold)".to_string(),
            8 => "AF Lock Only".to_string(),
            9 => "AF-On".to_string(),
            10 => "Flash Disable/Enable".to_string(),
            11 => "Bracketing Burst".to_string(),
            12 => "+NEF(RAW)".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Synchronized Release".to_string(),
            20 => "My Menu".to_string(),
            21 => "My Menu Top Item".to_string(),
            22 => "Playback".to_string(),
            23 => "Rating".to_string(),
            24 => "Protect".to_string(),
            25 => "Zoom".to_string(),
            26 => "Focus Peaking".to_string(),
            27 => "Flash Mode/Compensation".to_string(),
            28 => "Image Area".to_string(),
            30 => "Non-CPU Lens".to_string(),
            31 => "Active-D Lighting".to_string(),
            32 => "Exposure Delay Mode".to_string(),
            33 => "1 Stop Speed/Aperture".to_string(),
            34 => "White Balance".to_string(),
            35 => "Metering".to_string(),
            36 => "Auto Bracketing".to_string(),
            37 => "Multiple Exposure".to_string(),
            38 => "HDR Overlay".to_string(),
            39 => "Picture Control".to_string(),
            40 => "Quality".to_string(),
            41 => "Focus Mode/AF AreaMode".to_string(),
            42 => "Select Center Focus Point".to_string(),
            44 => "Record Movie".to_string(),
            45 => "Thumbnail On/Off".to_string(),
            46 => "View Histograms".to_string(),
            47 => "Choose Folder".to_string(),
            48 => "Power Aperture (Open)".to_string(),
            49 => "Power Aperture (Close)".to_string(),
            52 => "Microphone Sensitivity".to_string(),
            53 => "Release Mode".to_string(),
            57 => "Preset Focus Point".to_string(),
            58 => "AE/AWB Lock (hold)".to_string(),
            59 => "AF-AreaMode".to_string(),
            60 => "AF-AreaMode + AF-On".to_string(),
            61 => "Recall Shooting Functions".to_string(),
            64 => "Filtered Playback".to_string(),
            65 => "Same as AF-On".to_string(),
            66 => "Voice Memo".to_string(),
            70 => "Photo Shooting Bank".to_string(),
            71 => "ISO".to_string(),
            72 => "Shooting Mode".to_string(),
            73 => "Exposure Compensation".to_string(),
            76 => "Silent Mode".to_string(),
            78 => "LiveView Information".to_string(),
            79 => "AWB Lock (hold)".to_string(),
            80 => "Grid Display".to_string(),
            81 => "Starlight View".to_string(),
            82 => "Select To Send (PC)".to_string(),
            83 => "Select To Send (FTP)".to_string(),
            84 => "Pattern Tone Range".to_string(),
            85 => "Control Lock".to_string(),
            86 => "Save Focus Position".to_string(),
            87 => "Recall Focus Position".to_string(),
            88 => "Recall Shooting Functions (Hold)".to_string(),
            89 => "Set Picture Control (HLG)".to_string(),
            90 => "Skin Softening".to_string(),
            91 => "Portrait Impression Balance".to_string(),
            97 => "High Frequency Flicker Reduction".to_string(),
            98 => "Switch FX/DX".to_string(),
            99 => "View Mode (Photo LV)".to_string(),
            100 => "Photo Flicker Reduction".to_string(),
            101 => "Filtered Playback (Select Criteria)".to_string(),
            103 => "Start Series Playback".to_string(),
            104 => "View Assist".to_string(),
            105 => "Hi-Res Zoom+".to_string(),
            106 => "Hi-Res Zoom-".to_string(),
            108 => "Override Other Cameras".to_string(),
            109 => "DISP - Cycle Information Display (shooting)".to_string(),
            110 => "DISP - Cycle Information Display (playback)".to_string(),
            111 => "Resume Shooting".to_string(),
            112 => "Switch Eyes".to_string(),
            113 => "Power Zoom +".to_string(),
            114 => "Power Zoom -".to_string(),
            115 => "Delete".to_string(),
            116 => "Pixel Shift Shooting".to_string(),
            117 => "Cycle AF-area Mode".to_string(),
            118 => "Pixel Shift Shooting".to_string(),
            119 => "Move Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MovieAF-OnButton", 0xd3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xdf) {
        dm.push(("MovieLensControlRing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None (Disabled)".to_string(),
            2 => "ISO Sensitivity".to_string(),
            3 => "Exposure Compensation".to_string(),
            4 => "Power Aperture".to_string(),
            5 => "Hi-Res Zoom".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieLensControlRing",
            0xdf,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe1) {
        dm.push(("MovieMultiSelector".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xe5) {
        dm.push(("MovieAFSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 5", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "MovieAFSpeed",
                0xe5,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xe7) {
        dm.push(("MovieAFSpeedApply".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieAFSpeedApply",
            0xe7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe9) {
        dm.push((
            "MovieAFTrackingSensitivity".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 (High)".to_string(),
            1 => "2".to_string(),
            2 => "3".to_string(),
            3 => "4 (Normal)".to_string(),
            4 => "5".to_string(),
            5 => "6".to_string(),
            6 => "7 (Low)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieAFTrackingSensitivity",
            0xe9,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf1) {
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xf2) {
        dm.push(("ExtendedShutterSpeeds".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExtendedShutterSpeeds",
            0xf2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf5) {
        dm.push(("FocusPointPersistence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusPointPersistence",
            0xf5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x101) {
        dm.push(("FlashBurstPriority".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x13f) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_S".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x140) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_M".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x141) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_L".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x143) {
        dm.push((
            "LimitAF-AreaModeSel3DTracking".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x145) {
        dm.push(("PlaybackFlickUp".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x149) {
        dm.push(("PlaybackFlickDown".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x14d) {
        dm.push(("ISOStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOStepSize", 0x14d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x154) {
        dm.push(("TouchFn".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x155) {
        dm.push(("TouchFnRole".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x159) {
        dm.push(("TouchFnAreaWide".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x15b) {
        dm.push(("TouchFnAreaTall".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x15e) {
        dm.push(("EVFImageFrame".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x15f) {
        dm.push(("EVFGrid".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x161) {
        dm.push(("VirtualHorizonStyle".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x19f) {
        dm.push((
            "Func1ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1a1) {
        dm.push((
            "Func2ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1af) {
        dm.push((
            "MovieRecordButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1cf) {
        dm.push(("ControlRingResponse".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ControlRingResponse",
            0x1cf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x22f) {
        dm.push(("FocusPeakingDisplay".to_string(), Conv::Num(f64::from(v))));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsZ8` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsz8(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x1) {
        dm.push(("CustomSettingsBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsBank",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Release + Focus".to_string(),
            3 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x3,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AF-SPrioritySelection",
            0x5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        dm.push(("BlockShotAFResponse".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "1 (Quick)".to_string(),
            2 => "2".to_string(),
            3 => "3 (Normal)".to_string(),
            4 => "4".to_string(),
            5 => "5 (Delayed)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "BlockShotAFResponse",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb) {
        dm.push(("AFPointSel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFPointSel",
            0xb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd) {
        dm.push(("StoreByOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Focus Point".to_string(),
            2 => "Focus Point and AF-area mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "StoreByOrientation",
            0xd,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf) {
        dm.push(("AFActivation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFActivation",
            0xf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        dm.push((
            "AF-OnOutOfFocusRelease".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        dm.push((
            "LimitAF-AreaModeSelPinpoint".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        dm.push((
            "LimitAF-AreaModeSelWideAF_S".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        dm.push((
            "LimitAF-AreaModeSelWideAF_L".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push((
            "LimitAFAreaModeSelAuto".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x17) {
        dm.push((
            "ManualFocusPointIllumination".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x18) {
        dm.push(("DynamicAreaAFAssist".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x19) {
        dm.push(("AF-AssistIlluminator".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-AssistIlluminator",
            0x19,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1a) {
        dm.push((
            "ManualFocusRingInAFMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ManualFocusRingInAFMode",
            0x1a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x1b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1d) {
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (auto reset)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x1d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "8 mm".to_string(),
            1 => "12 mm".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x1f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x21) {
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x21,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x23) {
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x23,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x25) {
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x25,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x27) {
        dm.push((
            "FineTuneOptHighlightWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptHighlightWeighted",
                0x27,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x29) {
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On (Half Press)".to_string(),
            2 => "On (Burst Mode)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x29,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b) {
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x2b, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2d) {
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x2d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x31) {
        dm.push(("SelfTimerShotInterval".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.5 s".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            4 => "Min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SelfTimerShotInterval",
            0x31,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x33) {
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x33,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x35) {
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x35,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x37) {
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x37,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x39) {
        dm.push((
            "ImageReviewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageReviewMonitorOffTime",
            0x39,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3b) {
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CLModeShootingSpeed",
                0x3b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x3d, bo) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val eq -1 ?  'No Limit' : $val ) ", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "MaxContinuousRelease",
                0x3d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x41) {
        dm.push(("SyncReleaseMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x45) {
        dm.push((
            "LimitSelectableImageAreaDX".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x46) {
        dm.push((
            "LimitSelectableImageArea1To1".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x47) {
        dm.push((
            "LimitSelectableImageArea16To9".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x48) {
        dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FileNumberSequence",
            0x48,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x49) {
        dm.push(("FocusPeakingLevel".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x4b) {
        dm.push((
            "FocusPeakingHighlightColor".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x51) {
        dm.push(("ContinuousModeDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ContinuousModeDisplay",
            0x51,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x53) {
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("($val-144)/8", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1/60 s".to_string(),
                1 => "1/80 s".to_string(),
                2 => "1/100 s".to_string(),
                3 => "1/125 s".to_string(),
                4 => "1/160 s".to_string(),
                5 => "1/200 s".to_string(),
                6 => "1/250 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("FlashSyncSpeed", 0x53, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x55) {
        dm.push(("HighSpeedSync".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("HighSpeedSync", 0x55, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x57) {
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with(
            "my $t = ($val - 16) % 24; $t ? $val / 24  : 2 + ($val - 16) / 24",
            &base,
            &ctx,
        );
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1 s".to_string(),
                1 => "1/2 s".to_string(),
                2 => "1/4 s".to_string(),
                3 => "1/8 s".to_string(),
                4 => "1/15 s".to_string(),
                5 => "1/30 s".to_string(),
                6 => "1/60 s".to_string(),
                7 => "30 s".to_string(),
                8 => "15 s".to_string(),
                9 => "8 s".to_string(),
                10 => "4 s".to_string(),
                11 => "2 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashShutterSpeed",
                0x57,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x59) {
        dm.push(("FlashExposureCompArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FlashExposureCompArea",
            0x59,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5b) {
        dm.push((
            "AutoFlashISOSensitivity".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AutoFlashISOSensitivity",
            0x5b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5d) {
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x5d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5f) {
        dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Flash/Speed".to_string(),
            1 => "Flash/Speed/Aperture".to_string(),
            2 => "Flash/Aperture".to_string(),
            3 => "Flash Only".to_string(),
            4 => "Flash/ISO".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketModeM", 0x5f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x61) {
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AutoBracketOrder",
            0x61,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x63) {
        dm.push(("Func1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Func1Button",
            0x63,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x73) {
        dm.push(("Func2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Func2Button",
            0x73,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x83) {
        dm.push(("AFOnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFOnButton",
            0x83,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8f) {
        dm.push(("SubSelector".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x9b) {
        dm.push((
            "AssignMovieRecordButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x9f) {
        dm.push(("LensFunc1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LensFunc1Button",
            0x9f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa7) {
        dm.push(("LensFunc2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LensFunc2Button",
            0xa7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xad) {
        dm.push(("LensControlRing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None (Disabled)".to_string(),
            1 => "Focus (M/A)".to_string(),
            2 => "ISO Sensitivity".to_string(),
            3 => "Exposure Compensation".to_string(),
            4 => "Aperture".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensControlRing", 0xad, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xaf) {
        dm.push((
            "MultiSelectorShootMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MultiSelectorShootMode",
            0xaf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb3) {
        dm.push((
            "MultiSelectorPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MultiSelectorPlaybackMode",
            0xb3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb7) {
        dm.push(("ShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ShutterSpeedLock", 0xb7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb8) {
        dm.push(("ApertureLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ApertureLock", 0xb8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb9) {
        dm.push((
            "CmdDialsReverseRotExposureComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "CmdDialsReverseRotExposureComp",
                0xb9,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CmdDialsReverseRotExposureComp").is_some_and(|v| v == 0.0) {
        if let Some(v) = u8_at(data, 0xba) {
            dm.push((
                "CmdDialsReverseRotation".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "No".to_string(),
                1 => "Shutter Speed & Aperture".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CmdDialsReverseRotation",
                0xba,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(dm_get(dm, "CmdDialsReverseRotExposureComp").is_some_and(|v| v == 0.0)) {
        if let Some(v) = u8_at(data, 0xba) {
            dm.push((
                "CmdDialsReverseRotation".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Exposure Compensation".to_string(),
                1 => "Exposure Compensation, Shutter Speed & Aperture".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CmdDialsReverseRotation",
                0xba,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xbf) {
        dm.push(("UseDialWithoutHold".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xc1) {
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xc3) {
        dm.push(("MovieFunc1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieFunc1Button",
            0xc3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc7) {
        dm.push(("MovieFunc2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieFunc2Button",
            0xc7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xcb) {
        dm.push(("MovieAF-OnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieAF-OnButton",
            0xcb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd7) {
        dm.push(("MovieLensControlRing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None (Disabled)".to_string(),
            2 => "ISO Sensitivity".to_string(),
            3 => "Exposure Compensation".to_string(),
            4 => "Power Aperture".to_string(),
            5 => "Hi-Res Zoom".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieLensControlRing",
            0xd7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd9) {
        dm.push(("MovieMultiSelector".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xdd) {
        dm.push(("MovieAFSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 5", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "MovieAFSpeed",
                0xdd,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xdf) {
        dm.push(("MovieAFSpeedApply".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieAFSpeedApply",
            0xdf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe1) {
        dm.push((
            "MovieAFTrackingSensitivity".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 (High)".to_string(),
            1 => "2".to_string(),
            2 => "3".to_string(),
            3 => "4 (Normal)".to_string(),
            4 => "5".to_string(),
            5 => "6".to_string(),
            6 => "7 (Low)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieAFTrackingSensitivity",
            0xe1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x101) {
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x102) {
        dm.push(("ExtendedShutterSpeeds".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExtendedShutterSpeeds",
            0x102,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x103) {
        dm.push(("SubjectMotion".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SubjectMotion",
            0x103,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x105) {
        dm.push(("FocusPointPersistence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusPointPersistence",
            0x105,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x107) {
        dm.push((
            "AutoFocusModeRestrictions".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x10b) {
        dm.push(("CHModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CHModeShootingSpeed",
                0x10b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x111) {
        dm.push(("FlashBurstPriority".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x119) {
        dm.push(("Func3Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Func3Button",
            0x119,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14f) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_S".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x150) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_M".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x151) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_L".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x153) {
        dm.push((
            "LimitAF-AreaModeSel3DTracking".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x155) {
        dm.push(("PlaybackFlickUp".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x159) {
        dm.push(("PlaybackFlickDown".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x15d) {
        dm.push(("ISOStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOStepSize", 0x15d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x163) {
        dm.push(("ReverseFocusRing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ReverseFocusRing",
            0x163,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x164) {
        dm.push(("EVFImageFrame".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x165) {
        dm.push(("EVFGrid".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x167) {
        dm.push(("VirtualHorizonStyle".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1a5) {
        dm.push((
            "Func1ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1a7) {
        dm.push((
            "Func2ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1b5) {
        dm.push((
            "MovieRecordButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1c5) {
        dm.push(("WBButtonPlaybackMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WBButtonPlaybackMode",
            0x1c5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1cb) {
        dm.push((
            "CommandDialPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1cd) {
        dm.push((
            "CommandDialVideoPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1cf) {
        dm.push((
            "SubCommandDialPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1d1) {
        dm.push((
            "SubCommandDialVideoPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1d3) {
        dm.push(("FocusPointLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1d5) {
        dm.push(("ControlRingResponse".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ControlRingResponse",
            0x1d5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x211) {
        dm.push(("ZebraPatternToneRange".to_string(), Conv::Num(f64::from(v))));
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v != 0.0))
    {
        if let Some(v) = u8_at(data, 0x213) {
            dm.push(("MovieZebraPattern".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v == 1.0))
    {
        if let Some(v) = u8_at(data, 0x215) {
            dm.push((
                "MovieHighlightDisplayThreshold".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v == 2.0))
    {
        if let Some(v) = u8_at(data, 0x217) {
            dm.push((
                "MovieMidtoneDisplayValue".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v == 2.0))
    {
        if let Some(v) = u8_at(data, 0x219) {
            dm.push((
                "MovieMidtoneDisplayRange".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x21d) {
        dm.push(("MovieEVFGrid".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x225) {
        dm.push(("MovieShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x226) {
        dm.push(("MovieFocusPointLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x233) {
        dm.push(("MatrixMetering".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x234) {
        dm.push(("AF-CFocusDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CFocusDisplay",
            0x234,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x235) {
        dm.push(("FocusPeakingDisplay".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x237) {
        dm.push(("KeepExposure".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Shutter Speed".to_string(),
            2 => "ISO".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("KeepExposure", 0x237, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x249) {
        dm.push(("StarlightView".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x24b) {
        dm.push(("EVFWarmDisplayMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i8_at(data, 0x24d) {
        dm.push((
            "EVFWarmDisplayBrightness".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x24f) {
        dm.push(("EVFReleaseIndicator".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x259) {
        dm.push(("MovieApertureLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x25f) {
        dm.push(("FlickAdvanceDirection".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x287) {
        dm.push(("PreReleaseBurstLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "0.3 Sec".to_string(),
            2 => "0.5 Sec".to_string(),
            3 => "1 Sec".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PreReleaseBurstLength",
            0x287,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x289) {
        dm.push((
            "PostReleaseBurstLength".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 Sec".to_string(),
            1 => "2 Sec".to_string(),
            2 => "3 Sec".to_string(),
            3 => "Max".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PostReleaseBurstLength",
            0x289,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2a9) {
        dm.push((
            "ViewModeShowEffectsOfSettings".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x2ab) {
        dm.push(("DispButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "DispButton",
            0x2ab,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2f1, bo) {
        dm.push(("ExposureDelay".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "$val ? sprintf(\"%.1f sec\",$val/1000) : \"Off\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "ExposureDelay",
            0x2f1,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsZ9` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsz9(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x1) {
        dm.push(("CustomSettingsBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsBank",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Release + Focus".to_string(),
            3 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x3,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AF-SPrioritySelection",
            0x5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        dm.push(("BlockShotAFResponse".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "1 (Quick)".to_string(),
            2 => "2".to_string(),
            3 => "3 (Normal)".to_string(),
            4 => "4".to_string(),
            5 => "5 (Delayed)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "BlockShotAFResponse",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb) {
        dm.push(("AFPointSel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFPointSel",
            0xb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd) {
        dm.push(("StoreByOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Focus Point".to_string(),
            2 => "Focus Point and AF-area mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "StoreByOrientation",
            0xd,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf) {
        dm.push(("AFActivation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFActivation",
            0xf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        dm.push((
            "AF-OnOutOfFocusRelease".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        dm.push((
            "LimitAF-AreaModeSelPinpoint".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        dm.push((
            "LimitAF-AreaModeSelWideAF_S".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        dm.push((
            "LimitAF-AreaModeSelWideAF_L".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push((
            "LimitAFAreaModeSelAuto".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x17) {
        dm.push((
            "ManualFocusPointIllumination".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x18) {
        dm.push(("DynamicAreaAFAssist".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x19) {
        dm.push(("AF-AssistIlluminator".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-AssistIlluminator",
            0x19,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1a) {
        dm.push((
            "ManualFocusRingInAFMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ManualFocusRingInAFMode",
            0x1a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x1b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1d) {
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (auto reset)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x1d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "8 mm".to_string(),
            1 => "12 mm".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x1f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x21) {
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x21,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x23) {
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x23,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x25) {
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x25,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x27) {
        dm.push((
            "FineTuneOptHighlightWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptHighlightWeighted",
                0x27,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x29) {
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On (Half Press)".to_string(),
            2 => "On (Burst Mode)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x29,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b) {
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x2b, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2d) {
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x2d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x31) {
        dm.push(("SelfTimerShotInterval".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.5 s".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SelfTimerShotInterval",
            0x31,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x33) {
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x33,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x35) {
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x35,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x37) {
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x37,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x39) {
        dm.push((
            "ImageReviewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageReviewMonitorOffTime",
            0x39,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3b) {
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CLModeShootingSpeed",
                0x3b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x3d, bo) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val eq -1 ?  'No Limit' : $val ) ", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "MaxContinuousRelease",
                0x3d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x41) {
        dm.push(("SyncReleaseMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x45) {
        dm.push((
            "LimitSelectableImageAreaDX".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x46) {
        dm.push((
            "LimitSelectableImageArea1To1".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x47) {
        dm.push((
            "LimitSelectableImageArea16To9".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x48) {
        dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FileNumberSequence",
            0x48,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x49) {
        dm.push(("FocusPeakingLevel".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x4b) {
        dm.push((
            "FocusPeakingHighlightColor".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x51) {
        dm.push(("ContinuousModeDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ContinuousModeDisplay",
            0x51,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x53) {
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("($val-144)/8", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1/60 s".to_string(),
                1 => "1/80 s".to_string(),
                2 => "1/100 s".to_string(),
                3 => "1/125 s".to_string(),
                4 => "1/160 s".to_string(),
                5 => "1/200 s".to_string(),
                6 => "1/250 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("FlashSyncSpeed", 0x53, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x55) {
        dm.push(("HighSpeedSync".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("HighSpeedSync", 0x55, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x57) {
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with(
            "my $t = ($val - 16) % 24; $t ? $val / 24  : 2 + ($val - 16) / 24",
            &base,
            &ctx,
        );
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1 s".to_string(),
                1 => "1/2 s".to_string(),
                2 => "1/4 s".to_string(),
                3 => "1/8 s".to_string(),
                4 => "1/15 s".to_string(),
                5 => "1/30 s".to_string(),
                6 => "1/60 s".to_string(),
                7 => "30 s".to_string(),
                8 => "15 s".to_string(),
                9 => "8 s".to_string(),
                10 => "4 s".to_string(),
                11 => "2 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashShutterSpeed",
                0x57,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x59) {
        dm.push(("FlashExposureCompArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FlashExposureCompArea",
            0x59,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5b) {
        dm.push((
            "AutoFlashISOSensitivity".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AutoFlashISOSensitivity",
            0x5b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5d) {
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x5d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5f) {
        dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Flash/Speed".to_string(),
            1 => "Flash/Speed/Aperture".to_string(),
            2 => "Flash/Aperture".to_string(),
            3 => "Flash Only".to_string(),
            4 => "Flash/ISO".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketModeM", 0x5f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x61) {
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AutoBracketOrder",
            0x61,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x63) {
        dm.push(("Func1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Func1Button",
            0x63,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x73) {
        dm.push(("Func2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Func2Button",
            0x73,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x83) {
        dm.push(("AFOnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFOnButton",
            0x83,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8f) {
        dm.push(("SubSelector".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x9b) {
        dm.push((
            "AssignMovieRecordButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x9f) {
        dm.push(("LensFunc1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LensFunc1Button",
            0x9f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa7) {
        dm.push(("LensFunc2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LensFunc2Button",
            0xa7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xad) {
        dm.push(("LensControlRing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None (Disabled)".to_string(),
            1 => "Focus (M/A)".to_string(),
            2 => "ISO Sensitivity".to_string(),
            3 => "Exposure Compensation".to_string(),
            4 => "Aperture".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensControlRing", 0xad, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xaf) {
        dm.push((
            "MultiSelectorShootMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MultiSelectorShootMode",
            0xaf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb3) {
        dm.push((
            "MultiSelectorPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MultiSelectorPlaybackMode",
            0xb3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb7) {
        dm.push(("ShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ShutterSpeedLock", 0xb7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb8) {
        dm.push(("ApertureLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ApertureLock", 0xb8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb9) {
        dm.push((
            "CmdDialsReverseRotExposureComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "CmdDialsReverseRotExposureComp",
                0xb9,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CmdDialsReverseRotExposureComp").is_some_and(|v| v == 0.0) {
        if let Some(v) = u8_at(data, 0xba) {
            dm.push((
                "CmdDialsReverseRotation".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "No".to_string(),
                1 => "Shutter Speed & Aperture".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CmdDialsReverseRotation",
                0xba,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(dm_get(dm, "CmdDialsReverseRotExposureComp").is_some_and(|v| v == 0.0)) {
        if let Some(v) = u8_at(data, 0xba) {
            dm.push((
                "CmdDialsReverseRotation".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Exposure Compensation".to_string(),
                1 => "Exposure Compensation, Shutter Speed & Aperture".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CmdDialsReverseRotation",
                0xba,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xbf) {
        dm.push(("UseDialWithoutHold".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xc1) {
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xc3) {
        dm.push(("MovieFunc1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieFunc1Button",
            0xc3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc7) {
        dm.push(("MovieFunc2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieFunc2Button",
            0xc7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xcb) {
        dm.push(("MovieAF-OnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieAF-OnButton",
            0xcb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xcf) {
        dm.push(("MovieMultiSelector".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xd7) {
        dm.push(("MovieLensControlRing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None (Disabled)".to_string(),
            2 => "ISO Sensitivity".to_string(),
            3 => "Exposure Compensation".to_string(),
            4 => "Power Aperture".to_string(),
            5 => "Hi-Res Zoom".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieLensControlRing",
            0xd7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xdd) {
        dm.push(("MovieAFSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 5", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "MovieAFSpeed",
                0xdd,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xdf) {
        dm.push(("MovieAFSpeedApply".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieAFSpeedApply",
            0xdf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe1) {
        dm.push((
            "MovieAFTrackingSensitivity".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 (High)".to_string(),
            1 => "2".to_string(),
            2 => "3".to_string(),
            3 => "4 (Normal)".to_string(),
            4 => "5".to_string(),
            5 => "6".to_string(),
            6 => "7 (Low)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieAFTrackingSensitivity",
            0xe1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x101) {
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x102) {
        dm.push(("ExtendedShutterSpeeds".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExtendedShutterSpeeds",
            0x102,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x103) {
        dm.push(("SubjectMotion".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SubjectMotion",
            0x103,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x105) {
        dm.push(("FocusPointPersistence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusPointPersistence",
            0x105,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x107) {
        dm.push((
            "AutoFocusModeRestrictions".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x10b) {
        dm.push(("CHModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CHModeShootingSpeed",
                0x10b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x10d) {
        let v = (v & 0x2) >> 1;
        dm.push(("LimitReleaseModeSelCL".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x10d) {
        let v = (v & 0x4) >> 2;
        dm.push(("LimitReleaseModeSelCH".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x10d) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "LimitReleaseModeSelC30".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x10d) {
        let v = (v & 0x40) >> 6;
        dm.push((
            "LimitReleaseModeSelC120".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x10d) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "LimitReleaseModeSelSelf".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x111) {
        dm.push(("FlashBurstPriority".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x115) {
        dm.push(("VerticalFuncButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "VerticalFuncButton",
            0x115,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x119) {
        dm.push(("Func3Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Func3Button",
            0x119,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11d) {
        dm.push(("VerticalAFOnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "VerticalAFOnButton",
            0x11d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x125) {
        dm.push((
            "VerticalMultiSelectorPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x127) {
        dm.push(("MovieFunc3Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieFunc3Button",
            0x127,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14f) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_S".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x150) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_M".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x151) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_L".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x153) {
        dm.push((
            "LimitAF-AreaModeSel3DTracking".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x155) {
        dm.push(("PlaybackFlickUp".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x159) {
        dm.push(("PlaybackFlickDown".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x15d) {
        dm.push(("ISOStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOStepSize", 0x15d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x163) {
        dm.push(("ReverseFocusRing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ReverseFocusRing",
            0x163,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x164) {
        dm.push(("EVFImageFrame".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x165) {
        dm.push(("EVFGrid".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x167) {
        dm.push(("VirtualHorizonStyle".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x175) {
        dm.push(("Func4Button".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x17b) {
        dm.push(("AudioButton".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x17d) {
        dm.push(("QualityButton".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x18f) {
        dm.push(("VerticalMultiSelector".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1a5) {
        dm.push((
            "Func1ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1a7) {
        dm.push((
            "Func2ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1a9) {
        dm.push((
            "Func3ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1af) {
        dm.push((
            "Func4ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1b5) {
        dm.push((
            "MovieRecordButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1b7) {
        dm.push((
            "VerticalFuncButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1b9) {
        dm.push((
            "AudioButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1bf) {
        dm.push((
            "QualityButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1c5) {
        dm.push((
            "WhiteBalanceButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1cb) {
        dm.push((
            "CommandDialPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1cf) {
        dm.push((
            "SubCommandDialPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1d3) {
        dm.push(("FocusPointLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1d5) {
        dm.push(("ControlRingResponse".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ControlRingResponse",
            0x1d5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1e1) {
        dm.push((
            "VerticalMovieFuncButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1f9) {
        dm.push((
            "VerticalMovieAFOnButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x203) {
        dm.push(("MovieAFAreaMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x20f) {
        dm.push(("HDMIViewAssist".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x211) {
        dm.push(("ZebraPatternToneRange".to_string(), Conv::Num(f64::from(v))));
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v != 0.0))
    {
        if let Some(v) = u8_at(data, 0x213) {
            dm.push(("MovieZebraPattern".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v == 1.0))
    {
        if let Some(v) = u8_at(data, 0x215) {
            dm.push((
                "MovieHighlightDisplayThreshold".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v == 2.0))
    {
        if let Some(v) = u8_at(data, 0x217) {
            dm.push((
                "MovieMidtoneDisplayValue".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v == 2.0))
    {
        if let Some(v) = u8_at(data, 0x219) {
            dm.push((
                "MovieMidtoneDisplayRange".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x21d) {
        dm.push(("MovieEVFGrid".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x225) {
        dm.push(("MovieShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x226) {
        dm.push(("MovieFocusPointLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x233) {
        dm.push(("MatrixMetering".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x234) {
        dm.push(("AF-CFocusDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CFocusDisplay",
            0x234,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x235) {
        dm.push(("FocusPeakingDisplay".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x237) {
        dm.push(("KeepExposure".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Shutter Speed".to_string(),
            2 => "ISO".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("KeepExposure", 0x237, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x249) {
        dm.push(("StarlightView".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x24b) {
        dm.push(("EVFWarmDisplayMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i8_at(data, 0x24d) {
        dm.push((
            "EVFWarmDisplayBrightness".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x24f) {
        dm.push(("EVFReleaseIndicator".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x259) {
        dm.push(("MovieApertureLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x25f) {
        dm.push(("FlickAdvanceDirection".to_string(), Conv::Num(f64::from(v))));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsZ9v4` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsz9v4(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x1) {
        dm.push(("CustomSettingsBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsBank",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Release + Focus".to_string(),
            3 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x3,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AF-SPrioritySelection",
            0x5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        dm.push(("BlockShotAFResponse".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "1 (Quick)".to_string(),
            2 => "2".to_string(),
            3 => "3 (Normal)".to_string(),
            4 => "4".to_string(),
            5 => "5 (Delayed)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "BlockShotAFResponse",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb) {
        dm.push(("AFPointSel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFPointSel",
            0xb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd) {
        dm.push(("StoreByOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Focus Point".to_string(),
            2 => "Focus Point and AF-area mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "StoreByOrientation",
            0xd,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf) {
        dm.push(("AFActivation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFActivation",
            0xf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        dm.push((
            "AF-OnOutOfFocusRelease".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        dm.push((
            "LimitAF-AreaModeSelPinpoint".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        dm.push((
            "LimitAF-AreaModeSelWideAF_S".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        dm.push((
            "LimitAF-AreaModeSelWideAF_L".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        dm.push((
            "LimitAFAreaModeSelAuto".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x17) {
        dm.push((
            "ManualFocusPointIllumination".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x18) {
        dm.push(("DynamicAreaAFAssist".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x19) {
        dm.push(("AF-AssistIlluminator".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-AssistIlluminator",
            0x19,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1a) {
        dm.push((
            "ManualFocusRingInAFMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ManualFocusRingInAFMode",
            0x1a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b) {
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x1b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1d) {
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (auto reset)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x1d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "8 mm".to_string(),
            1 => "12 mm".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x1f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i8_at(data, 0x21) {
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x21,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x23) {
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x23,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x25) {
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x25,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i8_at(data, 0x27) {
        dm.push((
            "FineTuneOptHighlightWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptHighlightWeighted",
                0x27,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x29) {
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On (Half Press)".to_string(),
            2 => "On (Burst Mode)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x29,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b) {
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x2b, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2d) {
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x2d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x31) {
        dm.push(("SelfTimerShotInterval".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.5 s".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SelfTimerShotInterval",
            0x31,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x33) {
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x33,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x35) {
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x35,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x37) {
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x37,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x39) {
        dm.push((
            "ImageReviewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            11 => "30 min".to_string(),
            12 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageReviewMonitorOffTime",
            0x39,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3b) {
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CLModeShootingSpeed",
                0x3b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x3d, bo) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val eq -1 ?  'No Limit' : $val ) ", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "MaxContinuousRelease",
                0x3d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x41) {
        dm.push(("SyncReleaseMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x45) {
        dm.push((
            "LimitSelectableImageAreaDX".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x46) {
        dm.push((
            "LimitSelectableImageArea1To1".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x47) {
        dm.push((
            "LimitSelectableImageArea16To9".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x48) {
        dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FileNumberSequence",
            0x48,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x49) {
        dm.push(("FocusPeakingLevel".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x4b) {
        dm.push((
            "FocusPeakingHighlightColor".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x51) {
        dm.push(("ContinuousModeDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ContinuousModeDisplay",
            0x51,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x53) {
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("($val-144)/8", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1/60 s".to_string(),
                1 => "1/80 s".to_string(),
                2 => "1/100 s".to_string(),
                3 => "1/125 s".to_string(),
                4 => "1/160 s".to_string(),
                5 => "1/200 s".to_string(),
                6 => "1/250 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("FlashSyncSpeed", 0x53, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x55) {
        dm.push(("HighSpeedSync".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("HighSpeedSync", 0x55, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x57) {
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with(
            "my $t = ($val - 16) % 24; $t ? $val / 24  : 2 + ($val - 16) / 24",
            &base,
            &ctx,
        );
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1 s".to_string(),
                1 => "1/2 s".to_string(),
                2 => "1/4 s".to_string(),
                3 => "1/8 s".to_string(),
                4 => "1/15 s".to_string(),
                5 => "1/30 s".to_string(),
                6 => "1/60 s".to_string(),
                7 => "30 s".to_string(),
                8 => "15 s".to_string(),
                9 => "8 s".to_string(),
                10 => "4 s".to_string(),
                11 => "2 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashShutterSpeed",
                0x57,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x59) {
        dm.push(("FlashExposureCompArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FlashExposureCompArea",
            0x59,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5b) {
        dm.push((
            "AutoFlashISOSensitivity".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AutoFlashISOSensitivity",
            0x5b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5d) {
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x5d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5f) {
        dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Flash/Speed".to_string(),
            1 => "Flash/Speed/Aperture".to_string(),
            2 => "Flash/Aperture".to_string(),
            3 => "Flash Only".to_string(),
            4 => "Flash/ISO".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketModeM", 0x5f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x61) {
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AutoBracketOrder",
            0x61,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x63) {
        dm.push(("Func1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Func1Button",
            0x63,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x73) {
        dm.push(("Func2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Func2Button",
            0x73,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x83) {
        dm.push(("AFOnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFOnButton",
            0x83,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8f) {
        dm.push(("SubSelector".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x9b) {
        dm.push((
            "AssignMovieRecordButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x9f) {
        dm.push(("LensFunc1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LensFunc1Button",
            0x9f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa7) {
        dm.push(("LensFunc2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "LensFunc2Button",
            0xa7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xad) {
        dm.push(("LensControlRing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None (Disabled)".to_string(),
            1 => "Focus (M/A)".to_string(),
            2 => "ISO Sensitivity".to_string(),
            3 => "Exposure Compensation".to_string(),
            4 => "Aperture".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LensControlRing", 0xad, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xaf) {
        dm.push((
            "MultiSelectorShootMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MultiSelectorShootMode",
            0xaf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb3) {
        dm.push((
            "MultiSelectorPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MultiSelectorPlaybackMode",
            0xb3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb7) {
        dm.push(("ShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ShutterSpeedLock", 0xb7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb8) {
        dm.push(("ApertureLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ApertureLock", 0xb8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb9) {
        dm.push((
            "CmdDialsReverseRotExposureComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "CmdDialsReverseRotExposureComp",
                0xb9,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "CmdDialsReverseRotExposureComp").is_some_and(|v| v == 0.0) {
        if let Some(v) = u8_at(data, 0xba) {
            dm.push((
                "CmdDialsReverseRotation".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "No".to_string(),
                1 => "Shutter Speed & Aperture".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CmdDialsReverseRotation",
                0xba,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(dm_get(dm, "CmdDialsReverseRotExposureComp").is_some_and(|v| v == 0.0)) {
        if let Some(v) = u8_at(data, 0xba) {
            dm.push((
                "CmdDialsReverseRotation".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Exposure Compensation".to_string(),
                1 => "Exposure Compensation, Shutter Speed & Aperture".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CmdDialsReverseRotation",
                0xba,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xbf) {
        dm.push(("UseDialWithoutHold".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xc1) {
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xc3) {
        dm.push(("MovieFunc1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieFunc1Button",
            0xc3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc7) {
        dm.push(("MovieFunc2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieFunc2Button",
            0xc7,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xcb) {
        dm.push(("MovieAF-OnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieAF-OnButton",
            0xcb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xcf) {
        dm.push(("MovieMultiSelector".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0xd7) {
        dm.push(("MovieLensControlRing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None (Disabled)".to_string(),
            2 => "ISO Sensitivity".to_string(),
            3 => "Exposure Compensation".to_string(),
            4 => "Power Aperture".to_string(),
            5 => "Hi-Res Zoom".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieLensControlRing",
            0xd7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xdd) {
        dm.push(("MovieAFSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 5", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "MovieAFSpeed",
                0xdd,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xdf) {
        dm.push(("MovieAFSpeedApply".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieAFSpeedApply",
            0xdf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xe1) {
        dm.push((
            "MovieAFTrackingSensitivity".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 (High)".to_string(),
            1 => "2".to_string(),
            2 => "3".to_string(),
            3 => "4 (Normal)".to_string(),
            4 => "5".to_string(),
            5 => "6".to_string(),
            6 => "7 (Low)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieAFTrackingSensitivity",
            0xe1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x117) {
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x118) {
        dm.push(("ExtendedShutterSpeeds".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExtendedShutterSpeeds",
            0x118,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x119) {
        dm.push(("SubjectMotion".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SubjectMotion",
            0x119,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11b) {
        dm.push(("FocusPointPersistence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusPointPersistence",
            0x11b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11d) {
        dm.push((
            "AutoFocusModeRestrictions".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x121) {
        dm.push(("CHModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CHModeShootingSpeed",
                0x121,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x125) {
        let v = (v & 0x2) >> 1;
        dm.push(("LimitReleaseModeSelCL".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x125) {
        let v = (v & 0x4) >> 2;
        dm.push(("LimitReleaseModeSelCH".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x125) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "LimitReleaseModeSelC30".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x125) {
        let v = (v & 0x40) >> 6;
        dm.push((
            "LimitReleaseModeSelC120".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x125) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "LimitReleaseModeSelSelf".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x129) {
        dm.push(("FlashBurstPriority".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x12d) {
        dm.push(("VerticalFuncButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "VerticalFuncButton",
            0x12d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x131) {
        dm.push(("Func3Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Func3Button",
            0x131,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x135) {
        dm.push(("VerticalAFOnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "VerticalAFOnButton",
            0x135,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x13d) {
        dm.push((
            "VerticalMultiSelectorPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x13f) {
        dm.push(("MovieFunc3Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MovieFunc3Button",
            0x13f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x167) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_S".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x168) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_M".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x169) {
        dm.push((
            "LimitAF-AreaModeSelDynamic_L".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x16b) {
        dm.push((
            "LimitAF-AreaModeSel3DTracking".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x16d) {
        dm.push(("PlaybackFlickUp".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x171) {
        dm.push(("PlaybackFlickDown".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x175) {
        dm.push(("ISOStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOStepSize", 0x175, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x17b) {
        dm.push(("ReverseFocusRing".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ReverseFocusRing",
            0x17b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x17c) {
        dm.push(("EVFImageFrame".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x17d) {
        dm.push(("EVFGrid".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x17f) {
        dm.push(("VirtualHorizonStyle".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x18d) {
        dm.push(("Func4Button".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x193) {
        dm.push(("AudioButton".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x195) {
        dm.push(("QualityButton".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1a7) {
        dm.push(("VerticalMultiSelector".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1bd) {
        dm.push((
            "Func1ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1bf) {
        dm.push((
            "Func2ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1c1) {
        dm.push((
            "Func3ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1c7) {
        dm.push((
            "Func4ButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1cd) {
        dm.push((
            "MovieRecordButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1cf) {
        dm.push((
            "VerticalFuncButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1d1) {
        dm.push((
            "AudioButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1d7) {
        dm.push((
            "QualityButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1dd) {
        dm.push((
            "WhiteBalanceButtonPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1e3) {
        dm.push((
            "CommandDialPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() >= "05.00"))
    {
        if let Some(v) = u8_at(data, 0x1e5) {
            dm.push((
                "CommandDialVideoPlaybackMode".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1e7) {
        dm.push((
            "SubCommandDialPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if (dm_val(dm, "FirmwareVersion").is_some_and(conv_expr::Val::truthy)
        && dm_val(dm, "FirmwareVersion").is_some_and(|v| v.as_string().as_str() >= "05.00"))
    {
        if let Some(v) = u8_at(data, 0x1e9) {
            dm.push((
                "SubCommandDialVideoPlaybackMode".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1eb) {
        dm.push(("FocusPointLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1ed) {
        dm.push(("ControlRingResponse".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ControlRingResponse",
            0x1ed,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f9) {
        dm.push((
            "VerticalMovieFuncButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x211) {
        dm.push((
            "VerticalMovieAFOnButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x227) {
        dm.push(("HDMIViewAssist".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x229) {
        dm.push(("ZebraPatternToneRange".to_string(), Conv::Num(f64::from(v))));
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v != 0.0))
    {
        if let Some(v) = u8_at(data, 0x22b) {
            dm.push(("MovieZebraPattern".to_string(), Conv::Num(f64::from(v))));
        }
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v == 1.0))
    {
        if let Some(v) = u8_at(data, 0x22d) {
            dm.push((
                "MovieHighlightDisplayThreshold".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v == 2.0))
    {
        if let Some(v) = u8_at(data, 0x22f) {
            dm.push((
                "MovieMidtoneDisplayValue".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if (dm_val(dm, "ZebraPatternToneRange").is_some_and(conv_expr::Val::truthy)
        && dm_get(dm, "ZebraPatternToneRange").is_some_and(|v| v == 2.0))
    {
        if let Some(v) = u8_at(data, 0x231) {
            dm.push((
                "MovieMidtoneDisplayRange".to_string(),
                Conv::Num(f64::from(v)),
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x235) {
        dm.push(("MovieEVFGrid".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x23d) {
        dm.push(("MovieShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x23e) {
        dm.push(("MovieFocusPointLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x24b) {
        dm.push(("MatrixMetering".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x24c) {
        dm.push(("AF-CFocusDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CFocusDisplay",
            0x24c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x24d) {
        dm.push(("FocusPeakingDisplay".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x24f) {
        dm.push(("KeepExposure".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Shutter Speed".to_string(),
            2 => "ISO".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("KeepExposure", 0x24f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x261) {
        dm.push(("StarlightView".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x263) {
        dm.push(("EVFWarmDisplayMode".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = i8_at(data, 0x265) {
        dm.push((
            "EVFWarmDisplayBrightness".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x267) {
        dm.push(("EVFReleaseIndicator".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x271) {
        dm.push(("MovieApertureLock".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x277) {
        dm.push(("FlickAdvanceDirection".to_string(), Conv::Num(f64::from(v))));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD80` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd80(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x80) >> 7;
        dm.push(("Beep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Beep", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x40) >> 6;
        dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssist", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x20) >> 5;
        dm.push(("NoMemoryCard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release Locked".to_string(),
            1 => "Enable Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoMemoryCard", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x10) >> 4;
        dm.push(("ImageReview".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageReview", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x8) >> 3;
        dm.push(("Illumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Illumination", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x4) >> 2;
        dm.push(("MainDialExposureComp".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MainDialExposureComp",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x1;
        dm.push(("EVStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("EVStepSize", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x40) >> 6;
        dm.push(("AutoISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoISO", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x30) >> 4;
        dm.push(("AutoISOMax".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AutoISOMax",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0xf;
        dm.push((
            "AutoISOMinShutterSpeed".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/125 s".to_string(),
            1 => "1/100 s".to_string(),
            2 => "1/80 s".to_string(),
            3 => "1/60 s".to_string(),
            4 => "1/40 s".to_string(),
            5 => "1/30 s".to_string(),
            6 => "1/15 s".to_string(),
            7 => "1/8 s".to_string(),
            8 => "1/4 s".to_string(),
            9 => "1/2 s".to_string(),
            10 => "1 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoISOMinShutterSpeed",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AutoBracketSet".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE & Flash".to_string(),
            1 => "AE Only".to_string(),
            2 => "Flash Only".to_string(),
            3 => "WB Bracketing".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketSet", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x20) >> 5;
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0,-,+".to_string(),
            1 => "-,0,+".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketOrder", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0xe0) >> 5;
        dm.push(("MonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "5 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MonitorOffTime", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x1c) >> 2;
        dm.push(("MeteringTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "6 s".to_string(),
            2 => "8 s".to_string(),
            3 => "16 s".to_string(),
            4 => "30 s".to_string(),
            5 => "30 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MeteringTime", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = v & 0x3;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x1e) >> 1;
        dm.push(("AELockButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE/AF Lock".to_string(),
            1 => "AE Lock Only".to_string(),
            2 => "AF Lock Only".to_string(),
            3 => "AE Lock (hold)".to_string(),
            4 => "AF-ON".to_string(),
            5 => "FV Lock".to_string(),
            6 => "Focus Area Selection".to_string(),
            7 => "AE-L/AF-L/AF Area".to_string(),
            8 => "AE-L/AF Area".to_string(),
            9 => "AF-L/AF Area".to_string(),
            10 => "AF-ON/AF Area".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELockButton", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = v & 0x1;
        dm.push(("AELock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELock", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0xc0) >> 6;
        dm.push(("RemoteOnDuration".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 min".to_string(),
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RemoteOnDuration", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x80) >> 7;
        dm.push(("CommandDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard (Main Shutter, Sub Aperture)".to_string(),
            1 => "Reversed (Main Aperture, Sub Shutter)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("CommandDials", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x78) >> 3;
        dm.push(("FunctionButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "ISO Display".to_string(),
            1 => "Framing Grid".to_string(),
            2 => "AF-area Mode".to_string(),
            3 => "Center AF Area".to_string(),
            4 => "FV Lock".to_string(),
            5 => "Flash Off".to_string(),
            6 => "Matrix Metering".to_string(),
            7 => "Center-weighted".to_string(),
            8 => "Spot Metering".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FunctionButton", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x80) >> 7;
        dm.push(("GridDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GridDisplay", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x40) >> 6;
        dm.push(("ViewfinderWarning".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ViewfinderWarning", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0xc) >> 2;
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "6 mm".to_string(),
            1 => "8 mm".to_string(),
            2 => "10 mm".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x20) >> 5;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = v & 0x3;
        dm.push(("MB-D80Batteries".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "LR6 (AA Alkaline)".to_string(),
            1 => "HR6 (AA Ni-MH)".to_string(),
            2 => "FR6 (AA Lithium)".to_string(),
            3 => "ZR6 (AA Ni-Mg)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MB-D80Batteries", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x80) >> 7;
        dm.push(("FlashWarning".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashWarning", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x78) >> 3;
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** ($val - 6)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FlashShutterSpeed",
                0x7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x4) >> 2;
        dm.push(("AutoFP".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoFP", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x2) >> 1;
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = (v & 0xc0) >> 6;
        dm.push(("InternalFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Manual".to_string(),
            2 => "Repeating Flash".to_string(),
            3 => "Commander Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("InternalFlash", 0x8, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = v & 0x7;
        dm.push(("ManualFlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "ManualFlashOutput",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = (v & 0x70) >> 4;
        dm.push(("RepeatingFlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val-2)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "RepeatingFlashOutput",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = v & 0xf;
        dm.push(("RepeatingFlashCount".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val < 10 ? $val + 1 : 5 * ($val - 7)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "RepeatingFlashCount",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0xf0) >> 4;
        dm.push(("RepeatingFlashRate".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val < 10 ? $val + 1 : 10 * ($val - 8)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val Hz\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "RepeatingFlashRate",
                0xa,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = v & 0x3;
        dm.push(("CommanderChannel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "CommanderChannel",
                0xa,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0xc0) >> 6;
        dm.push((
            "CommanderInternalFlash".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Manual".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommanderInternalFlash",
            0xb,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0x30) >> 4;
        dm.push(("CommanderGroupAMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Auto Aperture".to_string(),
            2 => "Manual".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommanderGroupAMode",
            0xb,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0xc) >> 2;
        dm.push(("CommanderGroupBMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Auto Aperture".to_string(),
            2 => "Manual".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommanderGroupBMode",
            0xb,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc) {
        let v = v & 0x1f;
        dm.push((
            "CommanderInternalTTLComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CommanderInternalTTLComp",
                0xc,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xc) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CommanderInternalManualOutput".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "CommanderInternalManualOutput",
                0xc,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = v & 0x1f;
        dm.push((
            "CommanderGroupA_TTL-AAComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CommanderGroupA_TTL-AAComp",
                0xd,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CommanderGroupAManualOutput".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "CommanderGroupAManualOutput",
                0xd,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xe) {
        let v = v & 0x1f;
        dm.push((
            "CommanderGroupB_TTL-AAComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CommanderGroupB_TTL-AAComp",
                0xe,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xe) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CommanderGroupBManualOutput".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "CommanderGroupBManualOutput",
                0xe,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = (v & 0x80) >> 7;
        dm.push(("CenterAFArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Normal Zone".to_string(),
            1 => "Wide Zone".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("CenterAFArea", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = (v & 0x4) >> 2;
        dm.push(("FocusAreaSelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Wrap".to_string(),
            1 => "Wrap".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusAreaSelection",
            0xf,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = v & 0x3;
        dm.push(("AFAreaIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Off".to_string(),
            2 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFAreaIllumination",
            0xf,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AFAreaModeSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Single Area".to_string(),
            1 => "Dynamic Area".to_string(),
            2 => "Auto-area".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFAreaModeSetting",
            0x10,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD90` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd90(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x8) >> 3;
        dm.push(("LightSwitch".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "LCD Backlight".to_string(),
            1 => "LCD Backlight and Shooting Information".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LightSwitch", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x60) >> 5;
        dm.push(("AFAreaModeSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Single Area".to_string(),
            1 => "Dynamic Area".to_string(),
            2 => "Auto-area".to_string(),
            3 => "3D-tracking (11 points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAreaModeSetting", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x10) >> 4;
        dm.push(("CenterFocusPoint".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Normal Zone".to_string(),
            1 => "Wide Zone".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("CenterFocusPoint", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = v & 0x1;
        dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssist", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x6) >> 1;
        dm.push(("AFPointIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "On".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointIllumination",
            0x2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x8) >> 3;
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Wrap".to_string(),
            1 => "Wrap".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusPointWrap", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x1c) >> 2;
        dm.push(("AELockForMB-D80".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE Lock Only".to_string(),
            1 => "AF Lock Only".to_string(),
            2 => "AE Lock (hold)".to_string(),
            3 => "AF-On".to_string(),
            4 => "FV Lock".to_string(),
            5 => "Focus Point Selection".to_string(),
            7 => "AE/AF Lock".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELockForMB-D80", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = v & 0x3;
        dm.push(("MB-D80BatteryType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "LR6 (AA alkaline)".to_string(),
            1 => "HR6 (AA Ni-MH)".to_string(),
            2 => "FR6 (AA lithium)".to_string(),
            3 => "ZR6 (AA Ni-Mn)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MB-D80BatteryType", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x40) >> 6;
        dm.push(("Beep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Beep", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x2) >> 1;
        dm.push(("GridDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GridDisplay", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0xc) >> 2;
        dm.push(("ISODisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Show ISO/Easy ISO".to_string(),
            1 => "Show ISO Sensitivity".to_string(),
            3 => "Show Frame Count".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISODisplay", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = v & 0x1;
        dm.push(("ViewfinderWarning".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ViewfinderWarning", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x20) >> 5;
        dm.push(("NoMemoryCard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release Locked".to_string(),
            1 => "Enable Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoMemoryCard", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x4) >> 2;
        dm.push(("ScreenTips".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ScreenTips", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x8) >> 3;
        dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FileNumberSequence",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0xc0) >> 6;
        dm.push(("ShootingInfoDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            2 => "Manual (dark on light)".to_string(),
            3 => "Manual (light on dark)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoDisplay",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x20) >> 5;
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LCDIllumination", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = v & 0x1;
        dm.push(("EasyExposureComp".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("EasyExposureComp", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x80) >> 7;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReverseIndicators", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x40) >> 6;
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = (v & 0x60) >> 5;
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "6 mm".to_string(),
            1 => "8 mm".to_string(),
            2 => "10 mm".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = v & 0x7;
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "CLModeShootingSpeed",
            0xb,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0x40) >> 6;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0xb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0xe0) >> 5;
        dm.push(("AutoBracketSet".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE & Flash".to_string(),
            1 => "AE Only".to_string(),
            2 => "Flash Only".to_string(),
            3 => "WB Bracketing".to_string(),
            4 => "Active D-Lighting".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketSet", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0x10) >> 4;
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0,-,+".to_string(),
            1 => "-,0,+".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketOrder", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xe) {
        let v = (v & 0x78) >> 3;
        dm.push(("FuncButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "Framing Grid".to_string(),
            2 => "AF-area Mode".to_string(),
            3 => "Center Focus Point".to_string(),
            4 => "FV Lock".to_string(),
            5 => "Flash Off".to_string(),
            6 => "Matrix Metering".to_string(),
            7 => "Center-weighted Metering".to_string(),
            8 => "Spot Metering".to_string(),
            9 => "My Menu Top".to_string(),
            10 => "+ NEF (RAW)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FuncButton", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = (v & 0x18) >> 3;
        dm.push(("OKButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Not Used".to_string(),
            1 => "Select Center Focus Point".to_string(),
            2 => "Highlight Active Focus Point".to_string(),
            3 => "Not Used".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("OKButton", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x38) >> 3;
        dm.push(("AELockButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE/AF Lock".to_string(),
            1 => "AE Lock Only".to_string(),
            2 => "AF Lock Only".to_string(),
            3 => "AE Lock (hold)".to_string(),
            4 => "AF-ON".to_string(),
            5 => "FV Lock".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELockButton", 0x11, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "CommandDialsReverseRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsReverseRotation",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0xf0) >> 4;
        dm.push(("MeteringTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "6 s".to_string(),
            2 => "8 s".to_string(),
            3 => "16 s".to_string(),
            4 => "30 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            8 => "30 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MeteringTime", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = v & 0x3;
        dm.push(("RemoteOnDuration".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 min".to_string(),
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RemoteOnDuration", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0xc0) >> 6;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0x1e) >> 1;
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x14,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0xe0) >> 5;
        dm.push(("ImageReviewTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageReviewTime", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0xe0) >> 5;
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = v & 0xf;
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/60 s".to_string(),
            1 => "1/30 s".to_string(),
            2 => "1/15 s".to_string(),
            3 => "1/8 s".to_string(),
            4 => "1/4 s".to_string(),
            5 => "1/2 s".to_string(),
            6 => "1 s".to_string(),
            7 => "2 s".to_string(),
            8 => "4 s".to_string(),
            9 => "8 s".to_string(),
            10 => "15 s".to_string(),
            11 => "30 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashShutterSpeed",
            0x17,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x18) {
        let v = (v & 0xc0) >> 6;
        dm.push(("InternalFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Manual".to_string(),
            2 => "Repeating Flash".to_string(),
            3 => "Commander Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("InternalFlash", 0x18, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x18) {
        let v = v & 0x1f;
        dm.push(("ManualFlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val/3)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "ManualFlashOutput",
                0x18,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x19) {
        let v = (v & 0x70) >> 4;
        dm.push(("RepeatingFlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val-2)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "RepeatingFlashOutput",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x19) {
        let v = v & 0xf;
        dm.push(("RepeatingFlashCount".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val < 10 ? $val + 1 : 5 * ($val - 7)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "RepeatingFlashCount",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1a) {
        let v = (v & 0xf0) >> 4;
        dm.push(("RepeatingFlashRate".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val < 10 ? $val + 1 : 10 * ($val - 8)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val Hz\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "RepeatingFlashRate",
                0x1a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = (v & 0x80) >> 7;
        dm.push(("FlashWarning".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashWarning", 0x1f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = v & 0x1f;
        dm.push((
            "CommanderInternalTTLComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CommanderInternalTTLComp",
                0x1f,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = (v & 0x20) >> 5;
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x1f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = (v & 0x40) >> 6;
        dm.push(("AutoFP".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoFP", 0x1f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x20) {
        let v = v & 0x1f;
        dm.push((
            "CommanderGroupA_TTLComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CommanderGroupA_TTLComp",
                0x20,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x21) {
        let v = v & 0x1f;
        dm.push((
            "CommanderGroupB_TTLComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CommanderGroupB_TTLComp",
                0x21,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x22) {
        let v = (v & 0xc0) >> 6;
        dm.push(("LiveViewAF".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Face Priority".to_string(),
            1 => "Wide Area".to_string(),
            2 => "Normal Area".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LiveViewAF", 0x22, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD3` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd3(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x3;
        dm.push(("CustomSettingsBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsBank",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "CustomSettingsAllDefault".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Yes".to_string(),
            1 => "No".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsAllDefault",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Release + Focus".to_string(),
            2 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x20) >> 5;
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus".to_string(),
            1 => "Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-SPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x10) >> 4;
        dm.push(("AFPointSelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "51 Points".to_string(),
            1 => "11 Points".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPointSelection", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xc) >> 2;
        dm.push(("DynamicAFArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "9 Points".to_string(),
            1 => "21 Points".to_string(),
            2 => "51 Points".to_string(),
            3 => "51 Points (3D-tracking)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DynamicAFArea", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if !MODEL_RE_23.is_match(model) {
        if let Some(v) = u8_at(data, 0x1) {
            let v = v & 0x3;
            dm.push(("FocusTrackingLockOn".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Long".to_string(),
                1 => "Normal".to_string(),
                2 => "Short".to_string(),
                3 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FocusTrackingLockOn",
                0x1,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x80) >> 7;
        dm.push(("AFActivation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Shutter/AF-On".to_string(),
            1 => "AF-On Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFActivation", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x8) >> 3;
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Wrap".to_string(),
            1 => "Wrap".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusPointWrap", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0x2) {
            let v = (v & 0x60) >> 5;
            dm.push(("AFPointIllumination".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "On in Continuous Shooting and Manual Focusing".to_string(),
                1 => "On During Manual Focusing".to_string(),
                2 => "On in Continuous Shooting Modes".to_string(),
                3 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "AFPointIllumination",
                0x2,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(MODEL_RE_24.is_match(model)) {
        if let Some(v) = u8_at(data, 0x2) {
            let v = (v & 0x6) >> 1;
            dm.push(("AFPointIllumination".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Auto".to_string(),
                1 => "Off".to_string(),
                2 => "On".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "AFPointIllumination",
                0x2,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0x2) {
            let v = (v & 0x6) >> 1;
            dm.push(("AFPointBrightness".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Low".to_string(),
                1 => "Normal".to_string(),
                2 => "High".to_string(),
                3 => "Extra High".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFPointBrightness", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_25.is_match(model) {
        if let Some(v) = u8_at(data, 0x2) {
            let v = v & 0x1;
            dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "On".to_string(),
                1 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFAssist", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0x3) {
            let v = v & 0x7;
            dm.push(("AFOnButton".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "AF On".to_string(),
                1 => "AE/AF Lock".to_string(),
                2 => "AE Lock Only".to_string(),
                3 => "AE Lock (reset on release)".to_string(),
                4 => "AE Lock (hold)".to_string(),
                5 => "AF Lock Only".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AFOnButton", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0x3) {
            let v = (v & 0x70) >> 4;
            dm.push(("VerticalAFOnButton".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "AF On".to_string(),
                1 => "AE/AF Lock".to_string(),
                2 => "AE Lock Only".to_string(),
                3 => "AE Lock (reset on release)".to_string(),
                4 => "AE Lock (hold)".to_string(),
                5 => "AF Lock Only".to_string(),
                7 => "Same as AF On".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "VerticalAFOnButton",
                0x3,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_25.is_match(model) {
        if let Some(v) = u8_at(data, 0x3) {
            let v = (v & 0x70) >> 4;
            dm.push(("AF-OnForMB-D10".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "AF-On".to_string(),
                1 => "AE/AF Lock".to_string(),
                2 => "AE Lock Only".to_string(),
                3 => "AE Lock (reset on release)".to_string(),
                4 => "AE Lock (hold)".to_string(),
                5 => "AF Lock Only".to_string(),
                6 => "Same as FUNC Button".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AF-OnForMB-D10", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_23.is_match(model) {
        if let Some(v) = u8_at(data, 0x4) {
            let v = v & 0x7;
            dm.push(("FocusTrackingLockOn".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "5 (Long)".to_string(),
                1 => "4".to_string(),
                2 => "3 (Normal)".to_string(),
                3 => "2".to_string(),
                4 => "1 (Short)".to_string(),
                5 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FocusTrackingLockOn",
                0x4,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_23.is_match(model) {
        if let Some(v) = u8_at(data, 0x4) {
            let v = (v & 0x8) >> 3;
            dm.push(("AssignBktButton".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Auto Bracketing".to_string(),
                1 => "Multiple Exposure".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AssignBktButton", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_23.is_match(model) {
        if let Some(v) = u8_at(data, 0x4) {
            let v = (v & 0xc0) >> 6;
            dm.push(("MultiSelectorLiveView".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Reset".to_string(),
                1 => "Zoom On/Off".to_string(),
                2 => "Start Movie Recording".to_string(),
                3 => "Not Used".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "MultiSelectorLiveView",
                0x4,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_23.is_match(model) {
        if let Some(v) = u8_at(data, 0x4) {
            let v = (v & 0x30) >> 4;
            dm.push(("InitialZoomLiveView".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Low Magnification".to_string(),
                1 => "Medium Magnification".to_string(),
                2 => "High Magnification".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "InitialZoomLiveView",
                0x4,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0xc0) >> 6;
        dm.push(("ISOStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOStepSize", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x30) >> 4;
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0xc) >> 2;
        dm.push(("ExposureCompStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureCompStepSize",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = v & 0x3;
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (auto reset)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0x7) {
            let v = (v & 0xe0) >> 5;
            dm.push((
                "CenterWeightedAreaSize".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "8 mm".to_string(),
                1 => "12 mm".to_string(),
                2 => "15 mm".to_string(),
                3 => "20 mm".to_string(),
                4 => "Average".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CenterWeightedAreaSize",
                0x7,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(MODEL_RE_24.is_match(model)) {
        if let Some(v) = u8_at(data, 0x7) {
            let v = (v & 0xe0) >> 5;
            dm.push((
                "CenterWeightedAreaSize".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "6 mm".to_string(),
                1 => "8 mm".to_string(),
                2 => "10 mm".to_string(),
                3 => "13 mm".to_string(),
                4 => "Average".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CenterWeightedAreaSize",
                0x7,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = (v & 0xc0) >> 6;
        dm.push((
            "MultiSelectorShootMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Select Center Focus Point".to_string(),
            1 => "Highlight Active Focus Point".to_string(),
            2 => "Not Used".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorShootMode",
            0x9,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if !MODEL_RE_23.is_match(model) {
        if let Some(v) = u8_at(data, 0x9) {
            let v = (v & 0x30) >> 4;
            dm.push((
                "MultiSelectorPlaybackMode".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Thumbnail On/Off".to_string(),
                1 => "View Histograms".to_string(),
                2 => "Zoom On/Off".to_string(),
                3 => "Choose Folder".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "MultiSelectorPlaybackMode",
                0x9,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0x9) {
            let v = (v & 0xc) >> 2;
            dm.push(("InitialZoomSetting".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "High Magnification".to_string(),
                1 => "Medium Magnification".to_string(),
                2 => "Low Magnification".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "InitialZoomSetting",
                0x9,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(MODEL_RE_24.is_match(model)) {
        if let Some(v) = u8_at(data, 0x9) {
            let v = (v & 0xc) >> 2;
            dm.push(("InitialZoomSetting".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Low Magnification".to_string(),
                1 => "Medium Magnification".to_string(),
                2 => "High Magnification".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "InitialZoomSetting",
                0x9,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = v & 0x1;
        dm.push(("MultiSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Do Nothing".to_string(),
            1 => "Reset Meter-off Delay".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MultiSelector", 0x9, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0x40) >> 6;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = v & 0x7;
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "CLModeShootingSpeed",
            0xa,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0xa) {
            let v = (v & 0x30) >> 4;
            dm.push(("CHModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "9 fps".to_string(),
                1 => "10 fps".to_string(),
                2 => "11 fps".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CHModeShootingSpeed",
                0xa,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xb) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MaxContinuousRelease",
            0xb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc) {
        let v = (v & 0x20) >> 5;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReverseIndicators", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0xc) {
            let v = (v & 0x2) >> 1;
            dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "On".to_string(),
                1 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FileNumberSequence",
                0xc,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(MODEL_RE_24.is_match(model)) {
        if let Some(v) = u8_at(data, 0xc) {
            let v = (v & 0x8) >> 3;
            dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "On".to_string(),
                1 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FileNumberSequence",
                0xc,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0xc) {
            let v = (v & 0x80) >> 7;
            dm.push(("RearDisplay".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "ISO".to_string(),
                1 => "Exposures Remaining".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("RearDisplay", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0xc) {
            let v = (v & 0x40) >> 6;
            dm.push(("ViewfinderDisplay".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Frame Count".to_string(),
                1 => "Exposures Remaining".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ViewfinderDisplay", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_25.is_match(model) {
        if let Some(v) = u8_at(data, 0xc) {
            let v = (v & 0x4) >> 2;
            dm.push(("BatteryOrder".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "MB-D10 First".to_string(),
                1 => "Camera Battery First".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("BatteryOrder", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_25.is_match(model) {
        if let Some(v) = u8_at(data, 0xc) {
            let v = v & 0x3;
            dm.push(("MB-D10Batteries".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "LR6 (AA alkaline)".to_string(),
                1 => "HR6 (AA Ni-MH)".to_string(),
                2 => "FR6 (AA lithium)".to_string(),
                3 => "ZR6 (AA Ni-Mn)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("MB-D10Batteries", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_26.is_match(model) {
        if let Some(v) = u8_at(data, 0xc) {
            let v = (v & 0x10) >> 4;
            dm.push(("ScreenTips".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "On".to_string(),
                1 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ScreenTips", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0xc0) >> 6;
        dm.push(("Beep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "High".to_string(),
            1 => "Low".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Beep", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0x30) >> 4;
        dm.push(("ShootingInfoDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Auto".to_string(),
            2 => "Manual (dark on light)".to_string(),
            3 => "Manual (light on dark)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoDisplay",
            0xd,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_25.is_match(model) {
        if let Some(v) = u8_at(data, 0xd) {
            let v = (v & 0x2) >> 1;
            dm.push(("GridDisplay".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("GridDisplay", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_25.is_match(model) {
        if let Some(v) = u8_at(data, 0xd) {
            let v = v & 0x1;
            dm.push(("ViewfinderWarning".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "On".to_string(),
                1 => "Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("ViewfinderWarning", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_23.is_match(model) {
        if let Some(v) = u8_at(data, 0xd) {
            let v = v & 0x3;
            dm.push((
                "MultiSelectorPlaybackMode".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Thumbnail On/Off".to_string(),
                1 => "View Histograms".to_string(),
                2 => "Zoom On/Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "MultiSelectorPlaybackMode",
                0xd,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0xe) {
            let v = (v & 0xf8) >> 3;
            dm.push(("PreviewButton".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Preview".to_string(),
                2 => "FV Lock".to_string(),
                3 => "AE/AF Lock".to_string(),
                4 => "AE Lock Only".to_string(),
                5 => "AE Lock (reset on release)".to_string(),
                6 => "AE Lock (hold)".to_string(),
                7 => "AF Lock Only".to_string(),
                8 => "Flash Off".to_string(),
                9 => "Bracketing Burst".to_string(),
                10 => "Matrix Metering".to_string(),
                11 => "Center-weighted Metering".to_string(),
                12 => "Spot Metering".to_string(),
                13 => "Virtual Horizon".to_string(),
                15 => "Playback".to_string(),
                16 => "My Menu Top".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("PreviewButton", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(MODEL_RE_24.is_match(model)) {
        if let Some(v) = u8_at(data, 0xe) {
            let v = (v & 0xf8) >> 3;
            dm.push(("FuncButton".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Preview".to_string(),
                2 => "FV Lock".to_string(),
                3 => "AE/AF Lock".to_string(),
                4 => "AE Lock Only".to_string(),
                5 => "AE Lock (reset on release)".to_string(),
                6 => "AE Lock (hold)".to_string(),
                7 => "AF Lock Only".to_string(),
                9 => "Flash Off".to_string(),
                10 => "Bracketing Burst".to_string(),
                11 => "Matrix Metering".to_string(),
                12 => "Center-weighted Metering".to_string(),
                13 => "Spot Metering".to_string(),
                14 => "Playback".to_string(),
                15 => "My Menu Top".to_string(),
                16 => "+ NEF (RAW)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("FuncButton", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(MODEL_RE_24.is_match(model)) && !(true) && MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0xe) {
            let v = v & 0x7;
            dm.push((
                "PreviewButtonPlusDials".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Choose Image Area (FX/DX/5:4)".to_string(),
                2 => "One Step Speed/Aperture".to_string(),
                3 => "Choose Non-CPU Lens Number".to_string(),
                5 => "Choose Image Area (FX/DX)".to_string(),
                6 => "Shooting Bank Menu".to_string(),
                7 => "Dynamic AF Area".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "PreviewButtonPlusDials",
                0xe,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(MODEL_RE_24.is_match(model)) && !(true) && !(MODEL_RE_24.is_match(model)) {
        if let Some(v) = u8_at(data, 0xe) {
            let v = v & 0x7;
            dm.push(("FuncButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                2 => "One Step Speed/Aperture".to_string(),
                3 => "Choose Non-CPU Lens Number".to_string(),
                5 => "Auto Bracketing".to_string(),
                6 => "Dynamic AF Area".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FuncButtonPlusDials",
                0xe,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0xf) {
            let v = (v & 0xf8) >> 3;
            dm.push(("FuncButton".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Preview".to_string(),
                2 => "FV Lock".to_string(),
                3 => "AE/AF Lock".to_string(),
                4 => "AE Lock Only".to_string(),
                5 => "AE Lock (reset on release)".to_string(),
                6 => "AE Lock (hold)".to_string(),
                7 => "AF Lock Only".to_string(),
                8 => "Flash Off".to_string(),
                9 => "Bracketing Burst".to_string(),
                10 => "Matrix Metering".to_string(),
                11 => "Center-weighted Metering".to_string(),
                12 => "Spot Metering".to_string(),
                13 => "Virtual Horizon".to_string(),
                15 => "Playback".to_string(),
                16 => "My Menu Top".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("FuncButton", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(MODEL_RE_24.is_match(model)) {
        if let Some(v) = u8_at(data, 0xf) {
            let v = (v & 0xf8) >> 3;
            dm.push(("PreviewButton".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Preview".to_string(),
                2 => "FV Lock".to_string(),
                3 => "AE/AF Lock".to_string(),
                4 => "AE Lock Only".to_string(),
                5 => "AE Lock (reset on release)".to_string(),
                6 => "AE Lock (hold)".to_string(),
                7 => "AF Lock Only".to_string(),
                9 => "Flash Off".to_string(),
                10 => "Bracketing Burst".to_string(),
                11 => "Matrix Metering".to_string(),
                12 => "Center-weighted Metering".to_string(),
                13 => "Spot Metering".to_string(),
                14 => "Playback".to_string(),
                15 => "My Menu Top".to_string(),
                16 => "+ NEF (RAW)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("PreviewButton", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(MODEL_RE_24.is_match(model)) && !(true) && MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0xf) {
            let v = v & 0x7;
            dm.push(("FuncButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Choose Image Area (FX/DX/5:4)".to_string(),
                2 => "One Step Speed/Aperture".to_string(),
                3 => "Choose Non-CPU Lens Number".to_string(),
                4 => "Focus Point Selection".to_string(),
                5 => "Choose Image Area (FX/DX)".to_string(),
                6 => "Shooting Bank Menu".to_string(),
                7 => "Dynamic AF Area".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FuncButtonPlusDials",
                0xf,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(MODEL_RE_24.is_match(model)) && !(true) && !(MODEL_RE_24.is_match(model)) {
        if let Some(v) = u8_at(data, 0xf) {
            let v = v & 0x7;
            dm.push((
                "PreviewButtonPlusDials".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                2 => "One Step Speed/Aperture".to_string(),
                3 => "Choose Non-CPU Lens Number".to_string(),
                5 => "Auto Bracketing".to_string(),
                6 => "Dynamic AF Area".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "PreviewButtonPlusDials",
                0xf,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0x10) {
            let v = (v & 0xf8) >> 3;
            dm.push(("AELockButton".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Preview".to_string(),
                2 => "FV Lock".to_string(),
                3 => "AE/AF Lock".to_string(),
                4 => "AE Lock Only".to_string(),
                5 => "AE Lock (reset on release)".to_string(),
                6 => "AE Lock (hold)".to_string(),
                7 => "AF Lock Only".to_string(),
                8 => "Flash Off".to_string(),
                9 => "Bracketing Burst".to_string(),
                10 => "Matrix Metering".to_string(),
                11 => "Center-weighted Metering".to_string(),
                12 => "Spot Metering".to_string(),
                13 => "Virtual Horizon".to_string(),
                14 => "AF On".to_string(),
                15 => "Playback".to_string(),
                16 => "My Menu Top".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AELockButton", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(MODEL_RE_24.is_match(model)) {
        if let Some(v) = u8_at(data, 0x10) {
            let v = (v & 0xf8) >> 3;
            dm.push(("AELockButton".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Preview".to_string(),
                2 => "FV Lock".to_string(),
                3 => "AE/AF Lock".to_string(),
                4 => "AE Lock Only".to_string(),
                5 => "AE Lock (reset on release)".to_string(),
                6 => "AE Lock (hold)".to_string(),
                7 => "AF Lock Only".to_string(),
                8 => "AF On".to_string(),
                9 => "Flash Off".to_string(),
                10 => "Bracketing Burst".to_string(),
                11 => "Matrix Metering".to_string(),
                12 => "Center-weighted Metering".to_string(),
                13 => "Spot Metering".to_string(),
                14 => "Playback".to_string(),
                15 => "My Menu Top".to_string(),
                16 => "+ NEF (RAW)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AELockButton", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(MODEL_RE_24.is_match(model)) && !(true) && MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0x10) {
            let v = v & 0x7;
            dm.push(("AELockButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Choose Image Area (FX/DX/5:4)".to_string(),
                2 => "One Step Speed/Aperture".to_string(),
                3 => "Choose Non-CPU Lens Number".to_string(),
                5 => "Choose Image Area (FX/DX)".to_string(),
                6 => "Shooting Bank Menu".to_string(),
                7 => "Dynamic AF Area".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "AELockButtonPlusDials",
                0x10,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if !(MODEL_RE_24.is_match(model)) && !(true) && !(MODEL_RE_24.is_match(model)) {
        if let Some(v) = u8_at(data, 0x10) {
            let v = v & 0x7;
            dm.push(("AELockButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                3 => "Choose Non-CPU Lens Number".to_string(),
                5 => "Auto Bracketing".to_string(),
                6 => "Dynamic AF Area".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "AELockButtonPlusDials",
                0x10,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "CommandDialsReverseRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsReverseRotation",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x40) >> 6;
        dm.push((
            "CommandDialsChangeMainSub".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsChangeMainSub",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x20) >> 5;
        dm.push((
            "CommandDialsApertureSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Sub-command Dial".to_string(),
            1 => "Aperture Ring".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsApertureSetting",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "CommandDialsMenuAndPlayback".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsMenuAndPlayback",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x8) >> 3;
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LCDIllumination", 0x11, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x4) >> 2;
        dm.push(("PhotoInfoPlayback".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Info Up-down, Playback Left-right".to_string(),
            1 => "Info Left-right, Playback Up-down".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PhotoInfoPlayback",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = v & 0x1;
        dm.push((
            "ReleaseButtonToUseDial".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ReleaseButtonToUseDial",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x18) >> 3;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = v & 0x7;
        dm.push(("MonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "10 s".to_string(),
            1 => "20 s".to_string(),
            2 => "1 min".to_string(),
            3 => "5 min".to_string(),
            4 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MonitorOffTime", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if MODEL_RE_24.is_match(model) {
        if let Some(v) = u8_at(data, 0x14) {
            let v = (v & 0xe0) >> 5;
            dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1/250 s (auto FP)".to_string(),
                1 => "1/250 s".to_string(),
                2 => "1/200 s".to_string(),
                3 => "1/160 s".to_string(),
                4 => "1/125 s".to_string(),
                5 => "1/100 s".to_string(),
                6 => "1/80 s".to_string(),
                7 => "1/60 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("FlashSyncSpeed", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(MODEL_RE_24.is_match(model)) {
        if let Some(v) = u8_at(data, 0x14) {
            let v = (v & 0xf0) >> 4;
            dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1/320 s (auto FP)".to_string(),
                1 => "1/250 s (auto FP)".to_string(),
                2 => "1/250 s".to_string(),
                3 => "1/200 s".to_string(),
                4 => "1/160 s".to_string(),
                5 => "1/125 s".to_string(),
                6 => "1/100 s".to_string(),
                7 => "1/80 s".to_string(),
                8 => "1/60 s".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("FlashSyncSpeed", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = v & 0xf;
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/60 s".to_string(),
            1 => "1/30 s".to_string(),
            2 => "1/15 s".to_string(),
            3 => "1/8 s".to_string(),
            4 => "1/4 s".to_string(),
            5 => "1/2 s".to_string(),
            6 => "1 s".to_string(),
            7 => "2 s".to_string(),
            8 => "4 s".to_string(),
            9 => "8 s".to_string(),
            10 => "15 s".to_string(),
            11 => "30 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashShutterSpeed",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if !MODEL_RE_26.is_match(model) {
        if let Some(v) = u8_at(data, 0x15) {
            let v = (v & 0xc0) >> 6;
            dm.push(("AutoBracketSet".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "AE & Flash".to_string(),
                1 => "AE Only".to_string(),
                2 => "Flash Only".to_string(),
                3 => "WB Bracketing".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AutoBracketSet", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(!MODEL_RE_26.is_match(model)) {
        if let Some(v) = u8_at(data, 0x15) {
            let v = (v & 0xe0) >> 5;
            dm.push(("AutoBracketSet".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "AE & Flash".to_string(),
                1 => "AE Only".to_string(),
                2 => "Flash Only".to_string(),
                3 => "WB Bracketing".to_string(),
                4 => "ADL Bracketing".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AutoBracketSet", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(!MODEL_RE_26.is_match(model)) && !(true) && !MODEL_RE_26.is_match(model) {
        if let Some(v) = u8_at(data, 0x15) {
            let v = (v & 0x30) >> 4;
            dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Flash/Speed".to_string(),
                1 => "Flash/Speed/Aperture".to_string(),
                2 => "Flash/Aperture".to_string(),
                3 => "Flash Only".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AutoBracketModeM", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(!MODEL_RE_26.is_match(model)) && !(true) && !(!MODEL_RE_26.is_match(model)) {
        if let Some(v) = u8_at(data, 0x15) {
            let v = (v & 0x18) >> 3;
            dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Flash/Speed".to_string(),
                1 => "Flash/Speed/Aperture".to_string(),
                2 => "Flash/Aperture".to_string(),
                3 => "Flash Only".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AutoBracketModeM", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(!MODEL_RE_26.is_match(model))
        && !(true)
        && !(!MODEL_RE_26.is_match(model))
        && !(true)
        && !MODEL_RE_26.is_match(model)
    {
        if let Some(v) = u8_at(data, 0x15) {
            let v = (v & 0x8) >> 3;
            dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "0,-,+".to_string(),
                1 => "-,0,+".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AutoBracketOrder", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if !(!MODEL_RE_26.is_match(model))
        && !(true)
        && !(!MODEL_RE_26.is_match(model))
        && !(true)
        && !(!MODEL_RE_26.is_match(model))
    {
        if let Some(v) = u8_at(data, 0x15) {
            let v = (v & 0x4) >> 2;
            dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "0,-,+".to_string(),
                1 => "-,0,+".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("AutoBracketOrder", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = v & 0x1;
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0x80) >> 7;
        dm.push(("NoMemoryCard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release Locked".to_string(),
            1 => "Enable Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoMemoryCard", 0x16, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = v & 0xf;
        dm.push(("MeteringTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "6 s".to_string(),
            2 => "8 s".to_string(),
            3 => "16 s".to_string(),
            4 => "30 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            8 => "30 min".to_string(),
            9 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MeteringTime", 0x16, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = (v & 0xc0) >> 6;
        dm.push(("InternalFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Manual".to_string(),
            2 => "Repeating Flash".to_string(),
            3 => "Commander Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("InternalFlash", 0x17, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x19) {
        let v = (v & 0xe0) >> 5;
        dm.push(("ImageReviewTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageReviewTime", 0x19, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x19) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x19,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1a) {
        let v = (v & 0xe0) >> 5;
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x1a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1a) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x1a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD700` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd700(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x3;
        dm.push(("CustomSettingsBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsBank",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "CustomSettingsAllDefault".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Yes".to_string(),
            1 => "No".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsAllDefault",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Release + Focus".to_string(),
            2 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x20) >> 5;
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus".to_string(),
            1 => "Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-SPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x10) >> 4;
        dm.push(("AFPointSelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "51 Points".to_string(),
            1 => "11 Points".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPointSelection", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xc) >> 2;
        dm.push(("DynamicAFArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "9 Points".to_string(),
            1 => "21 Points".to_string(),
            2 => "51 Points".to_string(),
            3 => "51 Points (3D-tracking)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DynamicAFArea", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x80) >> 7;
        dm.push(("AFActivation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Shutter/AF-On".to_string(),
            1 => "AF-On Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFActivation", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x8) >> 3;
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Wrap".to_string(),
            1 => "Wrap".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusPointWrap", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x6) >> 1;
        dm.push(("AFPointIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Off".to_string(),
            2 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointIllumination",
            0x2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = v & 0x1;
        dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssist", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = v & 0x7;
        dm.push(("FocusTrackingLockOn".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "3 Normal".to_string(),
            1 => "4".to_string(),
            2 => "5 Long".to_string(),
            3 => "2".to_string(),
            4 => "1 Short".to_string(),
            5 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusTrackingLockOn",
            0x3,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x70) >> 4;
        dm.push(("AF-OnForMB-D10".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AF-On".to_string(),
            1 => "AE/AF Lock".to_string(),
            2 => "AE Lock Only".to_string(),
            3 => "AE Lock (reset on release)".to_string(),
            4 => "AE Lock (hold)".to_string(),
            5 => "AF Lock Only".to_string(),
            6 => "Same as FUNC Button".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AF-OnForMB-D10", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0xc0) >> 6;
        dm.push(("ISOStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOStepSize", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x30) >> 4;
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0xc) >> 2;
        dm.push(("ExposureCompStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureCompStepSize",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = v & 0x3;
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (auto reset)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x70) >> 4;
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "8 mm".to_string(),
            1 => "12 mm".to_string(),
            2 => "15 mm".to_string(),
            3 => "20 mm".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x6,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x6,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x30) >> 4;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = v & 0xf;
        dm.push(("MeteringTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "6 s".to_string(),
            2 => "8 s".to_string(),
            3 => "16 s".to_string(),
            4 => "30 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            8 => "30 min".to_string(),
            9 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MeteringTime", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = (v & 0x38) >> 3;
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = v & 0x7;
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = (v & 0x38) >> 3;
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x9,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = v & 0x7;
        dm.push(("ImageReviewTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageReviewTime", 0x9, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0xc0) >> 6;
        dm.push(("Beep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "High".to_string(),
            1 => "Low".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Beep", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0x30) >> 4;
        dm.push(("ShootingInfoDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Auto".to_string(),
            2 => "Manual (dark on light)".to_string(),
            3 => "Manual (light on dark)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoDisplay",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0x8) >> 3;
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LCDIllumination", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0x4) >> 2;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0x2) >> 1;
        dm.push(("GridDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GridDisplay", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0x40) >> 6;
        dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FileNumberSequence",
            0xb,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = v & 0x7;
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "CLModeShootingSpeed",
            0xb,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MaxContinuousRelease",
            0xc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0x8) >> 3;
        dm.push(("ScreenTips".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ScreenTips", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0x4) >> 2;
        dm.push(("BatteryOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "MB-D10 First".to_string(),
            1 => "Camera Battery First".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("BatteryOrder", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = v & 0x3;
        dm.push(("MB-D10BatteryType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "LR6 (AA alkaline)".to_string(),
            1 => "HR6 (AA Ni-MH)".to_string(),
            2 => "FR6 (AA lithium)".to_string(),
            3 => "ZR6 (AA Ni-Mn)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MB-D10BatteryType", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = (v & 0xf0) >> 4;
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/320 s (auto FP)".to_string(),
            1 => "1/250 s (auto FP)".to_string(),
            2 => "1/250 s".to_string(),
            3 => "1/200 s".to_string(),
            4 => "1/160 s".to_string(),
            5 => "1/125 s".to_string(),
            6 => "1/100 s".to_string(),
            7 => "1/80 s".to_string(),
            8 => "1/60 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashSyncSpeed", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = v & 0xf;
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/60 s".to_string(),
            1 => "1/30 s".to_string(),
            2 => "1/15 s".to_string(),
            3 => "1/8 s".to_string(),
            4 => "1/4 s".to_string(),
            5 => "1/2 s".to_string(),
            6 => "1 s".to_string(),
            7 => "2 s".to_string(),
            8 => "4 s".to_string(),
            9 => "8 s".to_string(),
            10 => "15 s".to_string(),
            11 => "30 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashShutterSpeed", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = (v & 0xc0) >> 6;
        dm.push(("FlashControlBuiltin".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Manual".to_string(),
                2 => "Repeating Flash".to_string(),
                3 => "Commander Mode".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlBuilt-in",
                0x10,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 1.0) {
        if let Some(v) = u8_at(data, 0x10) {
            let v = v & 0x1f;
            dm.push(("ManualFlashOutput".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("2 ** (-$val/3)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ManualFlashOutput",
                    0x10,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x11) {
            let v = (v & 0x70) >> 4;
            dm.push(("RepeatingFlashOutput".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("2 ** (-$val-2)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "RepeatingFlashOutput",
                    0x11,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x11) {
            let v = v & 0xf;
            dm.push(("RepeatingFlashCount".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val < 10 ? $val + 1 : 5 * ($val - 7)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "RepeatingFlashCount",
                    0x11,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x12) {
            let v = (v & 0xf0) >> 4;
            dm.push(("RepeatingFlashRate".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val < 10 ? $val + 1 : 10 * ($val - 8)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("\"$val Hz\"", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "RepeatingFlashRate",
                    0x12,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 3.0) {
        if let Some(v) = u8_at(data, 0x12) {
            let v = v & 0x3;
            dm.push((
                "CommanderInternalTTLChannel".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "1 ch".to_string(),
                1 => "2 ch".to_string(),
                2 => "3 ch".to_string(),
                3 => "4 ch".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "CommanderInternalTTLChannel",
                0x12,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 3.0) {
        if let Some(v) = u8_at(data, 0x14) {
            let v = v & 0x1f;
            dm.push((
                "CommanderInternalTTLCompBuiltin".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) =
                    conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx)
                {
                    cv = x;
                }
                tags.push(mk(
                    "CommanderInternalTTLCompBuiltin",
                    0x14,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 3.0) {
        if let Some(v) = u8_at(data, 0x15) {
            let v = v & 0x1f;
            dm.push((
                "CommanderInternalTTLCompGroupA".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) =
                    conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx)
                {
                    cv = x;
                }
                tags.push(mk(
                    "CommanderInternalTTLCompGroupA",
                    0x15,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 3.0) {
        if let Some(v) = u8_at(data, 0x16) {
            let v = v & 0x1f;
            dm.push((
                "CommanderInternalTTLCompGroupB".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) =
                    conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx)
                {
                    cv = x;
                }
                tags.push(mk(
                    "CommanderInternalTTLCompGroupB",
                    0x16,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x1a) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AutoBracketSet".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE & Flash".to_string(),
            1 => "AE Only".to_string(),
            2 => "Flash Only".to_string(),
            3 => "WB Bracketing".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketSet", 0x1a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1a) {
        let v = (v & 0x30) >> 4;
        dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Flash/Speed".to_string(),
            1 => "Flash/Speed/Aperture".to_string(),
            2 => "Flash/Aperture".to_string(),
            3 => "Flash Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketModeM", 0x1a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1a) {
        let v = (v & 0x8) >> 3;
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0,-,+".to_string(),
            1 => "-,0,+".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketOrder", 0x1a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1a) {
        let v = v & 0x1;
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x1a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1b) {
        let v = (v & 0xc0) >> 6;
        dm.push((
            "MultiSelectorShootMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Select Center Focus Point".to_string(),
            1 => "Highlight Active Focus Point".to_string(),
            2 => "Not Used".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorShootMode",
            0x1b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b) {
        let v = (v & 0x30) >> 4;
        dm.push((
            "MultiSelectorPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Thumbnail On/Off".to_string(),
            1 => "View Histograms".to_string(),
            2 => "Zoom On/Off".to_string(),
            3 => "Choose Folder".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorPlaybackMode",
            0x1b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b) {
        let v = (v & 0xc) >> 2;
        dm.push(("InitialZoomSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Low Magnification".to_string(),
            1 => "Medium Magnification".to_string(),
            2 => "High Magnification".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "InitialZoomSetting",
            0x1b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b) {
        let v = v & 0x1;
        dm.push(("MultiSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Do Nothing".to_string(),
            1 => "Reset Meter-off Delay".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MultiSelector", 0x1b, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1c) {
        let v = (v & 0xf8) >> 3;
        dm.push(("FuncButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            9 => "Flash Off".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "My Menu Top".to_string(),
            15 => "Live View".to_string(),
            16 => "+ NEF (RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FuncButton", 0x1c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1d) {
        let v = (v & 0xf8) >> 3;
        dm.push(("PreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-ON".to_string(),
            9 => "Flash Off".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "My Menu Top".to_string(),
            15 => "Live View".to_string(),
            16 => "+ NEF (RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PreviewButton", 0x1d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1e) {
        let v = (v & 0xf8) >> 3;
        dm.push(("AELockButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-ON".to_string(),
            9 => "Flash Off".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "My Menu Top".to_string(),
            15 => "Live View".to_string(),
            16 => "+ NEF (RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELockButton", 0x1e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = (v & 0x70) >> 4;
        dm.push(("FuncButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "One Step Speed/Aperture".to_string(),
            3 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Auto bracketing".to_string(),
            6 => "Dynamic AF Area".to_string(),
            7 => "Shutter speed & Aperture lock".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FuncButtonPlusDials",
            0x1f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = v & 0x7;
        dm.push((
            "PreviewButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "One Step Speed/Aperture".to_string(),
            3 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Auto bracketing".to_string(),
            6 => "Dynamic AF Area".to_string(),
            7 => "Shutter speed & Aperture lock".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PreviewButtonPlusDials",
            0x1f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x20) {
        let v = (v & 0x70) >> 4;
        dm.push(("AELockButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AELockButtonPlusDials",
            0x20,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x21) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "CommandDialsReverseRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsReverseRotation",
            0x21,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x21) {
        let v = (v & 0x40) >> 6;
        dm.push((
            "CommandDialsChangeMainSub".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsChangeMainSub",
            0x21,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x21) {
        let v = (v & 0x20) >> 5;
        dm.push((
            "CommandDialsApertureSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Sub-command Dial".to_string(),
            1 => "Aperture Ring".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsApertureSetting",
            0x21,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x21) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "CommandDialsMenuAndPlayback".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsMenuAndPlayback",
            0x21,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x21) {
        let v = (v & 0x8) >> 3;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ReverseIndicators",
            0x21,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x21) {
        let v = (v & 0x4) >> 2;
        dm.push(("PhotoInfoPlayback".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PhotoInfoPlayback",
            0x21,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x21) {
        let v = (v & 0x2) >> 1;
        dm.push(("NoMemoryCard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release Locked".to_string(),
            1 => "Enable Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoMemoryCard", 0x21, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x21) {
        let v = v & 0x1;
        dm.push((
            "ReleaseButtonToUseDial".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ReleaseButtonToUseDial",
            0x21,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD800` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd800(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0xc) {
        let v = (v & 0xe0) >> 5;
        dm.push(("AutoBracketingSet".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE & Flash".to_string(),
            1 => "AE Only".to_string(),
            2 => "Flash Only".to_string(),
            3 => "WB Bracketing".to_string(),
            4 => "Active D-Lighting".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketingSet", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xc) {
        let v = (v & 0x10) >> 4;
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0,-,+".to_string(),
            1 => "-,0,+".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketOrder", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xc) {
        let v = (v & 0xc) >> 2;
        dm.push(("AutoBracketingMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Flash/Speed".to_string(),
            1 => "Flash/Speed/Aperture".to_string(),
            2 => "Flash/Aperture".to_string(),
            3 => "Flash Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoBracketingMode",
            0xc,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0xf0) >> 4;
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/320 s (auto FP)".to_string(),
            1 => "1/250 s (auto FP)".to_string(),
            2 => "1/250 s".to_string(),
            3 => "1/200 s".to_string(),
            4 => "1/160 s".to_string(),
            5 => "1/125 s".to_string(),
            6 => "1/100 s".to_string(),
            7 => "1/80 s".to_string(),
            8 => "1/60 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashSyncSpeed", 0x16, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = v & 0xf;
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/60 s".to_string(),
            1 => "1/30 s".to_string(),
            2 => "1/15 s".to_string(),
            3 => "1/8 s".to_string(),
            4 => "1/4 s".to_string(),
            5 => "1/2 s".to_string(),
            6 => "1 s".to_string(),
            7 => "2 s".to_string(),
            8 => "4 s".to_string(),
            9 => "8 s".to_string(),
            10 => "15 s".to_string(),
            11 => "30 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashShutterSpeed",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = (v & 0xc0) >> 6;
        dm.push(("FlashControlBuiltin".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Manual".to_string(),
                2 => "Repeating Flash".to_string(),
                3 => "Commander Mode".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlBuilt-in",
                0x17,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 1.0) {
        if let Some(v) = u8_at(data, 0x17) {
            let v = v & 0x1f;
            dm.push(("ManualFlashOutput".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("2 ** (-$val/3)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ManualFlashOutput",
                    0x17,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x18) {
            let v = (v & 0x70) >> 4;
            dm.push(("RepeatingFlashOutput".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("2 ** (-$val-2)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "RepeatingFlashOutput",
                    0x18,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x18) {
            let v = v & 0xf;
            dm.push(("RepeatingFlashCount".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val < 10 ? $val + 1 : 5 * ($val - 7)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "RepeatingFlashCount",
                    0x18,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x19) {
            let v = (v & 0xf0) >> 4;
            dm.push(("RepeatingFlashRate".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val < 10 ? $val + 1 : 10 * ($val - 8)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("\"$val Hz\"", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "RepeatingFlashRate",
                    0x19,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x19) {
        let v = v & 0x3;
        dm.push(("CommanderChannel".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 1", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "CommanderChannel",
                0x19,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1b) {
        let v = (v & 0xc0) >> 6;
        dm.push((
            "CommanderInternalFlash".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Manual".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommanderInternalFlash",
            0x1b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1b) {
        let v = v & 0x1f;
        dm.push((
            "CommanderInternalManualOutput".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val/3)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "CommanderInternalManualOutput",
                0x1b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1c) {
        let v = (v & 0xc0) >> 6;
        dm.push(("CommanderGroupAMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Auto Aperture".to_string(),
            2 => "Manual".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommanderGroupAMode",
            0x1c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1c) {
        let v = v & 0x1f;
        dm.push((
            "CommanderGroupAManualOutput".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val/3)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "CommanderGroupAManualOutput",
                0x1c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1d) {
        let v = (v & 0xc0) >> 6;
        dm.push(("CommanderGroupBMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Auto Aperture".to_string(),
            2 => "Manual".to_string(),
            3 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommanderGroupBMode",
            0x1d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1d) {
        let v = v & 0x1f;
        dm.push((
            "CommanderGroupBManualOutput".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val/3)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "CommanderGroupBManualOutput",
                0x1d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1e) {
        let v = (v & 0x20) >> 5;
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x1e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1e) {
        let v = v & 0x1f;
        dm.push((
            "CommanderInternalTTLComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CommanderInternalTTLComp",
                0x1e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = v & 0x1f;
        dm.push((
            "CommanderGroupA_TTL-AAComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CommanderGroupA_TTL-AAComp",
                0x1f,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x20) {
        let v = v & 0x1f;
        dm.push((
            "CommanderGroupB_TTL-AAComp".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CommanderGroupB_TTL-AAComp",
                0x20,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD5000` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd5000(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x60) >> 5;
        dm.push(("AFAreaModeSetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Single Area".to_string(),
            1 => "Dynamic Area".to_string(),
            2 => "Auto-area".to_string(),
            3 => "3D-tracking (11 points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAreaModeSetting", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x1;
        dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssist", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0xc0) >> 6;
        dm.push(("Beep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Low".to_string(),
            2 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Beep", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x2) >> 1;
        dm.push(("GridDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GridDisplay", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x8) >> 3;
        dm.push(("ISODisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISODisplay", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x20) >> 5;
        dm.push(("NoMemoryCard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release Locked".to_string(),
            1 => "Enable Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoMemoryCard", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x8) >> 3;
        dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FileNumberSequence",
            0x3,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x10) >> 4;
        dm.push(("RangeFinder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RangeFinder", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x8) >> 3;
        dm.push(("DateImprint".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DateImprint", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x80) >> 7;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReverseIndicators", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x40) >> 6;
        dm.push(("EVStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("EVStepSize", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = (v & 0x40) >> 6;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0x9, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AutoBracketSet".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Exposure".to_string(),
            1 => "Active D-Lighting".to_string(),
            2 => "WB Bracketing".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketSet", 0xb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xc) {
        let v = (v & 0x38) >> 3;
        dm.push(("TimerFunctionButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Self-timer".to_string(),
            1 => "Release Mode".to_string(),
            2 => "Image Quality/Size".to_string(),
            3 => "ISO".to_string(),
            4 => "White Balance".to_string(),
            5 => "Active D-Lighting".to_string(),
            6 => "+ NEF (RAW)".to_string(),
            7 => "Auto Bracketing".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "TimerFunctionButton",
            0xc,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = (v & 0x38) >> 3;
        dm.push(("AELockButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE/AF Lock".to_string(),
            1 => "AE Lock Only".to_string(),
            2 => "AF Lock Only".to_string(),
            3 => "AE Lock (hold)".to_string(),
            4 => "AF-ON".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELockButton", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x10,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "CommandDialsReverseRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsReverseRotation",
            0x10,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x70) >> 4;
        dm.push(("MeteringTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "8 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "30 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MeteringTime", 0x11, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = v & 0x3;
        dm.push(("RemoteOnDuration".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 min".to_string(),
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RemoteOnDuration", 0x11, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0xc0) >> 6;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x1e) >> 1;
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x12,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0xe0) >> 5;
        dm.push(("ImageReviewTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "8 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageReviewTime", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0xe0) >> 5;
        dm.push(("PlaybackMenusTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "8 s".to_string(),
            1 => "12 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMenusTime",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0xc0) >> 6;
        dm.push(("InternalFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Manual".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("InternalFlash", 0x16, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = v & 0x1f;
        dm.push(("ManualFlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val/3)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "ManualFlashOutput",
                0x16,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x20) {
        let v = (v & 0x60) >> 5;
        dm.push(("LiveViewAF".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Face Priority".to_string(),
            1 => "Wide Area".to_string(),
            2 => "Normal Area".to_string(),
            3 => "Subject Tracking".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LiveViewAF", 0x20, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD5100` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd5100(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x80) >> 7;
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x1;
        dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssist", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0xc0) >> 6;
        dm.push(("Beep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Low".to_string(),
            2 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Beep", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x20) >> 5;
        dm.push(("NoMemoryCard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release Locked".to_string(),
            1 => "Enable Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoMemoryCard", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x8) >> 3;
        dm.push(("ISODisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISODisplay", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x8) >> 3;
        dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FileNumberSequence",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x10) >> 4;
        dm.push(("RangeFinder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RangeFinder", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x80) >> 7;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReverseIndicators", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x40) >> 6;
        dm.push(("EVStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("EVStepSize", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0x40) >> 6;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xc) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AutoBracketSet".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Exposure".to_string(),
            1 => "WB Bracketing".to_string(),
            2 => "Active D-Lighting".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketSet", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0x38) >> 3;
        dm.push(("TimerFunctionButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Self-timer".to_string(),
            1 => "Release Mode".to_string(),
            2 => "Image Quality/Size".to_string(),
            3 => "ISO".to_string(),
            4 => "White Balance".to_string(),
            5 => "Active D-Lighting".to_string(),
            6 => "+ NEF (RAW)".to_string(),
            7 => "Auto Bracketing".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "TimerFunctionButton",
            0xd,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = (v & 0x38) >> 3;
        dm.push(("AELockButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE/AF Lock".to_string(),
            1 => "AE Lock Only".to_string(),
            2 => "AF Lock Only".to_string(),
            3 => "AE Lock (hold)".to_string(),
            4 => "AF-ON".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELockButton", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "CommandDialsReverseRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsReverseRotation",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x70) >> 4;
        dm.push(("MeteringTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "8 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "30 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MeteringTime", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = v & 0x3;
        dm.push(("RemoteOnDuration".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 min".to_string(),
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "20 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RemoteOnDuration", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0xc0) >> 6;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = v & 0xf;
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x13,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0xe0) >> 5;
        dm.push(("ImageReviewTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "8 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageReviewTime", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "LiveViewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "3 min".to_string(),
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            4 => "20 min".to_string(),
            5 => "30 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewMonitorOffTime",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0xe0) >> 5;
        dm.push(("PlaybackMenusTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "8 s".to_string(),
            1 => "12 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMenusTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = (v & 0xc0) >> 6;
        dm.push(("InternalFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Manual".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("InternalFlash", 0x17, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = v & 0x1f;
        dm.push(("ManualFlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val/3)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "ManualFlashOutput",
                0x17,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD5200` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd5200(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x80) >> 7;
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x10) >> 4;
        dm.push(("NumberOfFocusPoints".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "39 Points".to_string(),
            1 => "11 Points".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "NumberOfFocusPoints",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x1;
        dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssist", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0xc0) >> 6;
        dm.push(("Beep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Low".to_string(),
            2 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Beep", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x20) >> 5;
        dm.push(("NoMemoryCard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release Locked".to_string(),
            1 => "Enable Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoMemoryCard", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x8) >> 3;
        dm.push(("ISODisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISODisplay", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x8) >> 3;
        dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FileNumberSequence",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x4) >> 2;
        dm.push(("RangeFinder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RangeFinder", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "ReverseExposureCompDial".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ReverseExposureCompDial",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x8) >> 3;
        dm.push((
            "ReverseShutterSpeedAperture".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ReverseShutterSpeedAperture",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x80) >> 7;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReverseIndicators", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x40) >> 6;
        dm.push(("EVStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("EVStepSize", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0x40) >> 6;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xc) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AutoBracketSet".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Exposure".to_string(),
            1 => "WB Bracketing".to_string(),
            2 => "Active D-Lighting".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketSet", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = v & 0x1f;
        dm.push(("FunctionButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-ON".to_string(),
            16 => "+ NEF (RAW)".to_string(),
            18 => "Active D-Lighting".to_string(),
            25 => "Live View".to_string(),
            26 => "Image Quality".to_string(),
            27 => "ISO".to_string(),
            28 => "White Balance".to_string(),
            29 => "HDR".to_string(),
            30 => "Auto Bracketing".to_string(),
            31 => "AF-area Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FunctionButton", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = v & 0xf;
        dm.push(("AELockButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-ON".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELockButton", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0xe0) >> 5;
        dm.push(("StandbyTimer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "8 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "30 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("StandbyTimer", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = v & 0x3;
        dm.push(("RemoteOnDuration".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 min".to_string(),
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RemoteOnDuration", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0xc0) >> 6;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = v & 0xf;
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x13,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0xe0) >> 5;
        dm.push(("ImageReviewTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "4 s".to_string(),
            2 => "8 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageReviewTime", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "LiveViewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            4 => "20 min".to_string(),
            5 => "30 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewMonitorOffTime",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0xe0) >> 5;
        dm.push(("PlaybackMenusTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "8 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMenusTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = (v & 0xc0) >> 6;
        dm.push(("InternalFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "TTL".to_string(),
            1 => "Manual".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("InternalFlash", 0x17, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = v & 0x1f;
        dm.push(("ManualFlashOutput".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("2 ** (-$val/3)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "ManualFlashOutput",
                0x17,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD7000` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd7000(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x80) >> 7;
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x20) >> 5;
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus".to_string(),
            1 => "Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-SPrioritySelection",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x10) >> 4;
        dm.push(("NumberOfFocusPoints".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "39 Points".to_string(),
            1 => "11 Points".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "NumberOfFocusPoints",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x7;
        dm.push(("FocusTrackingLockOn".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "1 Short".to_string(),
            2 => "2".to_string(),
            3 => "3 Normal".to_string(),
            4 => "4".to_string(),
            5 => "5 Long".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusTrackingLockOn",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x8) >> 3;
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Wrap".to_string(),
            1 => "Wrap".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusPointWrap", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x6) >> 1;
        dm.push(("AFPointIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "On".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointIllumination",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x1;
        dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssist", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x40) >> 6;
        dm.push(("BatteryOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "MB-D11 First".to_string(),
            1 => "Camera Battery First".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("BatteryOrder", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x1c) >> 2;
        dm.push(("AF-OnForMB-D11".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE/AF Lock".to_string(),
            1 => "AE Lock Only".to_string(),
            2 => "AF Lock Only".to_string(),
            3 => "AE Lock (hold)".to_string(),
            4 => "AF-ON".to_string(),
            5 => "FV Lock".to_string(),
            6 => "Same as FUNC Button".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AF-OnForMB-D11", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = v & 0x3;
        dm.push(("MB-D11BatteryType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "LR6 (AA alkaline)".to_string(),
            1 => "Ni-MH (AA Ni-MH)".to_string(),
            2 => "FR6 (AA lithium)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MB-D11BatteryType", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0xc0) >> 6;
        dm.push(("BeepPitch".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Low".to_string(),
            2 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("BeepPitch", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x20) >> 5;
        dm.push(("NoMemoryCard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release Locked".to_string(),
            1 => "Enable Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoMemoryCard", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0xc) >> 2;
        dm.push(("ISODisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Show ISO/Easy ISO".to_string(),
            1 => "Show ISO Sensitivity".to_string(),
            3 => "Show Frame Count".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISODisplay", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = (v & 0x2) >> 1;
        dm.push(("GridDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GridDisplay", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x3) {
        let v = v & 0x1;
        dm.push(("ViewfinderWarning".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ViewfinderWarning", 0x3, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0xc0) >> 6;
        dm.push(("ShootingInfoDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            2 => "Manual (dark on light)".to_string(),
            3 => "Manual (light on dark)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoDisplay",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x20) >> 5;
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LCDIllumination", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x8) >> 3;
        dm.push(("FileNumberSequence".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FileNumberSequence",
            0x4,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x4) >> 2;
        dm.push(("ScreenTips".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ScreenTips", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = v & 0x3;
        dm.push(("BeepVolume".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "1".to_string(),
            2 => "2".to_string(),
            3 => "3".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("BeepVolume", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x80) >> 7;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReverseIndicators", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = v & 0x3;
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On Auto Reset".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x40) >> 6;
        dm.push(("ExposureControlStep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStep",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x10) >> 4;
        dm.push(("ISOSensitivityStep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ISOSensitivityStep",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "6 mm".to_string(),
            1 => "8 mm".to_string(),
            2 => "10 mm".to_string(),
            3 => "13 mm".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0x40) >> 6;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = v & 0x7;
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "CLModeShootingSpeed",
            0xa,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MaxContinuousRelease",
            0xb,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc) {
        let v = (v & 0xe0) >> 5;
        dm.push(("AutoBracketSet".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE & Flash".to_string(),
            1 => "AE Only".to_string(),
            2 => "Flash Only".to_string(),
            3 => "WB Bracketing".to_string(),
            4 => "Active D-Lighting".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketSet", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xc) {
        let v = (v & 0x10) >> 4;
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0,-,+".to_string(),
            1 => "-,0,+".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketOrder", 0xc, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0xf8) >> 3;
        dm.push(("FuncButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Grid Display".to_string(),
            1 => "FV Lock".to_string(),
            2 => "Flash Off".to_string(),
            3 => "Matrix Metering".to_string(),
            4 => "Center-weighted Metering".to_string(),
            5 => "Spot Metering".to_string(),
            6 => "My Menu Top".to_string(),
            7 => "+ NEF (RAW)".to_string(),
            8 => "Active D-Lighting".to_string(),
            9 => "Preview".to_string(),
            10 => "AE/AF Lock".to_string(),
            11 => "AE Lock Only".to_string(),
            12 => "AF Lock Only".to_string(),
            13 => "AE Lock (hold)".to_string(),
            14 => "Bracketing Burst".to_string(),
            15 => "Playback".to_string(),
            16 => "1EV Step Speed/Aperture".to_string(),
            17 => "Choose Non-CPU Lens".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Start Movie Recording".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FuncButton", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xe) {
        let v = (v & 0xf8) >> 3;
        dm.push(("PreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Grid Display".to_string(),
            1 => "FV Lock".to_string(),
            2 => "Flash Off".to_string(),
            3 => "Matrix Metering".to_string(),
            4 => "Center-weighted Metering".to_string(),
            5 => "Spot Metering".to_string(),
            6 => "My Menu Top".to_string(),
            7 => "+ NEF (RAW)".to_string(),
            8 => "Active D-Lighting".to_string(),
            9 => "Preview".to_string(),
            10 => "AE/AF Lock".to_string(),
            11 => "AE Lock Only".to_string(),
            12 => "AF Lock Only".to_string(),
            13 => "AE Lock (hold)".to_string(),
            14 => "Bracketing Burst".to_string(),
            15 => "Playback".to_string(),
            16 => "1EV Step Speed/Aperture".to_string(),
            17 => "Choose Non-CPU Lens".to_string(),
            18 => "Virtual Horizon".to_string(),
            19 => "Start Movie Recording".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PreviewButton", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = (v & 0x18) >> 3;
        dm.push(("OKButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Select Center Focus Point".to_string(),
            2 => "Highlight Active Focus Point".to_string(),
            3 => "Not Used".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("OKButton", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = (v & 0x38) >> 3;
        dm.push(("AELockButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE/AF Lock".to_string(),
            1 => "AE Lock Only".to_string(),
            2 => "AF Lock Only".to_string(),
            3 => "AE Lock (hold)".to_string(),
            4 => "AF-ON".to_string(),
            5 => "FV Lock".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELockButton", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "CommandDialsReverseRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsReverseRotation",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x60) >> 5;
        dm.push((
            "CommandDialsChangeMainSub".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (A mode only)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsChangeMainSub",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x4) >> 2;
        dm.push((
            "CommandDialsApertureSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Sub-command Dial".to_string(),
            1 => "Aperture Ring".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsApertureSetting",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x18) >> 3;
        dm.push((
            "CommandDialsMenuAndPlayback".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            2 => "On (Image Review Exclude)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsMenuAndPlayback",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = v & 0x1;
        dm.push((
            "ReleaseButtonToUseDial".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ReleaseButtonToUseDial",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0xf0) >> 4;
        dm.push(("MeteringTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "6 s".to_string(),
            2 => "8 s".to_string(),
            3 => "16 s".to_string(),
            4 => "30 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            8 => "30 min".to_string(),
            9 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MeteringTime", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = v & 0x3;
        dm.push(("RemoteOnDuration".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 min".to_string(),
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RemoteOnDuration", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0xc0) >> 6;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0x30) >> 4;
        dm.push(("SelfTimerInterval".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.5 s".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SelfTimerInterval",
            0x13,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = v & 0xf;
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x13,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0xe0) >> 5;
        dm.push(("ImageReviewTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageReviewTime", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "LiveViewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewMonitorOffTime",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0xe0) >> 5;
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0xf0) >> 4;
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/320 s (auto FP)".to_string(),
            1 => "1/250 s (auto FP)".to_string(),
            2 => "1/250 s".to_string(),
            3 => "1/200 s".to_string(),
            4 => "1/160 s".to_string(),
            5 => "1/125 s".to_string(),
            6 => "1/100 s".to_string(),
            7 => "1/80 s".to_string(),
            8 => "1/60 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashSyncSpeed", 0x16, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = v & 0xf;
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/60 s".to_string(),
            1 => "1/30 s".to_string(),
            2 => "1/15 s".to_string(),
            3 => "1/8 s".to_string(),
            4 => "1/4 s".to_string(),
            5 => "1/2 s".to_string(),
            6 => "1 s".to_string(),
            7 => "2 s".to_string(),
            8 => "4 s".to_string(),
            9 => "8 s".to_string(),
            10 => "15 s".to_string(),
            11 => "30 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashShutterSpeed",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = (v & 0xc0) >> 6;
        dm.push(("FlashControlBuiltin".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "TTL".to_string(),
                1 => "Manual".to_string(),
                2 => "Repeating Flash".to_string(),
                3 => "Commander Mode".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "FlashControlBuilt-in",
                0x17,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 1.0) {
        if let Some(v) = u8_at(data, 0x17) {
            let v = v & 0x1f;
            dm.push(("ManualFlashOutput".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("2 ** (-$val/3)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "
            return 'Full' if $val > 0.99;
            Image::ExifTool::Exif::PrintExposureTime($val);
        ",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "ManualFlashOutput",
                    0x17,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x18) {
            let v = (v & 0x70) >> 4;
            dm.push(("RepeatingFlashOutput".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("2 ** (-$val-2)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with(
                    "Image::ExifTool::Exif::PrintExposureTime($val)",
                    &cv,
                    &ctx,
                ) {
                    cv = x;
                }
                tags.push(mk(
                    "RepeatingFlashOutput",
                    0x18,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x18) {
            let v = v & 0xf;
            dm.push(("RepeatingFlashCount".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val < 10 ? $val + 1 : 5 * ($val - 7)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "RepeatingFlashCount",
                    0x18,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 2.0) {
        if let Some(v) = u8_at(data, 0x19) {
            let v = (v & 0xf0) >> 4;
            dm.push(("RepeatingFlashRate".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("$val < 10 ? $val + 1 : 10 * ($val - 8)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("\"$val Hz\"", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "RepeatingFlashRate",
                    0x19,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 3.0) {
        if let Some(v) = u8_at(data, 0x1a) {
            let v = v & 0x1f;
            dm.push((
                "CommanderInternalTTLCompBuiltin".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) =
                    conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx)
                {
                    cv = x;
                }
                tags.push(mk(
                    "CommanderInternalTTLCompBuiltin",
                    0x1a,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 3.0) {
        if let Some(v) = u8_at(data, 0x1b) {
            let v = v & 0x1f;
            dm.push((
                "CommanderInternalTTLCompGroupA".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) =
                    conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx)
                {
                    cv = x;
                }
                tags.push(mk(
                    "CommanderInternalTTLCompGroupA",
                    0x1b,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FlashControlBuiltin").is_some_and(|v| v == 3.0) {
        if let Some(v) = u8_at(data, 0x1c) {
            let v = v & 0x1f;
            dm.push((
                "CommanderInternalTTLCompGroupB".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("($val - 9) / 3", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) =
                    conv_expr::eval_with("$val ? sprintf(\"%+.1f\",$val) : 0", &cv, &ctx)
                {
                    cv = x;
                }
                tags.push(mk(
                    "CommanderInternalTTLCompGroupB",
                    0x1c,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x1e) {
        let v = (v & 0x80) >> 7;
        dm.push(("FlashWarning".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashWarning", 0x1e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1e) {
        let v = (v & 0x20) >> 5;
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x1e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x22) {
        let v = (v & 0x60) >> 5;
        dm.push(("LiveViewAFAreaMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Face-Priority".to_string(),
            1 => "NormalArea".to_string(),
            2 => "WideArea".to_string(),
            3 => "SubjectTracking".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewAFAreaMode",
            0x22,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x22) {
        let v = (v & 0x2) >> 1;
        dm.push(("LiveViewAFMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AF-C".to_string(),
            1 => "AF-F".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LiveViewAFMode", 0x22, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x23) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x23,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD4` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd4(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x3;
        dm.push(("CustomSettingsBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsBank",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Release + Focus".to_string(),
            2 => "Focus".to_string(),
            3 => "Focus + Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x20) >> 5;
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus".to_string(),
            1 => "Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-SPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x10) >> 4;
        dm.push(("AFPointSelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "51 Points".to_string(),
            1 => "11 Points".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPointSelection", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x7;
        dm.push(("FocusTrackingLockOn".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "1 (Short)".to_string(),
            2 => "2".to_string(),
            3 => "3 (Normal)".to_string(),
            4 => "4".to_string(),
            5 => "5 (Long)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusTrackingLockOn",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x80) >> 7;
        dm.push(("AFActivation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Shutter/AF-On".to_string(),
            1 => "AF-On Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFActivation", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x8) >> 3;
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Wrap".to_string(),
            1 => "Wrap".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusPointWrap", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x40) >> 6;
        dm.push(("Pitch".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "High".to_string(),
            1 => "Low".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Pitch", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x20) >> 5;
        dm.push(("NoMemoryCard".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release Locked".to_string(),
            1 => "Enable Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoMemoryCard", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x2) >> 1;
        dm.push(("GridDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GridDisplay", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0xc0) >> 6;
        dm.push(("ShootingInfoDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "Auto".to_string(),
            2 => "Manual (dark on light)".to_string(),
            3 => "Manual (light on dark)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoDisplay",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x20) >> 5;
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LCDIllumination", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x4) >> 2;
        dm.push(("ScreenTips".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ScreenTips", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = v & 0x3;
        dm.push(("Beep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Low".to_string(),
            2 => "Medium".to_string(),
            3 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Beep", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x80) >> 7;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReverseIndicators", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x40) >> 6;
        dm.push(("RearDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "ISO".to_string(),
            1 => "Exposures Remaining".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RearDisplay", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x20) >> 5;
        dm.push(("ViewfinderDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Frame Count".to_string(),
            1 => "Exposures Remaining".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ViewfinderDisplay", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x18) >> 3;
        dm.push((
            "CommandDialsReverseRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Shutter Speed & Aperture".to_string(),
            2 => "Exposure Compensation".to_string(),
            3 => "Exposure Compensation, Shutter Speed & Aperture".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsReverseRotation",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = v & 0x3;
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (auto reset)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xc0) >> 6;
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x30) >> 4;
        dm.push(("ISOStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOStepSize", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xc) >> 2;
        dm.push(("ExposureCompStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureCompStepSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "8 mm".to_string(),
            1 => "12 mm".to_string(),
            2 => "15 mm".to_string(),
            3 => "20 mm".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0xc0) >> 6;
        dm.push((
            "MultiSelectorShootMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Select Center Focus Point (Reset)".to_string(),
            2 => "Preset Focus Point (Pre)".to_string(),
            3 => "Not Used (None)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorShootMode",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0x30) >> 4;
        dm.push((
            "MultiSelectorPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Thumbnail On/Off".to_string(),
            1 => "View Histograms".to_string(),
            2 => "Zoom On/Off".to_string(),
            3 => "Choose Folder".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorPlaybackMode",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = v & 0x1;
        dm.push(("MultiSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Do Nothing".to_string(),
            1 => "Reset Meter-off Delay".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MultiSelector", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0xc0) >> 6;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0xb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0x10) >> 4;
        dm.push(("CHModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "10 fps".to_string(),
            1 => "11 fps".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CHModeShootingSpeed",
            0xb,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = v & 0xf;
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "CLModeShootingSpeed",
            0xb,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MaxContinuousRelease",
            0xc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0xe0) >> 5;
        dm.push(("AutoBracketSet".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AE & Flash".to_string(),
            1 => "AE Only".to_string(),
            2 => "Flash Only".to_string(),
            3 => "WB Bracketing".to_string(),
            4 => "Active D-Lighting".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketSet", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0x10) >> 4;
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0,-,+".to_string(),
            1 => "-,0,+".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketOrder", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0xc) >> 2;
        dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Flash/Speed".to_string(),
            1 => "Flash/Speed/Aperture".to_string(),
            2 => "Flash/Aperture".to_string(),
            3 => "Flash Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketModeM", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xe) {
        let v = (v & 0xf8) >> 3;
        dm.push(("FuncButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            18 => "My Menu".to_string(),
            20 => "Grid Display".to_string(),
            21 => "Disable Synchronized Release".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FuncButton", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xe) {
        let v = v & 0x7;
        dm.push(("FuncButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (FX/DX/5:4)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            6 => "Shooting Bank Menu".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FuncButtonPlusDials",
            0xe,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = (v & 0xf8) >> 3;
        dm.push(("PreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            18 => "My Menu".to_string(),
            20 => "Grid Display".to_string(),
            21 => "Disable Synchronized Release".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PreviewButton", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = v & 0x7;
        dm.push((
            "PreviewButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (FX/DX/5:4)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            6 => "Shooting Bank Menu".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PreviewButtonPlusDials",
            0xf,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = v & 0x7;
        dm.push(("AssignBktButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto Bracketing".to_string(),
            1 => "Multiple Exposure".to_string(),
            2 => "HDR (high dynamic range)".to_string(),
            3 => "None".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AssignBktButton", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CommandDialsChangeMainSub".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Autofocus Off, Exposure Off".to_string(),
            1 => "Autofocus Off, Exposure On".to_string(),
            2 => "Autofocus Off, Exposure On (Mode A)".to_string(),
            4 => "Autofocus On, Exposure Off".to_string(),
            5 => "Autofocus On, Exposure On".to_string(),
            6 => "Autofocus On, Exposure On (Mode A)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsChangeMainSub",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x18) >> 3;
        dm.push((
            "CommandDialsMenuAndPlayback".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            2 => "On (Image Review Excluded)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsMenuAndPlayback",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x4) >> 2;
        dm.push((
            "CommandDialsApertureSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Sub-command Dial".to_string(),
            1 => "Aperture Ring".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsApertureSetting",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = v & 0x1;
        dm.push((
            "ReleaseButtonToUseDial".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ReleaseButtonToUseDial",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0xf0) >> 4;
        dm.push(("StandbyTimer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "6 s".to_string(),
            3 => "10 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            9 => "30 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("StandbyTimer", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0xc0) >> 6;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = v & 0xf;
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x14,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0x30) >> 4;
        dm.push(("SelfTimerShotInterval".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.5 s".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SelfTimerShotInterval",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "ImageReviewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            4 => "1 min".to_string(),
            5 => "5 min".to_string(),
            6 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageReviewMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "LiveViewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "5 min".to_string(),
            1 => "10 min".to_string(),
            2 => "15 min".to_string(),
            3 => "20 min".to_string(),
            4 => "30 min".to_string(),
            5 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0xe0) >> 5;
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = (v & 0xf0) >> 4;
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "1/250 s (auto FP)".to_string(),
            2 => "1/250 s".to_string(),
            3 => "1/200 s".to_string(),
            4 => "1/160 s".to_string(),
            5 => "1/125 s".to_string(),
            6 => "1/100 s".to_string(),
            7 => "1/80 s".to_string(),
            8 => "1/60 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashSyncSpeed", 0x17, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = v & 0xf;
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/60 s".to_string(),
            1 => "1/30 s".to_string(),
            2 => "1/15 s".to_string(),
            3 => "1/8 s".to_string(),
            4 => "1/4 s".to_string(),
            5 => "1/2 s".to_string(),
            6 => "1 s".to_string(),
            7 => "2 s".to_string(),
            8 => "4 s".to_string(),
            9 => "8 s".to_string(),
            10 => "15 s".to_string(),
            11 => "30 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashShutterSpeed",
            0x17,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = (v & 0x20) >> 5;
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x1f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x24) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x24,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x25) {
        let v = v & 0x1;
        dm.push(("PlaybackZoom".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Use Separate Zoom Buttons".to_string(),
            1 => "Use Either Zoom Button with Command Dial".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PlaybackZoom", 0x25, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x80) >> 7;
        dm.push(("ShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ShutterSpeedLock", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x40) >> 6;
        dm.push(("ApertureLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ApertureLock", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x30) >> 4;
        dm.push(("MovieShutterButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Take Photo".to_string(),
            1 => "Record Movies".to_string(),
            2 => "Live Frame Grab".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieShutterButton",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x4) >> 2;
        dm.push(("FlashExposureCompArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Entire frame".to_string(),
            1 => "Background only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashExposureCompArea",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x29) {
        let v = (v & 0x70) >> 4;
        dm.push(("MovieFunctionButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Power Aperture (open)".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieFunctionButton",
            0x29,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x29) {
        let v = v & 0x7;
        dm.push(("MoviePreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            2 => "Power Aperture (open)".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MoviePreviewButton",
            0x29,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2a) {
        let v = (v & 0x60) >> 5;
        dm.push(("VerticalMultiSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Same as Multi-Selector with Info(U/D) & Playback(R/L)".to_string(),
            1 => "Same as Multi-Selector with Info(R/L) & Playback(U/D)".to_string(),
            2 => "Focus Point Selection".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VerticalMultiSelector",
            0x2a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2a) {
        let v = v & 0x1f;
        dm.push(("VerticalFuncButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            18 => "My Menu".to_string(),
            20 => "Grid Display".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VerticalFuncButton",
            0x2a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "VerticalFuncButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (FX/DX/5:4)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            6 => "Shooting Bank Menu".to_string(),
            7 => "ISO Sensitivity".to_string(),
            8 => "Exposure Mode".to_string(),
            9 => "Exposure Compensation".to_string(),
            10 => "Metering".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VerticalFuncButtonPlusDials",
            0x2b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b) {
        let v = v & 0x7;
        dm.push((
            "AssignMovieRecordButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (FX/DX/5:4)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "ISO Sensitivity".to_string(),
            4 => "Shooting Bank Menu".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieRecordButton",
            0x2b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2e) {
        let v = (v & 0x80) >> 7;
        dm.push(("DynamicAreaAFDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DynamicAreaAFDisplay",
            0x2e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2e) {
        let v = (v & 0x60) >> 5;
        dm.push(("AFPointIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On in Continuous Shooting Modes".to_string(),
            2 => "On During Manual Focusing".to_string(),
            3 => "On in Continuous Shooting and Manual Focusing".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointIllumination",
            0x2e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2e) {
        let v = (v & 0x18) >> 3;
        dm.push(("StoreByOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Focus Point".to_string(),
            2 => "Focus Point and AF-area mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "StoreByOrientation",
            0x2e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2e) {
        let v = (v & 0x4) >> 2;
        dm.push((
            "GroupAreaAFIllumination".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Squares".to_string(),
            1 => "Dots".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "GroupAreaAFIllumination",
            0x2e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2e) {
        let v = v & 0x3;
        dm.push(("AFPointBrightness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Low".to_string(),
            1 => "Normal".to_string(),
            2 => "High".to_string(),
            3 => "Extra High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointBrightness",
            0x2e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x70) >> 4;
        dm.push(("AFOnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "AF On".to_string(),
            1 => "AE/AF Lock".to_string(),
            2 => "AE Lock Only".to_string(),
            3 => "AE Lock (reset on release)".to_string(),
            4 => "AE Lock (hold)".to_string(),
            5 => "AF Lock Only".to_string(),
            6 => "None".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFOnButton", 0x2f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = v & 0x7;
        dm.push(("VerticalAFOnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Same as AF On".to_string(),
            1 => "AF On".to_string(),
            2 => "AE/AF Lock".to_string(),
            3 => "AE Lock Only".to_string(),
            4 => "AE Lock (reset on release)".to_string(),
            5 => "AE Lock (hold)".to_string(),
            6 => "AF Lock Only".to_string(),
            7 => "None".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VerticalAFOnButton",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = (v & 0x80) >> 7;
        dm.push(("SubSelectorAssignment".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus Point Selection".to_string(),
            1 => "Same As Multi-selector".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubSelectorAssignment",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = v & 0x7;
        dm.push((
            "MovieSubSelectorAssignment".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Index Marking".to_string(),
            2 => "AE/AF Lock".to_string(),
            3 => "AE Lock Only".to_string(),
            4 => "AE Lock (hold)".to_string(),
            5 => "AF Lock Only".to_string(),
            6 => "View Photo Shooting Info".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieSubSelectorAssignment",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x31) {
        let v = (v & 0xf8) >> 3;
        dm.push(("SubSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            18 => "My Menu".to_string(),
            19 => "Reset".to_string(),
            20 => "Grid Display".to_string(),
            21 => "Disable Synchronized Release".to_string(),
            22 => "Remote Release Only".to_string(),
            23 => "Preview".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SubSelector", 0x31, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x31) {
        let v = v & 0x7;
        dm.push(("SubSelectorPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (FX/DX/5:4)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            6 => "Shooting Bank Menu".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubSelectorPlusDials",
            0x31,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_27.is_match(model) {
        if let Some(v) = u8_at(data, 0x32) {
            let v = (v & 0x80) >> 7;
            dm.push(("MatrixMetering".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Face Detection On".to_string(),
                1 => "Face Detection Off".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("MatrixMetering", 0x32, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    if MODEL_RE_27.is_match(model) {
        if let Some(v) = u8_at(data, 0x32) {
            let v = (v & 0x30) >> 4;
            dm.push(("LiveViewButtonOptions".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Enable".to_string(),
                1 => "Enable (standby time active)".to_string(),
                2 => "Disable".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "LiveViewButtonOptions",
                0x32,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_27.is_match(model) {
        if let Some(v) = u8_at(data, 0x32) {
            let v = v & 0x3;
            dm.push(("AFModeRestrictions".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "AF-C".to_string(),
                2 => "AF-S".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "AFModeRestrictions",
                0x32,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_27.is_match(model) {
        if let Some(v) = u8_at(data, 0x33) {
            let v = (v & 0x7e) >> 1;
            dm.push((
                "LimitAFAreaModeSelection".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Auto-area".to_string(),
                1 => "Group-area".to_string(),
                2 => "3D-tracking".to_string(),
                3 => "Dynamic area (51 points)".to_string(),
                4 => "Dynamic area (21 points)".to_string(),
                5 => "Dynamic area (9 points)".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "LimitAFAreaModeSelection",
                0x33,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x34) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "MovieFunctionButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieFunctionButtonPlusDials",
            0x34,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x34) {
        let v = v & 0x1;
        dm.push((
            "MoviePreviewButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MoviePreviewButtonPlusDials",
            0x34,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x35) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "MovieSubSelectorAssignmentPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieSubSelectorAssignmentPlusDials",
            0x35,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if MODEL_RE_27.is_match(model) {
        if let Some(v) = u8_at(data, 0x36) {
            let v = v & 0x1f;
            dm.push(("AssignRemoteFnButton".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "None".to_string(),
                1 => "Preview".to_string(),
                2 => "FV Lock".to_string(),
                3 => "AE/AF Lock".to_string(),
                4 => "AE Lock Only".to_string(),
                5 => "AE Lock (reset on release)".to_string(),
                7 => "AF Lock Only".to_string(),
                8 => "AF-On".to_string(),
                16 => "+NEF(RAW)".to_string(),
                25 => "Live View".to_string(),
                26 => "Flash Disable/Enable".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "AssignRemoteFnButton",
                0x36,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_27.is_match(model) {
        if let Some(v) = u8_at(data, 0x37) {
            let v = v & 0x3f;
            dm.push((
                "LensFocusFunctionButtons".to_string(),
                Conv::Num(f64::from(v)),
            ));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                3 => "AE/AF Lock".to_string(),
                4 => "AE Lock Only".to_string(),
                7 => "AF Lock Only".to_string(),
                21 => "Disable Synchronized Release".to_string(),
                22 => "Remote Release Only".to_string(),
                24 => "Preset focus Point".to_string(),
                26 => "Flash Disable/Enable".to_string(),
                32 => "AF-Area Mode:  Single-point AF".to_string(),
                33 => "AF-Area Mode: Dynamic-area AF (9 points)".to_string(),
                34 => "AF-Area Mode: Dynamic-area AF (21 points)".to_string(),
                35 => "AF-Area Mode: Dynamic-area AF (51 points)".to_string(),
                36 => "AF-Area Mode: Group-area AF".to_string(),
                37 => "AF-Area Mode: Auto area AF".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "LensFocusFunctionButtons",
                0x37,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD610` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd610(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x80) >> 7;
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x20) >> 5;
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Focus".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-SPrioritySelection",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = (v & 0x10) >> 4;
        dm.push(("NumberOfFocusPoints".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "39 Points".to_string(),
            1 => "11 Points".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "NumberOfFocusPoints",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x7;
        dm.push(("FocusTrackingLockOn".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "1 Short".to_string(),
            2 => "2".to_string(),
            3 => "3 Normal".to_string(),
            4 => "4".to_string(),
            5 => "5 Long".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FocusTrackingLockOn",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x8) >> 3;
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Wrap".to_string(),
            1 => "Wrap".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusPointWrap", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x6) >> 1;
        dm.push(("AFPointIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "On".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointIllumination",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x1;
        dm.push(("AFAssist".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFAssist", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = v & 0x3;
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On Auto Reset".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x40) >> 6;
        dm.push(("ExposureControlStep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStep",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x10) >> 4;
        dm.push(("ISOSensitivityStep".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ISOSensitivityStep",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "8 mm".to_string(),
            1 => "12 mm".to_string(),
            2 => "15 mm".to_string(),
            3 => "20 mm".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x11,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0xf0) >> 4;
        dm.push(("StandbyTimer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "6 s".to_string(),
            2 => "10 s".to_string(),
            3 => "30 s".to_string(),
            4 => "1 min".to_string(),
            5 => "5 min".to_string(),
            6 => "10 min".to_string(),
            7 => "30 min".to_string(),
            8 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("StandbyTimer", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = v & 0x3;
        dm.push(("RemoteOnDuration".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1 min".to_string(),
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "20 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("RemoteOnDuration", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0xc0) >> 6;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0x30) >> 4;
        dm.push(("SelfTimerShotInterval".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.5 s".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SelfTimerShotInterval",
            0x13,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = v & 0xf;
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x13,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "ImageReviewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            4 => "1 min".to_string(),
            5 => "5 min".to_string(),
            6 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageReviewMonitorOffTime",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "LiveViewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "5 min".to_string(),
            1 => "10 min".to_string(),
            2 => "15 min".to_string(),
            3 => "20 min".to_string(),
            4 => "30 min".to_string(),
            5 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewMonitorOffTime",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0xe0) >> 5;
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x23) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x23,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD5` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd5(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x3;
        dm.push(("CustomSettingsBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsBank",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Release + Focus".to_string(),
            2 => "Focus".to_string(),
            3 => "Focus + Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x20) >> 5;
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus".to_string(),
            1 => "Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-SPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x10) >> 4;
        dm.push(("NumberOfFocusPoints".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "55 Points".to_string(),
            1 => "15 Points".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "NumberOfFocusPoints",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x8) >> 3;
        dm.push((
            "Three-DTrackingFaceDetection".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "Three-DTrackingFaceDetection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x7;
        dm.push(("BlockShotAFResponse".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "BlockShotAFResponse",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x8) >> 3;
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Wrap".to_string(),
            1 => "Wrap".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusPointWrap", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x6) >> 1;
        dm.push(("AFPointBrightness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "On".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPointBrightness", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x8) >> 3;
        dm.push(("ISODisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Show ISO Sensitivity".to_string(),
            1 => "Show Frame Count".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISODisplay", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x2) >> 1;
        dm.push(("GridDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GridDisplay", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x20) >> 5;
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LCDIllumination", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x8) >> 3;
        dm.push((
            "ElectronicFront-CurtainShutter".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ElectronicFront-CurtainShutter",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x80) >> 7;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReverseIndicators", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x18) >> 3;
        dm.push((
            "CommandDialsReverseRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Shutter Speed & Aperture".to_string(),
            2 => "Exposure Compensation".to_string(),
            3 => "Exposure Compensation, Shutter Speed & Aperture".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsReverseRotation",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = v & 0x3;
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (auto reset)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xc0) >> 6;
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x30) >> 4;
        dm.push(("ISOStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOStepSize", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xc) >> 2;
        dm.push(("ExposureCompStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureCompStepSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "8 mm".to_string(),
            1 => "12 mm".to_string(),
            2 => "15 mm".to_string(),
            3 => "20 mm".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "MultiSelectorShootMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Select Center Focus Point (Reset)".to_string(),
            1 => "Zoom On/Off".to_string(),
            2 => "Preset Focus Point (Pre)".to_string(),
            4 => "Not Used (None)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorShootMode",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = v & 0x1;
        dm.push(("MultiSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Do Nothing".to_string(),
            1 => "Reset Meter-off Delay".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MultiSelector", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0xc0) >> 6;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0xb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = v & 0xf;
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "CLModeShootingSpeed",
            0xb,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MaxContinuousRelease",
            0xc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0x10) >> 4;
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0,-,+".to_string(),
            1 => "-,0,+".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketOrder", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0xc) >> 2;
        dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Flash/Speed".to_string(),
            1 => "Flash/Speed/Aperture".to_string(),
            2 => "Flash/Aperture".to_string(),
            3 => "Flash Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketModeM", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xe) {
        let v = v & 0x3f;
        dm.push(("Func1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            21 => "Disable Synchronized Release".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            58 => "AF-Area Mode + AF-On (Group Area AF - HL)".to_string(),
            59 => "AF-Area Mode + AF-On (Group Area AF - VL)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Func1Button", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = v & 0x3f;
        dm.push(("PreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            21 => "Disable Synchronized Release".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            58 => "AF-Area Mode + AF-On (Group Area AF - HL)".to_string(),
            59 => "AF-Area Mode + AF-On (Group Area AF - VL)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PreviewButton", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = v & 0x7;
        dm.push(("AssignBktButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto Bracketing".to_string(),
            1 => "Multiple Exposure".to_string(),
            2 => "HDR (high dynamic range)".to_string(),
            3 => "None".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AssignBktButton", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CommandDialsChangeMainSub".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Autofocus Off, Exposure Off".to_string(),
            1 => "Autofocus Off, Exposure On".to_string(),
            2 => "Autofocus Off, Exposure On (Mode A)".to_string(),
            4 => "Autofocus On, Exposure Off".to_string(),
            5 => "Autofocus On, Exposure On".to_string(),
            6 => "Autofocus On, Exposure On (Mode A)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsChangeMainSub",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x18) >> 3;
        dm.push((
            "CommandDialsMenuAndPlayback".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            2 => "On (Image Review Excluded)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsMenuAndPlayback",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x4) >> 2;
        dm.push((
            "CommandDialsApertureSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Sub-command Dial".to_string(),
            1 => "Aperture Ring".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsApertureSetting",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = v & 0x1;
        dm.push((
            "ReleaseButtonToUseDial".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ReleaseButtonToUseDial",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0xf0) >> 4;
        dm.push(("StandbyTimer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "6 s".to_string(),
            3 => "10 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            9 => "30 min".to_string(),
            10 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("StandbyTimer", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0xc0) >> 6;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0x30) >> 4;
        dm.push(("SelfTimerShotInterval".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.5 s".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SelfTimerShotInterval",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = v & 0xf;
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x14,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "ImageReviewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageReviewMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "LiveViewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            4 => "20 min".to_string(),
            5 => "30 min".to_string(),
            6 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0xe0) >> 5;
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            2 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            2 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = (v & 0xf0) >> 4;
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            2 => "1/250 s (auto FP)".to_string(),
            3 => "1/250 s".to_string(),
            5 => "1/200 s".to_string(),
            6 => "1/160 s".to_string(),
            7 => "1/125 s".to_string(),
            8 => "1/100 s".to_string(),
            9 => "1/80 s".to_string(),
            10 => "1/60 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashSyncSpeed", 0x17, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = v & 0xf;
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/60 s".to_string(),
            1 => "1/30 s".to_string(),
            2 => "1/15 s".to_string(),
            3 => "1/8 s".to_string(),
            4 => "1/4 s".to_string(),
            5 => "1/2 s".to_string(),
            6 => "1 s".to_string(),
            7 => "2 s".to_string(),
            8 => "4 s".to_string(),
            9 => "8 s".to_string(),
            10 => "15 s".to_string(),
            11 => "30 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashShutterSpeed",
            0x17,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = (v & 0x20) >> 5;
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x1f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x24) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x24,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x25) {
        let v = (v & 0xc0) >> 6;
        dm.push(("MultiSelectorLiveView".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Reset".to_string(),
            1 => "Zoom".to_string(),
            3 => "Not Used".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorLiveView",
            0x25,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x80) >> 7;
        dm.push(("ShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ShutterSpeedLock", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x40) >> 6;
        dm.push(("ApertureLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ApertureLock", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x10) >> 4;
        dm.push(("MovieShutterButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Take Photo".to_string(),
            1 => "Record Movies".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieShutterButton",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x4) >> 2;
        dm.push(("FlashExposureCompArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Entire Frame".to_string(),
            1 => "Background Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashExposureCompArea",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "AutoFlashISOSensitivity".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Subject and Background".to_string(),
            1 => "Subject Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoFlashISOSensitivity",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x29) {
        let v = (v & 0xf0) >> 4;
        dm.push(("MovieFunc1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            2 => "Power Aperture (close)".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            11 => "Exposure Compensation -".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MovieFunc1Button", 0x29, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x29) {
        let v = v & 0xf;
        dm.push(("MoviePreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Power Aperture (open)".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            10 => "Exposure Compensation +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MoviePreviewButton",
            0x29,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2a) {
        let v = v & 0xf;
        dm.push(("Func1ButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "Func1ButtonPlusDials",
            0x2a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b) {
        let v = v & 0xf;
        dm.push((
            "PreviewButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PreviewButtonPlusDials",
            0x2b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2d) {
        let v = v & 0xf;
        dm.push((
            "AssignMovieRecordButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            11 => "Exposure Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieRecordButtonPlusDials",
            0x2d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2e) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptHighlightWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptHighlightWeighted",
                0x2e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x80) >> 7;
        dm.push(("DynamicAreaAFDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DynamicAreaAFDisplay",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x40) >> 6;
        dm.push(("AFPointIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On During Manual Focusing".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointIllumination",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x18) >> 3;
        dm.push(("StoreByOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Focus Point".to_string(),
            2 => "Focus Point and AF-area Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "StoreByOrientation",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = (v & 0x80) >> 7;
        dm.push(("MatrixMetering".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Face Detection On".to_string(),
            1 => "Face Detection Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MatrixMetering", 0x30, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = (v & 0x30) >> 4;
        dm.push(("LiveViewButtonOptions".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Enable".to_string(),
            1 => "Enable (Standby Timer Active)".to_string(),
            2 => "Disable".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewButtonOptions",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = v & 0x3;
        dm.push(("AFModeRestrictions".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Restrictions".to_string(),
            1 => "AF-C".to_string(),
            2 => "AF-S".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFModeRestrictions",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x31) {
        let v = (v & 0x7e) >> 1;
        dm.push((
            "LimitAFAreaModeSelection".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto-area".to_string(),
            1 => "Group-area".to_string(),
            2 => "3D-tracking".to_string(),
            3 => "Dynamic area (153 points)".to_string(),
            4 => "Dynamic area (72 points)".to_string(),
            5 => "Dynamic area (25 points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LimitAFAreaModeSelection",
            0x31,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x34) {
        let v = v & 0x3f;
        dm.push((
            "LensFocusFunctionButtons".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            24 => "Preset Focus Point".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LensFocusFunctionButtons",
            0x34,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x42) {
        let v = v & 0xff;
        dm.push(("VerticalMultiSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Same as Multi-Selector with Info(U/D) & Playback(R/L)".to_string(),
            8 => "Same as Multi-Selector with Info(R/L) & Playback(U/D)".to_string(),
            128 => "Focus Point Selection".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VerticalMultiSelector",
            0x42,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x43) {
        let v = v & 0x3f;
        dm.push(("VerticalFuncButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            18 => "Reset Focus Point".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            22 => "Remote Release Only".to_string(),
            23 => "Preset Focus Point".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            54 => "Highlight Active Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VerticalFuncButton",
            0x43,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x44) {
        let v = v & 0xf;
        dm.push(("VerticalFuncPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            10 => "ISO Sensitivity".to_string(),
            11 => "Exposure Mode".to_string(),
            12 => "Exposure Compensation".to_string(),
            13 => "Metering".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VerticalFuncPlusDials",
            0x44,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x46) {
        let v = v & 0x3f;
        dm.push(("AF-OnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AF-OnButton", 0x46, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x47) {
        let v = (v & 0x80) >> 7;
        dm.push(("SubSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus Point Selection".to_string(),
            1 => "Same as MultiSelector".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SubSelector", 0x47, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x48) {
        let v = v & 0x3f;
        dm.push(("SubSelectorCenter".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            18 => "Reset Focus Point".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            22 => "Remote Release Only".to_string(),
            23 => "Preset Focus Point".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            54 => "Highlight Active Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubSelectorCenter",
            0x48,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x49) {
        let v = v & 0xf;
        dm.push(("SubSelectorPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubSelectorPlusDials",
            0x49,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4a) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "AssignMovieSubselector".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            5 => "AE/AF Lock".to_string(),
            6 => "AE Lock (Only)".to_string(),
            7 => "AE Lock (Hold)".to_string(),
            8 => "AF Lock (Only)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieSubselector",
            0x4a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4b) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "AssignMovieFunc1ButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieFunc1ButtonPlusDials",
            0x4b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4b) {
        let v = v & 0x1;
        dm.push((
            "AssignMoviePreviewButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (DX/1.3x)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMoviePreviewButtonPlusDials",
            0x4b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4c) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "AssignMovieSubselectorPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieSubselectorPlusDials",
            0x4c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4d) {
        let v = (v & 0x80) >> 7;
        dm.push(("SyncReleaseMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Sync".to_string(),
            1 => "Sync".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SyncReleaseMode", 0x4d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "Three-DTrackingWatchArea".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Wide".to_string(),
            1 => "Normal".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "Three-DTrackingWatchArea",
            0x4e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = (v & 0x60) >> 5;
        dm.push(("SubjectMotion".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Steady".to_string(),
            1 => "Middle".to_string(),
            2 => "Erratic".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SubjectMotion", 0x4e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = (v & 0x8) >> 3;
        dm.push(("AFActivation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Shutter/AF-On".to_string(),
            1 => "AF-On Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFActivation", 0x4e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = v & 0x3;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On (Half Press)".to_string(),
            2 => "On (Burst Mode)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x4e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4f) {
        let v = v & 0x7f;
        dm.push(("VerticalAFOnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            100 => "Same as AF-On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VerticalAFOnButton",
            0x4f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x50) {
        let v = v & 0x3f;
        dm.push(("Func2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            21 => "Disable Synchronized Release".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Func2Button", 0x50, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x51) {
        let v = v & 0xf;
        dm.push(("Func2ButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "Func2ButtonPlusDials",
            0x51,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x52) {
        let v = (v & 0x70) >> 4;
        dm.push((
            "AssignMovieFunc2Button".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieFunc2Button",
            0x52,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x53) {
        let v = v & 0x3;
        dm.push(("Func3Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Voice Memo".to_string(),
            2 => "Rating".to_string(),
            3 => "Connect To Network".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Func3Button", 0x53, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::NikonCustom::SettingsD500` -- FORMAT int8u, FIRST_ENTRY 0.
fn nikoncustom_settingsd500(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "NikonCustom";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x3;
        dm.push(("CustomSettingsBank".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "A".to_string(),
            1 => "B".to_string(),
            2 => "C".to_string(),
            3 => "D".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CustomSettingsBank",
            0x0,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xc0) >> 6;
        dm.push(("AF-CPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Release".to_string(),
            1 => "Release + Focus".to_string(),
            2 => "Focus".to_string(),
            3 => "Focus + Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-CPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x20) >> 5;
        dm.push(("AF-SPrioritySelection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus".to_string(),
            1 => "Release".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AF-SPrioritySelection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x10) >> 4;
        dm.push(("NumberOfFocusPoints".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "55 Points".to_string(),
            1 => "15 Points".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "NumberOfFocusPoints",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x8) >> 3;
        dm.push((
            "Three-DTrackingFaceDetection".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "Three-DTrackingFaceDetection",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x7;
        dm.push(("BlockShotAFResponse".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "BlockShotAFResponse",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x8) >> 3;
        dm.push(("FocusPointWrap".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Wrap".to_string(),
            1 => "Wrap".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FocusPointWrap", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x6) >> 1;
        dm.push(("AFPointBrightness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "On".to_string(),
            2 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFPointBrightness", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x8) >> 3;
        dm.push(("ISODisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Show ISO Sensitivity".to_string(),
            1 => "Show Frame Count".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISODisplay", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4) {
        let v = (v & 0x2) >> 1;
        dm.push(("GridDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GridDisplay", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x20) >> 5;
        dm.push(("LCDIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("LCDIllumination", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x5) {
        let v = (v & 0x8) >> 3;
        dm.push((
            "ElectronicFront-CurtainShutter".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ElectronicFront-CurtainShutter",
            0x5,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x80) >> 7;
        dm.push(("ReverseIndicators".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "+ 0 -".to_string(),
            1 => "- 0 +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ReverseIndicators", 0x6, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = (v & 0x18) >> 3;
        dm.push((
            "CommandDialsReverseRotation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Shutter Speed & Aperture".to_string(),
            2 => "Exposure Compensation".to_string(),
            3 => "Exposure Compensation, Shutter Speed & Aperture".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsReverseRotation",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6) {
        let v = v & 0x3;
        dm.push((
            "EasyExposureCompensation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "On (auto reset)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "EasyExposureCompensation",
            0x6,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xc0) >> 6;
        dm.push((
            "ExposureControlStepSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureControlStepSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0x30) >> 4;
        dm.push(("ISOStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ISOStepSize", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x7) {
        let v = (v & 0xc) >> 2;
        dm.push(("ExposureCompStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/3 EV".to_string(),
            1 => "1/2 EV".to_string(),
            2 => "1 EV".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExposureCompStepSize",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CenterWeightedAreaSize".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "6 mm".to_string(),
            1 => "8 mm".to_string(),
            2 => "10 mm".to_string(),
            3 => "13 mm".to_string(),
            4 => "Average".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CenterWeightedAreaSize",
            0x8,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x8) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptMatrixMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptMatrixMetering",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "FineTuneOptCenterWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptCenterWeighted",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x9) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptSpotMetering".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptSpotMetering",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "MultiSelectorShootMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Select Center Focus Point (Reset)".to_string(),
            2 => "Preset Focus Point (Pre)".to_string(),
            3 => "Highlight Active Focus Point".to_string(),
            4 => "Not Used (None)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorShootMode",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = (v & 0xc) >> 2;
        dm.push((
            "MultiSelectorPlaybackMode".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Thumbnail On/Off".to_string(),
            1 => "View Histograms".to_string(),
            2 => "Zoom On/Off".to_string(),
            3 => "Choose Folder".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorPlaybackMode",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xa) {
        let v = v & 0x1;
        dm.push(("MultiSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Do Nothing".to_string(),
            1 => "Reset Meter-off Delay".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MultiSelector", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = (v & 0xc0) >> 6;
        dm.push(("ExposureDelayMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureDelayMode", 0xb, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xb) {
        let v = v & 0xf;
        dm.push(("CLModeShootingSpeed".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val fps\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "CLModeShootingSpeed",
            0xb,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xc) {
        dm.push(("MaxContinuousRelease".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MaxContinuousRelease",
            0xc,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0x10) >> 4;
        dm.push(("AutoBracketOrder".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0,-,+".to_string(),
            1 => "-,0,+".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketOrder", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xd) {
        let v = (v & 0xc) >> 2;
        dm.push(("AutoBracketModeM".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Flash/Speed".to_string(),
            1 => "Flash/Speed/Aperture".to_string(),
            2 => "Flash/Aperture".to_string(),
            3 => "Flash Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AutoBracketModeM", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xe) {
        let v = v & 0x3f;
        dm.push(("Func1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Func1Button", 0xe, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = v & 0x3f;
        dm.push(("PreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            22 => "Remote Release Only".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PreviewButton", 0xf, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = v & 0x7;
        dm.push(("AssignBktButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto Bracketing".to_string(),
            1 => "Multiple Exposure".to_string(),
            2 => "HDR (high dynamic range)".to_string(),
            3 => "None".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AssignBktButton", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "CommandDialsChangeMainSub".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Autofocus Off, Exposure Off".to_string(),
            1 => "Autofocus Off, Exposure On".to_string(),
            2 => "Autofocus Off, Exposure On (Mode A)".to_string(),
            4 => "Autofocus On, Exposure Off".to_string(),
            5 => "Autofocus On, Exposure On".to_string(),
            6 => "Autofocus On, Exposure On (Mode A)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsChangeMainSub",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x18) >> 3;
        dm.push((
            "CommandDialsMenuAndPlayback".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            2 => "On (Image Review Excluded)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsMenuAndPlayback",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = (v & 0x4) >> 2;
        dm.push((
            "CommandDialsApertureSetting".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Sub-command Dial".to_string(),
            1 => "Aperture Ring".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "CommandDialsApertureSetting",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = v & 0x1;
        dm.push((
            "ReleaseButtonToUseDial".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ReleaseButtonToUseDial",
            0x12,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x13) {
        let v = (v & 0xf0) >> 4;
        dm.push(("StandbyTimer".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "6 s".to_string(),
            3 => "10 s".to_string(),
            5 => "30 s".to_string(),
            6 => "1 min".to_string(),
            7 => "5 min".to_string(),
            8 => "10 min".to_string(),
            9 => "30 min".to_string(),
            10 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("StandbyTimer", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0xc0) >> 6;
        dm.push(("SelfTimerTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "5 s".to_string(),
            2 => "10 s".to_string(),
            3 => "20 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SelfTimerTime", 0x14, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = (v & 0x30) >> 4;
        dm.push(("SelfTimerShotInterval".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "0.5 s".to_string(),
            1 => "1 s".to_string(),
            2 => "2 s".to_string(),
            3 => "3 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SelfTimerShotInterval",
            0x14,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x14) {
        let v = v & 0xf;
        dm.push(("SelfTimerShotCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SelfTimerShotCount",
            0x14,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "ImageReviewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "2 s".to_string(),
            1 => "4 s".to_string(),
            3 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ImageReviewMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "LiveViewMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "5 min".to_string(),
            2 => "10 min".to_string(),
            3 => "15 min".to_string(),
            4 => "20 min".to_string(),
            5 => "30 min".to_string(),
            6 => "No Limit".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewMonitorOffTime",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0xe0) >> 5;
        dm.push(("MenuMonitorOffTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            2 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MenuMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x16) {
        let v = (v & 0x1c) >> 2;
        dm.push((
            "ShootingInfoMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            2 => "10 s".to_string(),
            4 => "20 s".to_string(),
            5 => "1 min".to_string(),
            6 => "5 min".to_string(),
            7 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShootingInfoMonitorOffTime",
            0x16,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = (v & 0xf0) >> 4;
        dm.push(("FlashSyncSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            2 => "1/250 s (auto FP)".to_string(),
            3 => "1/250 s".to_string(),
            5 => "1/200 s".to_string(),
            6 => "1/160 s".to_string(),
            7 => "1/125 s".to_string(),
            8 => "1/100 s".to_string(),
            9 => "1/80 s".to_string(),
            10 => "1/60 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashSyncSpeed", 0x17, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x17) {
        let v = v & 0xf;
        dm.push(("FlashShutterSpeed".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "1/60 s".to_string(),
            1 => "1/30 s".to_string(),
            2 => "1/15 s".to_string(),
            3 => "1/8 s".to_string(),
            4 => "1/4 s".to_string(),
            5 => "1/2 s".to_string(),
            6 => "1 s".to_string(),
            7 => "2 s".to_string(),
            8 => "4 s".to_string(),
            9 => "8 s".to_string(),
            10 => "15 s".to_string(),
            11 => "30 s".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashShutterSpeed",
            0x17,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1f) {
        let v = (v & 0x20) >> 5;
        dm.push(("ModelingFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ModelingFlash", 0x1f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x24) {
        let v = (v & 0xe0) >> 5;
        dm.push((
            "PlaybackMonitorOffTime".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "4 s".to_string(),
            1 => "10 s".to_string(),
            2 => "20 s".to_string(),
            3 => "1 min".to_string(),
            4 => "5 min".to_string(),
            5 => "10 min".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PlaybackMonitorOffTime",
            0x24,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x25) {
        let v = (v & 0xc0) >> 6;
        dm.push(("MultiSelectorLiveView".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Reset".to_string(),
            1 => "Zoom".to_string(),
            3 => "Not Used".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MultiSelectorLiveView",
            0x25,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x80) >> 7;
        dm.push(("ShutterSpeedLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ShutterSpeedLock", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x40) >> 6;
        dm.push(("ApertureLock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ApertureLock", 0x26, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x10) >> 4;
        dm.push(("MovieShutterButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Take Photo".to_string(),
            1 => "Record Movies".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MovieShutterButton",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x4) >> 2;
        dm.push(("FlashExposureCompArea".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Entire Frame".to_string(),
            1 => "Background Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashExposureCompArea",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x26) {
        let v = (v & 0x2) >> 1;
        dm.push((
            "AutoFlashISOSensitivity".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Subject and Background".to_string(),
            1 => "Subject Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AutoFlashISOSensitivity",
            0x26,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x29) {
        let v = (v & 0xf0) >> 4;
        dm.push(("MovieFunc1Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            2 => "Power Aperture (close)".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            11 => "Exposure Compensation -".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MovieFunc1Button", 0x29, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x29) {
        let v = v & 0xf;
        dm.push(("MoviePreviewButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Power Aperture (open)".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            10 => "Exposure Compensation +".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "MoviePreviewButton",
            0x29,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2a) {
        let v = v & 0xf;
        dm.push(("Func1ButtonPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (DX/1.3x)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "Func1ButtonPlusDials",
            0x2a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2b) {
        let v = v & 0xf;
        dm.push((
            "PreviewButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (DX/1.3x)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PreviewButtonPlusDials",
            0x2b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2d) {
        let v = v & 0xf;
        dm.push((
            "AssignMovieRecordButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (DX/1.3x)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            11 => "Exposure Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieRecordButtonPlusDials",
            0x2d,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2e) {
        let v = v & 0xf;
        dm.push((
            "FineTuneOptHighlightWeighted".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val > 0x7 ? $val - 0x10 : $val) / 6", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? sprintf(\"%+.2f\", $val) : 0", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FineTuneOptHighlightWeighted",
                0x2e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x80) >> 7;
        dm.push(("DynamicAreaAFDisplay".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DynamicAreaAFDisplay",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x40) >> 6;
        dm.push(("AFPointIllumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On During Manual Focusing".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFPointIllumination",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x18) >> 3;
        dm.push(("StoreByOrientation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Focus Point".to_string(),
            2 => "Focus Point and AF-area Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "StoreByOrientation",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2f) {
        let v = (v & 0x4) >> 2;
        dm.push((
            "GroupAreaAFIllumination".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Squares".to_string(),
            1 => "Dots".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "GroupAreaAFIllumination",
            0x2f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = (v & 0x80) >> 7;
        dm.push(("MatrixMetering".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Face Detection On".to_string(),
            1 => "Face Detection Off".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MatrixMetering", 0x30, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = (v & 0x30) >> 4;
        dm.push(("LiveViewButtonOptions".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Enable".to_string(),
            1 => "Enable (Standby Timer Active)".to_string(),
            2 => "Disable".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LiveViewButtonOptions",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x30) {
        let v = v & 0x3;
        dm.push(("AFModeRestrictions".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Restrictions".to_string(),
            1 => "AF-C".to_string(),
            2 => "AF-S".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AFModeRestrictions",
            0x30,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x31) {
        let v = (v & 0x7e) >> 1;
        dm.push((
            "LimitAFAreaModeSelection".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto-area".to_string(),
            1 => "Group-area".to_string(),
            2 => "3D-tracking".to_string(),
            3 => "Dynamic area (153 points)".to_string(),
            4 => "Dynamic area (72 points)".to_string(),
            5 => "Dynamic area (25 points)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LimitAFAreaModeSelection",
            0x31,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x34) {
        let v = v & 0x3f;
        dm.push((
            "LensFocusFunctionButtons".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            24 => "Preset Focus Point".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "LensFocusFunctionButtons",
            0x34,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x42) {
        let v = v & 0xff;
        dm.push(("VerticalMultiSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Same as Multi-Selector with Info(U/D) & Playback(R/L)".to_string(),
            8 => "Same as Multi-Selector with Info(R/L) & Playback(U/D)".to_string(),
            128 => "Focus Point Selection".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "VerticalMultiSelector",
            0x42,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x43) {
        let v = v & 0x3f;
        dm.push((
            "AssignMB-D17FuncButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            18 => "Reset Focus Point".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            22 => "Remote Release Only".to_string(),
            23 => "Preset Focus Point".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            54 => "Highlight Active Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMB-D17FuncButton",
            0x43,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x44) {
        let v = v & 0xf;
        dm.push((
            "AssignMB-D17FuncButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (DX/1.3x)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            3 => "One Step Speed / Aperture".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            5 => "Active D-Lighting".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            8 => "Exposure Delay Mode".to_string(),
            10 => "ISO Sensitivity".to_string(),
            11 => "Exposure Mode".to_string(),
            12 => "Exposure Compensation".to_string(),
            13 => "Metering Mode".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMB-D17FuncButtonPlusDials",
            0x44,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x46) {
        let v = v & 0x3f;
        dm.push(("AF-OnButton".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AF-OnButton", 0x46, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x47) {
        let v = (v & 0x80) >> 7;
        dm.push(("SubSelector".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Focus Point Selection".to_string(),
            1 => "Same as MultiSelector".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SubSelector", 0x47, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x48) {
        let v = v & 0x3f;
        dm.push(("SubSelectorCenter".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Preview".to_string(),
            2 => "FV Lock".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            10 => "Bracketing Burst".to_string(),
            11 => "Matrix Metering".to_string(),
            12 => "Center-weighted Metering".to_string(),
            13 => "Spot Metering".to_string(),
            14 => "Playback".to_string(),
            15 => "My Menu Top Item".to_string(),
            16 => "+NEF(RAW)".to_string(),
            17 => "Virtual Horizon".to_string(),
            18 => "Reset Focus Point".to_string(),
            19 => "Grid Display".to_string(),
            20 => "My Menu".to_string(),
            22 => "Remote Release Only".to_string(),
            23 => "Preset Focus Point".to_string(),
            26 => "Flash Disable/Enable".to_string(),
            27 => "Highlight-weighted Metering".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            49 => "Sync Release (Master Only)".to_string(),
            50 => "Sync Release (Remote Only)".to_string(),
            54 => "Highlight Active Focus Point".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubSelectorCenter",
            0x48,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x49) {
        let v = v & 0xf;
        dm.push(("SubSelectorPlusDials".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (DX/1.3x)".to_string(),
            2 => "Shutter Speed & Aperture Lock".to_string(),
            4 => "Choose Non-CPU Lens Number".to_string(),
            7 => "Photo Shooting Menu Bank".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubSelectorPlusDials",
            0x49,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4a) {
        let v = (v & 0xf0) >> 4;
        dm.push((
            "AssignMovieSubselector".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            5 => "AE/AF Lock".to_string(),
            6 => "AE Lock (Only)".to_string(),
            7 => "AE Lock (Hold)".to_string(),
            8 => "AF Lock (Only)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieSubselector",
            0x4a,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4b) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "AssignMovieFunc1ButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (DX/1.3x)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieFunc1ButtonPlusDials",
            0x4b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4b) {
        let v = v & 0x1;
        dm.push((
            "AssignMoviePreviewButtonPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (DX/1.3x)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMoviePreviewButtonPlusDials",
            0x4b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4c) {
        let v = (v & 0x10) >> 4;
        dm.push((
            "AssignMovieSubselectorPlusDials".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            1 => "Choose Image Area (DX/1.3x)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieSubselectorPlusDials",
            0x4c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4d) {
        let v = (v & 0x80) >> 7;
        dm.push(("SyncReleaseMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Sync".to_string(),
            1 => "Sync".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SyncReleaseMode", 0x4d, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = (v & 0x80) >> 7;
        dm.push((
            "Three-DTrackingWatchArea".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Wide".to_string(),
            1 => "Normal".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "Three-DTrackingWatchArea",
            0x4e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = (v & 0x60) >> 5;
        dm.push(("SubjectMotion".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Steady".to_string(),
            1 => "Middle".to_string(),
            2 => "Erratic".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SubjectMotion", 0x4e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = (v & 0x8) >> 3;
        dm.push(("AFActivation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Shutter/AF-On".to_string(),
            1 => "AF-On Only".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFActivation", 0x4e, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x4e) {
        let v = v & 0x3;
        dm.push((
            "ShutterReleaseButtonAE-L".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On (Half Press)".to_string(),
            2 => "On (Burst Mode)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShutterReleaseButtonAE-L",
            0x4e,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x4f) {
        let v = v & 0x7f;
        dm.push((
            "AssignMB-D17AF-OnButton".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "AE/AF Lock".to_string(),
            4 => "AE Lock Only".to_string(),
            5 => "AE Lock (reset on release)".to_string(),
            6 => "AE Lock (hold)".to_string(),
            7 => "AF Lock Only".to_string(),
            8 => "AF-On".to_string(),
            36 => "AF-Area Mode (Single)".to_string(),
            37 => "AF-Area Mode (Dynamic Area 25 Points)".to_string(),
            38 => "AF-Area Mode (Dynamic Area 72 Points)".to_string(),
            39 => "AF-Area Mode (Dynamic Area 152 Points)".to_string(),
            40 => "AF-Area Mode (Group Area AF)".to_string(),
            41 => "AF-Area Mode (Auto Area AF)".to_string(),
            42 => "AF-Area Mode + AF-On (Single)".to_string(),
            43 => "AF-Area Mode + AF-On (Dynamic Area 25 Points)".to_string(),
            44 => "AF-Area Mode + AF-On (Dynamic Area 72 Points)".to_string(),
            45 => "AF-Area Mode + AF-On (Dynamic Area 152 Points)".to_string(),
            46 => "AF-Area Mode + AF-On (Group Area AF)".to_string(),
            47 => "AF-Area Mode + AF-On (Auto Area AF)".to_string(),
            100 => "Same as Camera AF-On Button".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMB-D17AF-OnButton",
            0x4f,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x50) {
        let v = v & 0x3f;
        dm.push(("Func2Button".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            15 => "My Menu Top Item".to_string(),
            20 => "My Menu".to_string(),
            55 => "Rating".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Func2Button", 0x50, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x52) {
        let v = (v & 0x70) >> 4;
        dm.push((
            "AssignMovieFunc2Button".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "None".to_string(),
            3 => "Index Marking".to_string(),
            4 => "View Photo Shooting Info".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AssignMovieFunc2Button",
            0x52,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Nintendo::CameraInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn nintendo_camerainfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Nintendo";
    const GRP2: &str = "Image";
    const PRIO: i32 = crate::tag::PRIORITY_EXPLICIT_ZERO;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, false) {
        tags.push(mk(
            "ModelID",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push(("TimeStamp".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("ConvertUnixTime($val + 10957 * 24 * 3600)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "TimeStamp",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x18, 4, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with("\"0x\" . unpack(\"H*\",$val)", &cv, &ctx) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        tags.push(mk(
            "InternalSerialNumber",
            0x18,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = f32_at(data, 0x28, bo) {
        dm.push(("Parallax".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.2f\", $val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "Parallax",
            0x28,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x30, bo) {
        dm.push(("Category".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "(none)".to_string(),
            4096 => "Mii".to_string(),
            8192 => "Man".to_string(),
            16384 => "Woman".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Category", 0x30, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::Olympus::MOV1` -- FORMAT int8u, FIRST_ENTRY 0.
fn olympus_mov1(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 24, true) {
        tags.push(mk(
            "Make",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x18, 8, true) {
        tags.push(mk(
            "Model",
            0x18,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x26, bo) {
        dm.push(("ExposureUnknown".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = rat64u_at(data, 0x2a, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::Exif::PrintFNumber($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "FNumber",
            0x2a,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64s_at(data, 0x32, bo) {
        dm.push(("ExposureCompensation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "ExposureCompensation",
            0x32,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64u_at(data, 0x48, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f mm\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FocalLength",
            0x48,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Olympus::MOV2` -- FORMAT int8u, FIRST_ENTRY 0.
fn olympus_mov2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 24, true) {
        tags.push(mk(
            "Make",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x18, 24, true) {
        tags.push(mk(
            "Model",
            0x18,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x36, bo) {
        dm.push(("ExposureTime".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val ? 10 / $val : 0", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("Image::ExifTool::Exif::PrintExposureTime($val)", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ExposureTime",
                0x36,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = rat64u_at(data, 0x3a, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::Exif::PrintFNumber($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "FNumber",
            0x3a,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64s_at(data, 0x42, bo) {
        dm.push(("ExposureCompensation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "ExposureCompensation",
            0x42,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64u_at(data, 0x58, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f mm\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FocalLength",
            0x58,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xc1, bo) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ISO",
            0xc1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Olympus::prms` -- FORMAT int8u, FIRST_ENTRY 0.
fn olympus_prms(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x12, 24, true) {
        tags.push(mk(
            "Make",
            0x12,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x2c, 24, true) {
        tags.push(mk(
            "Model",
            0x2c,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x83, 24, true) {
        tags.push(mk(
            "DateTime1",
            0x83,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x9d, 24, true) {
        tags.push(mk(
            "DateTime2",
            0x9d,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x17f, 32, true) {
        tags.push(mk(
            "LensModel",
            0x17f,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Olympus::AFInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn olympus_afinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i8_at(data, 0x62c) {
        dm.push(("CAFSensitivity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "CAFSensitivity",
            0x62c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Olympus::AFTargetInfo` -- FORMAT int16u, FIRST_ENTRY 0.
fn olympus_aftargetinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "AFFrameSize",
                0x0,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "AFFocusArea",
                0x2,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0xc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "AFSelectedArea",
                0x6,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Olympus::MovableInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn olympus_movableinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push(("ISO".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ISO",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x2c, 16, true) {
        tags.push(mk(
            "EncoderVersion",
            0x2c,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x3c, 16, true) {
        tags.push(mk(
            "DecoderVersion",
            0x3c,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x83..) {
        tags.extend(olympus_thumbnail(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Olympus::SubjectDetectInfo` -- FORMAT int16u, FIRST_ENTRY 0.
fn olympus_subjectdetectinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x0 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "SubjectDetectFrameSize",
                0x0,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "SubjectDetectArea",
                0x2,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0xc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "SubjectDetectDetail",
                0x6,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x14, bo) {
        dm.push(("SubjectDetectStatus".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Data".to_string(),
            257 => "Subject and L1 Detail Detected".to_string(),
            258 => "Subject and L2 Detail Detected".to_string(),
            260 => "Subject Detected, No Details".to_string(),
            515 => "Face and Eye Detected".to_string(),
            516 => "Face Detected".to_string(),
            771 => "Subject Detail or Eye Detected".to_string(),
            772 => "No Subject or Face Detected".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "SubjectDetectStatus",
            0xa,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Olympus::Thumbnail` -- FORMAT int32u, FIRST_ENTRY 0.
fn olympus_thumbnail(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push(("ThumbnailWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailWidth",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push(("ThumbnailHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailHeight",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xc, bo) {
        dm.push(("ThumbnailLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailLength",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x10, bo) {
        dm.push(("ThumbnailOffset".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailOffset",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Olympus::AVI` -- FORMAT int8u, FIRST_ENTRY 0.
fn olympus_avi(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x12, 24, true) {
        tags.push(mk(
            "Make",
            0x12,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x2c, 24, true) {
        tags.push(mk(
            "Model",
            0x2c,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64u_at(data, 0x5e, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::Exif::PrintFNumber($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "FNumber",
            0x5e,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x83, 24, true) {
        tags.push(mk(
            "DateTime1",
            0x83,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x9d, 24, true) {
        tags.push(mk(
            "DateTime2",
            0x9d,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x129..) {
        tags.extend(olympus_thmb2(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Olympus::MP4` -- FORMAT int8u, FIRST_ENTRY 0.
fn olympus_mp4(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 24, true) {
        tags.push(mk(
            "Make",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x18, 24, true) {
        tags.push(mk(
            "Model",
            0x18,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64u_at(data, 0x28, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::Exif::PrintFNumber($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "FNumber",
            0x28,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64s_at(data, 0x30, bo) {
        dm.push(("ExposureCompensation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::Exif::PrintFraction($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "ExposureCompensation",
            0x30,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if prefix_matches(
        data.get(0x68..).unwrap_or(&[]),
        &[
            Some(&[(68, 68)]),
            Some(&[(73, 73)]),
            Some(&[(71, 71)]),
            Some(&[(73, 73)]),
        ],
    ) {
        if let Some(sub) = data.get(0x68..) {
            tags.extend(olympus_movableinfo(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    if prefix_matches(
        data.get(0x72..).unwrap_or(&[]),
        &[
            Some(&[(68, 68)]),
            Some(&[(73, 73)]),
            Some(&[(71, 71)]),
            Some(&[(73, 73)]),
        ],
    ) {
        if let Some(sub) = data.get(0x72..) {
            tags.extend(olympus_movableinfo(
                sub, make, model, bo, file_type, format, dm,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Olympus::WAV` -- FORMAT int8u, FIRST_ENTRY 0.
fn olympus_wav(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Audio";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0xc, 16, true) {
        tags.push(mk(
            "Model",
            0xc,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x1c, bo) {
        dm.push(("FileNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.4d\", $val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FileNumber",
            0x1c,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x26, 12, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with(
            "
            return undef unless $val =~ /^(\\d{2})(\\d{2})(\\d{2})(\\d{2})(\\d{2})(\\d{2})$/;
            my $y = $1 < 70 ? \"20$1\" : \"19$1\";
            return \"$y:$2:$3 $4:$5:$6\";
        ",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "DateTimeOriginal",
            0x26,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x32, 12, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with(
            "
            return undef unless $val =~ /^(\\d{2})(\\d{2})(\\d{2})(\\d{2})(\\d{2})(\\d{2})$/;
            my $y = $1 < 70 ? \"20$1\" : \"19$1\";
            return \"$y:$2:$3 $4:$5:$6\";
        ",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "DateTimeEnd",
            0x32,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x3e, 6, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) =
            conv_expr::eval_with("$val =~ s/^(\\d{2})(\\d{2})/$1:$2:/; $val", &cv, &ctx)
        {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        tags.push(mk(
            "RecordingTime",
            0x3e,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x200, bo) {
        dm.push(("Duration".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "Duration",
                0x200,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x20a, bo) {
        dm.push(("Index01".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index01",
                    0x20a,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x214, bo) {
        dm.push(("Index02".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index02",
                    0x214,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x21e, bo) {
        dm.push(("Index03".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index03",
                    0x21e,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x228, bo) {
        dm.push(("Index04".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index04",
                    0x228,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x232, bo) {
        dm.push(("Index05".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index05",
                    0x232,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x23c, bo) {
        dm.push(("Index06".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index06",
                    0x23c,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x246, bo) {
        dm.push(("Index07".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index07",
                    0x246,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x250, bo) {
        dm.push(("Index08".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index08",
                    0x250,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x25a, bo) {
        dm.push(("Index09".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index09",
                    0x25a,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x264, bo) {
        dm.push(("Index10".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index10",
                    0x264,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x26e, bo) {
        dm.push(("Index11".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index11",
                    0x26e,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x278, bo) {
        dm.push(("Index12".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index12",
                    0x278,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x282, bo) {
        dm.push(("Index13".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index13",
                    0x282,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x28c, bo) {
        dm.push(("Index14".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index14",
                    0x28c,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x296, bo) {
        dm.push(("Index15".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index15",
                    0x296,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u32_at(data, 0x2a0, bo) {
        dm.push(("Index16".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val == 0xffffffff ? undef : $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("ConvertDuration($val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "Index16",
                    0x2a0,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Olympus::FocusInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn olympus_focusinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("FocusInfoVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val=~s/\\0+$//; $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FocusInfoVersion",
                0x0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x209) {
        dm.push(("AutoFocus".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x210) {
        dm.push(("SceneDetect".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "SceneDetect",
            0x210,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x211) {
        dm.push(("SceneArea".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x212) {
        dm.push(("SceneDetectData".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x300) {
        dm.push(("ZoomStepCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ZoomStepCount",
            0x300,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x301) {
        dm.push(("FocusStepCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusStepCount",
            0x301,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x303) {
        dm.push(("FocusStepInfinity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusStepInfinity",
            0x303,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x304) {
        dm.push(("FocusStepNear".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusStepNear",
            0x304,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x305, bo) {
        dm.push(("FocusDistance".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with(
            "
            my ($a,$b) = split ' ',$val;
            return 0 if $a == 0xffffffff;
            return $a / 1000;
        ",
            &cv,
            &ctx,
        );
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val ? \"$val m\" : \"inf\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FocusDistance",
                0x305,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_28.is_match(model) {
        if let Some(v) = u8_at(data, 0x308) {
            dm.push(("AFPoint".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("($val & 0x1f) . \" \" . ($val & 0xffe0)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "AFPoint",
                    0x308,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if !(MODEL_RE_28.is_match(model)) && MODEL_RE_29.is_match(model) {
        if let Some(v) = u8_at(data, 0x308) {
            dm.push(("AFPoint".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("($val & 0x1f) . \" \" . ($val & 0xffe0)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "AFPoint",
                    0x308,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if !(MODEL_RE_28.is_match(model))
        && !(MODEL_RE_29.is_match(model))
        && !MODEL_RE_30.is_match(model)
    {
        if let Some(v) = u8_at(data, 0x308) {
            dm.push(("AFPoint".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let rc = conv_expr::eval_with(
                "($val or $$self{Model} ne \"E-P1\") ? $val : undef",
                &base,
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                let base = rc.unwrap_or(base);
                #[allow(clippy::cast_possible_truncation)]
                let s = match base.as_num() as i64 {
                    0 => "Left (or n/a)".to_string(),
                    1 => "Center (horizontal)".to_string(),
                    2 => "Right".to_string(),
                    3 => "Center (vertical)".to_string(),
                    255 => "None".to_string(),
                    other => other.to_string(),
                };
                #[allow(clippy::cast_possible_truncation)]
                let raw = Value::I32(base.as_num() as i32);
                tags.push(mk("AFPoint", 0x308, s, raw, GRP0, GRP1, GRP2, PRIO));
            }
        }
    }
    if !(MODEL_RE_28.is_match(model))
        && !(MODEL_RE_29.is_match(model))
        && !(!MODEL_RE_30.is_match(model))
    {
        if let Some(v) = u8_at(data, 0x308) {
            dm.push(("AFPoint".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "AFPoint",
                0x308,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if MODEL_RE_31.is_match(model) {
        if let Some(v) = u8_at(data, 0x31b) {
            dm.push(("AFPointDetails".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let vc = conv_expr::eval_with("(($val >> 13) & 0x7) . \" \" . (($val >> 12) & 0x1) . \" \" .  (($val >> 11) & 0x1) . \" \" .

                          (($val >> 8) & 0x3) . \" \" . (($val >> 7) & 0x1) . \" \" . (($val >> 5) & 0x1) . \" \" .

                          (($val >> 4) & 0x1) . \" \" . (($val >> 3) & 0x1) . \" \" . ($val & 0x7)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "AFPointDetails",
                    0x31b,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if !(MODEL_RE_31.is_match(model)) {
        if let Some(v) = u8_at(data, 0x31b) {
            dm.push(("AFPointDetails".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "AFPointDetails",
                0x31b,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x1201) {
        dm.push(("ExternalFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ExternalFlash",
            0x1201,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1203) {
        dm.push((
            "ExternalFlashGuideNumber".to_string(),
            Conv::Num(f64::from(v)),
        ));
    }
    if let Some(v) = u8_at(data, 0x1204) {
        dm.push(("ExternalFlashBounce".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Bounce or Off".to_string(),
            1 => "Direct".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExternalFlashBounce",
            0x1204,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1205) {
        dm.push(("ExternalFlashZoom".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ExternalFlashZoom",
            0x1205,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1208) {
        dm.push(("InternalFlash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "InternalFlash",
            0x1208,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1209) {
        dm.push(("ManualFlash".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "
            my ($a,$b) = split ' ',$val;
            return 'Off' unless $a;
            $b = ($b == 1) ? 'Full' : \"1/$b\";
            return \"On ($b strength)\";
        ",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "ManualFlash",
            0x1209,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x120a) {
        dm.push(("MacroLED".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MacroLED", 0x120a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if !(dm_get(dm, "ImageStabilization").is_some()) {
        if let Some(v) = u8_at(data, 0x1600) {
            dm.push(("ImageStabilization".to_string(), Conv::Num(f64::from(v))));
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let base = Conv::Num(f64::from(v));
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            $val =~ /^\\0{4}/ ? 'Off' : 'On, ' .
            (unpack('x44C',$val) & 0x01 ? 'Mode 1' : 'Mode 2')
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "ImageStabilization",
                0x1600,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x2100) {
        dm.push(("AntiShockWaitingTime".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AntiShockWaitingTime",
            0x2100,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x328..) {
        tags.extend(olympus_afinfo(sub, make, model, bo, file_type, format, dm));
    }
    tags
}

/// `Image::ExifTool::Olympus::CameraSettings` -- FORMAT int8u, FIRST_ENTRY 0.
fn olympus_camerasettings(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push(("CameraSettingsVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val=~s/\\0+$//; $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "CameraSettingsVersion",
                0x0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x100) {
        dm.push(("PreviewImageValid".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PreviewImageValid",
            0x100,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x101) {
        dm.push(("PreviewImageStart".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreviewImageStart",
            0x101,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x102) {
        dm.push(("PreviewImageLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreviewImageLength",
            0x102,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x200) {
        dm.push(("ExposureMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "Manual".to_string(),
            2 => "Program".to_string(),
            3 => "Aperture-priority AE".to_string(),
            4 => "Shutter speed priority AE".to_string(),
            5 => "Program-shift".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ExposureMode", 0x200, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x201) {
        dm.push(("AELock".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AELock", 0x201, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x202) {
        dm.push(("MeteringMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            2 => "Center-weighted average".to_string(),
            3 => "Spot".to_string(),
            5 => "ESP".to_string(),
            261 => "Pattern+AF".to_string(),
            515 => "Spot+Highlight control".to_string(),
            1027 => "Spot+Shadow control".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MeteringMode", 0x202, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x203) {
        dm.push(("ExposureShift".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ExposureShift",
            0x203,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x204) {
        dm.push(("NDFilter".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NDFilter", 0x204, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x300) {
        dm.push(("MacroMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            2 => "Super Macro".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MacroMode", 0x300, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x301) {
        dm.push(("FocusMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusMode",
            0x301,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x302) {
        dm.push(("FocusProcess".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusProcess",
            0x302,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x303) {
        dm.push(("AFSearch".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Not Ready".to_string(),
            1 => "Ready".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFSearch", 0x303, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x304) {
        dm.push(("AFAreas".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("Image::ExifTool::Olympus::PrintAFAreas($val)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "AFAreas",
            0x304,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x305) {
        dm.push(("AFPointSelected".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val =~ s/\\S* //; $val", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with(
                "
            return 'n/a' if $val =~ /undef/;
            sprintf(\"(%d%%,%d%%) (%d%%,%d%%)\", map {$_ * 100} split(\" \",$val));
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "AFPointSelected",
                0x305,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x306) {
        dm.push(("AFFineTune".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("AFFineTune", 0x306, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x307) {
        dm.push(("AFFineTuneAdj".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AFFineTuneAdj",
            0x307,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x308) {
        dm.push(("FocusBracketStepSize".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FocusBracketStepSize",
            0x308,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x309) {
        dm.push(("AISubjectTrackingMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            256 => "Motorsports; Object Not Found".to_string(),
            257 => "Motorsports; Racing Car Found".to_string(),
            258 => "Motorsports; Car Found".to_string(),
            259 => "Motorsports; Motorcyle Found".to_string(),
            512 => "Airplanes; Object Not Found".to_string(),
            513 => "Airplanes; Passenger/Transport Plane Found".to_string(),
            514 => "Airplanes; Small Plane/Fighter Jet Found".to_string(),
            515 => "Airplanes; Helicopter Found".to_string(),
            768 => "Trains; Object Not Found".to_string(),
            769 => "Trains; Object Found".to_string(),
            1024 => "Birds; Object Not Found".to_string(),
            1025 => "Birds; Object Found".to_string(),
            1280 => "Dogs & Cats; Object Not Found".to_string(),
            1281 => "Dogs & Cats; Object Found".to_string(),
            1536 => "Human; Object Not Found".to_string(),
            1537 => "Human; Object Found".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "AISubjectTrackingMode",
            0x309,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x400) {
        dm.push(("FlashMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "On".to_string(),
            1 => "Fill-in".to_string(),
            2 => "Red-eye".to_string(),
            3 => "Slow-sync".to_string(),
            4 => "Forced On".to_string(),
            5 => "2nd Curtain".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FlashMode", 0x400, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x401) {
        dm.push(("FlashExposureComp".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FlashExposureComp",
            0x401,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x403) {
        dm.push(("FlashRemoteControl".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Channel 1, Low".to_string(),
            2 => "Channel 2, Low".to_string(),
            3 => "Channel 3, Low".to_string(),
            4 => "Channel 4, Low".to_string(),
            9 => "Channel 1, Mid".to_string(),
            10 => "Channel 2, Mid".to_string(),
            11 => "Channel 3, Mid".to_string(),
            12 => "Channel 4, Mid".to_string(),
            17 => "Channel 1, High".to_string(),
            18 => "Channel 2, High".to_string(),
            19 => "Channel 3, High".to_string(),
            20 => "Channel 4, High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "FlashRemoteControl",
            0x403,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x404) {
        dm.push(("FlashControlMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FlashControlMode",
            0x404,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x405) {
        dm.push(("FlashIntensity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FlashIntensity",
            0x405,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x406) {
        dm.push(("ManualFlashStrength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ManualFlashStrength",
            0x406,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x500) {
        dm.push(("WhiteBalance2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Auto".to_string(),
            1 => "Auto (Keep Warm Color Off)".to_string(),
            16 => "7500K (Fine Weather with Shade)".to_string(),
            17 => "6000K (Cloudy)".to_string(),
            18 => "5300K (Fine Weather)".to_string(),
            20 => "3000K (Tungsten light)".to_string(),
            21 => "3600K (Tungsten light-like)".to_string(),
            22 => "Auto Setup".to_string(),
            23 => "5500K (Flash)".to_string(),
            33 => "6600K (Daylight fluorescent)".to_string(),
            34 => "4500K (Neutral white fluorescent)".to_string(),
            35 => "4000K (Cool white fluorescent)".to_string(),
            36 => "White Fluorescent".to_string(),
            48 => "3600K (Tungsten light-like)".to_string(),
            67 => "Underwater".to_string(),
            256 => "One Touch WB 1".to_string(),
            257 => "One Touch WB 2".to_string(),
            258 => "One Touch WB 3".to_string(),
            259 => "One Touch WB 4".to_string(),
            512 => "Custom WB 1".to_string(),
            513 => "Custom WB 2".to_string(),
            514 => "Custom WB 3".to_string(),
            515 => "Custom WB 4".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WhiteBalance2", 0x500, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x501) {
        dm.push((
            "WhiteBalanceTemperature".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("$val ? $val : \"Auto\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "WhiteBalanceTemperature",
            0x501,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x502) {
        dm.push(("WhiteBalanceBracket".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "WhiteBalanceBracket",
            0x502,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x503) {
        dm.push(("CustomSaturation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "
            my ($a,$b,$c)=split ' ',$val;
            if ($self->{Model} =~ /^E-1\\b/) {
                $a-=$b; $c-=$b;
                return \"CS$a (min CS0, max CS$c)\";
            } else {
                return \"$a (min $b, max $c)\";
            }
        ",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "CustomSaturation",
            0x503,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x504) {
        dm.push(("ModifiedSaturation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "CM1 (Red Enhance)".to_string(),
            2 => "CM2 (Green Enhance)".to_string(),
            3 => "CM3 (Blue Enhance)".to_string(),
            4 => "CM4 (Skin Tones)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ModifiedSaturation",
            0x504,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x505) {
        dm.push(("ContrastSetting".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "my @v=split \" \",$val; \"$v[0] (min $v[1], max $v[2])\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "ContrastSetting",
            0x505,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x506) {
        dm.push(("SharpnessSetting".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "my @v=split \" \",$val; \"$v[0] (min $v[1], max $v[2])\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "SharpnessSetting",
            0x506,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x507) {
        dm.push(("ColorSpace".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "sRGB".to_string(),
            1 => "Adobe RGB".to_string(),
            2 => "Pro Photo RGB".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ColorSpace", 0x507, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x509) {
        dm.push(("SceneMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            6 => "Auto".to_string(),
            7 => "Sport".to_string(),
            8 => "Portrait".to_string(),
            9 => "Landscape+Portrait".to_string(),
            10 => "Landscape".to_string(),
            11 => "Night Scene".to_string(),
            12 => "Self Portrait".to_string(),
            13 => "Panorama".to_string(),
            14 => "2 in 1".to_string(),
            15 => "Movie".to_string(),
            16 => "Landscape+Portrait".to_string(),
            17 => "Night+Portrait".to_string(),
            18 => "Indoor".to_string(),
            19 => "Fireworks".to_string(),
            20 => "Sunset".to_string(),
            21 => "Beauty Skin".to_string(),
            22 => "Macro".to_string(),
            23 => "Super Macro".to_string(),
            24 => "Food".to_string(),
            25 => "Documents".to_string(),
            26 => "Museum".to_string(),
            27 => "Shoot & Select".to_string(),
            28 => "Beach & Snow".to_string(),
            29 => "Self Protrait+Timer".to_string(),
            30 => "Candle".to_string(),
            31 => "Available Light".to_string(),
            32 => "Behind Glass".to_string(),
            33 => "My Mode".to_string(),
            34 => "Pet".to_string(),
            35 => "Underwater Wide1".to_string(),
            36 => "Underwater Macro".to_string(),
            37 => "Shoot & Select1".to_string(),
            38 => "Shoot & Select2".to_string(),
            39 => "High Key".to_string(),
            40 => "Digital Image Stabilization".to_string(),
            41 => "Auction".to_string(),
            42 => "Beach".to_string(),
            43 => "Snow".to_string(),
            44 => "Underwater Wide2".to_string(),
            45 => "Low Key".to_string(),
            46 => "Children".to_string(),
            47 => "Vivid".to_string(),
            48 => "Nature Macro".to_string(),
            49 => "Underwater Snapshot".to_string(),
            50 => "Shooting Guide".to_string(),
            54 => "Face Portrait".to_string(),
            57 => "Bulb".to_string(),
            59 => "Smile Shot".to_string(),
            60 => "Quick Shutter".to_string(),
            63 => "Slow Shutter".to_string(),
            64 => "Bird Watching".to_string(),
            65 => "Multiple Exposure".to_string(),
            66 => "e-Portrait".to_string(),
            67 => "Soft Background Shot".to_string(),
            142 => "Hand-held Starlight".to_string(),
            154 => "HDR".to_string(),
            197 => "Panning".to_string(),
            203 => "Light Trails".to_string(),
            204 => "Backlight HDR".to_string(),
            205 => "Silent".to_string(),
            206 => "Multi Focus Shot".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("SceneMode", 0x509, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x50a) {
        dm.push(("NoiseReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Noise Reduction".to_string(),
            1 => "Noise Filter".to_string(),
            2 => "Noise Filter (ISO Boost)".to_string(),
            3 => "Auto".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoiseReduction", 0x50a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x50b) {
        dm.push(("DistortionCorrection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DistortionCorrection",
            0x50b,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x50c) {
        dm.push(("ShadingCompensation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ShadingCompensation",
            0x50c,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x50d) {
        dm.push(("CompressionFactor".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "CompressionFactor",
            0x50d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x50f) {
        dm.push(("Gradation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Gradation",
            0x50f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x520) {
        dm.push(("PictureMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PictureMode",
            0x520,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x521) {
        dm.push(("PictureModeSaturation".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "my @v=split \" \",$val; \"$v[0] (min $v[1], max $v[2])\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "PictureModeSaturation",
            0x521,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x522) {
        dm.push(("PictureModeHue".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x523) {
        dm.push(("PictureModeContrast".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "my @v=split \" \",$val; \"$v[0] (min $v[1], max $v[2])\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "PictureModeContrast",
            0x523,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x524) {
        dm.push(("PictureModeSharpness".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "my @v=split \" \",$val; \"$v[0] (min $v[1], max $v[2])\"",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "PictureModeSharpness",
            0x524,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x525) {
        dm.push(("PictureModeBWFilter".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "n/a".to_string(),
            1 => "Neutral".to_string(),
            2 => "Yellow".to_string(),
            3 => "Orange".to_string(),
            4 => "Red".to_string(),
            5 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "PictureModeBWFilter",
            0x525,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x526) {
        dm.push(("PictureModeTone".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "n/a".to_string(),
            1 => "Neutral".to_string(),
            2 => "Sepia".to_string(),
            3 => "Blue".to_string(),
            4 => "Purple".to_string(),
            5 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureModeTone", 0x526, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x527) {
        dm.push(("NoiseFilter".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "NoiseFilter",
            0x527,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x529) {
        dm.push(("ArtFilter".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ArtFilter",
            0x529,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x52c) {
        dm.push(("MagicFilter".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MagicFilter",
            0x52c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x52d) {
        dm.push(("PictureModeEffect".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PictureModeEffect",
            0x52d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x52e) {
        dm.push(("ToneLevel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ToneLevel",
            0x52e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x52f) {
        dm.push(("ArtFilterEffect".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ArtFilterEffect",
            0x52f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x532) {
        dm.push(("ColorCreatorEffect".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorCreatorEffect",
            0x532,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x537) {
        dm.push((
            "MonochromeProfileSettings".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MonochromeProfileSettings",
            0x537,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x538) {
        dm.push(("FilmGrainEffect".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Low".to_string(),
            2 => "Medium".to_string(),
            3 => "High".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("FilmGrainEffect", 0x538, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x539) {
        dm.push(("ColorProfileSettings".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ColorProfileSettings",
            0x539,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x53a) {
        dm.push(("MonochromeVignetting".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MonochromeVignetting",
            0x53a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x53b) {
        dm.push(("MonochromeColor".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "(none)".to_string(),
            1 => "Normal".to_string(),
            2 => "Sepia".to_string(),
            3 => "Blue".to_string(),
            4 => "Purple".to_string(),
            5 => "Green".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MonochromeColor", 0x53b, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x600) {
        dm.push(("DriveMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("
            my ($a,$b,$c,$d,$e,$f) = split ' ',$val;
            if ($b) {
                $b = ', Shot ' . $b;
            } else {
                $b = '';
            }
            if (not defined $e or $e == 4) {   #KG: personally, I'd like to skip 'Electronic shutter' since this is the defacto default setting
                $e = '';
            } else {
                $e = '; ' . ({ 0 => 'Mechanical shutter' , 2 => 'Anti-shock' }->{$e} || \"Unknown ($e)\");
            }
            if ($a == 5 and defined $c) {
                $a = DecodeBits($c, { #6
                    0 => 'AE',
                    1 => 'WB',
                    2 => 'FL',
                    3 => 'MF',
                    4 => 'ISO', #forum8906
                    5 => 'AE Auto', #forum8906
                    6 => 'Focus', #PH
                }) . ' Bracketing';
                $a =~ s/, /+/g;
            } elsif ($f) { #25

                  my %f = (

                      0x01 => 'Single Shot',
                      0x02 => 'Sequential L',
                      0x03 => 'Sequential H',
                      0x07 => 'Sequential',

                      0x11 => 'Single Shot',
                      0x12 => 'Sequential L',
                      0x13 => 'Sequential H',
                      0x14 => 'Self-Timer 12 sec',
                      0x15 => 'Self-Timer 2 sec',
                      0x16 => 'Custom Self-Timer',
                      0x17 => 'Sequential',

                      0x21 => 'Single Shot',
                      0x22 => 'Sequential L',
                      0x23 => 'Sequential H',
                      0x24 => 'Self-Timer 2 sec',
                      0x25 => 'Self-Timer 12 sec',
                      0x26 => 'Custom Self-Timer',
                      0x27 => 'Sequential',
                      0x28 => 'Sequential SH1',
                      0x29 => 'Sequential SH2',
                      0x30 => 'HighRes Shot',  # only E-M models
                      0x41 => 'ProCap H',
                      0x42 => 'ProCap L',
                      0x43 => 'ProCap',
                      0x48 => 'ProCap SH1',
                      0x49 => 'ProCap SH2',
                  );
                  $a = $f{$f} || \"Unknown ($f)\";
            } else {
                  my %a = (
                      0 => 'Single Shot',
                      1 => 'Continuous Shooting',
                      2 => 'Exposure Bracketing',
                      3 => 'White Balance Bracketing',
                      4 => 'Exposure+WB Bracketing', #6
                  );
                  $a = $a{$a} || \"Unknown ($a)\";
            }
            return \"$a$b$e\";
        ", &cv, &ctx) { cv = x; }
        tags.push(mk(
            "DriveMode",
            0x600,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x601) {
        dm.push(("PanoramaMode".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with(
            "
            my ($a,$b) = split ' ',$val;
            return 'Off' unless $a;
            my %a = (
                1 => 'Left to Right',
                2 => 'Right to Left',
                3 => 'Bottom to Top',
                4 => 'Top to Bottom',
            );
            return(($a{$a} || \"Unknown ($a)\") . ', Shot ' . $b);
        ",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        tags.push(mk(
            "PanoramaMode",
            0x601,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x603) {
        dm.push(("ImageQuality2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "SQ".to_string(),
            2 => "HQ".to_string(),
            3 => "SHQ".to_string(),
            4 => "RAW".to_string(),
            5 => "SQ (5)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ImageQuality2", 0x603, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x604) {
        dm.push(("ImageStabilization".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                0 => "Off".to_string(),
                1 => "On, S-IS1 (All Direction Shake IS)".to_string(),
                2 => "On, S-IS2 (Vertical Shake IS)".to_string(),
                3 => "On, S-IS3 (Horizontal Shake IS)".to_string(),
                4 => "On, S-IS Auto".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk(
                "ImageStabilization",
                0x604,
                s,
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x804) {
        dm.push(("StackedImage".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "StackedImage",
            0x804,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x821) {
        dm.push(("ISOAutoSettings".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ISOAutoSettings",
            0x821,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x900) {
        dm.push(("ManometerPressure".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 10", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val kPa\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "ManometerPressure",
                0x900,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x901) {
        dm.push(("ManometerReading".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with(
            "my @a=split(\" \",$val); $_ /= 10 foreach @a; \"@a\"",
            &cv,
            &ctx,
        );
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) =
                conv_expr::eval_with("$val=~s/(\\S+) (\\S+)/$1 m, $2 ft/; $val", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "ManometerReading",
                0x901,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x902) {
        dm.push(("ExtendedWBDetect".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ExtendedWBDetect",
            0x902,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x903) {
        dm.push(("RollAngle".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val=~s/ 1$// ? -$val/10 : \"n/a\"", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "RollAngle",
                0x903,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x904) {
        dm.push(("PitchAngle".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val =~ s/ 1$// ? $val / 10 : \"n/a\"", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "PitchAngle",
                0x904,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x908) {
        dm.push(("DateTimeUTC".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "DateTimeUTC",
            0x908,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(sub) = data.get(0x30a..) {
        tags.extend(olympus_aftargetinfo(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = data.get(0x30b..) {
        tags.extend(olympus_subjectdetectinfo(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Olympus::thmb2` -- FORMAT int8u, FIRST_ENTRY 0.
/// Incomplete: a field of variable length moves every entry after
/// it, and this reads them where they would be without it.
fn olympus_thmb2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Olympus";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("ThumbnailWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailWidth",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("ThumbnailHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailHeight",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push(("ThumbnailLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailLength",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Panasonic::Data1` -- FORMAT int8u, FIRST_ENTRY 0.
fn panasonic_data1(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Leica";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x16, bo) {
        dm.push(("LensType".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let vc = conv_expr::eval_with("(($val >> 2) & 0xffff) . \" \" . ($val & 0x3)", &base, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            let base = vc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            let s = match base.as_num() as i64 {
                1 => "Elmarit-M 21mm f/2.8".to_string(),
                3 => "Elmarit-M 28mm f/2.8 (III)".to_string(),
                4 => "Tele-Elmarit-M 90mm f/2.8 (II)".to_string(),
                5 => "Summilux-M 50mm f/1.4 (II)".to_string(),
                6 => "Summicron-M 35mm f/2 (IV)".to_string(),
                7 => "Summicron-M 90mm f/2 (II)".to_string(),
                9 => "Elmarit-M 135mm f/2.8 (I/II)".to_string(),
                11 => "Summaron-M 28mm f/5.6".to_string(),
                12 => "Thambar-M 90mm f/2.2".to_string(),
                16 => "Tri-Elmar-M 16-18-21mm f/4 ASPH.".to_string(),
                23 => "Summicron-M 50mm f/2 (III)".to_string(),
                24 => "Elmarit-M 21mm f/2.8 ASPH.".to_string(),
                25 => "Elmarit-M 24mm f/2.8 ASPH.".to_string(),
                26 => "Summicron-M 28mm f/2 ASPH.".to_string(),
                27 => "Elmarit-M 28mm f/2.8 (IV)".to_string(),
                28 => "Elmarit-M 28mm f/2.8 ASPH.".to_string(),
                29 => "Summilux-M 35mm f/1.4 ASPH.".to_string(),
                30 => "Summicron-M 35mm f/2 ASPH.".to_string(),
                31 => "Noctilux-M 50mm f/1".to_string(),
                32 => "Summilux-M 50mm f/1.4 ASPH.".to_string(),
                33 => "Summicron-M 50mm f/2 (IV, V)".to_string(),
                34 => "Elmar-M 50mm f/2.8".to_string(),
                35 => "Summilux-M 75mm f/1.4".to_string(),
                36 => "Apo-Summicron-M 75mm f/2 ASPH.".to_string(),
                37 => "Apo-Summicron-M 90mm f/2 ASPH.".to_string(),
                38 => "Elmarit-M 90mm f/2.8".to_string(),
                39 => "Macro-Elmar-M 90mm f/4".to_string(),
                40 => "Macro-Adapter M".to_string(),
                41 => "Apo-Summicron-M 50mm f/2 ASPH.".to_string(),
                42 => "Tri-Elmar-M 28-35-50mm f/4 ASPH.".to_string(),
                43 => "Summarit-M 35mm f/2.5".to_string(),
                44 => "Summarit-M 50mm f/2.5".to_string(),
                45 => "Summarit-M 75mm f/2.5".to_string(),
                46 => "Summarit-M 90mm f/2.5".to_string(),
                47 => "Summilux-M 21mm f/1.4 ASPH.".to_string(),
                48 => "Summilux-M 24mm f/1.4 ASPH.".to_string(),
                49 => "Noctilux-M 50mm f/0.95 ASPH.".to_string(),
                50 => "Elmar-M 24mm f/3.8 ASPH.".to_string(),
                51 => "Super-Elmar-M 21mm f/3.4 Asph".to_string(),
                52 => "Apo-Telyt-M 18mm f/3.8 ASPH.".to_string(),
                53 => "Apo-Telyt-M 135mm f/3.4".to_string(),
                58 => "Noctilux-M 75mm f/1.25 ASPH.".to_string(),
                other => other.to_string(),
            };
            #[allow(clippy::cast_possible_truncation)]
            let raw = Value::I32(base.as_num() as i32);
            tags.push(mk("LensType", 0x16, s, raw, GRP0, GRP1, GRP2, PRIO));
        }
    }
    tags
}

/// `Image::ExifTool::Panasonic::Data2` -- FORMAT int8u, FIRST_ENTRY 0.
fn panasonic_data2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Leica";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    tags
}

/// `Image::ExifTool::Panasonic::FaceDetInfo` -- FORMAT int16u, FIRST_ENTRY 0.
fn panasonic_facedetinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Panasonic";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("NumFacePositions".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "NumFacePositions",
                0x0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{NumFacePositions} < 1 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face1Position",
                    0x1,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0xa + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{NumFacePositions} < 2 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face2Position",
                    0x5,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x12 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{NumFacePositions} < 3 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face3Position",
                    0x9,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x1a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{NumFacePositions} < 4 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face4Position",
                    0xd,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x22 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{NumFacePositions} < 5 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "Face5Position",
                    0x11,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Panasonic::FaceRecInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn panasonic_facerecinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Panasonic";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("FacesRecognized".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FacesRecognized",
                0x0,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x4, 20, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$$self{FacesRecognized} < 1 ? undef : $val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "RecognizedFace1Name",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x18 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesRecognized} < 1 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "RecognizedFace1Position",
                    0x18,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(text) = text_at(data, 0x20, 20, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$$self{FacesRecognized} < 1 ? undef : $val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "RecognizedFace1Age",
                0x20,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x34, 20, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$$self{FacesRecognized} < 2 ? undef : $val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "RecognizedFace2Name",
                0x34,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x48 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesRecognized} < 2 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "RecognizedFace2Position",
                    0x48,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(text) = text_at(data, 0x50, 20, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$$self{FacesRecognized} < 2 ? undef : $val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "RecognizedFace2Age",
                0x50,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x64, 20, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$$self{FacesRecognized} < 3 ? undef : $val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "RecognizedFace3Name",
                0x64,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u16_at(data, 0x78 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let rc = conv_expr::eval_with(
                "$$self{FacesRecognized} < 3 ? undef : $val",
                &Conv::Str(s.clone()),
                &ctx,
            );
            if rc.as_ref() != Some(&Conv::Undef) {
                tags.push(mk(
                    "RecognizedFace3Position",
                    0x78,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(text) = text_at(data, 0x80, 20, true) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$$self{FacesRecognized} < 3 ? undef : $val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "RecognizedFace3Age",
                0x80,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Panasonic::FocusInfo` -- FORMAT int16u, FIRST_ENTRY 0.
fn panasonic_focusinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Leica";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("FocusDistance".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val < 65535 ? \"$val m\" : \"inf\"", &cv, &ctx)
            {
                cv = x;
            }
            tags.push(mk(
                "FocusDistance",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("FocalLength".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f mm\",$val)", &cv, &ctx) {
                    cv = x;
                }
                tags.push(mk(
                    "FocalLength",
                    0x1,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Panasonic::SerialInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn panasonic_serialinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Leica";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x4, 8, true) {
        tags.push(mk(
            "SerialNumber",
            0x4,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Panasonic::ShotInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn panasonic_shotinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Leica";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("FileIndex".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FileIndex",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Panasonic::TimeInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn panasonic_timeinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Panasonic";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 8, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("$val =~ /^\\0/ ? undef : $val", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            if let Some(x) = conv_expr::eval_with(
                "sprintf(\"%s:%s:%s %s:%s:%s.%s\", unpack \"H4H2H2H2H2H2H2\", $val)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "PanasonicDateTime",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x10, bo) {
        dm.push(("TimeLapseShotNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "TimeLapseShotNumber",
            0x10,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Panasonic::Type2` -- FORMAT int16u, FIRST_ENTRY 0.
fn panasonic_type2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Panasonic";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 4, true) {
        tags.push(mk(
            "MakerNoteType",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x6, bo) {
        dm.push(("Gain".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Gain",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::PanasonicRaw::DistortionInfo` -- FORMAT int16s, FIRST_ENTRY 0.
fn panasonicraw_distortioninfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "PanasonicRaw";
    const GRP1: &str = "PanasonicRaw";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = i16_at(data, 0x4, bo) {
        dm.push(("DistortionParam02".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 32768", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DistortionParam02",
                0x2,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x8, bo) {
        dm.push(("DistortionParam04".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 32768", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DistortionParam04",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xa, bo) {
        dm.push(("DistortionScale".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("1 / (1 + $val/32768)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DistortionScale",
                0x5,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0xe, bo) {
        let v = v & 0xf;
        dm.push(("DistortionCorrection".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "DistortionCorrection",
            0x7,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x10, bo) {
        dm.push(("DistortionParam08".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 32768", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DistortionParam08",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x12, bo) {
        dm.push(("DistortionParam09".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 32768", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DistortionParam09",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x16, bo) {
        dm.push(("DistortionParam11".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 32768", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "DistortionParam11",
                0xb,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = i16_at(data, 0x18, bo) {
        dm.push(("DistortionN".to_string(), Conv::Num(f64::from(v))));
    }
    tags
}

/// `Image::ExifTool::PanasonicRaw::WBInfo` -- FORMAT int16u, FIRST_ENTRY 0.
fn panasonicraw_wbinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "PanasonicRaw";
    const GRP1: &str = "PanasonicRaw";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("NumWBEntries".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "NumWBEntries",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("WBType1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType1", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevels1",
                0x2,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x8, bo) {
        dm.push(("WBType2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType2", 0x4, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0xa + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevels2",
                0x5,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xe, bo) {
        dm.push(("WBType3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType3", 0x7, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x10 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevels3",
                0x8,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x14, bo) {
        dm.push(("WBType4".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType4", 0xa, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x16 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevels4",
                0xb,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x1a, bo) {
        dm.push(("WBType5".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType5", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x1c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevels5",
                0xe,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x20, bo) {
        dm.push(("WBType6".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType6", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x22 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevels6",
                0x11,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x26, bo) {
        dm.push(("WBType7".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType7", 0x13, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x28 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RBLevels7",
                0x14,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::PanasonicRaw::WBInfo2` -- FORMAT int16u, FIRST_ENTRY 0.
fn panasonicraw_wbinfo2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "PanasonicRaw";
    const GRP1: &str = "PanasonicRaw";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("NumWBEntries".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "NumWBEntries",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("WBType1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType1", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x4 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGBLevels1",
                0x2,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0xa, bo) {
        dm.push(("WBType2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType2", 0x5, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0xc + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGBLevels2",
                0x6,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x12, bo) {
        dm.push(("WBType3".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType3", 0x9, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x14 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGBLevels3",
                0xa,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x1a, bo) {
        dm.push(("WBType4".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType4", 0xd, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x1c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGBLevels4",
                0xe,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x22, bo) {
        dm.push(("WBType5".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType5", 0x11, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x24 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGBLevels5",
                0x12,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x2a, bo) {
        dm.push(("WBType6".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType6", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x2c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGBLevels6",
                0x16,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x32, bo) {
        dm.push(("WBType7".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Daylight".to_string(),
            2 => "Fluorescent".to_string(),
            3 => "Tungsten (Incandescent)".to_string(),
            4 => "Flash".to_string(),
            9 => "Fine Weather".to_string(),
            10 => "Cloudy".to_string(),
            11 => "Shade".to_string(),
            12 => "Daylight Fluorescent".to_string(),
            13 => "Day White Fluorescent".to_string(),
            14 => "Cool White Fluorescent".to_string(),
            15 => "White Fluorescent".to_string(),
            16 => "Warm White Fluorescent".to_string(),
            17 => "Standard Light A".to_string(),
            18 => "Standard Light B".to_string(),
            19 => "Standard Light C".to_string(),
            20 => "D55".to_string(),
            21 => "D65".to_string(),
            22 => "D75".to_string(),
            23 => "D50".to_string(),
            24 => "ISO Studio Tungsten".to_string(),
            25 => "Daylight".to_string(),
            26 => "Day White".to_string(),
            27 => "Cool White".to_string(),
            28 => "White".to_string(),
            29 => "Warm White".to_string(),
            30 => "Daylight LED".to_string(),
            31 => "Day White LED".to_string(),
            32 => "Cool White LED".to_string(),
            33 => "White LED".to_string(),
            34 => "Warm White LED".to_string(),
            255 => "Other".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("WBType7", 0x19, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x34 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "WB_RGBLevels7",
                0x1a,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Parrot::ARCoreAccel` -- FORMAT int8u, FIRST_ENTRY 0.
fn parrot_arcoreaccel(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "Parrot";
    const GRP1: &str = "Parrot";
    const GRP2: &str = "Location";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x5, 14, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "GetFloat(\\$val,0) . \" \" . GetFloat(\\$val,5) . \" \" . GetFloat(\\$val,10)",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "Accelerometer",
                0x5,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Parrot::ARCoreAccel0` -- FORMAT int8u, FIRST_ENTRY 0.
fn parrot_arcoreaccel0(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "Parrot";
    const GRP1: &str = "Parrot";
    const GRP2: &str = "Location";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x9, 14, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "GetFloat(\\$val,0) . \" \" . GetFloat(\\$val,5) . \" \" . GetFloat(\\$val,10)",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "Accelerometer",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Parrot::ARCoreGyro` -- FORMAT int8u, FIRST_ENTRY 0.
fn parrot_arcoregyro(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "Parrot";
    const GRP1: &str = "Parrot";
    const GRP2: &str = "Location";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x5, 14, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "GetFloat(\\$val,0) . \" \" . GetFloat(\\$val,5) . \" \" . GetFloat(\\$val,10)",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "Gyroscope",
                0x5,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Parrot::ARCoreGyro0` -- FORMAT int8u, FIRST_ENTRY 0.
fn parrot_arcoregyro0(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "Parrot";
    const GRP1: &str = "Parrot";
    const GRP2: &str = "Location";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x9, 14, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with(
            "GetFloat(\\$val,0) . \" \" . GetFloat(\\$val,5) . \" \" . GetFloat(\\$val,10)",
            &cv,
            &ctx,
        );
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "Gyroscope",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Parrot::ARCoreVideo` -- FORMAT int8u, FIRST_ENTRY 0.
fn parrot_arcorevideo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "Parrot";
    const GRP1: &str = "Parrot";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    tags
}

/// `Image::ExifTool::Parrot::ARCoreCustom` -- FORMAT int8u, FIRST_ENTRY 0.
fn parrot_arcorecustom(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "Parrot";
    const GRP1: &str = "Parrot";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    tags
}

/// `Image::ExifTool::Pentax::Junk2` -- FORMAT int8u, FIRST_ENTRY 0.
/// Incomplete: a field of variable length moves every entry after
/// it, and this reads them where they would be without it.
fn pentax_junk2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Pentax";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x12, 24, true) {
        tags.push(mk(
            "Make",
            0x12,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x2c, 24, true) {
        tags.push(mk(
            "Model",
            0x2c,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64u_at(data, 0x5e, bo) {
        dm.push(("FNumber".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"%.1f\",$val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "FNumber",
            0x5e,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x83, 24, true) {
        tags.push(mk(
            "DateTime1",
            0x83,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x9d, 24, true) {
        tags.push(mk(
            "DateTime2",
            0x9d,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x12b, bo) {
        dm.push(("ThumbnailWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailWidth",
            0x12b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x12d, bo) {
        dm.push(("ThumbnailHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailHeight",
            0x12d,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x12f, bo) {
        dm.push(("ThumbnailLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailLength",
            0x12f,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Pentax::CAFPointInfo` -- FORMAT int8u, FIRST_ENTRY 0.
/// Incomplete: a field of variable length moves every entry after
/// it, and this reads them where they would be without it.
fn pentax_cafpointinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Pentax";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x1) {
        dm.push(("NumCAFPoints".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("($val & 0x0f) * ($val >> 4); $val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let vc = conv_expr::eval_with("($val >> 4) * ($val & 0x0f)", &cv, &ctx);
            if vc.as_ref() != Some(&Conv::Undef) {
                if let Some(x) = vc {
                    cv = x;
                }
                let raw = Value::F64(cv.as_num());
                tags.push(mk(
                    "NumCAFPoints",
                    0x1,
                    cv.as_string(),
                    raw,
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if let Some(v) = u8_at(data, 0x1) {
        dm.push(("CAFGridSize".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("($val >> 4) . \" \" . ($val & 0x0f)", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("$val =~ tr/ /x/; $val", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "CAFGridSize",
                0x1,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Photoshop::PixelInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn photoshop_pixelinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "Photoshop";
    const GRP1: &str = "Photoshop";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = f64_at(data, 0x4, bo) {
        dm.push(("PixelAspectRatio".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PixelAspectRatio",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::QuickTime::HTCBinary` -- FORMAT int32u, FIRST_ENTRY 0.
fn quicktime_htcbinary(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "HTC";
    const GRP2: &str = "Video";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    tags
}

/// `Image::ExifTool::QuickTime::AV1Config` -- FORMAT int8u, FIRST_ENTRY 0.
fn quicktime_av1config(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "QuickTime";
    const GRP1: &str = "QuickTime";
    const GRP2: &str = "Video";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        let v = v & 0x7f;
        dm.push((
            "AV1ConfigurationVersion".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AV1ConfigurationVersion",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xe0) >> 5;
        dm.push(("SeqProfile".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x1f;
        dm.push(("SeqLevelIdx0".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x80) >> 7;
        dm.push(("SeqTier0".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x40) >> 6;
        dm.push(("HighBitDepth".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x20) >> 5;
        dm.push(("TwelveBit".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = (v & 0x1c) >> 2;
        dm.push(("ChromaFormat".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "YUV 4:4:4".to_string(),
            2 => "YUV 4:2:2".to_string(),
            3 => "YUV 4:2:0".to_string(),
            7 => "Monochrome 4:0:0".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ChromaFormat", 0x2, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x2) {
        let v = v & 0x3;
        dm.push(("ChromaSamplePosition".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Vertical".to_string(),
            2 => "Colocated".to_string(),
            3 => "(reserved)".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ChromaSamplePosition",
            0x2,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x3) {
        dm.push(("InitialDelaySamples".to_string(), Conv::Num(f64::from(v))));
    }
    tags
}

/// `Image::ExifTool::QuickTime::ContentLightLevel` -- FORMAT int16u, FIRST_ENTRY 0.
fn quicktime_contentlightlevel(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "QuickTime";
    const GRP1: &str = "QuickTime";
    const GRP2: &str = "Video";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("MaxContentLightLevel".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MaxContentLightLevel",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push((
            "MaxPicAverageLightLevel".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MaxPicAverageLightLevel",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::QuickTime::Flip` -- FORMAT int32u, FIRST_ENTRY 0.
/// Incomplete: a field of variable length moves every entry after
/// it, and this reads them where they would be without it.
fn quicktime_flip(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "QuickTime";
    const GRP1: &str = "MakerNotes";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push(("PreviewImageWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreviewImageWidth",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push(("PreviewImageHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreviewImageHeight",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x34, bo) {
        dm.push(("PreviewImageLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PreviewImageLength",
            0xd,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x38, 16, true) {
        tags.push(mk(
            "SerialNumber",
            0xe,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::QuickTime::HEVCConfig` -- FORMAT int8u, FIRST_ENTRY 0.
fn quicktime_hevcconfig(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "QuickTime";
    const GRP1: &str = "QuickTime";
    const GRP2: &str = "Video";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x0) {
        dm.push((
            "HEVCConfigurationVersion".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "HEVCConfigurationVersion",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0xc0) >> 6;
        dm.push(("GeneralProfileSpace".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Conforming".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "GeneralProfileSpace",
            0x1,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = (v & 0x20) >> 5;
        dm.push(("GeneralTierFlag".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Main Tier".to_string(),
            1 => "High Tier".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GeneralTierFlag", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x1) {
        let v = v & 0x1f;
        dm.push(("GeneralProfileIDC".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No Profile".to_string(),
            1 => "Main".to_string(),
            2 => "Main 10".to_string(),
            3 => "Main Still Picture".to_string(),
            4 => "Format Range Extensions".to_string(),
            5 => "High Throughput".to_string(),
            6 => "Multiview Main".to_string(),
            7 => "Scalable Main".to_string(),
            8 => "3D Main".to_string(),
            9 => "Screen Content Coding Extensions".to_string(),
            10 => "Scalable Format Range Extensions".to_string(),
            11 => "High Throughput Screen Content Coding Extensions".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("GeneralProfileIDC", 0x1, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u32_at(data, 0x2, bo) {
        dm.push((
            "GenProfileCompatibilityFlags".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("Image::ExifTool::DecodeBits($val, { 31 => 'No Profile', 30 => 'Main', 29 => 'Main 10', 28 => 'Main Still Picture', 27 => 'Format Range Extensions', 26 => 'High Throughput', 25 => 'Multiview Main', 24 => 'Scalable Main', 23 => '3D Main', 22 => 'Screen Content Coding Extensions', 21 => 'Scalable Format Range Extensions', 20 => 'High Throughput Screen Content Coding Extensions' })", &cv, &ctx) { cv = x; }
        tags.push(mk(
            "GenProfileCompatibilityFlags",
            0x2,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..6 {
            match u8_at(data, 0x6 + k * 1) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "ConstraintIndicatorFlags",
                0x6,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0xc) {
        dm.push(("GeneralLevelIDC".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) =
            conv_expr::eval_with("sprintf(\"%d (level %.1f)\", $val, $val/30)", &cv, &ctx)
        {
            cv = x;
        }
        tags.push(mk(
            "GeneralLevelIDC",
            0xc,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0xd, bo) {
        let v = v & 0xfff;
        dm.push((
            "MinSpatialSegmentationIDC".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MinSpatialSegmentationIDC",
            0xd,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0xf) {
        let v = v & 0x3;
        dm.push(("ParallelismType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ParallelismType",
            0xf,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x10) {
        let v = v & 0x3;
        dm.push(("ChromaFormat".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Monochrome".to_string(),
            1 => "4:2:0".to_string(),
            2 => "4:2:2".to_string(),
            3 => "4:4:4".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("ChromaFormat", 0x10, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x11) {
        let v = v & 0x7;
        dm.push(("BitDepthLuma".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 8", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "BitDepthLuma",
                0x11,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x12) {
        let v = v & 0x7;
        dm.push(("BitDepthChroma".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val + 8", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "BitDepthChroma",
                0x12,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x13, bo) {
        dm.push(("AverageFrameRate".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 256", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "AverageFrameRate",
                0x13,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0xc0) >> 6;
        dm.push(("ConstantFrameRate".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Unknown".to_string(),
            1 => "Constant Frame Rate".to_string(),
            2 => "Each Temporal Layer is Constant Frame Rate".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "ConstantFrameRate",
            0x15,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0x38) >> 3;
        dm.push(("NumTemporalLayers".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "NumTemporalLayers",
            0x15,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x15) {
        let v = (v & 0x4) >> 2;
        dm.push(("TemporalIDNested".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "No".to_string(),
            1 => "Yes".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("TemporalIDNested", 0x15, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    tags
}

/// `Image::ExifTool::RIFF::AVIHeader` -- FORMAT int32u, FIRST_ENTRY 0.
fn riff_aviheader(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "RIFF";
    const GRP1: &str = "RIFF";
    const GRP2: &str = "Video";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x0, bo) {
        dm.push(("FrameRate".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val ? 1e6 / $val : undef", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            let mut cv = base;
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("int($val * 1000 + 0.5) / 1000", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FrameRate",
                0x0,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push(("MaxDataRate".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("
            my ($unit, $div) = $self->Options('ByteUnit') eq 'Binary' ? ('KiB/s',1024) : ('kB/s',1000);
            my $tmp = $val / $div;
            $tmp > 9999 and $tmp /= $div, $unit =~ s/^./M/;
            sprintf('%.4g %s', $tmp, $unit);
        ", &cv, &ctx) { cv = x; }
        tags.push(mk(
            "MaxDataRate",
            0x1,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x10, bo) {
        dm.push(("FrameCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FrameCount",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x18, bo) {
        dm.push(("StreamCount".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "StreamCount",
            0x6,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x20, bo) {
        dm.push(("ImageWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ImageWidth",
            0x8,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x24, bo) {
        dm.push(("ImageHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ImageHeight",
            0x9,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Reconyx::HyperFire` -- FORMAT int16u, FIRST_ENTRY 0.
fn reconyx_hyperfire(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Reconyx";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("MakerNoteVersion".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("sprintf(\"0x%.4x\", $val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "MakerNoteVersion",
            0x0,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x2 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$val=~tr/ /./; $val", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "FirmwareVersion",
                0x1,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x8 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "
            my @v = split(' ',$val);
            sprintf('%.4x:%.2x:%.2x', $v[0], $v[1]>>8, $v[1]&0xff);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareDate",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0xc, 2, true) {
        tags.push(mk(
            "TriggerMode",
            0x6,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0xe + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$val =~ s/ / of /; $val", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "Sequence",
                0x7,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x12 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) =
                conv_expr::eval_with("my @v=split(\" \",$val); ($v[0]<<16) + $v[1]", &cv, &ctx)
            {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "EventNumber",
                0x9,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..6 {
            match u16_at(data, 0x16 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "
            my @a = split ' ', $val;

            if ($a[0] & 0xff00 and not $a[0] & 0xff) {
                $_ = ($_ >> 8) | (($_ & 0xff) << 8) foreach @a;
            }
            sprintf('%.4d:%.2d:%.2d %.2d:%.2d:%.2d', @a[5,3,4,2,1,0]);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "DateTimeOriginal",
                0xb,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x24, bo) {
        dm.push(("MoonPhase".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "New".to_string(),
            1 => "New Crescent".to_string(),
            2 => "First Quarter".to_string(),
            3 => "Waxing Gibbous".to_string(),
            4 => "Full".to_string(),
            5 => "Waning Gibbous".to_string(),
            6 => "Last Quarter".to_string(),
            7 => "Old Crescent".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MoonPhase", 0x12, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0x26, bo) {
        dm.push((
            "AmbientTemperatureFahrenheit".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val F\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "AmbientTemperatureFahrenheit",
            0x13,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x28, bo) {
        dm.push(("AmbientTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "AmbientTemperature",
            0x14,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = utf16_at(data, 0x2a, 30, bo) {
        tags.push(mk(
            "SerialNumber",
            0x15,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x48, bo) {
        dm.push(("Contrast".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Contrast",
            0x24,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4a, bo) {
        dm.push(("Brightness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Brightness",
            0x25,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4c, bo) {
        dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Sharpness",
            0x26,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4e, bo) {
        dm.push(("Saturation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Saturation",
            0x27,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x50, bo) {
        dm.push(("InfraredIlluminator".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk(
            "InfraredIlluminator",
            0x28,
            s,
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x52, bo) {
        dm.push(("MotionSensitivity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MotionSensitivity",
            0x29,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x54, bo) {
        dm.push(("BatteryVoltage".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val V\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "BatteryVoltage",
                0x2a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x56, 22, true) {
        tags.push(mk(
            "UserLabel",
            0x2b,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Reconyx::HyperFire2` -- FORMAT int8u, FIRST_ENTRY 0.
fn reconyx_hyperfire2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Reconyx";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x10, bo) {
        dm.push(("FileNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FileNumber",
            0x10,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x12, bo) {
        dm.push(("DirectoryNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "DirectoryNumber",
            0x12,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x14 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "
            my @a = split / /, $val;
            sprintf('%.4d:%.2d:%.2d %.2d:%.2d:%.2d',
                ($a[0] >> 9)  + 1980, # year
                ($a[0] >> 5)  & 0x0f, # month
                 $a[0]        & 0x1f, # day
                ($a[1] >> 11) & 0x1f, # hour
                ($a[1] >> 5)  & 0x3f, # minute
                ($a[1]        & 0x1f) * 2  # second
            );
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "DirectoryCreateDate",
                0x14,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x2a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "my @a = split \" \",$val; sprintf(\"%d.%d%c\",@a)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x2a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x30 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "my ($y,$d) = split \" \", $val; sprintf(\"%.4x:%.2x:%.2x\",$y,$d>>8,$d&0xff)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareDate",
                0x30,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x34, 2, true) {
        tags.push(mk(
            "TriggerMode",
            0x34,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x36 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$val =~ s/ / of /; $val", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "Sequence",
                0x36,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x3a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) =
                conv_expr::eval_with("my @a=split \" \",$val;($a[0]<<16)+$a[1]", &cv, &ctx)
            {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "EventNumber",
                0x3a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..6 {
            match u16_at(data, 0x3e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "
            my @a = split ' ', $val;
            sprintf('%.4d:%.2d:%.2d %.2d:%.2d:%.2d', reverse @a);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "DateTimeOriginal",
                0x3e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x4a, bo) {
        dm.push(("DayOfWeek".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Sunday".to_string(),
            1 => "Monday".to_string(),
            2 => "Tuesday".to_string(),
            3 => "Wednesday".to_string(),
            4 => "Thursday".to_string(),
            5 => "Friday".to_string(),
            6 => "Saturday".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DayOfWeek", 0x4a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x4c, bo) {
        dm.push(("MoonPhase".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "New".to_string(),
            1 => "New Crescent".to_string(),
            2 => "First Quarter".to_string(),
            3 => "Waxing Gibbous".to_string(),
            4 => "Full".to_string(),
            5 => "Waning Gibbous".to_string(),
            6 => "Last Quarter".to_string(),
            7 => "Old Crescent".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MoonPhase", 0x4c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0x4e, bo) {
        dm.push((
            "AmbientTemperatureFahrenheit".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val F\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "AmbientTemperatureFahrenheit",
            0x4e,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x50, bo) {
        dm.push(("AmbientTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "AmbientTemperature",
            0x50,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x52, bo) {
        dm.push(("Contrast".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Contrast",
            0x52,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x54, bo) {
        dm.push(("Brightness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Brightness",
            0x54,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x56, bo) {
        dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Sharpness",
            0x56,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x58, bo) {
        dm.push(("Saturation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Saturation",
            0x58,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x5a, bo) {
        dm.push(("Flash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Flash",
            0x5a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x5c, bo) {
        dm.push(("AmbientInfrared".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AmbientInfrared",
            0x5c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x5e, bo) {
        dm.push(("AmbientLight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AmbientLight",
            0x5e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x60, bo) {
        dm.push(("MotionSensitivity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MotionSensitivity",
            0x60,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x62, bo) {
        dm.push(("BatteryVoltage".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "BatteryVoltage",
            0x62,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x64, bo) {
        dm.push(("BatteryVoltageAvg".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "BatteryVoltageAvg",
            0x64,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x66, bo) {
        dm.push(("BatteryType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "BatteryType",
            0x66,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x68, 22, true) {
        tags.push(mk(
            "UserLabel",
            0x68,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = utf16_at(data, 0x7e, 30, bo) {
        tags.push(mk(
            "SerialNumber",
            0x7e,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Reconyx::HyperFire4K` -- FORMAT int8u, FIRST_ENTRY 0.
fn reconyx_hyperfire4k(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Reconyx";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x1a, 7, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with(
            "sprintf(\"%.2x.%.2x %.4x:%.2x:%.2x Rev.%s\", unpack(\"CCvCCa\", $val))",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        tags.push(mk(
            "FirmwareVersion",
            0x1a,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x21, 7, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with(
            "sprintf(\"%.2x.%.2x %.4x:%.2x:%.2x Rev.%s\", unpack(\"CCvCCa\", $val))",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        tags.push(mk(
            "UIBFirmwareVersion",
            0x21,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x28, 1, false) {
        tags.push(mk(
            "TriggerMode",
            0x28,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u8_at(data, 0x29 + k * 1) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$val =~ s/ / of /; $val", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "Sequence",
                0x29,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x2b, bo) {
        dm.push(("EventNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "EventNumber",
            0x2b,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x2f, 7, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with(
            "sprintf(\"%.4d:%.2d:%.2d %.2d:%.2d:%.2d\", reverse unpack(\"C5v\",$val))",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "DateTimeOriginal",
            0x2f,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x36) {
        dm.push(("DayOfWeek".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "Sunday".to_string(),
            2 => "Monday".to_string(),
            3 => "Tuesday".to_string(),
            4 => "Wednesday".to_string(),
            5 => "Thursday".to_string(),
            6 => "Friday".to_string(),
            7 => "Saturday".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DayOfWeek", 0x36, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x37) {
        dm.push(("MoonPhase".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "New".to_string(),
            1 => "New Crescent".to_string(),
            2 => "First Quarter".to_string(),
            3 => "Waxing Gibbous".to_string(),
            4 => "Full".to_string(),
            5 => "Waning Gibbous".to_string(),
            6 => "Last Quarter".to_string(),
            7 => "Old Crescent".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MoonPhase", 0x37, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0x38, bo) {
        dm.push((
            "AmbientTemperatureFahrenheit".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val F\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "AmbientTemperatureFahrenheit",
            0x38,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x3a, bo) {
        dm.push(("AmbientTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "AmbientTemperature",
            0x3a,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x3c, bo) {
        dm.push(("Contrast".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Contrast",
            0x3c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x3e, bo) {
        dm.push(("Brightness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Brightness",
            0x3e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x40, bo) {
        dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Sharpness",
            0x40,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x42, bo) {
        dm.push(("Saturation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Saturation",
            0x42,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x44) {
        dm.push(("Flash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Flash",
            0x44,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x45, bo) {
        dm.push(("AmbientLight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AmbientLight",
            0x45,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x49, bo) {
        dm.push(("MotionSensitivity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MotionSensitivity",
            0x49,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4b, bo) {
        dm.push(("BatteryVoltage".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "BatteryVoltage",
                0x4b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x4d, bo) {
        dm.push(("BatteryVoltageAvg".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "BatteryVoltageAvg",
                0x4d,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x4f, bo) {
        dm.push(("BatteryType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "NiMH".to_string(),
            2 => "Lithium".to_string(),
            3 => "External".to_string(),
            4 => "SC10 Solar".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("BatteryType", 0x4f, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(text) = text_at(data, 0x51, 51, true) {
        tags.push(mk(
            "UserLabel",
            0x51,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x84, 15, true) {
        tags.push(mk(
            "SerialNumber",
            0x84,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x93, bo) {
        dm.push(("DirectoryNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "DirectoryNumber",
            0x93,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x95, bo) {
        dm.push(("FileNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FileNumber",
            0x95,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Reconyx::MicroFire` -- FORMAT int8u, FIRST_ENTRY 0.
fn reconyx_microfire(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Reconyx";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x10, bo) {
        dm.push(("FileNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FileNumber",
            0x10,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x12, bo) {
        dm.push(("DirectoryNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "DirectoryNumber",
            0x12,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x14 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "
            my @a = split / /, $val;
            sprintf('%.4d:%.2d:%.2d %.2d:%.2d:%.2d',
                ($a[0] >> 9)  + 1980, # year
                ($a[0] >> 5)  & 0x0f, # month
                 $a[0]        & 0x1f, # day
                ($a[1] >> 11) & 0x1f, # hour
                ($a[1] >> 5)  & 0x3f, # minute
                ($a[1]        & 0x1f) * 2  # second
            );
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "DirectoryCreateDate",
                0x14,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..8 {
            match u16_at(data, 0x18 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with(
                "join \"-\", map sprintf(\"%.4x\",$_), split / /, $val",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            tags.push(mk(
                "SDCardID",
                0x18,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x2a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "my @a = split \" \",$val; sprintf(\"%d.%d%c\",@a)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareVersion",
                0x2a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x30 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "my ($y,$d) = split \" \", $val; sprintf(\"%.4x:%.2x:%.2x\",$y,$d>>8,$d&0xff)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "FirmwareDate",
                0x30,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x34 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "my @a = split \" \",$val; sprintf(\"%d.%d%c\",@a)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "BluetoothFirmwareVersion",
                0x34,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x3a, bo) {
        dm.push(("BluetoothFirmwareDate".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with(
            "
            sprintf('%.4d:%.2d:%.2d',
                ($val >> 9)  + 1980, # year
                ($val >> 5)  & 0x0f, # month
                 $val        & 0x1f, # day
            );
        ",
            &cv,
            &ctx,
        );
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "BluetoothFirmwareDate",
                0x3a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..3 {
            match u16_at(data, 0x3c + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "my @a = split \" \",$val; sprintf(\"%d.%d%c\",@a)",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "WifiFirmwareVersion",
                0x3c,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x42, bo) {
        dm.push(("WifiFirmwareDate".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with(
            "
            sprintf('%.4d:%.2d:%.2d',
                ($val >> 9)  + 1980, # year
                ($val >> 5)  & 0x0f, # month
                 $val        & 0x1f, # day
            );
        ",
            &cv,
            &ctx,
        );
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "WifiFirmwareDate",
                0x42,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x44, 2, true) {
        tags.push(mk(
            "TriggerMode",
            0x44,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x46 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$val =~ s/ / of /; $val", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "Sequence",
                0x46,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0x4a + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) =
                conv_expr::eval_with("my @a=split \" \",$val;($a[0]<<16)+$a[1]", &cv, &ctx)
            {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "EventNumber",
                0x4a,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..6 {
            match u16_at(data, 0x4e + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "
            my @a = split ' ', $val;
            sprintf('%.4d:%.2d:%.2d %.2d:%.2d:%.2d', @a[5,3,4,2,1,0]);
        ",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "DateTimeOriginal",
                0x4e,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x5a, bo) {
        dm.push(("DayOfWeek".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            1 => "Sunday".to_string(),
            2 => "Monday".to_string(),
            3 => "Tuesday".to_string(),
            4 => "Wednesday".to_string(),
            5 => "Thursday".to_string(),
            6 => "Friday".to_string(),
            7 => "Saturday".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DayOfWeek", 0x5a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x5c, bo) {
        dm.push(("MoonPhase".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "New".to_string(),
            1 => "New Crescent".to_string(),
            2 => "First Quarter".to_string(),
            3 => "Waxing Gibbous".to_string(),
            4 => "Full".to_string(),
            5 => "Waning Gibbous".to_string(),
            6 => "Last Quarter".to_string(),
            7 => "Old Crescent".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MoonPhase", 0x5c, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0x5e, bo) {
        dm.push((
            "AmbientTemperatureFahrenheit".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val F\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "AmbientTemperatureFahrenheit",
            0x5e,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x60, bo) {
        dm.push(("AmbientTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "AmbientTemperature",
            0x60,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x62, bo) {
        dm.push(("Contrast".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Contrast",
            0x62,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x64, bo) {
        dm.push(("Brightness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Brightness",
            0x64,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x66, bo) {
        dm.push(("Sharpness".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Sharpness",
            0x66,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x68, bo) {
        dm.push(("Saturation".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Saturation",
            0x68,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x6a) {
        dm.push(("Flash".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "Flash",
            0x6a,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x6c, bo) {
        dm.push(("AmbientInfrared".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AmbientInfrared",
            0x6c,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x6e, bo) {
        dm.push(("AmbientLight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "AmbientLight",
            0x6e,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x70, bo) {
        dm.push(("MotionSensitivity".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "MotionSensitivity",
            0x70,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x72, bo) {
        dm.push(("BatteryVoltage".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "BatteryVoltage",
                0x72,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x74, bo) {
        dm.push(("BatteryType".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Lithium".to_string(),
            1 => "NiMH".to_string(),
            2 => "Alkaline".to_string(),
            3 => "Lead Acid".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("BatteryType", 0x74, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(text) = text_at(data, 0x76, 22, true) {
        tags.push(mk(
            "UserLabel",
            0x76,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = utf16_at(data, 0x8c, 30, bo) {
        tags.push(mk(
            "SerialNumber",
            0x8c,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Reconyx::UltraFire` -- FORMAT int8u, FIRST_ENTRY 0.
fn reconyx_ultrafire(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Reconyx";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x18, 7, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with(
            "sprintf(\"%.2x.%.2x %.4x:%.2x:%.2x Rev.%s\", unpack(\"CCvCCa\", $val))",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        tags.push(mk(
            "FirmwareVersion",
            0x18,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x1f, 7, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with(
            "sprintf(\"%.2x.%.2x %.4x:%.2x:%.2x Rev.%s\", unpack(\"CCvCCa\", $val))",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        tags.push(mk(
            "Micro1Version",
            0x1f,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x26, 7, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with(
            "sprintf(\"%.2x.%.2x %.4x:%.2x:%.2x Rev.%s\", unpack(\"CCvCCa\", $val))",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        tags.push(mk(
            "BootLoaderVersion",
            0x26,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x2d, 7, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        if let Some(x) = conv_expr::eval_with(
            "sprintf(\"%.2x.%.2x %.4x:%.2x:%.2x Rev.%s\", unpack(\"CCvCCa\", $val))",
            &cv,
            &ctx,
        ) {
            cv = x;
        }
        let raw = Value::String(cv.as_string());
        tags.push(mk(
            "Micro2Version",
            0x2d,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x34, 1, false) {
        tags.push(mk(
            "TriggerMode",
            0x34,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u8_at(data, 0x35 + k * 1) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$val =~ s/ / of /; $val", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "Sequence",
                0x35,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u32_at(data, 0x37, bo) {
        dm.push(("EventNumber".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "EventNumber",
            0x37,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..7 {
            match u8_at(data, 0x3b + k * 1) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            let ctx = Ctx {
                make,
                model,
                file_type,
                dm,
            };
            let mut cv = Conv::Str(s.clone());
            if let Some(x) = conv_expr::eval_with(
                "sprintf(\"%.4d:%.2d:%.2d %.2d:%.2d:%.2d\", reverse unpack(\"C5v\",$val))",
                &cv,
                &ctx,
            ) {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            if let Some(x) = conv_expr::eval_with("$self->ConvertDateTime($val)", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "DateTimeOriginal",
                0x3b,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u8_at(data, 0x42) {
        dm.push(("DayOfWeek".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Sunday".to_string(),
            1 => "Monday".to_string(),
            2 => "Tuesday".to_string(),
            3 => "Wednesday".to_string(),
            4 => "Thursday".to_string(),
            5 => "Friday".to_string(),
            6 => "Saturday".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("DayOfWeek", 0x42, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u8_at(data, 0x43) {
        dm.push(("MoonPhase".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "New".to_string(),
            1 => "New Crescent".to_string(),
            2 => "First Quarter".to_string(),
            3 => "Waxing Gibbous".to_string(),
            4 => "Full".to_string(),
            5 => "Waning Gibbous".to_string(),
            6 => "Last Quarter".to_string(),
            7 => "Old Crescent".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("MoonPhase", 0x43, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = i16_at(data, 0x44, bo) {
        dm.push((
            "AmbientTemperatureFahrenheit".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val F\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "AmbientTemperatureFahrenheit",
            0x44,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = i16_at(data, 0x46, bo) {
        dm.push(("AmbientTemperature".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let raw = Value::F64(cv.as_num());
        if let Some(x) = conv_expr::eval_with("\"$val C\"", &cv, &ctx) {
            cv = x;
        }
        tags.push(mk(
            "AmbientTemperature",
            0x46,
            cv.as_string(),
            raw,
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x48) {
        dm.push(("Illumination".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "On".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("Illumination", 0x48, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x49, bo) {
        dm.push(("BatteryVoltage".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val / 1000", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            if let Some(x) = conv_expr::eval_with("\"$val V\"", &cv, &ctx) {
                cv = x;
            }
            tags.push(mk(
                "BatteryVoltage",
                0x49,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(text) = text_at(data, 0x4b, 15, true) {
        tags.push(mk(
            "SerialNumber",
            0x4b,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x5a, 21, true) {
        tags.push(mk(
            "UserLabel",
            0x5a,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Ricoh::FaceInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn ricoh_faceinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Ricoh";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0xb5) {
        dm.push(("FacesDetected".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let rc = conv_expr::eval_with("$val", &base, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            let base = rc.unwrap_or(base);
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "FacesDetected",
                0xb5,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    {
        let mut parts = Vec::new();
        for k in 0..2 {
            match u16_at(data, 0xb6 + k * 2, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "FaceDetectFrameSize",
                0xb6,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 1.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0xbc + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face1Position",
                    0xbc,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 2.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0xc8 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face2Position",
                    0xc8,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 3.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0xd4 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face3Position",
                    0xd4,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 4.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0xe0 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face4Position",
                    0xe0,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 5.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0xec + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face5Position",
                    0xec,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 6.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0xf8 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face6Position",
                    0xf8,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 7.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x104 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face7Position",
                    0x104,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    if dm_get(dm, "FacesDetected").is_some_and(|v| v >= 8.0) {
        {
            let mut parts = Vec::new();
            for k in 0..4 {
                match u16_at(data, 0x110 + k * 2, bo) {
                    Some(x) => parts.push(x.to_string()),
                    None => {
                        parts.clear();
                        break;
                    }
                }
            }
            if !parts.is_empty() {
                let s = parts.join(" ");
                tags.push(mk(
                    "Face8Position",
                    0x110,
                    s.clone(),
                    Value::String(s),
                    GRP0,
                    GRP1,
                    GRP2,
                    PRIO,
                ));
            }
        }
    }
    tags
}

/// `Image::ExifTool::Ricoh::AVI` -- FORMAT int8u, FIRST_ENTRY 0.
fn ricoh_avi(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Ricoh";
    const GRP2: &str = "Video";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    tags
}

/// `Image::ExifTool::Ricoh::Subdir` -- FORMAT int8u, FIRST_ENTRY 0.
fn ricoh_subdir(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Ricoh";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u8_at(data, 0x4) {
        dm.push(("ManufactureDate1".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ManufactureDate1",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x5) {
        dm.push(("ManufactureDate2".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ManufactureDate2",
            0x5,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u8_at(data, 0x2a) {
        dm.push(("NoiseReduction".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Off".to_string(),
            1 => "Weak".to_string(),
            2 => "Strong".to_string(),
            3 => "Max".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("NoiseReduction", 0x2a, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(sub) = data.get(0x1a..) {
        tags.extend(ricoh_faceinfo(sub, make, model, bo, file_type, format, dm));
    }
    if let Some(sub) = data.get(0x29..) {
        tags.extend(ricoh_firmwareinfo(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    if let Some(sub) = data.get(0x2c..) {
        tags.extend(ricoh_serialinfo(
            sub, make, model, bo, file_type, format, dm,
        ));
    }
    tags
}

/// `Image::ExifTool::Ricoh::FirmwareInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn ricoh_firmwareinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Ricoh";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 12, true) {
        tags.push(mk(
            "FirmwareRevision",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0xc, 12, true) {
        tags.push(mk(
            "FirmwareRevision2",
            0xc,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Ricoh::SerialInfo` -- FORMAT int8u, FIRST_ENTRY 0.
fn ricoh_serialinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Ricoh";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x0, 16, true) {
        tags.push(mk(
            "BodyFirmware",
            0x0,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x10, 16, true) {
        tags.push(mk(
            "BodySerialNumber",
            0x10,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x20, 16, true) {
        tags.push(mk(
            "LensFirmware",
            0x20,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(text) = text_at(data, 0x30, 16, true) {
        tags.push(mk(
            "LensSerialNumber",
            0x30,
            text.clone(),
            Value::String(text),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Samsung::DualShotExtra` -- FORMAT int32u, FIRST_ENTRY 0.
fn samsung_dualshotextra(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Samsung";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(text) = text_at(data, 0x20, 64, false) {
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let mut cv = Conv::Str(text);
        let rc = conv_expr::eval_with("undef", &cv, &ctx);
        if rc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = rc {
                cv = x;
            }
            let raw = Value::String(cv.as_string());
            tags.push(mk(
                "DualShotDummy",
                0x8,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "DepthMapTagPos").is_some_and(conv_expr::Val::truthy) {
        if let Some(v) = u32_at(data, 0x40, bo) {
            dm.push(("DepthMapWidth".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "DepthMapWidth",
                0x10,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if dm_val(dm, "DepthMapTagPos").is_some_and(conv_expr::Val::truthy) {
        if let Some(v) = u32_at(data, 0x44, bo) {
            dm.push(("DepthMapHeight".to_string(), Conv::Num(f64::from(v))));
            let base = Conv::Num(f64::from(v));
            #[allow(clippy::cast_possible_truncation)]
            tags.push(mk(
                "DepthMapHeight",
                0x11,
                base.as_string(),
                Value::I32(base.as_num() as i32),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Samsung::Thumbnail2` -- FORMAT int32u, FIRST_ENTRY 0.
fn samsung_thumbnail2(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Samsung";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push(("ThumbnailWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailWidth",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push(("ThumbnailHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailHeight",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xc, bo) {
        dm.push(("ThumbnailLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailLength",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x10, bo) {
        dm.push(("ThumbnailOffset".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailOffset",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Samsung::Thumbnail` -- FORMAT int32u, FIRST_ENTRY 0.
fn samsung_thumbnail(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Samsung";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push(("ThumbnailWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailWidth",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push(("ThumbnailHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailHeight",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xc, bo) {
        dm.push(("ThumbnailLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailLength",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x10, bo) {
        dm.push(("ThumbnailOffset".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailOffset",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Samsung::OrientationInfo` -- FORMAT rational64s, FIRST_ENTRY 0.
fn samsung_orientationinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Samsung";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = rat64s_at(data, 0x0, bo) {
        dm.push(("YawAngle".to_string(), Conv::Num(f64::from(v))));
    }
    if let Some(v) = rat64s_at(data, 0x8, bo) {
        dm.push(("PitchAngle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PitchAngle",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = rat64s_at(data, 0x10, bo) {
        dm.push(("RollAngle".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "RollAngle",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// `Image::ExifTool::Samsung::PictureWizard` -- FORMAT int16u, FIRST_ENTRY 0.
fn samsung_picturewizard(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Samsung";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u16_at(data, 0x0, bo) {
        dm.push(("PictureWizardMode".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        let s = match base.as_num() as i64 {
            0 => "Standard".to_string(),
            1 => "Vivid".to_string(),
            2 => "Portrait".to_string(),
            3 => "Landscape".to_string(),
            4 => "Forest".to_string(),
            5 => "Retro".to_string(),
            6 => "Cool".to_string(),
            7 => "Calm".to_string(),
            8 => "Classic".to_string(),
            9 => "Custom1".to_string(),
            10 => "Custom2".to_string(),
            11 => "Custom3".to_string(),
            255 => "n/a".to_string(),
            other => other.to_string(),
        };
        #[allow(clippy::cast_possible_truncation)]
        let raw = Value::I32(base.as_num() as i32);
        tags.push(mk("PictureWizardMode", 0x0, s, raw, GRP0, GRP1, GRP2, PRIO));
    }
    if let Some(v) = u16_at(data, 0x2, bo) {
        dm.push(("PictureWizardColor".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "PictureWizardColor",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u16_at(data, 0x4, bo) {
        dm.push((
            "PictureWizardSaturation".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 4", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "PictureWizardSaturation",
                0x2,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x6, bo) {
        dm.push((
            "PictureWizardSharpness".to_string(),
            Conv::Num(f64::from(v)),
        ));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 4", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "PictureWizardSharpness",
                0x3,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    if let Some(v) = u16_at(data, 0x8, bo) {
        dm.push(("PictureWizardContrast".to_string(), Conv::Num(f64::from(v))));
        let ctx = Ctx {
            make,
            model,
            file_type,
            dm,
        };
        let base = Conv::Num(f64::from(v));
        let mut cv = base;
        let vc = conv_expr::eval_with("$val - 4", &cv, &ctx);
        if vc.as_ref() != Some(&Conv::Undef) {
            if let Some(x) = vc {
                cv = x;
            }
            let raw = Value::F64(cv.as_num());
            tags.push(mk(
                "PictureWizardContrast",
                0x4,
                cv.as_string(),
                raw,
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Sanyo::FaceInfo` -- FORMAT int32u, FIRST_ENTRY 0.
fn sanyo_faceinfo(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Sanyo";
    const GRP2: &str = "Image";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x0, bo) {
        dm.push(("FacesDetected".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "FacesDetected",
            0x0,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    {
        let mut parts = Vec::new();
        for k in 0..4 {
            match u32_at(data, 0x10 + k * 4, bo) {
                Some(x) => parts.push(x.to_string()),
                None => {
                    parts.clear();
                    break;
                }
            }
        }
        if !parts.is_empty() {
            let s = parts.join(" ");
            tags.push(mk(
                "FacePosition",
                0x4,
                s.clone(),
                Value::String(s),
                GRP0,
                GRP1,
                GRP2,
                PRIO,
            ));
        }
    }
    tags
}

/// `Image::ExifTool::Sanyo::Thumbnail` -- FORMAT int32u, FIRST_ENTRY 0.
fn sanyo_thumbnail(
    data: &[u8],
    make: &str,
    model: &str,
    bo: ByteOrder,
    file_type: &str,
    format: &str,
    dm: &mut State,
) -> Vec<Tag> {
    const GRP0: &str = "MakerNotes";
    const GRP1: &str = "Sanyo";
    const GRP2: &str = "Camera";
    const PRIO: i32 = 0;
    let mut tags = Vec::new();
    let _ = (data, make, model, bo, file_type, format, &dm);
    if let Some(v) = u32_at(data, 0x4, bo) {
        dm.push(("ThumbnailWidth".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailWidth",
            0x1,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x8, bo) {
        dm.push(("ThumbnailHeight".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailHeight",
            0x2,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0xc, bo) {
        dm.push(("ThumbnailLength".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailLength",
            0x3,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    if let Some(v) = u32_at(data, 0x10, bo) {
        dm.push(("ThumbnailOffset".to_string(), Conv::Num(f64::from(v))));
        let base = Conv::Num(f64::from(v));
        #[allow(clippy::cast_possible_truncation)]
        tags.push(mk(
            "ThumbnailOffset",
            0x4,
            base.as_string(),
            Value::I32(base.as_num() as i32),
            GRP0,
            GRP1,
            GRP2,
            PRIO,
        ));
    }
    tags
}

/// Which sub-table a Main-table id opens, by the conditions ExifTool writes
/// on it.
///
/// `None` means no arm matched, which for an id whose arms are all
/// sub-directories means ExifTool extracts nothing at all.
#[must_use]
pub fn variant_for(
    module: &str,
    tag: u16,
    make: &str,
    model: &str,
    data: &[u8],
    count: usize,
    format: &str,
) -> Option<&'static str> {
    let _ = (make, model, data, count, format);
    match (module, tag) {
        ("Canon", 0x000d) => {
            if (count != 0 && MODEL_RE_32.is_match(model)) {
                return Some("Canon::CameraInfo1D");
            }
            if MODEL_RE_33.is_match(model) {
                return Some("Canon::CameraInfo1DmkII");
            }
            if MODEL_RE_34.is_match(model) {
                return Some("Canon::CameraInfo1DmkIIN");
            }
            if MODEL_RE_35.is_match(model) {
                return Some("Canon::CameraInfo1DmkIII");
            }
            if MODEL_RE_36.is_match(model) {
                return Some("Canon::CameraInfo1DmkIV");
            }
            if MODEL_RE_37.is_match(model) {
                return Some("Canon::CameraInfo1DX");
            }
            if MODEL_RE_38.is_match(model) {
                return Some("Canon::CameraInfo5D");
            }
            if MODEL_RE_39.is_match(model) {
                return Some("Canon::CameraInfo5DmkII");
            }
            if MODEL_RE_40.is_match(model) {
                return Some("Canon::CameraInfo5DmkIII");
            }
            if MODEL_RE_41.is_match(model) {
                return Some("Canon::CameraInfo6D");
            }
            if MODEL_RE_42.is_match(model) {
                return Some("Canon::CameraInfo7D");
            }
            if MODEL_RE_43.is_match(model) {
                return Some("Canon::CameraInfo40D");
            }
            if MODEL_RE_44.is_match(model) {
                return Some("Canon::CameraInfo50D");
            }
            if MODEL_RE_10.is_match(model) {
                return Some("Canon::CameraInfo60D");
            }
            if MODEL_RE_45.is_match(model) {
                return Some("Canon::CameraInfo70D");
            }
            if MODEL_RE_46.is_match(model) {
                return Some("Canon::CameraInfo80D");
            }
            if MODEL_RE_47.is_match(model) {
                return Some("Canon::CameraInfo450D");
            }
            if MODEL_RE_48.is_match(model) {
                return Some("Canon::CameraInfo500D");
            }
            if MODEL_RE_49.is_match(model) {
                return Some("Canon::CameraInfo550D");
            }
            if MODEL_RE_50.is_match(model) {
                return Some("Canon::CameraInfo600D");
            }
            if MODEL_RE_51.is_match(model) {
                return Some("Canon::CameraInfo650D");
            }
            if MODEL_RE_52.is_match(model) {
                return Some("Canon::CameraInfo650D");
            }
            if MODEL_RE_53.is_match(model) {
                return Some("Canon::CameraInfo750D");
            }
            if MODEL_RE_54.is_match(model) {
                return Some("Canon::CameraInfo750D");
            }
            if MODEL_RE_55.is_match(model) {
                return Some("Canon::CameraInfo1000D");
            }
            if MODEL_RE_56.is_match(model) {
                return Some("Canon::CameraInfo600D");
            }
            if MODEL_RE_11.is_match(model) {
                return Some("Canon::CameraInfo60D");
            }
            if MODEL_RE_57.is_match(model) {
                return Some("Canon::CameraInfoR6");
            }
            if MODEL_RE_58.is_match(model) {
                return Some("Canon::CameraInfoR6m2");
            }
            if MODEL_RE_59.is_match(model) {
                return Some("Canon::CameraInfoR6m3");
            }
            if MODEL_RE_60.is_match(model) {
                return Some("Canon::CameraInfoG5XII");
            }
            if (format == "int32u" && (count == 138 || count == 148)) {
                return Some("Canon::CameraInfoPowerShot");
            }
            if (format == "int32u"
                && (count == 156
                    || (count == 162 || (count == 167 || (count == 171 || count == 264)))))
            {
                return Some("Canon::CameraInfoPowerShot2");
            }
            if format.starts_with("int32") {
                return Some("Canon::CameraInfoUnknown32");
            }
            if format.starts_with("int16") {
                return Some("Canon::CameraInfoUnknown16");
            }
            Some("Canon::CameraInfoUnknown")
        }
        ("Canon", 0x4001) => {
            if count == 582 {
                return Some("Canon::ColorData1");
            }
            if count == 653 {
                return Some("Canon::ColorData2");
            }
            if count == 796 {
                return Some("Canon::ColorData3");
            }
            if (count == 692
                || (count == 674
                    || (count == 702
                        || (count == 1227
                            || (count == 1250
                                || (count == 1251
                                    || (count == 1337 || (count == 1338 || count == 1346))))))))
            {
                return Some("Canon::ColorData4");
            }
            if count == 5120 {
                return Some("Canon::ColorData5");
            }
            if (count == 1273 || count == 1275) {
                return Some("Canon::ColorData6");
            }
            if (count == 1312 || (count == 1313 || (count == 1316 || count == 1506))) {
                return Some("Canon::ColorData7");
            }
            if (count == 1560 || (count == 1592 || (count == 1353 || count == 1602))) {
                return Some("Canon::ColorData8");
            }
            if (count == 1816 || (count == 1820 || count == 1824)) {
                return Some("Canon::ColorData9");
            }
            if (count == 2024 || count == 3656) {
                return Some("Canon::ColorData10");
            }
            if ((count == 3973 || count == 3778)
                && prefix_matches(data.get(0..).unwrap_or(&[]), &[Some(&[(48, 64)])]))
            {
                return Some("Canon::ColorData11");
            }
            if (count == 4528 || count == 3778) {
                return Some("Canon::ColorData12");
            }
            Some("Canon::ColorDataUnknown")
        }
        ("Canon", 0x0096) => {
            if MODEL_RE_61.is_match(model) {
                return Some("Canon::SerialInfo");
            }
            None
        }
        ("Canon", 0x4015) => {
            if (prefix_matches(data.get(0..).unwrap_or(&[]), &[Some(&[(0, 0)])])
                && !prefix_matches_any(
                    data.get(0..).unwrap_or(&[]),
                    &[
                        &[
                            Some(&[(0, 0)]),
                            Some(&[(0, 0)]),
                            Some(&[(0, 0)]),
                            Some(&[(0, 0)]),
                        ],
                        &[
                            Some(&[(0, 0)]),
                            Some(&[(64, 64)]),
                            Some(&[(220, 220)]),
                            Some(&[(5, 5)]),
                        ],
                    ],
                ))
            {
                return Some("Canon::VignettingCorr");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[Some(&[(1, 1), (2, 2), (16, 16), (32, 32)])],
            ) && !prefix_matches_any(
                data.get(0..).unwrap_or(&[]),
                &[
                    &[
                        Some(&[(0, 0)]),
                        Some(&[(0, 0)]),
                        Some(&[(0, 0)]),
                        Some(&[(0, 0)]),
                    ],
                    &[
                        Some(&[(2, 2)]),
                        Some(&[(80, 80)]),
                        Some(&[(124, 124)]),
                        Some(&[(4, 4)]),
                    ],
                ],
            )) {
                return Some("Canon::VignettingCorrUnknown");
            }
            if !prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(0, 0)]),
                    Some(&[(0, 0)]),
                    Some(&[(0, 0)]),
                    Some(&[(0, 0)]),
                ],
            ) {
                return Some("Canon::VignettingCorrUnknown");
            }
            None
        }
        ("Nikon", 0x0091) => {
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(48, 48)]),
                    Some(&[(57, 57)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD40");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(48, 48)]),
                    Some(&[(56, 56)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD80");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(49, 49)]),
                    Some(&[(51, 51)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD90");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(49, 49)]),
                    Some(&[(48, 48)]),
                ],
            ) && count == 5399)
            {
                return Some("Nikon::ShotInfoD3a");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(49, 49)]),
                    Some(&[(48, 48)]),
                ],
            ) && (count == 5408 || count == 5412))
            {
                return Some("Nikon::ShotInfoD3b");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(49, 49)]),
                    Some(&[(52, 52)]),
                ],
            ) && count == 5409)
            {
                return Some("Nikon::ShotInfoD3X");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(49, 49)]),
                    Some(&[(56, 56)]),
                ],
            ) && (count == 5356 || count == 5388))
            {
                return Some("Nikon::ShotInfoD3S");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(49, 49)]),
                    Some(&[(48, 48)]),
                ],
            ) && count == 5291)
            {
                return Some("Nikon::ShotInfoD300a");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(49, 49)]),
                    Some(&[(48, 48)]),
                ],
            ) && count == 5303)
            {
                return Some("Nikon::ShotInfoD300b");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(49, 49)]),
                    Some(&[(54, 54)]),
                ],
            ) && count == 5311)
            {
                return Some("Nikon::ShotInfoD300S");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(49, 49)]),
                    Some(&[(50, 50)]),
                ],
            ) && count == 5312)
            {
                return Some("Nikon::ShotInfoD700");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(52, 52)]),
                    Some(&[(53, 53)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD780");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(52, 52)]),
                    Some(&[(50, 50)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD7500");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(50, 50)]),
                    Some(&[(50, 50)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD800");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(51, 51)]),
                    Some(&[(51, 51)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD810");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(52, 52)]),
                    Some(&[(51, 51)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD850");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(49, 49)]),
                    Some(&[(53, 53)]),
                ],
            ) && count == 6745)
            {
                return Some("Nikon::ShotInfoD5000");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(50, 50)]),
                    Some(&[(49, 49)]),
                ],
            ) && count == 8902)
            {
                return Some("Nikon::ShotInfoD5100");
            }
            if (prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(50, 50)]),
                    Some(&[(54, 54)]),
                ],
            ) && count == 11587)
            {
                return Some("Nikon::ShotInfoD5200");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(50, 50)]),
                    Some(&[(48, 48)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD7000");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(50, 50)]),
                    Some(&[(51, 51)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD4");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(51, 51)]),
                    Some(&[(49, 49)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD4S");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(51, 51)]),
                    Some(&[(56, 56), (57, 57)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD500");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(52, 52)]),
                    Some(&[(54, 54)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD6");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(50, 50)]),
                    Some(&[(51, 51)]),
                    Some(&[(50, 50)]),
                ],
            ) {
                return Some("Nikon::ShotInfoD610");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(56, 56)]),
                    Some(&[(48, 48)]),
                    Some(&[
                        (48, 48),
                        (49, 49),
                        (50, 50),
                        (51, 51),
                        (52, 52),
                        (55, 55),
                        (56, 56),
                    ]),
                ],
            ) {
                return Some("Nikon::ShotInfoZ7II");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(56, 56)]),
                    Some(&[(48, 48)]),
                    Some(&[(54, 54)]),
                ],
            ) {
                return Some("Nikon::ShotInfoZ8");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[
                    Some(&[(48, 48)]),
                    Some(&[(56, 56)]),
                    Some(&[(48, 48)]),
                    Some(&[(53, 53)]),
                ],
            ) {
                return Some("Nikon::ShotInfoZ9");
            }
            if prefix_matches(
                data.get(0..).unwrap_or(&[]),
                &[Some(&[(48, 48)]), Some(&[(50, 50), (56, 56)])],
            ) {
                return Some("Nikon::ShotInfo");
            }
            Some("Nikon::ShotInfo")
        }
        _ => None,
    }
}

/// The byte order a table is written in, where the SubDirectory that opens it
/// says so. Nikon reads a D700's ShotInfo big-endian and a D810's
/// little-endian, inside files that are otherwise the same.
#[must_use]
pub fn table_byte_order(table: &str) -> Option<ByteOrder> {
    match table {
        "Nikon::ShotInfo" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD300S" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD300a" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD300b" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD3S" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD3X" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD3a" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD3b" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD4" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD40" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD4S" => Some(ByteOrder::LittleEndian),
        "Nikon::ShotInfoD500" => Some(ByteOrder::LittleEndian),
        "Nikon::ShotInfoD5000" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD5100" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD5200" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD6" => Some(ByteOrder::LittleEndian),
        "Nikon::ShotInfoD610" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD700" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD7000" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD7500" => Some(ByteOrder::LittleEndian),
        "Nikon::ShotInfoD780" => Some(ByteOrder::LittleEndian),
        "Nikon::ShotInfoD80" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD800" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoD810" => Some(ByteOrder::LittleEndian),
        "Nikon::ShotInfoD850" => Some(ByteOrder::LittleEndian),
        "Nikon::ShotInfoD90" => Some(ByteOrder::BigEndian),
        "Nikon::ShotInfoZ6III" => Some(ByteOrder::LittleEndian),
        "Nikon::ShotInfoZ7II" => Some(ByteOrder::LittleEndian),
        "Nikon::ShotInfoZ8" => Some(ByteOrder::LittleEndian),
        "Nikon::ShotInfoZ9" => Some(ByteOrder::LittleEndian),
        _ => None,
    }
}

/// What a Main-table id stores on the file while testing its own condition.
///
/// `($$self{CameraInfoCount} = $count) and ...` is an assignment used as the
/// test: ExifTool keeps the block's own length whether or not that arm is the
/// one taken, and the sub-table indexes its last fields from it. The caller
/// seeds the state with this before decoding.
#[must_use]
pub fn count_member(module: &str, tag: u16) -> Option<&'static str> {
    match (module, tag) {
        ("Canon", 0x000d) => Some("CameraInfoCount"),
        _ => None,
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    /// Every generated pattern must compile: a bad one would otherwise
    /// only surface as a panic on whichever file first reaches it.
    #[test]
    fn every_model_pattern_compiles() {
        LazyLock::force(&MODEL_RE_0);
        LazyLock::force(&MODEL_RE_1);
        LazyLock::force(&MODEL_RE_2);
        LazyLock::force(&MODEL_RE_3);
        LazyLock::force(&MODEL_RE_4);
        LazyLock::force(&MODEL_RE_5);
        LazyLock::force(&MODEL_RE_6);
        LazyLock::force(&MODEL_RE_7);
        LazyLock::force(&MODEL_RE_8);
        LazyLock::force(&MODEL_RE_9);
        LazyLock::force(&MODEL_RE_10);
        LazyLock::force(&MODEL_RE_11);
        LazyLock::force(&MODEL_RE_12);
        LazyLock::force(&MODEL_RE_13);
        LazyLock::force(&MODEL_RE_14);
        LazyLock::force(&MODEL_RE_15);
        LazyLock::force(&MODEL_RE_16);
        LazyLock::force(&MODEL_RE_17);
        LazyLock::force(&MODEL_RE_18);
        LazyLock::force(&MODEL_RE_19);
        LazyLock::force(&MODEL_RE_20);
        LazyLock::force(&MODEL_RE_21);
        LazyLock::force(&MODEL_RE_22);
        LazyLock::force(&MODEL_RE_23);
        LazyLock::force(&MODEL_RE_24);
        LazyLock::force(&MODEL_RE_25);
        LazyLock::force(&MODEL_RE_26);
        LazyLock::force(&MODEL_RE_27);
        LazyLock::force(&MODEL_RE_28);
        LazyLock::force(&MODEL_RE_29);
        LazyLock::force(&MODEL_RE_30);
        LazyLock::force(&MODEL_RE_31);
        LazyLock::force(&MODEL_RE_32);
        LazyLock::force(&MODEL_RE_33);
        LazyLock::force(&MODEL_RE_34);
        LazyLock::force(&MODEL_RE_35);
        LazyLock::force(&MODEL_RE_36);
        LazyLock::force(&MODEL_RE_37);
        LazyLock::force(&MODEL_RE_38);
        LazyLock::force(&MODEL_RE_39);
        LazyLock::force(&MODEL_RE_40);
        LazyLock::force(&MODEL_RE_41);
        LazyLock::force(&MODEL_RE_42);
        LazyLock::force(&MODEL_RE_43);
        LazyLock::force(&MODEL_RE_44);
        LazyLock::force(&MODEL_RE_45);
        LazyLock::force(&MODEL_RE_46);
        LazyLock::force(&MODEL_RE_47);
        LazyLock::force(&MODEL_RE_48);
        LazyLock::force(&MODEL_RE_49);
        LazyLock::force(&MODEL_RE_50);
        LazyLock::force(&MODEL_RE_51);
        LazyLock::force(&MODEL_RE_52);
        LazyLock::force(&MODEL_RE_53);
        LazyLock::force(&MODEL_RE_54);
        LazyLock::force(&MODEL_RE_55);
        LazyLock::force(&MODEL_RE_56);
        LazyLock::force(&MODEL_RE_57);
        LazyLock::force(&MODEL_RE_58);
        LazyLock::force(&MODEL_RE_59);
        LazyLock::force(&MODEL_RE_60);
        LazyLock::force(&MODEL_RE_61);
    }
}
